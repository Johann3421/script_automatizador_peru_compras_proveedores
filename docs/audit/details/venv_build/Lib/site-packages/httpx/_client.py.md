# Documentación Técnica: `venv_build/Lib/site-packages/httpx/_client.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/httpx/_client.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2019
- **Fecha de inspección:** 2026-08-05 18:36:08

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `UseClientDefault` (Línea 94)
- **Docstring:** _For some parameters such as `auth=...` and `timeout=...` we need to be able
to indicate the default "unset" state, in a way that is distinctly different
to using `None`.

The default "unset" state indicates that whatever default is set on the
client should be used. This is different to setting `None`, which
explicitly disables the parameter, possibly overriding a client default.

For example we use `timeout=USE_CLIENT_DEFAULT` in the `request()` signature.
Omitting the `timeout` parameter will send a request using whatever default
timeout has been configured on the client. Including `timeout=None` will
ensure no timeout is used.

Note that user code shouldn't need to use the `USE_CLIENT_DEFAULT` constant,
but it is used internally when a parameter is not included._

#### Clase `ClientState` (Línea 125)
- **Docstring:** _Sin docstring._

#### Clase `BoundSyncStream` (Línea 139)
- **Docstring:** _A byte stream that is bound to a given response instance, and that
ensures the `response.elapsed` is set once the response is closed._
- **Métodos:**
  - `def __init__(self, stream, response, start)` (Línea 145): Sin docstring.
  - `def __iter__(self)` (Línea 152): Sin docstring.
  - `def close(self)` (Línea 156): Sin docstring.

#### Clase `BoundAsyncStream` (Línea 162)
- **Docstring:** _An async byte stream that is bound to a given response instance, and that
ensures the `response.elapsed` is set once the response is closed._
- **Métodos:**
  - `def __init__(self, stream, response, start)` (Línea 168): Sin docstring.
  - `def __aiter__(self)` (Línea 175): Sin docstring.
  - `def aclose(self)` (Línea 179): Sin docstring.

#### Clase `BaseClient` (Línea 188)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 189): Sin docstring.
  - `def is_closed(self)` (Línea 224): Check if the client being closed
  - `def trust_env(self)` (Línea 231): Sin docstring.
  - `def _enforce_trailing_slash(self, url)` (Línea 234): Sin docstring.
  - `def _get_proxy_map(self, proxy, allow_env_proxies)` (Línea 239): Sin docstring.
  - `def timeout(self)` (Línea 254): Sin docstring.
  - `def timeout(self, timeout)` (Línea 258): Sin docstring.
  - `def event_hooks(self)` (Línea 262): Sin docstring.
  - `def event_hooks(self, event_hooks)` (Línea 266): Sin docstring.
  - `def auth(self)` (Línea 273): Authentication class used when none is passed at the request-level.

See also [Authentication][0].

[0]: /quickstart/#authentication
  - `def auth(self, auth)` (Línea 284): Sin docstring.
  - `def base_url(self)` (Línea 288): Base URL to use when sending requests with relative URLs.
  - `def base_url(self, url)` (Línea 295): Sin docstring.
  - `def headers(self)` (Línea 299): HTTP headers to include when sending requests.
  - `def headers(self, headers)` (Línea 306): Sin docstring.
  - `def cookies(self)` (Línea 319): Cookie values to include when sending requests.
  - `def cookies(self, cookies)` (Línea 326): Sin docstring.
  - `def params(self)` (Línea 330): Query parameters to include in the URL when sending requests.
  - `def params(self, params)` (Línea 337): Sin docstring.
  - `def build_request(self, method, url)` (Línea 340): Build and return a request instance.

* The `params`, `headers` and `cookies` arguments
are merged with any values set on the client.
* The `url` argument is merged with any `base_url` set on the client.

See also: [Request instances][0]

[0]: /advanced/clients/#request-instances
  - `def _merge_url(self, url)` (Línea 391): Merge a URL argument together with any 'base_url' on the client,
to create the URL used for the outgoing request.
  - `def _merge_cookies(self, cookies)` (Línea 413): Merge a cookies argument together with any cookies on the client,
to create the cookies used for the outgoing request.
  - `def _merge_headers(self, headers)` (Línea 424): Merge a headers argument together with any headers on the client,
to create the headers used for the outgoing request.
  - `def _merge_queryparams(self, params)` (Línea 433): Merge a queryparams argument together with any queryparams on the client,
to create the queryparams used for the outgoing request.
  - `def _build_auth(self, auth)` (Línea 445): Sin docstring.
  - `def _build_request_auth(self, request, auth)` (Línea 457): Sin docstring.
  - `def _build_redirect_request(self, request, response)` (Línea 475): Given a request and a redirect response, return a new request that
should be used to effect the redirect.
  - `def _redirect_method(self, request, response)` (Línea 494): When being redirected we may want to change the method of the request
based on certain specs or browser behavior.
  - `def _redirect_url(self, request, response)` (Línea 517): Return the URL for the redirect to follow.
  - `def _redirect_headers(self, request, url, method)` (Línea 546): Return the headers that should be used for the redirect request.
  - `def _redirect_stream(self, request, method)` (Línea 573): Return the body that should be used for the redirect request.
  - `def _set_timeout(self, request)` (Línea 584): Sin docstring.

#### Clase `Client` (Línea 594)
- **Docstring:** _An HTTP client, with connection pooling, HTTP/2, redirects, cookie persistence, etc.

It can be shared between threads.

Usage:

```python
>>> client = httpx.Client()
>>> response = client.get('https://example.org')
```

**Parameters:**

* **auth** - *(optional)* An authentication class to use when sending
requests.
* **params** - *(optional)* Query parameters to include in request URLs, as
a string, dictionary, or sequence of two-tuples.
* **headers** - *(optional)* Dictionary of HTTP headers to include when
sending requests.
* **cookies** - *(optional)* Dictionary of Cookie items to include when
sending requests.
* **verify** - *(optional)* Either `True` to use an SSL context with the
default CA bundle, `False` to disable verification, or an instance of
`ssl.SSLContext` to use a custom context.
* **http2** - *(optional)* A boolean indicating if HTTP/2 support should be
enabled. Defaults to `False`.
* **proxy** - *(optional)* A proxy URL where all the traffic should be routed.
* **timeout** - *(optional)* The timeout configuration to use when sending
requests.
* **limits** - *(optional)* The limits configuration to use.
* **max_redirects** - *(optional)* The maximum number of redirect responses
that should be followed.
* **base_url** - *(optional)* A URL to use as the base when building
request URLs.
* **transport** - *(optional)* A transport class to use for sending requests
over the network.
* **trust_env** - *(optional)* Enables or disables usage of environment
variables for configuration.
* **default_encoding** - *(optional)* The default encoding to use for decoding
response text, if no charset information is included in a response Content-Type
header. Set to a callable for automatic character set detection. Default: "utf-8"._
- **Métodos:**
  - `def __init__(self)` (Línea 639): Sin docstring.
  - `def _init_transport(self, verify, cert, trust_env, http1, http2, limits, transport)` (Línea 718): Sin docstring.
  - `def _init_proxy_transport(self, proxy, verify, cert, trust_env, http1, http2, limits)` (Línea 740): Sin docstring.
  - `def _transport_for_url(self, url)` (Línea 760): Returns the transport instance that should be used for a given URL.
This will either be the standard connection pool, or a proxy.
  - `def request(self, method, url)` (Línea 771): Build and send a request.

Equivalent to:

```python
request = client.build_request(...)
response = client.send(request, ...)
```

See `Client.build_request()`, `Client.send()` and
[Merging of configuration][0] for how the various parameters
are merged with client-level configuration.

[0]: /advanced/clients/#merging-of-configuration
  - `def stream(self, method, url)` (Línea 828): Alternative to `httpx.request()` that streams the response body
instead of loading it into memory at once.

**Parameters**: See `httpx.request`.

See also: [Streaming Responses][0]

[0]: /quickstart#streaming-responses
  - `def send(self, request)` (Línea 879): Send a request.

The request is sent as-is, unmodified.

Typically you'll want to build one with `Client.build_request()`
so that any client-level configuration is merged into the request,
but passing an explicit `httpx.Request()` is supported as well.

See also: [Request instances][0]

[0]: /advanced/clients/#request-instances
  - `def _send_handling_auth(self, request, auth, follow_redirects, history)` (Línea 930): Sin docstring.
  - `def _send_handling_redirects(self, request, follow_redirects, history)` (Línea 964): Sin docstring.
  - `def _send_single_request(self, request)` (Línea 1001): Sends a single request, without handling any redirections.
  - `def get(self, url)` (Línea 1036): Send a `GET` request.

**Parameters**: See `httpx.request`.
  - `def options(self, url)` (Línea 1065): Send an `OPTIONS` request.

**Parameters**: See `httpx.request`.
  - `def head(self, url)` (Línea 1094): Send a `HEAD` request.

**Parameters**: See `httpx.request`.
  - `def post(self, url)` (Línea 1123): Send a `POST` request.

**Parameters**: See `httpx.request`.
  - `def put(self, url)` (Línea 1160): Send a `PUT` request.

**Parameters**: See `httpx.request`.
  - `def patch(self, url)` (Línea 1197): Send a `PATCH` request.

**Parameters**: See `httpx.request`.
  - `def delete(self, url)` (Línea 1234): Send a `DELETE` request.

**Parameters**: See `httpx.request`.
  - `def close(self)` (Línea 1263): Close transport and proxies.
  - `def __enter__(self)` (Línea 1275): Sin docstring.
  - `def __exit__(self, exc_type, exc_value, traceback)` (Línea 1293): Sin docstring.

#### Clase `AsyncClient` (Línea 1307)
- **Docstring:** _An asynchronous HTTP client, with connection pooling, HTTP/2, redirects,
cookie persistence, etc.

It can be shared between tasks.

Usage:

```python
>>> async with httpx.AsyncClient() as client:
>>>     response = await client.get('https://example.org')
```

**Parameters:**

* **auth** - *(optional)* An authentication class to use when sending
requests.
* **params** - *(optional)* Query parameters to include in request URLs, as
a string, dictionary, or sequence of two-tuples.
* **headers** - *(optional)* Dictionary of HTTP headers to include when
sending requests.
* **cookies** - *(optional)* Dictionary of Cookie items to include when
sending requests.
* **verify** - *(optional)* Either `True` to use an SSL context with the
default CA bundle, `False` to disable verification, or an instance of
`ssl.SSLContext` to use a custom context.
* **http2** - *(optional)* A boolean indicating if HTTP/2 support should be
enabled. Defaults to `False`.
* **proxy** - *(optional)* A proxy URL where all the traffic should be routed.
* **timeout** - *(optional)* The timeout configuration to use when sending
requests.
* **limits** - *(optional)* The limits configuration to use.
* **max_redirects** - *(optional)* The maximum number of redirect responses
that should be followed.
* **base_url** - *(optional)* A URL to use as the base when building
request URLs.
* **transport** - *(optional)* A transport class to use for sending requests
over the network.
* **trust_env** - *(optional)* Enables or disables usage of environment
variables for configuration.
* **default_encoding** - *(optional)* The default encoding to use for decoding
response text, if no charset information is included in a response Content-Type
header. Set to a callable for automatic character set detection. Default: "utf-8"._
- **Métodos:**
  - `def __init__(self)` (Línea 1353): Sin docstring.
  - `def _init_transport(self, verify, cert, trust_env, http1, http2, limits, transport)` (Línea 1432): Sin docstring.
  - `def _init_proxy_transport(self, proxy, verify, cert, trust_env, http1, http2, limits)` (Línea 1454): Sin docstring.
  - `def _transport_for_url(self, url)` (Línea 1474): Returns the transport instance that should be used for a given URL.
This will either be the standard connection pool, or a proxy.
  - `def request(self, method, url)` (Línea 1485): Build and send a request.

Equivalent to:

```python
request = client.build_request(...)
response = await client.send(request, ...)
```

See `AsyncClient.build_request()`, `AsyncClient.send()`
and [Merging of configuration][0] for how the various parameters
are merged with client-level configuration.

[0]: /advanced/clients/#merging-of-configuration
  - `def stream(self, method, url)` (Línea 1543): Alternative to `httpx.request()` that streams the response body
instead of loading it into memory at once.

**Parameters**: See `httpx.request`.

See also: [Streaming Responses][0]

[0]: /quickstart#streaming-responses
  - `def send(self, request)` (Línea 1594): Send a request.

The request is sent as-is, unmodified.

Typically you'll want to build one with `AsyncClient.build_request()`
so that any client-level configuration is merged into the request,
but passing an explicit `httpx.Request()` is supported as well.

See also: [Request instances][0]

[0]: /advanced/clients/#request-instances
  - `def _send_handling_auth(self, request, auth, follow_redirects, history)` (Línea 1645): Sin docstring.
  - `def _send_handling_redirects(self, request, follow_redirects, history)` (Línea 1679): Sin docstring.
  - `def _send_single_request(self, request)` (Línea 1717): Sends a single request, without handling any redirections.
  - `def get(self, url)` (Línea 1751): Send a `GET` request.

**Parameters**: See `httpx.request`.
  - `def options(self, url)` (Línea 1780): Send an `OPTIONS` request.

**Parameters**: See `httpx.request`.
  - `def head(self, url)` (Línea 1809): Send a `HEAD` request.

**Parameters**: See `httpx.request`.
  - `def post(self, url)` (Línea 1838): Send a `POST` request.

**Parameters**: See `httpx.request`.
  - `def put(self, url)` (Línea 1875): Send a `PUT` request.

**Parameters**: See `httpx.request`.
  - `def patch(self, url)` (Línea 1912): Send a `PATCH` request.

**Parameters**: See `httpx.request`.
  - `def delete(self, url)` (Línea 1949): Send a `DELETE` request.

**Parameters**: See `httpx.request`.
  - `def aclose(self)` (Línea 1978): Close transport and proxies.
  - `def __aenter__(self)` (Línea 1990): Sin docstring.
  - `def __aexit__(self, exc_type, exc_value, traceback)` (Línea 2008): Sin docstring.

### Funciones independientes:

#### `def _is_https_redirect(url, location)` (Línea 62)
- **Propósito:** Return 'True' if 'location' is a HTTPS upgrade of 'url'
- **Firma:** `def _is_https_redirect(url, location)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _port_or_default(url)` (Línea 77)
- **Propósito:** Sin docstring.
- **Firma:** `def _port_or_default(url)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _same_origin(url, other)` (Línea 83)
- **Propósito:** Return 'True' if the given URLs share the same origin.
- **Firma:** `def _same_origin(url, other)`
- **Retorno / Efectos:** Consulta código fuente.
