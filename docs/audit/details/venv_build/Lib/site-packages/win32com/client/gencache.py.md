# Documentación Técnica: `venv_build/Lib/site-packages/win32com/client/gencache.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/win32com/client/gencache.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 818
- **Fecha de inspección:** 2026-08-05 18:36:40

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def __init__()` (Línea 68)
- **Propósito:** Sin docstring.
- **Firma:** `def __init__()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _SaveDicts()` (Línea 79)
- **Propósito:** Sin docstring.
- **Firma:** `def _SaveDicts()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _LoadDicts()` (Línea 93)
- **Propósito:** Sin docstring.
- **Firma:** `def _LoadDicts()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ModuleMutex(module_name)` (Línea 140)
- **Propósito:** Given the output of GetGeneratedFilename, acquire a named mutex for that module

This is required so that writes (generation) don't interfere with each other and with reads (import)
- **Firma:** `def ModuleMutex(module_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetGeneratedFileName(clsid, lcid, major, minor)` (Línea 155)
- **Propósito:** Given the clsid, lcid, major and  minor for a type lib, return
the file name (no extension) providing this support.
- **Firma:** `def GetGeneratedFileName(clsid, lcid, major, minor)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def SplitGeneratedFileName(fname)` (Línea 162)
- **Propósito:** Reverse of GetGeneratedFileName()
- **Firma:** `def SplitGeneratedFileName(fname)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetGeneratePath()` (Línea 167)
- **Propósito:** Returns the name of the path to generate to.
Checks the directory is OK.
- **Firma:** `def GetGeneratePath()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetClassForProgID(progid)` (Línea 191)
- **Propósito:** Get a Python class for a Program ID

Given a Program ID, return a Python class which wraps the COM object

Returns the Python class, or None if no module is available.

Params
progid -- A COM ProgramID or IID (eg, "Word.Application")
- **Firma:** `def GetClassForProgID(progid)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetClassForCLSID(clsid)` (Línea 205)
- **Propósito:** Get a Python class for a CLSID

Given a CLSID, return a Python class which wraps the COM object

Returns the Python class, or None if no module is available.

Params
clsid -- A COM CLSID (or string repr of one)
- **Firma:** `def GetClassForCLSID(clsid)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetModuleForProgID(progid)` (Línea 228)
- **Propósito:** Get a Python module for a Program ID

Given a Program ID, return a Python module which contains the
class which wraps the COM object.

Returns the Python module, or None if no module is available.

Params
progid -- A COM ProgramID or IID (eg, "Word.Application")
- **Firma:** `def GetModuleForProgID(progid)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetModuleForCLSID(clsid)` (Línea 246)
- **Propósito:** Get a Python module for a CLSID

Given a CLSID, return a Python module which contains the
class which wraps the COM object.

Returns the Python module, or None if no module is available.

Params
progid -- A COM CLSID (ie, not the description)
- **Firma:** `def GetModuleForCLSID(clsid)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetModuleForTypelib(typelibCLSID, lcid, major, minor)` (Línea 290)
- **Propósito:** Get a Python module for a type library ID

Given the CLSID of a typelibrary, return an imported Python module,
else None

Params
typelibCLSID -- IID of the type library.
major -- Integer major version.
minor -- Integer minor version
lcid -- Integer LCID for the library.
- **Firma:** `def GetModuleForTypelib(typelibCLSID, lcid, major, minor)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def MakeModuleForTypelib(typelibCLSID, lcid, major, minor, progressInstance, bForDemand, bBuildHidden)` (Línea 312)
- **Propósito:** Generate support for a type library.

Given the IID, LCID and version information for a type library, generate
and import the necessary support files.

Returns the Python module.  No exceptions are caught.

Params
typelibCLSID -- IID of the type library.
major -- Integer major version.
minor -- Integer minor version.
lcid -- Integer LCID for the library.
progressInstance -- Instance to use as progress indicator, or None to
                    use the GUI progress bar.
- **Firma:** `def MakeModuleForTypelib(typelibCLSID, lcid, major, minor, progressInstance, bForDemand, bBuildHidden)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def MakeModuleForTypelibInterface(typelib_ob, progressInstance, bForDemand, bBuildHidden)` (Línea 347)
- **Propósito:** Generate support for a type library.

Given a PyITypeLib interface generate and import the necessary support files.  This is useful
for getting makepy support for a typelibrary that is not registered - the caller can locate
and load the type library itself, rather than relying on COM to find it.

Returns the Python module.

Params
typelib_ob -- The type library itself
progressInstance -- Instance to use as progress indicator, or None to
                    use the GUI progress bar.
- **Firma:** `def MakeModuleForTypelibInterface(typelib_ob, progressInstance, bForDemand, bBuildHidden)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def EnsureModuleForTypelibInterface(typelib_ob, progressInstance, bForDemand, bBuildHidden)` (Línea 382)
- **Propósito:** Check we have support for a type library, generating if not.

Given a PyITypeLib interface generate and import the necessary
support files if necessary. This is useful for getting makepy support
for a typelibrary that is not registered - the caller can locate and
load the type library itself, rather than relying on COM to find it.

Returns the Python module.

Params
typelib_ob -- The type library itself
progressInstance -- Instance to use as progress indicator, or None to
                    use the GUI progress bar.
- **Firma:** `def EnsureModuleForTypelibInterface(typelib_ob, progressInstance, bForDemand, bBuildHidden)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ForgetAboutTypelibInterface(typelib_ob)` (Línea 419)
- **Propósito:** Drop any references to a typelib previously added with EnsureModuleForTypelibInterface and forDemand
- **Firma:** `def ForgetAboutTypelibInterface(typelib_ob)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def EnsureModule(typelibCLSID, lcid, major, minor, progressInstance, bValidateFile, bForDemand, bBuildHidden)` (Línea 442)
- **Propósito:** Ensure Python support is loaded for a type library, generating if necessary.

Given the IID, LCID and version information for a type library, check and if
necessary (re)generate, then import the necessary support files. If we regenerate the file, there
is no way to totally snuff out all instances of the old module in Python, and thus we will regenerate the file more than necessary,
unless makepy/genpy is modified accordingly.


Returns the Python module.  No exceptions are caught during the generate process.

Params
typelibCLSID -- IID of the type library.
major -- Integer major version.
minor -- Integer minor version
lcid -- Integer LCID for the library.
progressInstance -- Instance to use as progress indicator, or None to
                    use the GUI progress bar.
bValidateFile -- Whether or not to perform cache validation or not
bForDemand -- Should a complete generation happen now, or on demand?
bBuildHidden -- Should hidden members/attributes etc be generated?
- **Firma:** `def EnsureModule(typelibCLSID, lcid, major, minor, progressInstance, bValidateFile, bForDemand, bBuildHidden)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def EnsureDispatch(prog_id, bForDemand)` (Línea 636)
- **Propósito:** Given a COM prog_id, return an object that is using makepy support, building if necessary
- **Firma:** `def EnsureDispatch(prog_id, bForDemand)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def AddModuleToCache(typelibclsid, lcid, major, minor, verbose, bFlushNow)` (Línea 661)
- **Propósito:** Add a newly generated file to the cache dictionary.
- **Firma:** `def AddModuleToCache(typelibclsid, lcid, major, minor, verbose, bFlushNow)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetGeneratedInfos()` (Línea 692)
- **Propósito:** Sin docstring.
- **Firma:** `def GetGeneratedInfos()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _GetModule(fname)` (Línea 742)
- **Propósito:** Given the name of a module in the gen_py directory, import and return it.
- **Firma:** `def _GetModule(fname)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def Rebuild(verbose)` (Línea 750)
- **Propósito:** Rebuild the cache indexes from the file system.
- **Firma:** `def Rebuild(verbose)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _Dump()` (Línea 773)
- **Propósito:** Sin docstring.
- **Firma:** `def _Dump()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def usage()` (Línea 786)
- **Propósito:** Sin docstring.
- **Firma:** `def usage()`
- **Retorno / Efectos:** Consulta código fuente.
