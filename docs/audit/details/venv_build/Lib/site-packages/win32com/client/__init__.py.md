# Documentación Técnica: `venv_build/Lib/site-packages/win32com/client/__init__.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/win32com/client/__init__.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 704
- **Fecha de inspección:** 2026-08-05 18:36:40

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `CDispatch` (Línea 148)
- **Docstring:** _The dynamic class used as a last resort.
The purpose of this overriding of dynamic.CDispatch is to perpetuate the policy
of using the makepy generated wrapper Python class instead of dynamic.CDispatch
if/when possible._
- **Métodos:**
  - `def _wrap_dispatch_(self, ob, userName, returnCLSID)` (Línea 156): Sin docstring.
  - `def __dir__(self)` (Línea 159): Sin docstring.

#### Clase `Constants` (Línea 215)
- **Docstring:** _A container for generated COM constants._
- **Métodos:**
  - `def __init__(self)` (Línea 218): Sin docstring.
  - `def __getattr__(self, a)` (Línea 221): Sin docstring.

#### Clase `EventsProxy` (Línea 248)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, ob)` (Línea 249): Sin docstring.
  - `def __del__(self)` (Línea 252): Sin docstring.
  - `def __getattr__(self, attr)` (Línea 260): Sin docstring.
  - `def __setattr__(self, attr, val)` (Línea 263): Sin docstring.

#### Clase `DispatchBaseClass` (Línea 523)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, oobj)` (Línea 524): Sin docstring.
  - `def __dir__(self)` (Línea 543): Sin docstring.
  - `def __repr__(self)` (Línea 558): Sin docstring.
  - `def __eq__(self, other)` (Línea 571): Sin docstring.
  - `def __ne__(self, other)` (Línea 575): Sin docstring.
  - `def _ApplyTypes_(self, dispid, wFlags, retType, argTypes, user, resultCLSID)` (Línea 579): Sin docstring.
  - `def __getattr__(self, attr)` (Línea 586): Sin docstring.
  - `def __setattr__(self, attr, value)` (Línea 592): Sin docstring.
  - `def _get_good_single_object_(self, obj, obUserName, resultCLSID)` (Línea 602): Sin docstring.
  - `def _get_good_object_(self, obj, obUserName, resultCLSID)` (Línea 605): Sin docstring.

#### Clase `CoClassBaseClass` (Línea 627)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, oobj)` (Línea 628): Sin docstring.
  - `def __repr__(self)` (Línea 633): Sin docstring.
  - `def __getattr__(self, attr)` (Línea 636): Sin docstring.
  - `def __setattr__(self, attr, value)` (Línea 642): Sin docstring.
  - `def __call__(self)` (Línea 661): Sin docstring.
  - `def __str__(self)` (Línea 664): Sin docstring.
  - `def __int__(self)` (Línea 667): Sin docstring.
  - `def __iter__(self)` (Línea 670): Sin docstring.
  - `def __len__(self)` (Línea 673): Sin docstring.
  - `def __bool__(self)` (Línea 676): Sin docstring.

#### Clase `VARIANT` (Línea 685)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, vt, value)` (Línea 686): Sin docstring.
  - `def _get_value(self)` (Línea 692): Sin docstring.
  - `def _set_value(self, newval)` (Línea 695): Sin docstring.
  - `def _del_value(self)` (Línea 698): Sin docstring.
  - `def __repr__(self)` (Línea 703): Sin docstring.

### Funciones independientes:

#### `def __WrapDispatch(dispatch, userName, resultCLSID, typeinfo, clsctx, WrapperClass)` (Línea 21)
- **Propósito:** Helper function to return a makepy generated class for a CLSID if it exists,
otherwise cope by using CDispatch.
- **Firma:** `def __WrapDispatch(dispatch, userName, resultCLSID, typeinfo, clsctx, WrapperClass)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetObject(Pathname, Class, clsctx)` (Línea 57)
- **Propósito:** Mimic VB's GetObject() function.

ob = GetObject(Class = "ProgID") or GetObject(Class = clsid) will
connect to an already running instance of the COM object.

ob = GetObject(r"c:\blah\blah\foo.xls") (aka the COM moniker syntax)
will return a ready to use Python wrapping of the required COM object.

Note: You must specifiy one or the other of these arguments. I know
this isn't pretty, but it is what VB does. Blech. If you don't
I'll throw ValueError at you. :)

This will most likely throw pythoncom.com_error if anything fails.
- **Firma:** `def GetObject(Pathname, Class, clsctx)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetActiveObject(Class, clsctx)` (Línea 89)
- **Propósito:** Python friendly version of GetObject's ProgID/CLSID functionality.
- **Firma:** `def GetActiveObject(Class, clsctx)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def Moniker(Pathname, clsctx)` (Línea 99)
- **Propósito:** Python friendly version of GetObject's moniker functionality.
- **Firma:** `def Moniker(Pathname, clsctx)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def Dispatch(dispatch, userName, resultCLSID, typeinfo, clsctx)` (Línea 108)
- **Propósito:** Creates a Dispatch based COM object.
- **Firma:** `def Dispatch(dispatch, userName, resultCLSID, typeinfo, clsctx)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def DispatchEx(clsid, machine, userName, resultCLSID, typeinfo, clsctx)` (Línea 120)
- **Propósito:** Creates a Dispatch based COM object on a specific machine.
- **Firma:** `def DispatchEx(clsid, machine, userName, resultCLSID, typeinfo, clsctx)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def CastTo(ob, target, typelib)` (Línea 163)
- **Propósito:** 'Cast' a COM object to another interface
- **Firma:** `def CastTo(ob, target, typelib)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _event_setattr_(self, attr, val)` (Línea 234)
- **Propósito:** Sin docstring.
- **Firma:** `def _event_setattr_(self, attr, val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def __get_disp_and_event_classes(dispatch)` (Línea 267)
- **Propósito:** Sin docstring.
- **Firma:** `def __get_disp_and_event_classes(dispatch)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def DispatchWithEvents(clsid, user_event_class)` (Línea 300)
- **Propósito:** Create a COM object that can fire events to a user defined class.
clsid -- The ProgID or CLSID of the object to create.
user_event_class -- A Python class object that responds to the events.

This requires makepy support for the COM object being created.  If
this support does not exist it will be automatically generated by
this function.  If the object does not support makepy, a TypeError
exception will be raised.

The result is a class instance that both represents the COM object
and handles events from the COM object.

It is important to note that the returned instance is not a direct
instance of the user_event_class, but an instance of a temporary
class object that derives from three classes:
* The makepy generated class for the COM object
* The makepy generated class for the COM events
* The user_event_class as passed to this function.

If this is not suitable, see the getevents function for an alternative
technique of handling events.

Object Lifetimes:  Whenever the object returned from this function is
cleaned-up by Python, the events will be disconnected from
the COM object.  This is almost always what should happen,
but see the documentation for getevents() for more details.

Example:

>>> class IEEvents:
...    def OnVisible(self, visible):
...       print("Visible changed:", visible)
...
>>> ie = DispatchWithEvents("InternetExplorer.Application", IEEvents)
>>> ie.Visible = 1
Visible changed: 1
- **Firma:** `def DispatchWithEvents(clsid, user_event_class)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def WithEvents(disp, user_event_class)` (Línea 352)
- **Propósito:** Similar to DispatchWithEvents - except that the returned
object is *not* also usable as the original Dispatch object - that is
the returned object is not dispatchable.

The difference is best summarised by example.

>>> class IEEvents:
...    def OnVisible(self, visible):
...       print("Visible changed:", visible)
...
>>> ie = Dispatch("InternetExplorer.Application")
>>> ie_events = WithEvents(ie, IEEvents)
>>> ie.Visible = 1
Visible changed: 1

Compare with the code sample for DispatchWithEvents, where you get a
single object that is both the interface and the event handler.  Note that
the event handler instance will *not* be able to use 'self.' to refer to
IE's methods and properties.

This is mainly useful where using DispatchWithEvents causes
circular reference problems that the simple proxy doesn't deal with
- **Firma:** `def WithEvents(disp, user_event_class)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def getevents(clsid)` (Línea 389)
- **Propósito:** Determine the default outgoing interface for a class, given
either a clsid or progid. It returns a class - you can
conveniently derive your own handler from this class and implement
the appropriate methods.

This method relies on the classes produced by makepy. You must use
either makepy or the gencache module to ensure that the
appropriate support classes have been generated for the com server
that you will be handling events from.

Beware of COM circular references.  When the Events class is connected
to the COM object, the COM object itself keeps a reference to the Python
events class.  Thus, neither the Events instance or the COM object will
ever die by themselves.  The 'close' method on the events instance
must be called to break this chain and allow standard Python collection
rules to manage object lifetimes.  Note that DispatchWithEvents() does
work around this problem by the use of a proxy object, but if you use
the getevents() function yourself, you must make your own arrangements
to manage this circular reference issue.

Beware of creating Python circular references: this will happen if your
handler has a reference to an object that has a reference back to
the event source. Call the 'close' method to break the chain.

Example:

>>>win32com.client.gencache.EnsureModule('{EAB22AC0-30C1-11CF-A7EB-0000C05BAE0B}',0,1,1)
<module 'win32com.gen_py.....
>>>
>>> class InternetExplorerEvents(win32com.client.getevents("InternetExplorer.Application.1")):
...    def OnVisible(self, Visible):
...        print("Visibility changed: ", Visible)
...
>>>
>>> ie=win32com.client.Dispatch("InternetExplorer.Application.1")
>>> events=InternetExplorerEvents(ie)
>>> ie.Visible=1
Visibility changed:  1
>>>
- **Firma:** `def getevents(clsid)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def Record(name, object)` (Línea 446)
- **Propósito:** Creates a new record object, given the name of the record,
and an object from the same type library.

Example usage would be:
  app = win32com.client.Dispatch("Some.Application")
  point = win32com.client.Record("SomeAppPoint", app)
  point.x = 0
  point.y = 0
  app.MoveTo(point)
- **Firma:** `def Record(name, object)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register_record_class(cls)` (Línea 480)
- **Propósito:** Register a subclass of com_record to enable creation of the represented record objects.

A subclass of com_record requires the following class attributes to be instantiable:

    TLBID : The GUID of the containing TypeLibrary as a string.
    MJVER : The major version number of the TypeLibrary as an integer.
    MNVER : The minor version number of the TypeLibrary as an integer.
    LCID  : The LCID of the TypeLibrary as an integer.
    GUID  : The GUID of the COM Record as a string.

with GUID strings in {xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx} notation.

To instantiate such a subclasses it has to be registered via this function.
- **Firma:** `def register_record_class(cls)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_good_single_object_(obj, obUserName, resultCLSID)` (Línea 610)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_good_single_object_(obj, obUserName, resultCLSID)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_good_object_(obj, obUserName, resultCLSID)` (Línea 616)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_good_object_(obj, obUserName, resultCLSID)`
- **Retorno / Efectos:** Consulta código fuente.
