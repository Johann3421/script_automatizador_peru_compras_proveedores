# Documentación Técnica: `venv_build/Lib/site-packages/win32com/server/register.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/win32com/server/register.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 662
- **Fecha de inspección:** 2026-08-05 18:36:41

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def _set_subkeys(keyName, valueDict, base)` (Línea 26)
- **Propósito:** Sin docstring.
- **Firma:** `def _set_subkeys(keyName, valueDict, base)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _set_string(path, value, base)` (Línea 35)
- **Propósito:** Set a string value in the registry.
- **Firma:** `def _set_string(path, value, base)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_string(path, base)` (Línea 41)
- **Propósito:** Get a string value from the registry.
- **Firma:** `def _get_string(path, base)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _remove_key(path, base)` (Línea 50)
- **Propósito:** Remove a string from the registry.
- **Firma:** `def _remove_key(path, base)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def recurse_delete_key(path, base)` (Línea 61)
- **Propósito:** Recursively delete registry keys.

This is needed since you can't blast a key when subkeys exist.
- **Firma:** `def recurse_delete_key(path, base)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _cat_registrar()` (Línea 93)
- **Propósito:** Sin docstring.
- **Firma:** `def _cat_registrar()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_localserver_exe(mustfind)` (Línea 102)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_localserver_exe(mustfind)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_localserver_module()` (Línea 135)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_localserver_module()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterServer(clsid, pythonInstString, desc, progID, verProgID, defIcon, threadingModel, policy, catids, other, addPyComCat, dispatcher, clsctx, addnPath)` (Línea 151)
- **Propósito:** Registers a Python object as a COM Server.  This enters almost all necessary
information in the system registry, allowing COM to use the object.

clsid -- The (unique) CLSID of the server.
pythonInstString -- A string holding the instance name that will be created
              whenever COM requests a new object.
desc -- The description of the COM object.
progID -- The user name of this object (eg, Word.Document)
verProgId -- The user name of this version's implementation (eg Word.6.Document)
defIcon -- The default icon for the object.
threadingModel -- The threading model this object supports.
policy -- The policy to use when creating this object.
catids -- A list of category ID's this object belongs in.
other -- A dictionary of extra items to be registered.
addPyComCat -- A flag indicating if the object should be added to the list
         of Python servers installed on the machine.  If None (the default)
         then it will be registered when running from python source, but
         not registered if running in a frozen environment.
dispatcher -- The dispatcher to use when creating this object.
clsctx -- One of the CLSCTX_* constants.
addnPath -- An additional path the COM framework will add to sys.path
            before attempting to create the object.
- **Firma:** `def RegisterServer(clsid, pythonInstString, desc, progID, verProgID, defIcon, threadingModel, policy, catids, other, addPyComCat, dispatcher, clsctx, addnPath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetUnregisterServerKeys(clsid, progID, verProgID, customKeys)` (Línea 337)
- **Propósito:** Given a server, return a list of of ("key", root), which are keys recursively
and uncondtionally deleted at unregister or uninstall time.
- **Firma:** `def GetUnregisterServerKeys(clsid, progID, verProgID, customKeys)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterServer(clsid, progID, verProgID, customKeys)` (Línea 360)
- **Propósito:** Unregisters a Python COM server.
- **Firma:** `def UnregisterServer(clsid, progID, verProgID, customKeys)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRegisteredServerOption(clsid, optionName)` (Línea 376)
- **Propósito:** Given a CLSID for a server and option name, return the option value
- **Firma:** `def GetRegisteredServerOption(clsid, optionName)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get(ob, attr, default)` (Línea 382)
- **Propósito:** Sin docstring.
- **Firma:** `def _get(ob, attr, default)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterClasses()` (Línea 400)
- **Propósito:** Sin docstring.
- **Firma:** `def RegisterClasses()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterClasses()` (Línea 486)
- **Propósito:** Sin docstring.
- **Firma:** `def UnregisterClasses()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterInfoClasses()` (Línea 522)
- **Propósito:** Sin docstring.
- **Firma:** `def UnregisterInfoClasses()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ReExecuteElevated(flags)` (Línea 535)
- **Propósito:** Sin docstring.
- **Firma:** `def ReExecuteElevated(flags)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UseCommandLine()` (Línea 625)
- **Propósito:** Sin docstring.
- **Firma:** `def UseCommandLine()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterPyComCategory()` (Línea 646)
- **Propósito:** Register the Python COM Server component category.
- **Firma:** `def RegisterPyComCategory()`
- **Retorno / Efectos:** Consulta código fuente.
