# Documentación Técnica: `venv_build/Lib/site-packages/altgraph/Graph.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/altgraph/Graph.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 682
- **Fecha de inspección:** 2026-08-05 18:36:07

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `Graph` (Línea 21)
- **Docstring:** _The Graph class represents a directed graph with *N* nodes and *E* edges.

Naming conventions:

- the prefixes such as *out*, *inc* and *all* will refer to methods
  that operate on the outgoing, incoming or all edges of that node.

  For example: :py:meth:`inc_degree` will refer to the degree of the node
  computed over the incoming edges (the number of neighbours linking to
  the node).

- the prefixes such as *forw* and *back* will refer to the
  orientation of the edges used in the method with respect to the node.

  For example: :py:meth:`forw_bfs` will start at the node then use the
  outgoing edges to traverse the graph (goes forward)._
- **Métodos:**
  - `def __init__(self, edges)` (Línea 41): Initialization
  - `def __repr__(self)` (Línea 61): Sin docstring.
  - `def add_node(self, node, node_data)` (Línea 67): Adds a new node to the graph.  Arbitrary data can be attached to the
node via the node_data parameter.  Adding the same node twice will be
silently ignored.

The node must be a hashable value.
  - `def add_edge(self, head_id, tail_id, edge_data, create_nodes)` (Línea 89): Adds a directed edge going from head_id to tail_id.
Arbitrary data can be attached to the edge via edge_data.
It may create the nodes if adding edges between nonexisting ones.

:param head_id: head node
:param tail_id: tail node
:param edge_data: (optional) data attached to the edge
:param create_nodes: (optional) creates the head_id or tail_id
    node in case they did not exist
  - `def hide_edge(self, edge)` (Línea 124): Hides an edge from the graph. The edge may be unhidden at some later
time.
  - `def hide_node(self, node)` (Línea 137): Hides a node from the graph.  The incoming and outgoing edges of the
node will also be hidden.  The node may be unhidden at some later time.
  - `def restore_node(self, node)` (Línea 151): Restores a previously hidden node back into the graph and restores
all of its incoming and outgoing edges.
  - `def restore_edge(self, edge)` (Línea 164): Restores a previously hidden edge back into the graph.
  - `def restore_all_edges(self)` (Línea 177): Restores all hidden edges.
  - `def restore_all_nodes(self)` (Línea 187): Restores all hidden nodes.
  - `def __contains__(self, node)` (Línea 194): Test whether a node is in the graph
  - `def edge_by_id(self, edge)` (Línea 200): Returns the edge that connects the head_id and tail_id nodes
  - `def edge_by_node(self, head, tail)` (Línea 212): Returns the edge that connects the head_id and tail_id nodes
  - `def number_of_nodes(self)` (Línea 221): Returns the number of nodes
  - `def number_of_edges(self)` (Línea 227): Returns the number of edges
  - `def __iter__(self)` (Línea 233): Iterates over all nodes in the graph
  - `def node_list(self)` (Línea 239): Return a list of the node ids for all visible nodes in the graph.
  - `def edge_list(self)` (Línea 245): Returns an iterator for all visible nodes in the graph.
  - `def number_of_hidden_edges(self)` (Línea 251): Returns the number of hidden edges
  - `def number_of_hidden_nodes(self)` (Línea 257): Returns the number of hidden nodes
  - `def hidden_node_list(self)` (Línea 263): Returns the list with the hidden nodes
  - `def hidden_edge_list(self)` (Línea 269): Returns a list with the hidden edges
  - `def describe_node(self, node)` (Línea 275): return node, node data, outgoing edges, incoming edges for node
  - `def describe_edge(self, edge)` (Línea 282): return edge, edge data, head, tail for edge
  - `def node_data(self, node)` (Línea 289): Returns the data associated with a node
  - `def edge_data(self, edge)` (Línea 295): Returns the data associated with an edge
  - `def update_edge_data(self, edge, edge_data)` (Línea 301): Replace the edge data for a specific edge
  - `def head(self, edge)` (Línea 307): Returns the node of the head of the edge.
  - `def tail(self, edge)` (Línea 313): Returns node of the tail of the edge.
  - `def out_nbrs(self, node)` (Línea 319): List of nodes connected by outgoing edges
  - `def inc_nbrs(self, node)` (Línea 325): List of nodes connected by incoming edges
  - `def all_nbrs(self, node)` (Línea 331): List of nodes connected by incoming and outgoing edges
  - `def out_edges(self, node)` (Línea 337): Returns a list of the outgoing edges
  - `def inc_edges(self, node)` (Línea 346): Returns a list of the incoming edges
  - `def all_edges(self, node)` (Línea 355): Returns a list of incoming and outging edges.
  - `def out_degree(self, node)` (Línea 361): Returns the number of outgoing edges
  - `def inc_degree(self, node)` (Línea 367): Returns the number of incoming edges
  - `def all_degree(self, node)` (Línea 373): The total degree of a node
  - `def _topo_sort(self, forward)` (Línea 379): Topological sort.

Returns a list of nodes where the successors (based on outgoing and
incoming edges selected by the forward parameter) of any given node
appear in the sequence after that node.
  - `def forw_topo_sort(self)` (Línea 426): Topological sort.

Returns a list of nodes where the successors (based on outgoing edges)
of any given node appear in the sequence after that node.
  - `def back_topo_sort(self)` (Línea 435): Reverse topological sort.

Returns a list of nodes where the successors (based on incoming edges)
of any given node appear in the sequence after that node.
  - `def _bfs_subgraph(self, start_id, forward)` (Línea 444): Private method creates a subgraph in a bfs order.

The forward parameter specifies whether it is a forward or backward
traversal.
  - `def forw_bfs_subgraph(self, start_id)` (Línea 472): Creates and returns a subgraph consisting of the breadth first
reachable nodes based on their outgoing edges.
  - `def back_bfs_subgraph(self, start_id)` (Línea 479): Creates and returns a subgraph consisting of the breadth first
reachable nodes based on the incoming edges.
  - `def iterdfs(self, start, end, forward)` (Línea 486): Collecting nodes in some depth first traversal.

The forward parameter specifies whether it is a forward or backward
traversal.
  - `def iterdata(self, start, end, forward, condition)` (Línea 513): Perform a depth-first walk of the graph (as ``iterdfs``)
and yield the item data of every node where condition matches. The
condition callback is only called when node_data is not None.
  - `def _iterbfs(self, start, end, forward)` (Línea 546): The forward parameter specifies whether it is a forward or backward
traversal.  Returns a list of tuples where the first value is the hop
value the second value is the node id.
  - `def forw_bfs(self, start, end)` (Línea 573): Returns a list of nodes in some forward BFS order.

Starting from the start node the breadth first search proceeds along
outgoing edges.
  - `def back_bfs(self, start, end)` (Línea 582): Returns a list of nodes in some backward BFS order.

Starting from the start node the breadth first search proceeds along
incoming edges.
  - `def forw_dfs(self, start, end)` (Línea 591): Returns a list of nodes in some forward DFS order.

Starting with the start node the depth first search proceeds along
outgoing edges.
  - `def back_dfs(self, start, end)` (Línea 600): Returns a list of nodes in some backward DFS order.

Starting from the start node the depth first search proceeds along
incoming edges.
  - `def connected(self)` (Línea 609): Returns :py:data:`True` if the graph's every node can be reached from
every other node.
  - `def clust_coef(self, node)` (Línea 621): Computes and returns the local clustering coefficient of node.

The local cluster coefficient is proportion of the actual number of
edges between neighbours of node and the maximum number of edges
between those neighbours.

See "Local Clustering Coefficient" on
<http://en.wikipedia.org/wiki/Clustering_coefficient>
for a formal definition.
  - `def get_hops(self, start, end, forward)` (Línea 652): Computes the hop distance to all nodes centered around a node.

First order neighbours are at hop 1, their neigbours are at hop 2 etc.
Uses :py:meth:`forw_bfs` or :py:meth:`back_bfs` depending on the value
of the forward parameter.  If the distance between all neighbouring
nodes is 1 the hop number corresponds to the shortest distance between
the nodes.

:param start: the starting node
:param end: ending node (optional). When not specified will search the
    whole graph.
:param forward: directionality parameter (optional).
    If C{True} (default) it uses L{forw_bfs} otherwise L{back_bfs}.
:return: returns a list of tuples where each tuple contains the
    node and the hop.

Typical usage::

    >>> print (graph.get_hops(1, 8))
    >>> [(1, 0), (2, 1), (3, 1), (4, 2), (5, 3), (7, 4), (8, 5)]
    # node 1 is at 0 hops
    # node 2 is at 1 hop
    # ...
    # node 8 is at 5 hops
