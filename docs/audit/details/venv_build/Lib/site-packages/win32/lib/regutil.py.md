# Documentación Técnica: `venv_build/Lib/site-packages/win32/lib/regutil.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/win32/lib/regutil.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 385
- **Fecha de inspección:** 2026-08-05 18:36:40

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def BuildDefaultPythonKey()` (Línea 15)
- **Propósito:** Builds a string containing the path to the current registry key.

The Python registry key contains the Python version.  This function
uses the version of the DLL used by the current process to get the
registry key currently in use.
- **Firma:** `def BuildDefaultPythonKey()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRootKey()` (Línea 25)
- **Propósito:** Retrieves the Registry root in use by Python.
- **Firma:** `def GetRootKey()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRegistryDefaultValue(subkey, rootkey)` (Línea 36)
- **Propósito:** A helper to return the default value for a key in the registry.
- **Firma:** `def GetRegistryDefaultValue(subkey, rootkey)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def SetRegistryDefaultValue(subKey, value, rootkey)` (Línea 43)
- **Propósito:** A helper to set the default value for a key in the registry
- **Firma:** `def SetRegistryDefaultValue(subKey, value, rootkey)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetAppPathsKey()` (Línea 57)
- **Propósito:** Sin docstring.
- **Firma:** `def GetAppPathsKey()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterPythonExe(exeFullPath, exeAlias, exeAppPath)` (Línea 61)
- **Propósito:** Register a .exe file that uses Python.

Registers the .exe with the OS.  This allows the specified .exe to
be run from the command-line or start button without using the full path,
and also to setup application specific path (ie, os.environ['PATH']).

Currently the exeAppPath is not supported, so this function is general
purpose, and not specific to Python at all.  Later, exeAppPath may provide
a reasonable default that is used.

exeFullPath -- The full path to the .exe
exeAlias = None -- An alias for the exe - if none, the base portion
          of the filename is used.
exeAppPath -- Not supported.
- **Firma:** `def RegisterPythonExe(exeFullPath, exeAlias, exeAppPath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRegisteredExe(exeAlias)` (Línea 87)
- **Propósito:** Get a registered .exe
- **Firma:** `def GetRegisteredExe(exeAlias)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterPythonExe(exeAlias)` (Línea 92)
- **Propósito:** Unregister a .exe file that uses Python.
- **Firma:** `def UnregisterPythonExe(exeAlias)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterNamedPath(name, path)` (Línea 104)
- **Propósito:** Register a named path - ie, a named PythonPath entry.
- **Firma:** `def RegisterNamedPath(name, path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterNamedPath(name)` (Línea 112)
- **Propósito:** Unregister a named path - ie, a named PythonPath entry.
- **Firma:** `def UnregisterNamedPath(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRegisteredNamedPath(name)` (Línea 125)
- **Propósito:** Get a registered named path, or None if it doesn't exist.
- **Firma:** `def GetRegisteredNamedPath(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterModule(modName, modPath)` (Línea 140)
- **Propósito:** Register an explicit module in the registry.  This forces the Python import
mechanism to locate this module directly, without a sys.path search.  Thus
a registered module need not appear in sys.path at all.

modName -- The name of the module, as used by import.
modPath -- The full path and file name of the module.
- **Firma:** `def RegisterModule(modName, modPath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterModule(modName)` (Línea 158)
- **Propósito:** Unregister an explicit module in the registry.

modName -- The name of the module, as used by import.
- **Firma:** `def UnregisterModule(modName)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetRegisteredHelpFile(helpDesc)` (Línea 174)
- **Propósito:** Given a description, return the registered entry.
- **Firma:** `def GetRegisteredHelpFile(helpDesc)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterHelpFile(helpFile, helpPath, helpDesc, bCheckFile)` (Línea 189)
- **Propósito:** Register a help file in the registry.

  Note that this used to support writing to the Windows Help
  key, however this is no longer done, as it seems to be incompatible.

helpFile -- the base name of the help file.
helpPath -- the path to the help file
helpDesc -- A description for the help file.  If None, the helpFile param is used.
bCheckFile -- A flag indicating if the file existence should be checked.
- **Firma:** `def RegisterHelpFile(helpFile, helpPath, helpDesc, bCheckFile)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def UnregisterHelpFile(helpFile, helpDesc)` (Línea 214)
- **Propósito:** Unregister a help file in the registry.

helpFile -- the base name of the help file.
helpDesc -- A description for the help file.  If None, the helpFile param is used.
- **Firma:** `def UnregisterHelpFile(helpFile, helpDesc)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterCoreDLL(coredllName)` (Línea 251)
- **Propósito:** Registers the core DLL in the registry.

If no params are passed, the name of the Python DLL used in
the current process is used and registered.
- **Firma:** `def RegisterCoreDLL(coredllName)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterFileExtensions(defPyIcon, defPycIcon, runCommand)` (Línea 277)
- **Propósito:** Register the core Python file extensions.

defPyIcon -- The default icon to use for .py files, in 'fname,offset' format.
defPycIcon -- The default icon to use for .pyc files, in 'fname,offset' format.
runCommand -- The command line to use for running .py files
- **Firma:** `def RegisterFileExtensions(defPyIcon, defPycIcon, runCommand)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterShellCommand(shellCommand, exeCommand, shellUserCommand)` (Línea 344)
- **Propósito:** Sin docstring.
- **Firma:** `def RegisterShellCommand(shellCommand, exeCommand, shellUserCommand)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def RegisterDDECommand(shellCommand, ddeApp, ddeTopic, ddeCommand)` (Línea 366)
- **Propósito:** Sin docstring.
- **Firma:** `def RegisterDDECommand(shellCommand, ddeApp, ddeTopic, ddeCommand)`
- **Retorno / Efectos:** Consulta código fuente.
