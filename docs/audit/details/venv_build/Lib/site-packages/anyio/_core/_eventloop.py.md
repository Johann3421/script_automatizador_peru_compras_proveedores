# Documentación Técnica: `venv_build/Lib/site-packages/anyio/_core/_eventloop.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/anyio/_core/_eventloop.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 240
- **Fecha de inspección:** 2026-08-05 18:36:07

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def run(func)` (Línea 38)
- **Propósito:** Run the given coroutine function in an asynchronous event loop.

The current thread must not be already running an event loop.

:param func: a coroutine function
:param args: positional arguments to ``func``
:param backend: name of the asynchronous event loop implementation – currently
    either ``asyncio`` or ``trio``
:param backend_options: keyword arguments to call the backend ``run()``
    implementation with (documented :ref:`here <backend options>`)
:return: the return value of the coroutine function
:raises RuntimeError: if an asynchronous event loop is already running in this
    thread
:raises LookupError: if the named backend is not found
- **Firma:** `def run(func)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sleep(delay)` (Línea 88)
- **Propósito:** Pause the current task for the specified duration.

:param delay: the duration, in seconds
- **Firma:** `def sleep(delay)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sleep_forever()` (Línea 98)
- **Propósito:** Pause the current task until it's cancelled.

This is a shortcut for ``sleep(math.inf)``.

.. versionadded:: 3.1
- **Firma:** `def sleep_forever()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sleep_until(deadline)` (Línea 110)
- **Propósito:** Pause the current task until the given time.

:param deadline: the absolute time to wake up at (according to the internal
    monotonic clock of the event loop)

.. versionadded:: 3.1
- **Firma:** `def sleep_until(deadline)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def current_time()` (Línea 124)
- **Propósito:** Return the current value of the event loop's internal clock.

:return: the clock value (seconds)
:raises NoEventLoopError: if no supported asynchronous event loop is running in the
    current thread
- **Firma:** `def current_time()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_all_backends()` (Línea 136)
- **Propósito:** Return a tuple of the names of all built-in backends.
- **Firma:** `def get_all_backends()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_available_backends()` (Línea 141)
- **Propósito:** Test for the availability of built-in backends.

:return a tuple of the built-in backend names that were successfully imported

.. versionadded:: 4.12
- **Firma:** `def get_available_backends()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_cancelled_exc_class()` (Línea 162)
- **Propósito:** Return the current async library's cancellation exception class.

:raises NoEventLoopError: if no supported asynchronous event loop is running in the
    current thread
- **Firma:** `def get_cancelled_exc_class()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def claim_worker_thread(backend_class, token)` (Línea 179)
- **Propósito:** Sin docstring.
- **Firma:** `def claim_worker_thread(backend_class, token)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_async_backend(asynclib_name)` (Línea 191)
- **Propósito:** Sin docstring.
- **Firma:** `def get_async_backend(asynclib_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def current_async_library()` (Línea 211)
- **Propósito:** Sin docstring.
- **Firma:** `def current_async_library()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_current_async_library(asynclib_name)` (Línea 230)
- **Propósito:** Sin docstring.
- **Firma:** `def set_current_async_library(asynclib_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def reset_current_async_library(token)` (Línea 238)
- **Propósito:** Sin docstring.
- **Firma:** `def reset_current_async_library(token)`
- **Retorno / Efectos:** Consulta código fuente.
