# Documentación Técnica: `venv_build/Lib/site-packages/packaging/version.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/packaging/version.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1231
- **Fecha de inspección:** 2026-08-05 18:36:15

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_VersionReplace` (Línea 81)
- **Docstring:** _Sin docstring._

#### Clase `InvalidVersion` (Línea 125)
- **Docstring:** _Raised when a version string is not a valid version.

>>> Version("invalid")
Traceback (most recent call last):
    ...
packaging.version.InvalidVersion: Invalid version: 'invalid'_

#### Clase `_BaseVersion` (Línea 135)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __hash__(self)` (Línea 146): Sin docstring.
  - `def __lt__(self, other)` (Línea 152): Sin docstring.
  - `def __le__(self, other)` (Línea 158): Sin docstring.
  - `def __eq__(self, other)` (Línea 164): Sin docstring.
  - `def __ge__(self, other)` (Línea 170): Sin docstring.
  - `def __gt__(self, other)` (Línea 176): Sin docstring.
  - `def __ne__(self, other)` (Línea 182): Sin docstring.

#### Clase `_Version` (Línea 327)
- **Docstring:** _Sin docstring._

#### Clase `Version` (Línea 336)
- **Docstring:** _This class abstracts handling of a project's versions.

A :class:`Version` instance is comparison aware and can be compared and
sorted using the standard Python interfaces.

>>> v1 = Version("1.0a5")
>>> v2 = Version("1.0")
>>> v1
<Version('1.0a5')>
>>> v2
<Version('1.0')>
>>> v1 < v2
True
>>> v1 == v2
False
>>> v1 > v2
False
>>> v1 >= v2
False
>>> v1 <= v2
True

:class:`Version` is immutable; use :meth:`__replace__` to change
part of a version.

Instances are safe to serialize with :mod:`pickle`. They use a stable
format so the same pickle can be loaded in future packaging releases.

.. versionchanged:: 26.2

    Added a stable pickle format. Pickles created with packaging 26.2+ can
    be unpickled with future releases.  Backward compatibility with pickles
    from packaging < 26.2 is supported but may be removed in a future
    release._
- **Métodos:**
  - `def __init__(self, version)` (Línea 404): Initialize a Version object.

:param version:
    The string representation of a version which will be parsed and normalized
    before use.
:raises InvalidVersion:
    If the ``version`` does not conform to PEP 440 in any way then this
    exception will be raised.
  - `def from_parts(cls)` (Línea 455): Return a new version composed of the various parts.

This allows you to build a version without going though a string and
running a regular expression. It normalizes pre-release strings. The
``release=`` keyword argument is required.

>>> Version.from_parts(release=(1,2,3))
<Version('1.2.3')>
>>> Version.from_parts(release=(0,1,0), pre=("b", 1))
<Version('0.1.0b1')>

:param epoch:
:param release: This version tuple is required

.. versionadded:: 26.1
  - `def __replace__(self)` (Línea 501): __replace__(*, epoch=..., release=..., pre=..., post=..., dev=..., local=...)

Return a new version with parts replaced.

This returns a new version (unless no parts were changed). The
pre-release is normalized. Setting a value to ``None`` clears it.

>>> v = Version("1.2.3")
>>> v.__replace__(pre=("a", 1))
<Version('1.2.3a1')>

:param int | None epoch:
:param tuple[int, ...] | None release:
:param tuple[str, int] | None pre:
:param int | None post:
:param int | None dev:
:param str | None local:

.. versionadded:: 26.0
.. versionchanged:: 26.1

   The pre-release portion is now normalized.
  - `def _key(self)` (Línea 560): Sin docstring.
  - `def __hash__(self)` (Línea 574): Sin docstring.
  - `def __lt__(self, other)` (Línea 592): Sin docstring.
  - `def __le__(self, other)` (Línea 619): Sin docstring.
  - `def __eq__(self, other)` (Línea 646): Sin docstring.
  - `def __ge__(self, other)` (Línea 673): Sin docstring.
  - `def __gt__(self, other)` (Línea 700): Sin docstring.
  - `def __ne__(self, other)` (Línea 727): Sin docstring.
  - `def __getstate__(self)` (Línea 754): Sin docstring.
  - `def __setstate__(self, state)` (Línea 776): Sin docstring.
  - `def _version(self)` (Línea 823): Sin docstring.
  - `def _version(self, value)` (Línea 830): Sin docstring.
  - `def __repr__(self)` (Línea 840): A representation of the Version that shows all internal state.

>>> Version('1.0.0')
<Version('1.0.0')>
  - `def __str__(self)` (Línea 848): A string representation of the version that can be round-tripped.

>>> str(Version("1.0a5"))
'1.0a5'
  - `def _str(self)` (Línea 880): Internal property for match_args
  - `def epoch(self)` (Línea 885): The epoch of the version.

>>> Version("2.0.0").epoch
0
>>> Version("1!2.0.0").epoch
1
  - `def release(self)` (Línea 896): The components of the "release" segment of the version.

>>> Version("1.2.3").release
(1, 2, 3)
>>> Version("2.0.0").release
(2, 0, 0)
>>> Version("1!2.0.0.post0").release
(2, 0, 0)

Includes trailing zeroes but not the epoch or any pre-release / development /
post-release suffixes.
  - `def pre(self)` (Línea 912): The pre-release segment of the version.

>>> print(Version("1.2.3").pre)
None
>>> Version("1.2.3a1").pre
('a', 1)
>>> Version("1.2.3b1").pre
('b', 1)
>>> Version("1.2.3rc1").pre
('rc', 1)
  - `def post(self)` (Línea 927): The post-release number of the version.

>>> print(Version("1.2.3").post)
None
>>> Version("1.2.3.post1").post
1
  - `def dev(self)` (Línea 938): The development number of the version.

>>> print(Version("1.2.3").dev)
None
>>> Version("1.2.3.dev1").dev
1
  - `def local(self)` (Línea 949): The local version segment of the version.

>>> print(Version("1.2.3").local)
None
>>> Version("1.2.3+abc").local
'abc'
  - `def public(self)` (Línea 963): The public portion of the version.

This returns a string. If you want a :class:`Version` again and care
about performance, use ``v.__replace__(local=None)`` instead.

>>> Version("1.2.3").public
'1.2.3'
>>> Version("1.2.3+abc").public
'1.2.3'
>>> Version("1!1.2.3dev1+abc").public
'1!1.2.3.dev1'
  - `def base_version(self)` (Línea 979): The "base version" of the version.

This returns a string. If you want a :class:`Version` again and care
about performance, use
``v.__replace__(pre=None, post=None, dev=None, local=None)`` instead.

>>> Version("1.2.3").base_version
'1.2.3'
>>> Version("1.2.3+abc").base_version
'1.2.3'
>>> Version("1!1.2.3dev1+abc").base_version
'1!1.2.3'

The "base version" is the public version of the project without any pre or post
release markers.
  - `def is_prerelease(self)` (Línea 1000): Whether this version is a pre-release.

>>> Version("1.2.3").is_prerelease
False
>>> Version("1.2.3a1").is_prerelease
True
>>> Version("1.2.3b1").is_prerelease
True
>>> Version("1.2.3rc1").is_prerelease
True
>>> Version("1.2.3dev1").is_prerelease
True
  - `def is_postrelease(self)` (Línea 1017): Whether this version is a post-release.

>>> Version("1.2.3").is_postrelease
False
>>> Version("1.2.3.post1").is_postrelease
True
  - `def is_devrelease(self)` (Línea 1028): Whether this version is a development release.

>>> Version("1.2.3").is_devrelease
False
>>> Version("1.2.3.dev1").is_devrelease
True
  - `def major(self)` (Línea 1039): The first item of :attr:`release` or ``0`` if unavailable.

>>> Version("1.2.3").major
1
  - `def minor(self)` (Línea 1048): The second item of :attr:`release` or ``0`` if unavailable.

>>> Version("1.2.3").minor
2
>>> Version("1").minor
0
  - `def micro(self)` (Línea 1059): The third item of :attr:`release` or ``0`` if unavailable.

>>> Version("1.2.3").micro
3
>>> Version("1").micro
0

#### Clase `_TrimmedRelease` (Línea 1070)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, version)` (Línea 1073): Sin docstring.
  - `def release(self)` (Línea 1086): Release segment without any trailing zeros.

>>> _TrimmedRelease('1.0.0').release
(1,)
>>> _TrimmedRelease('0.0').release
(0,)

### Funciones independientes:

#### `def __dir__()` (Línea 66)
- **Propósito:** Sin docstring.
- **Firma:** `def __dir__()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_pre()` (Línea 90)
- **Propósito:** Normalize the pre-release segment of a version string.

Returns a lowercase version of the string if not a known pre-release
identifier.

>>> normalize_pre('alpha')
'a'
>>> normalize_pre('BETA')
'b'
>>> normalize_pre('rc')
'rc'

:param letter:

.. versionadded:: 26.1
- **Firma:** `def normalize_pre()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse(version)` (Línea 111)
- **Propósito:** Parse the given version string.

This is identical to the :class:`Version` constructor.

>>> parse('1.0.dev1')
<Version('1.0.dev1')>

:param version: The version string to parse.
:raises InvalidVersion: When the version string is not a valid version.
- **Firma:** `def parse(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_epoch()` (Línea 266)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_epoch()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_release()` (Línea 274)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_release()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_pre()` (Línea 286)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_pre()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_post()` (Línea 299)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_post()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_dev()` (Línea 308)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_dev()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_local()` (Línea 317)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_local()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _parse_letter_version(letter, number)` (Línea 1104)
- **Propósito:** Sin docstring.
- **Firma:** `def _parse_letter_version(letter, number)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _parse_local_version(local)` (Línea 1131)
- **Propósito:** Takes a string like ``"abc.1.twelve"`` and turns it into
``("abc", 1, "twelve")``.
- **Firma:** `def _parse_local_version(local)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _cmpkey(epoch, release, pre, post, dev, local)` (Línea 1157)
- **Propósito:** Build a comparison key for PEP 440 ordering.

Returns ``(epoch, release, suffix)`` or
``(epoch, release, suffix, local)`` so that plain tuple
comparison gives the correct order.

Trailing zeros are stripped from the release so that ``1.0.0 == 1``.

The suffix is a flat 6-int tuple that encodes pre/post/dev:
``(pre_rank, pre_n, post_rank, post_n, dev_rank, dev_n)``

pre_rank: dev-only=-1, a=0, b=1, rc=2, no-pre=3
    Dev-only releases (no pre or post) get -1 so they sort before
    any alpha/beta/rc.  Releases without a pre-release tag get 3
    so they sort after rc.
post_rank: no-post=0, post=1
    Releases without a post segment sort before those with one.
dev_rank: dev=0, no-dev=1
    Releases without a dev segment sort after those with one.

Local segments use ``(n, "")`` for ints and ``(-1, s)`` for strings,
following PEP 440: strings sort before ints, strings compare
lexicographically, ints compare numerically, and shorter segments
sort before longer when prefixes match.  Versions without a local
segment sort before those with one (3-tuple < 4-tuple).

>>> _cmpkey(0, (1, 0, 0), None, None, None, None)
(0, (1,), (3, 0, 0, 0, 1, 0))
>>> _cmpkey(0, (1,), ("a", 1), None, None, None)
(0, (1,), (0, 1, 0, 0, 1, 0))
>>> _cmpkey(0, (1,), None, None, None, ("ubuntu", 1))
(0, (1,), (3, 0, 0, 0, 1, 0), ((-1, 'ubuntu'), (1, '')))
- **Firma:** `def _cmpkey(epoch, release, pre, post, dev, local)`
- **Retorno / Efectos:** Consulta código fuente.
