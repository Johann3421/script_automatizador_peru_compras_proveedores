# Documentación Técnica: `venv_build/Lib/site-packages/packaging/specifiers.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/packaging/specifiers.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1943
- **Fecha de inspección:** 2026-08-05 18:36:15

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_BoundaryKind` (Línea 82)
- **Docstring:** _Where a boundary marker sits in the version ordering._

#### Clase `_BoundaryVersion` (Línea 90)
- **Docstring:** _A point on the version line between two real PEP 440 versions.

Some specifier semantics imply boundaries between real versions:
``<=1.0`` includes ``1.0+local`` and ``>1.0`` excludes
``1.0.post0``.  No real :class:`Version` falls on those boundaries,
so this class creates values that sort between the real versions
on either side.

Two kinds exist, shown relative to a base version V::

    V < V+local < AFTER_LOCALS(V) < V.post0 < AFTER_POSTS(V)

``AFTER_LOCALS`` sits after V and every V+local, but before
V.post0.  Upper bound of ``<=V``, ``==V``, ``!=V``.

``AFTER_POSTS`` sits after every V.postN, but before the next
release segment.  Lower bound of ``>V`` (final or pre-release V)
to exclude post-releases per PEP 440._
- **Métodos:**
  - `def __init__(self, version, kind)` (Línea 113): Sin docstring.
  - `def _is_family(self, other)` (Línea 118): Is ``other`` a version that this boundary sorts above?
  - `def __eq__(self, other)` (Línea 133): Sin docstring.
  - `def __lt__(self, other)` (Línea 138): Sin docstring.
  - `def __hash__(self)` (Línea 145): Sin docstring.
  - `def __repr__(self)` (Línea 148): Sin docstring.

#### Clase `_LowerBound` (Línea 153)
- **Docstring:** _Lower bound of a version range.

A version *v* of ``None`` means unbounded below (-inf).
At equal versions, ``[v`` sorts before ``(v`` because an inclusive
bound starts earlier._
- **Métodos:**
  - `def __init__(self, version, inclusive)` (Línea 163): Sin docstring.
  - `def __eq__(self, other)` (Línea 167): Sin docstring.
  - `def __lt__(self, other)` (Línea 172): Sin docstring.
  - `def __hash__(self)` (Línea 185): Sin docstring.
  - `def __repr__(self)` (Línea 188): Sin docstring.

#### Clase `_UpperBound` (Línea 194)
- **Docstring:** _Upper bound of a version range.

A version *v* of ``None`` means unbounded above (+inf).
At equal versions, ``v)`` sorts before ``v]`` because an exclusive
bound ends earlier._
- **Métodos:**
  - `def __init__(self, version, inclusive)` (Línea 204): Sin docstring.
  - `def __eq__(self, other)` (Línea 208): Sin docstring.
  - `def __lt__(self, other)` (Línea 213): Sin docstring.
  - `def __hash__(self)` (Línea 226): Sin docstring.
  - `def __repr__(self)` (Línea 229): Sin docstring.

#### Clase `InvalidSpecifier` (Línea 344)
- **Docstring:** _Raised when attempting to create a :class:`Specifier` with a specifier
string that is invalid.

>>> Specifier("lolwat")
Traceback (most recent call last):
    ...
packaging.specifiers.InvalidSpecifier: Invalid specifier: 'lolwat'_

#### Clase `BaseSpecifier` (Línea 356)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _str(self)` (Línea 361): Internal property for match_args
  - `def __str__(self)` (Línea 366): Returns the str representation of this Specifier-like object. This
should be representative of the Specifier itself.
  - `def __hash__(self)` (Línea 373): Returns a hash value for this Specifier-like object.
  - `def __eq__(self, other)` (Línea 379): Returns a boolean representing whether or not the two Specifier-like
objects are equal.

:param other: The other object to check against.
  - `def prereleases(self)` (Línea 389): Whether or not pre-releases as a whole are allowed.

This can be set to either ``True`` or ``False`` to explicitly enable or disable
prereleases or it can be set to ``None`` (the default) to use default semantics.
  - `def prereleases(self, value)` (Línea 397): Setter for :attr:`prereleases`.

:param value: The value to set.
  - `def contains(self, item, prereleases)` (Línea 404): Determines if the given item is contained within this specifier.
  - `def filter(self, iterable, prereleases, key)` (Línea 410): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 418): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 426): Takes an iterable of items and filters them so that only items which
are contained within this specifier are allowed in it.

#### Clase `Specifier` (Línea 438)
- **Docstring:** _This class abstracts handling of version specifiers.

.. tip::

    It is generally not required to instantiate this manually. You should instead
    prefer to work with :class:`SpecifierSet` instead, which can parse
    comma-separated version specifiers (which is what package metadata contains).

Instances are safe to serialize with :mod:`pickle`. They use a stable
format so the same pickle can be loaded in future packaging releases.

.. versionchanged:: 26.2

    Added a stable pickle format. Pickles created with packaging 26.2+ can
    be unpickled with future releases.  Backward compatibility with pickles
    from packaging < 26.2 is supported but may be removed in a future
    release._
- **Métodos:**
  - `def __init__(self, spec, prereleases)` (Línea 573): Initialize a Specifier instance.

:param spec:
    The string representation of a specifier which will be parsed and
    normalized before use.
:param prereleases:
    This tells the specifier if it should accept prerelease versions if
    applicable or not. The default of ``None`` will autodetect it from the
    given specifiers.
:raises InvalidSpecifier:
    If the given specifier is invalid (i.e. bad syntax).
  - `def _get_spec_version(self, version)` (Línea 611): One element cache, as only one spec Version is needed per Specifier.
  - `def _require_spec_version(self, version)` (Línea 623): Get spec version, asserting it's valid (not for === operator).

This method should only be called for operators where version
strings are guaranteed to be valid PEP 440 versions (not ===).
  - `def _to_ranges(self)` (Línea 633): Convert this specifier to sorted, non-overlapping version ranges.

Each standard operator maps to one or two ranges.  ``===`` is
modeled as full range (actual check done separately).  Cached.
  - `def _wildcard_ranges(self, op, ver_str)` (Línea 657): Sin docstring.
  - `def _standard_ranges(self, op, ver_str)` (Línea 670): Sin docstring.
  - `def prereleases(self)` (Línea 733): Sin docstring.
  - `def prereleases(self, value)` (Línea 761): Sin docstring.
  - `def __getstate__(self)` (Línea 764): Sin docstring.
  - `def __setstate__(self, state)` (Línea 770): Sin docstring.
  - `def operator(self)` (Línea 805): The operator of this specifier.

>>> Specifier("==1.2.3").operator
'=='
  - `def version(self)` (Línea 814): The version of this specifier.

>>> Specifier("==1.2.3").version
'1.2.3'
  - `def __repr__(self)` (Línea 822): A representation of the Specifier that shows all internal state.

>>> Specifier('>=1.0.0')
<Specifier('>=1.0.0')>
>>> Specifier('>=1.0.0', prereleases=False)
<Specifier('>=1.0.0', prereleases=False)>
>>> Specifier('>=1.0.0', prereleases=True)
<Specifier('>=1.0.0', prereleases=True)>
  - `def __str__(self)` (Línea 840): A string representation of the Specifier that can be round-tripped.

>>> str(Specifier('>=1.0.0'))
'>=1.0.0'
>>> str(Specifier('>=1.0.0', prereleases=False))
'>=1.0.0'
  - `def _canonical_spec(self)` (Línea 851): Sin docstring.
  - `def __hash__(self)` (Línea 864): Sin docstring.
  - `def __eq__(self, other)` (Línea 867): Whether or not the two Specifier-like objects are equal.

:param other: The other object to check against.

The value of :attr:`prereleases` is ignored.

>>> Specifier("==1.2.3") == Specifier("== 1.2.3.0")
True
>>> (Specifier("==1.2.3", prereleases=False) ==
...  Specifier("==1.2.3", prereleases=True))
True
>>> Specifier("==1.2.3") == "==1.2.3"
True
>>> Specifier("==1.2.3") == Specifier("==1.2.4")
False
>>> Specifier("==1.2.3") == Specifier("~=1.2.3")
False
  - `def _get_operator(self, op)` (Línea 896): Sin docstring.
  - `def _compare_compatible(self, prospective, spec)` (Línea 902): Sin docstring.
  - `def _get_wildcard_split(self, spec)` (Línea 922): Cached split of a wildcard spec into components and numeric length.

>>> Specifier("==1.*")._get_wildcard_split("1.*")
(['0', '1'], 2)
>>> Specifier("==3.10.*")._get_wildcard_split("3.10.*")
(['0', '3', '10'], 3)
  - `def _compare_equal(self, prospective, spec)` (Línea 938): Sin docstring.
  - `def _compare_not_equal(self, prospective, spec)` (Línea 974): Sin docstring.
  - `def _compare_less_than_equal(self, prospective, spec)` (Línea 977): Sin docstring.
  - `def _compare_greater_than_equal(self, prospective, spec)` (Línea 983): Sin docstring.
  - `def _compare_less_than(self, prospective, spec_str)` (Línea 989): Sin docstring.
  - `def _compare_greater_than(self, prospective, spec_str)` (Línea 1014): Sin docstring.
  - `def _compare_arbitrary(self, prospective, spec)` (Línea 1046): Sin docstring.
  - `def __contains__(self, item)` (Línea 1049): Return whether or not the item is contained in this specifier.

:param item: The item to check for.

This is used for the ``in`` operator and behaves the same as
:meth:`contains` with no ``prereleases`` argument passed.

>>> "1.2.3" in Specifier(">=1.2.3")
True
>>> Version("1.2.3") in Specifier(">=1.2.3")
True
>>> "1.0.0" in Specifier(">=1.2.3")
False
>>> "1.3.0a1" in Specifier(">=1.2.3")
True
>>> "1.3.0a1" in Specifier(">=1.2.3", prereleases=True)
True
  - `def contains(self, item, prereleases)` (Línea 1070): Return whether or not the item is contained in this specifier.

:param item:
    The item to check for, which can be a version string or a
    :class:`Version` instance.
:param prereleases:
    Whether or not to match prereleases with this Specifier. If set to
    ``None`` (the default), it will follow the recommendation from
    :pep:`440` and match prereleases, as there are no other versions.

>>> Specifier(">=1.2.3").contains("1.2.3")
True
>>> Specifier(">=1.2.3").contains(Version("1.2.3"))
True
>>> Specifier(">=1.2.3").contains("1.0.0")
False
>>> Specifier(">=1.2.3").contains("1.3.0a1")
True
>>> Specifier(">=1.2.3", prereleases=False).contains("1.3.0a1")
False
>>> Specifier(">=1.2.3").contains("1.3.0a1")
True
  - `def filter(self, iterable, prereleases, key)` (Línea 1098): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 1106): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 1113): Filter items in the given iterable, that match the specifier.

:param iterable:
    An iterable that can contain version strings and :class:`Version` instances.
    The items in the iterable will be filtered according to the specifier.
:param prereleases:
    Whether or not to allow prereleases in the returned iterator. If set to
    ``None`` (the default), it will follow the recommendation from :pep:`440`
    and match prereleases if there are no other versions.
:param key:
    A callable that takes a single argument (an item from the iterable) and
    returns a version string or :class:`Version` instance to be used for
    filtering.

>>> list(Specifier(">=1.2.3").filter(["1.2", "1.3", "1.5a1"]))
['1.3']
>>> list(Specifier(">=1.2.3").filter(["1.2", "1.2.3", "1.3", Version("1.4")]))
['1.2.3', '1.3', <Version('1.4')>]
>>> list(Specifier(">=1.2.3").filter(["1.2", "1.5a1"]))
['1.5a1']
>>> list(Specifier(">=1.2.3").filter(["1.3", "1.5a1"], prereleases=True))
['1.3', '1.5a1']
>>> list(Specifier(">=1.2.3", prereleases=True).filter(["1.3", "1.5a1"]))
['1.3', '1.5a1']
>>> list(Specifier(">=1.2.3").filter(
... [{"ver": "1.2"}, {"ver": "1.3"}],
... key=lambda x: x["ver"]))
[{'ver': '1.3'}]

#### Clase `SpecifierSet` (Línea 1334)
- **Docstring:** _This class abstracts handling of a set of version specifiers.

It can be passed a single specifier (``>=3.0``), a comma-separated list of
specifiers (``>=3.0,!=3.1``), or no specifier at all.

Instances are safe to serialize with :mod:`pickle`. They use a stable
format so the same pickle can be loaded in future packaging
releases.

.. versionchanged:: 26.2

    Added a stable pickle format. Pickles created with
    packaging 26.2+ can be unpickled with future releases.
    Backward compatibility with pickles from
    packaging < 26.2 is supported but may be removed in a future
    release._
- **Métodos:**
  - `def __init__(self, specifiers, prereleases)` (Línea 1362): Initialize a SpecifierSet instance.

:param specifiers:
    The string representation of a specifier or a comma-separated list of
    specifiers which will be parsed and normalized before use.
    May also be an iterable of ``Specifier`` instances, which will be used
    as is.
:param prereleases:
    This tells the SpecifierSet if it should accept prerelease versions if
    applicable or not. The default of ``None`` will autodetect it from the
    given specifiers.

:raises InvalidSpecifier:
    If the given ``specifiers`` are not parseable than this exception will be
    raised.
  - `def _canonical_specs(self)` (Línea 1407): Deduplicate, sort, and cache specs for order-sensitive operations.
  - `def prereleases(self)` (Línea 1417): Sin docstring.
  - `def prereleases(self, value)` (Línea 1437): Sin docstring.
  - `def __getstate__(self)` (Línea 1441): Sin docstring.
  - `def __setstate__(self, state)` (Línea 1447): Sin docstring.
  - `def __repr__(self)` (Línea 1504): A representation of the specifier set that shows all internal state.

Note that the ordering of the individual specifiers within the set may not
match the input string.

>>> SpecifierSet('>=1.0.0,!=2.0.0')
<SpecifierSet('!=2.0.0,>=1.0.0')>
>>> SpecifierSet('>=1.0.0,!=2.0.0', prereleases=False)
<SpecifierSet('!=2.0.0,>=1.0.0', prereleases=False)>
>>> SpecifierSet('>=1.0.0,!=2.0.0', prereleases=True)
<SpecifierSet('!=2.0.0,>=1.0.0', prereleases=True)>
  - `def __str__(self)` (Línea 1525): A string representation of the specifier set that can be round-tripped.

Note that the ordering of the individual specifiers within the set may not
match the input string.

>>> str(SpecifierSet(">=1.0.0,!=1.0.1"))
'!=1.0.1,>=1.0.0'
>>> str(SpecifierSet(">=1.0.0,!=1.0.1", prereleases=False))
'!=1.0.1,>=1.0.0'
  - `def __hash__(self)` (Línea 1538): Sin docstring.
  - `def __and__(self, other)` (Línea 1541): Return a SpecifierSet which is a combination of the two sets.

:param other: The other object to combine with.

>>> SpecifierSet(">=1.0.0,!=1.0.1") & '<=2.0.0,!=2.0.1'
<SpecifierSet('!=1.0.1,!=2.0.1,<=2.0.0,>=1.0.0')>
>>> SpecifierSet(">=1.0.0,!=1.0.1") & SpecifierSet('<=2.0.0,!=2.0.1')
<SpecifierSet('!=1.0.1,!=2.0.1,<=2.0.0,>=1.0.0')>
  - `def __eq__(self, other)` (Línea 1574): Whether or not the two SpecifierSet-like objects are equal.

:param other: The other object to check against.

The value of :attr:`prereleases` is ignored.

>>> SpecifierSet(">=1.0.0,!=1.0.1") == SpecifierSet(">=1.0.0,!=1.0.1")
True
>>> (SpecifierSet(">=1.0.0,!=1.0.1", prereleases=False) ==
...  SpecifierSet(">=1.0.0,!=1.0.1", prereleases=True))
True
>>> SpecifierSet(">=1.0.0,!=1.0.1") == ">=1.0.0,!=1.0.1"
True
>>> SpecifierSet(">=1.0.0,!=1.0.1") == SpecifierSet(">=1.0.0")
False
>>> SpecifierSet(">=1.0.0,!=1.0.1") == SpecifierSet(">=1.0.0,!=1.0.2")
False
  - `def __len__(self)` (Línea 1600): Returns the number of specifiers in this specifier set.
  - `def __iter__(self)` (Línea 1604): Returns an iterator over all the underlying :class:`Specifier` instances
in this specifier set.

>>> sorted(SpecifierSet(">=1.0.0,!=1.0.1"), key=str)
[<Specifier('!=1.0.1')>, <Specifier('>=1.0.0')>]
  - `def _get_ranges(self)` (Línea 1614): Intersect all specifiers into a single list of version ranges.

Returns an empty list when unsatisfiable.  ``===`` specs are
modeled as full range; string matching is checked separately
by :meth:`_check_arbitrary_unsatisfiable`.
  - `def is_unsatisfiable(self)` (Línea 1636): Check whether this specifier set can never be satisfied.

Returns True if no version can satisfy all specifiers simultaneously.

>>> SpecifierSet(">=2.0,<1.0").is_unsatisfiable()
True
>>> SpecifierSet(">=1.0,<2.0").is_unsatisfiable()
False
>>> SpecifierSet("").is_unsatisfiable()
False
>>> SpecifierSet("==1.0,!=1.0").is_unsatisfiable()
True
  - `def _check_prerelease_only_ranges(self)` (Línea 1669): With prereleases=False, check if every range contains only
pre-release versions (which would be excluded from matching).
  - `def _check_arbitrary_unsatisfiable(self)` (Línea 1682): Check === (arbitrary equality) specs for unsatisfiability.

=== uses case-insensitive string comparison, so the only candidate
that can match ``===V`` is the literal string V.  This method
checks whether that candidate is excluded by other specifiers.
  - `def __contains__(self, item)` (Línea 1721): Return whether or not the item is contained in this specifier.

:param item: The item to check for.

This is used for the ``in`` operator and behaves the same as
:meth:`contains` with no ``prereleases`` argument passed.

>>> "1.2.3" in SpecifierSet(">=1.0.0,!=1.0.1")
True
>>> Version("1.2.3") in SpecifierSet(">=1.0.0,!=1.0.1")
True
>>> "1.0.1" in SpecifierSet(">=1.0.0,!=1.0.1")
False
>>> "1.3.0a1" in SpecifierSet(">=1.0.0,!=1.0.1")
True
>>> "1.3.0a1" in SpecifierSet(">=1.0.0,!=1.0.1", prereleases=True)
True
  - `def contains(self, item, prereleases, installed)` (Línea 1742): Return whether or not the item is contained in this SpecifierSet.

:param item:
    The item to check for, which can be a version string or a
    :class:`Version` instance.
:param prereleases:
    Whether or not to match prereleases with this SpecifierSet. If set to
    ``None`` (the default), it will follow the recommendation from :pep:`440`
    and match prereleases, as there are no other versions.
:param installed:
    Whether or not the item is installed. If set to ``True``, it will
    accept prerelease versions even if the specifier does not allow them.

>>> SpecifierSet(">=1.0.0,!=1.0.1").contains("1.2.3")
True
>>> SpecifierSet(">=1.0.0,!=1.0.1").contains(Version("1.2.3"))
True
>>> SpecifierSet(">=1.0.0,!=1.0.1").contains("1.0.1")
False
>>> SpecifierSet(">=1.0.0,!=1.0.1").contains("1.3.0a1")
True
>>> SpecifierSet(">=1.0.0,!=1.0.1", prereleases=False).contains("1.3.0a1")
False
>>> SpecifierSet(">=1.0.0,!=1.0.1").contains("1.3.0a1", prereleases=True)
True
  - `def filter(self, iterable, prereleases, key)` (Línea 1788): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 1796): Sin docstring.
  - `def filter(self, iterable, prereleases, key)` (Línea 1803): Filter items in the given iterable, that match the specifiers in this set.

:param iterable:
    An iterable that can contain version strings and :class:`Version` instances.
    The items in the iterable will be filtered according to the specifier.
:param prereleases:
    Whether or not to allow prereleases in the returned iterator. If set to
    ``None`` (the default), it will follow the recommendation from :pep:`440`
    and match prereleases if there are no other versions.
:param key:
    A callable that takes a single argument (an item from the iterable) and
    returns a version string or :class:`Version` instance to be used for
    filtering.

>>> list(SpecifierSet(">=1.2.3").filter(["1.2", "1.3", "1.5a1"]))
['1.3']
>>> list(SpecifierSet(">=1.2.3").filter(["1.2", "1.3", Version("1.4")]))
['1.3', <Version('1.4')>]
>>> list(SpecifierSet(">=1.2.3").filter(["1.2", "1.5a1"]))
['1.5a1']
>>> list(SpecifierSet(">=1.2.3").filter(["1.3", "1.5a1"], prereleases=True))
['1.3', '1.5a1']
>>> list(SpecifierSet(">=1.2.3", prereleases=True).filter(["1.3", "1.5a1"]))
['1.3', '1.5a1']
>>> list(SpecifierSet(">=1.2.3").filter(
... [{"ver": "1.2"}, {"ver": "1.3"}],
... key=lambda x: x["ver"]))
[{'ver': '1.3'}]

An "empty" SpecifierSet will filter items based on the presence of prerelease
versions in the set.

>>> list(SpecifierSet("").filter(["1.3", "1.5a1"]))
['1.3']
>>> list(SpecifierSet("").filter(["1.5a1"]))
['1.5a1']
>>> list(SpecifierSet("", prereleases=True).filter(["1.3", "1.5a1"]))
['1.3', '1.5a1']
>>> list(SpecifierSet("").filter(["1.3", "1.5a1"], prereleases=True))
['1.3', '1.5a1']
  - `def _filter_versions(self, iterable, key, prereleases)` (Línea 1899): Filter versions against all specifiers in a single pass.

Uses Cost Based Ordering: specifiers are sorted by _operator_cost so
that cheap range operators reject versions early, avoiding expensive
wildcard or compatible operators on versions that would have been
rejected anyway.

### Funciones independientes:

#### `def __dir__()` (Línea 48)
- **Propósito:** Sin docstring.
- **Firma:** `def __dir__()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_spec()` (Línea 52)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_spec()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_pre()` (Línea 61)
- **Propósito:** Sin docstring.
- **Firma:** `def _validate_pre()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _trim_release(release)` (Línea 74)
- **Propósito:** Strip trailing zeros from a release tuple for normalized comparison.
- **Firma:** `def _trim_release(release)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _range_is_empty(lower, upper)` (Línea 246)
- **Propósito:** True when the range defined by *lower* and *upper* contains no versions.
- **Firma:** `def _range_is_empty(lower, upper)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _intersect_ranges(left, right)` (Línea 255)
- **Propósito:** Intersect two sorted, non-overlapping range lists (two-pointer merge).
- **Firma:** `def _intersect_ranges(left, right)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _next_prefix_dev0(version)` (Línea 281)
- **Propósito:** Smallest version in the next prefix: 1.2 -> 1.3.dev0.
- **Firma:** `def _next_prefix_dev0(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _base_dev0(version)` (Línea 287)
- **Propósito:** The .dev0 of a version's base release: 1.2 -> 1.2.dev0.
- **Firma:** `def _base_dev0(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _coerce_version(version)` (Línea 292)
- **Propósito:** Sin docstring.
- **Firma:** `def _coerce_version(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _public_version(version)` (Línea 301)
- **Propósito:** Sin docstring.
- **Firma:** `def _public_version(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _post_base(version)` (Línea 307)
- **Propósito:** The version that *version* is a post-release of.

1.0.post1 -> 1.0, 1.0a1.post0 -> 1.0a1, 1.0.post0.dev1 -> 1.0.
- **Firma:** `def _post_base(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _earliest_prerelease(version)` (Línea 315)
- **Propósito:** Earliest pre-release of *version*.

1.2 -> 1.2.dev0, 1.2.post1 -> 1.2.post1.dev0.
- **Firma:** `def _earliest_prerelease(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _nearest_non_prerelease(v)` (Línea 323)
- **Propósito:** Smallest non-pre-release version at or above *v*, or None.
- **Firma:** `def _nearest_non_prerelease(v)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _pep440_filter_prereleases(iterable, key)` (Línea 1198)
- **Propósito:** Filter per PEP 440: exclude prereleases unless no finals exist.
- **Firma:** `def _pep440_filter_prereleases(iterable, key)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _version_split(version)` (Línea 1240)
- **Propósito:** Split version into components.

The split components are intended for version comparison. The logic does
not attempt to retain the original version string, so joining the
components back with :func:`_version_join` may not produce the original
version string.
- **Firma:** `def _version_split(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _version_join(components)` (Línea 1262)
- **Propósito:** Join split version components into a version string.

This function assumes the input came from :func:`_version_split`, where the
first component must be the epoch (either empty or numeric), and all other
components numeric.
- **Firma:** `def _version_join(components)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_not_suffix(segment)` (Línea 1273)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_not_suffix(segment)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _numeric_prefix_len(split)` (Línea 1279)
- **Propósito:** Count leading numeric components in a :func:`_version_split` result.

>>> _numeric_prefix_len(["0", "1", "2", "a1"])
3
- **Firma:** `def _numeric_prefix_len(split)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _left_pad(split, target_numeric_len)` (Línea 1293)
- **Propósito:** Pad a :func:`_version_split` result with ``"0"`` segments to reach
``target_numeric_len`` numeric components.  Suffix segments are preserved.

>>> _left_pad(["0", "1", "a1"], 4)
['0', '1', '0', '0', 'a1']
- **Firma:** `def _left_pad(split, target_numeric_len)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _operator_cost(op_entry)` (Línea 1307)
- **Propósito:** Sort key for Cost Based Ordering of specifier operators in _filter_versions.

Operators run sequentially on a shrinking candidate set, so operators that
reject the most versions should run first to minimize work for later ones.

Tier 0: Exact equality (==, ===), likely to narrow candidates to one version
Tier 1: Range checks (>=, <=, >, <), cheap and usually reject a large portion
Tier 2: Wildcard equality (==.*) and compatible release (~=), more expensive
Tier 3: Exact !=, cheap but rarely rejects
Tier 4: Wildcard !=.*, expensive and rarely rejects
- **Firma:** `def _operator_cost(op_entry)`
- **Retorno / Efectos:** Consulta código fuente.
