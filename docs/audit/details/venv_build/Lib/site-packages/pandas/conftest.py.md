# Documentación Técnica: `venv_build/Lib/site-packages/pandas/conftest.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/conftest.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2159
- **Fecha de inspección:** 2026-08-05 18:36:15

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def pytest_addoption(parser)` (Línea 104)
- **Propósito:** Sin docstring.
- **Firma:** `def pytest_addoption(parser)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pytest_sessionstart(session)` (Línea 112)
- **Propósito:** Sin docstring.
- **Firma:** `def pytest_sessionstart(session)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ignore_doctest_warning(item, path, message)` (Línea 134)
- **Propósito:** Ignore doctest warning.

Parameters
----------
item : pytest.Item
    pytest test item.
path : str
    Module path to Python object, e.g. "pandas.DataFrame.append". A
    warning will be filtered when item.name ends with in given path. So it is
    sufficient to specify e.g. "DataFrame.append".
message : str
    Message to be filtered.
- **Firma:** `def ignore_doctest_warning(item, path, message)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pytest_collection_modifyitems(items, config)` (Línea 152)
- **Propósito:** Sin docstring.
- **Firma:** `def pytest_collection_modifyitems(items, config)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def add_doctest_imports(doctest_namespace)` (Línea 259)
- **Propósito:** Make `np` and `pd` names available for doctests.
- **Firma:** `def add_doctest_imports(doctest_namespace)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def configure_tests()` (Línea 268)
- **Propósito:** Configure settings for all tests and test modules.
- **Firma:** `def configure_tests()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def axis(request)` (Línea 279)
- **Propósito:** Fixture for returning the axis numbers of a DataFrame.
- **Firma:** `def axis(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def observed(request)` (Línea 287)
- **Propósito:** Pass in the observed keyword to groupby for [True, False]
This indicates whether categoricals should return values for
values which are not in the grouper [False / None], or only values which
appear in the grouper [True]. [None] is supported for future compatibility
if we decide to change the default (and would need to warn if this
parameter is not passed).
- **Firma:** `def observed(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ordered(request)` (Línea 300)
- **Propósito:** Boolean 'ordered' parameter for Categorical.
- **Firma:** `def ordered(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def dropna(request)` (Línea 308)
- **Propósito:** Boolean 'dropna' parameter.
- **Firma:** `def dropna(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sort(request)` (Línea 316)
- **Propósito:** Boolean 'sort' parameter.
- **Firma:** `def sort(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def skipna(request)` (Línea 324)
- **Propósito:** Boolean 'skipna' parameter.
- **Firma:** `def skipna(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def keep(request)` (Línea 332)
- **Propósito:** Valid values for the 'keep' parameter used in
.duplicated or .drop_duplicates
- **Firma:** `def keep(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def inclusive_endpoints_fixture(request)` (Línea 341)
- **Propósito:** Fixture for trying all interval 'inclusive' parameters.
- **Firma:** `def inclusive_endpoints_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def closed(request)` (Línea 349)
- **Propósito:** Fixture for trying all interval closed parameters.
- **Firma:** `def closed(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def other_closed(request)` (Línea 357)
- **Propósito:** Secondary closed fixture to allow parametrizing over all pairs of closed.
- **Firma:** `def other_closed(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def compression(request)` (Línea 375)
- **Propósito:** Fixture for trying common compression types in compression tests.
- **Firma:** `def compression(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def compression_only(request)` (Línea 392)
- **Propósito:** Fixture for trying common compression types in compression tests excluding
uncompressed case.
- **Firma:** `def compression_only(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def writable(request)` (Línea 401)
- **Propósito:** Fixture that an array is writable.
- **Firma:** `def writable(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def join_type(request)` (Línea 409)
- **Propósito:** Fixture for trying all types of join operations.
- **Firma:** `def join_type(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nselect_method(request)` (Línea 417)
- **Propósito:** Fixture for trying all nselect methods.
- **Firma:** `def nselect_method(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def na_action(request)` (Línea 425)
- **Propósito:** Fixture for 'na_action' argument in map.
- **Firma:** `def na_action(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ascending(request)` (Línea 433)
- **Propósito:** Fixture for 'na_action' argument in sort_values/sort_index/rank.
- **Firma:** `def ascending(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def rank_method(request)` (Línea 441)
- **Propósito:** Fixture for 'rank' argument in rank.
- **Firma:** `def rank_method(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def as_index(request)` (Línea 449)
- **Propósito:** Fixture for 'as_index' argument in groupby.
- **Firma:** `def as_index(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def cache(request)` (Línea 457)
- **Propósito:** Fixture for 'cache' argument in to_datetime.
- **Firma:** `def cache(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parallel(request)` (Línea 465)
- **Propósito:** Fixture for parallel keyword argument for numba.jit.
- **Firma:** `def parallel(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nogil(request)` (Línea 477)
- **Propósito:** Fixture for nogil keyword argument for numba.jit.
- **Firma:** `def nogil(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nopython(request)` (Línea 485)
- **Propósito:** Fixture for nopython keyword argument for numba.jit.
- **Firma:** `def nopython(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nulls_fixture(request)` (Línea 496)
- **Propósito:** Fixture for each null type in pandas.
- **Firma:** `def nulls_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def unique_nulls_fixture(request)` (Línea 507)
- **Propósito:** Fixture for each null type in pandas, each null type exactly once.
- **Firma:** `def unique_nulls_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def np_nat_fixture(request)` (Línea 519)
- **Propósito:** Fixture for each NaT type in numpy.
- **Firma:** `def np_nat_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def frame_or_series(request)` (Línea 536)
- **Propósito:** Fixture to parametrize over DataFrame and Series.
- **Firma:** `def frame_or_series(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_or_series(request)` (Línea 544)
- **Propósito:** Fixture to parametrize over Index and Series, made necessary by a mypy
bug, giving an error:

List item 0 has incompatible type "Type[Series]"; expected "Type[PandasObject]"

See GH#29725
- **Firma:** `def index_or_series(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_or_series_or_array(request)` (Línea 557)
- **Propósito:** Fixture to parametrize over Index, Series, and ExtensionArray
- **Firma:** `def index_or_series_or_array(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def box_with_array(request)` (Línea 565)
- **Propósito:** Fixture to test behavior for Index, Series, DataFrame, and pandas Array
classes
- **Firma:** `def box_with_array(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def dict_subclass()` (Línea 577)
- **Propósito:** Fixture for a dictionary subclass.
- **Firma:** `def dict_subclass()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def non_dict_mapping_subclass()` (Línea 590)
- **Propósito:** Fixture for a non-mapping dictionary subclass.
- **Firma:** `def non_dict_mapping_subclass()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def multiindex_year_month_day_dataframe_random_data()` (Línea 615)
- **Propósito:** DataFrame with 3 level MultiIndex (year, month, day) covering
first 100 business days from 2000-01-01 with random data
- **Firma:** `def multiindex_year_month_day_dataframe_random_data()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def lexsorted_two_level_string_multiindex()` (Línea 633)
- **Propósito:** 2-level MultiIndex, lexsorted, with string names.
- **Firma:** `def lexsorted_two_level_string_multiindex()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def multiindex_dataframe_random_data(lexsorted_two_level_string_multiindex)` (Línea 645)
- **Propósito:** DataFrame with 2 level MultiIndex with random data
- **Firma:** `def multiindex_dataframe_random_data(lexsorted_two_level_string_multiindex)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _create_multiindex()` (Línea 657)
- **Propósito:** MultiIndex used to test the general functionality of this object
- **Firma:** `def _create_multiindex()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _create_mi_with_dt64tz_level()` (Línea 677)
- **Propósito:** MultiIndex with a level that is a tzaware DatetimeIndex.
- **Firma:** `def _create_mi_with_dt64tz_level()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index(request)` (Línea 737)
- **Propósito:** Fixture for many "simple" kinds of indices.

These indices are unlikely to cover corner cases, e.g.
    - no names
    - no NaTs/NaNs
    - no values near implementation bounds
    - ...
- **Firma:** `def index(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_flat(request)` (Línea 756)
- **Propósito:** index fixture, but excluding MultiIndex cases.
- **Firma:** `def index_flat(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_with_missing(request)` (Línea 775)
- **Propósito:** Fixture for indices with missing values.

Integer-dtype and empty cases are excluded because they cannot hold missing
values.

MultiIndex is excluded because isna() is not defined for MultiIndex.
- **Firma:** `def index_with_missing(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def string_series()` (Línea 802)
- **Propósito:** Fixture for Series of floats with Index of unique strings
- **Firma:** `def string_series()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def object_series()` (Línea 814)
- **Propósito:** Fixture for Series of dtype object with Index of unique strings
- **Firma:** `def object_series()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def datetime_series()` (Línea 824)
- **Propósito:** Fixture for Series of floats with DatetimeIndex
- **Firma:** `def datetime_series()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _create_series(index)` (Línea 835)
- **Propósito:** Helper for the _series dict
- **Firma:** `def _create_series(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def series_with_simple_index(index)` (Línea 849)
- **Propósito:** Fixture for tests on series with changing types of indices.
- **Firma:** `def series_with_simple_index(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_or_series_obj(request)` (Línea 868)
- **Propósito:** Fixture for tests on indexes, series and series with a narrow dtype
copy to avoid mutation, e.g. setting .name
- **Firma:** `def index_or_series_obj(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def index_or_series_memory_obj(request)` (Línea 890)
- **Propósito:** Fixture for tests on indexes, series, series with a narrow dtype and
series with empty objects type
copy to avoid mutation, e.g. setting .name
- **Firma:** `def index_or_series_memory_obj(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def int_frame()` (Línea 903)
- **Propósito:** Fixture for DataFrame of ints with index of unique strings

Columns are ['A', 'B', 'C', 'D']
- **Firma:** `def int_frame()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def float_frame()` (Línea 917)
- **Propósito:** Fixture for DataFrame of floats with index of unique strings

Columns are ['A', 'B', 'C', 'D'].
- **Firma:** `def float_frame()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def rand_series_with_duplicate_datetimeindex()` (Línea 931)
- **Propósito:** Fixture for Series with a DatetimeIndex that has duplicates.
- **Firma:** `def rand_series_with_duplicate_datetimeindex()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ea_scalar_and_dtype(request)` (Línea 967)
- **Propósito:** Fixture that tests each scalar and datetime type.
- **Firma:** `def ea_scalar_and_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_arithmetic_operators(request)` (Línea 980)
- **Propósito:** Fixture for dunder names for common arithmetic operations.
- **Firma:** `def all_arithmetic_operators(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_binary_operators(request)` (Línea 1017)
- **Propósito:** Fixture for operator and roperator arithmetic, comparison, and logical ops.
- **Firma:** `def all_binary_operators(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_arithmetic_functions(request)` (Línea 1042)
- **Propósito:** Fixture for operator and roperator arithmetic functions.

Notes
-----
This includes divmod and rdivmod, whereas all_arithmetic_operators
does not.
- **Firma:** `def all_arithmetic_functions(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_numeric_reductions(request)` (Línea 1071)
- **Propósito:** Fixture for numeric reduction names.
- **Firma:** `def all_numeric_reductions(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_boolean_reductions(request)` (Línea 1082)
- **Propósito:** Fixture for boolean reduction names.
- **Firma:** `def all_boolean_reductions(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_reductions(request)` (Línea 1093)
- **Propósito:** Fixture for all (boolean + numeric) reduction names.
- **Firma:** `def all_reductions(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def comparison_op(request)` (Línea 1110)
- **Propósito:** Fixture for operator module comparison functions.
- **Firma:** `def comparison_op(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def compare_operators_no_eq_ne(request)` (Línea 1118)
- **Propósito:** Fixture for dunder names for compare operations except == and !=

* >=
* >
* <
* <=
- **Firma:** `def compare_operators_no_eq_ne(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_logical_operators(request)` (Línea 1133)
- **Propósito:** Fixture for dunder names for common logical operations

* |
* &
* ^
- **Firma:** `def all_logical_operators(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_numeric_accumulations(request)` (Línea 1148)
- **Propósito:** Fixture for numeric accumulation names
- **Firma:** `def all_numeric_accumulations(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def strict_data_files(pytestconfig)` (Línea 1159)
- **Propósito:** Returns the configuration for the test setting `--no-strict-data-files`.
- **Firma:** `def strict_data_files(pytestconfig)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def datapath(strict_data_files)` (Línea 1167)
- **Propósito:** Get the path to a data file.

Parameters
----------
path : str
    Path to the file, relative to ``pandas/tests/``

Returns
-------
path including ``pandas/tests``.

Raises
------
ValueError
    If the path doesn't exist and the --no-strict-data-files option is not set.
- **Firma:** `def datapath(strict_data_files)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def tz_naive_fixture(request)` (Línea 1235)
- **Propósito:** Fixture for trying timezones including default (None): {0}
- **Firma:** `def tz_naive_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def tz_aware_fixture(request)` (Línea 1244)
- **Propósito:** Fixture for trying explicit timezones: {0}
- **Firma:** `def tz_aware_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def utc_fixture(request)` (Línea 1258)
- **Propósito:** Fixture to provide variants of UTC timezone strings and tzinfo objects.
- **Firma:** `def utc_fixture(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def unit(request)` (Línea 1269)
- **Propósito:** datetime64 units we support.
- **Firma:** `def unit(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def string_dtype(request)` (Línea 1283)
- **Propósito:** Parametrized fixture for string dtypes.

* str
* 'str'
* 'U'
- **Firma:** `def string_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def string_dtype_no_object(request)` (Línea 1308)
- **Propósito:** Parametrized fixture for string dtypes.
* 'string[python]' (NA variant)
* 'string[pyarrow]' (NA variant)
* 'str' (NaN variant, with pyarrow)
* 'str' (NaN variant, without pyarrow)
- **Firma:** `def string_dtype_no_object(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nullable_string_dtype(request)` (Línea 1328)
- **Propósito:** Parametrized fixture for string dtypes.

* 'string[python]'
* 'string[pyarrow]'
- **Firma:** `def nullable_string_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pyarrow_string_dtype(request)` (Línea 1344)
- **Propósito:** Parametrized fixture for string dtypes backed by Pyarrow.

* 'str[pyarrow]'
* 'string[pyarrow]'
- **Firma:** `def pyarrow_string_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def string_storage(request)` (Línea 1360)
- **Propósito:** Parametrized fixture for pd.options.mode.string_storage.

* 'python'
* 'pyarrow'
- **Firma:** `def string_storage(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def string_dtype_arguments(request)` (Línea 1384)
- **Propósito:** Parametrized fixture for StringDtype storage and na_value.

* 'python' + pd.NA
* 'pyarrow' + pd.NA
* 'pyarrow' + np.nan
- **Firma:** `def string_dtype_arguments(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def dtype_backend(request)` (Línea 1401)
- **Propósito:** Parametrized fixture for pd.options.mode.string_storage.

* 'python'
* 'pyarrow'
- **Firma:** `def dtype_backend(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def bytes_dtype(request)` (Línea 1417)
- **Propósito:** Parametrized fixture for bytes dtypes.

* bytes
* 'bytes'
- **Firma:** `def bytes_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def object_dtype(request)` (Línea 1428)
- **Propósito:** Parametrized fixture for object dtypes.

* object
* 'object'
- **Firma:** `def object_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_string_dtype(request)` (Línea 1454)
- **Propósito:** Parametrized fixture for string dtypes.
* 'object'
* 'string[python]' (NA variant)
* 'string[pyarrow]' (NA variant)
* 'str' (NaN variant, with pyarrow)
* 'str' (NaN variant, without pyarrow)
- **Firma:** `def any_string_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def datetime64_dtype(request)` (Línea 1476)
- **Propósito:** Parametrized fixture for datetime64 dtypes.

* 'datetime64[ns]'
* 'M8[ns]'
- **Firma:** `def datetime64_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def timedelta64_dtype(request)` (Línea 1487)
- **Propósito:** Parametrized fixture for timedelta64 dtypes.

* 'timedelta64[ns]'
* 'm8[ns]'
- **Firma:** `def timedelta64_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def fixed_now_ts()` (Línea 1498)
- **Propósito:** Fixture emits fixed Timestamp.now()
- **Firma:** `def fixed_now_ts()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def float_numpy_dtype(request)` (Línea 1508)
- **Propósito:** Parameterized fixture for float dtypes.

* float
* 'float32'
* 'float64'
- **Firma:** `def float_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def float_ea_dtype(request)` (Línea 1520)
- **Propósito:** Parameterized fixture for float dtypes.

* 'Float32'
* 'Float64'
- **Firma:** `def float_ea_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_float_dtype(request)` (Línea 1531)
- **Propósito:** Parameterized fixture for float dtypes.

* float
* 'float32'
* 'float64'
* 'Float32'
* 'Float64'
- **Firma:** `def any_float_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def complex_dtype(request)` (Línea 1545)
- **Propósito:** Parameterized fixture for complex dtypes.

* complex
* 'complex64'
* 'complex128'
- **Firma:** `def complex_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def complex_or_float_dtype(request)` (Línea 1557)
- **Propósito:** Parameterized fixture for complex and numpy float dtypes.

* complex
* 'complex64'
* 'complex128'
* float
* 'float32'
* 'float64'
- **Firma:** `def complex_or_float_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_signed_int_numpy_dtype(request)` (Línea 1572)
- **Propósito:** Parameterized fixture for signed integer dtypes.

* int
* 'int8'
* 'int16'
* 'int32'
* 'int64'
- **Firma:** `def any_signed_int_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_unsigned_int_numpy_dtype(request)` (Línea 1586)
- **Propósito:** Parameterized fixture for unsigned integer dtypes.

* 'uint8'
* 'uint16'
* 'uint32'
* 'uint64'
- **Firma:** `def any_unsigned_int_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_int_numpy_dtype(request)` (Línea 1599)
- **Propósito:** Parameterized fixture for any integer dtype.

* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
- **Firma:** `def any_int_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_int_ea_dtype(request)` (Línea 1617)
- **Propósito:** Parameterized fixture for any nullable integer dtype.

* 'UInt8'
* 'Int8'
* 'UInt16'
* 'Int16'
* 'UInt32'
* 'Int32'
* 'UInt64'
* 'Int64'
- **Firma:** `def any_int_ea_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_int_dtype(request)` (Línea 1634)
- **Propósito:** Parameterized fixture for any nullable integer dtype.

* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
* 'UInt8'
* 'Int8'
* 'UInt16'
* 'Int16'
* 'UInt32'
* 'Int32'
* 'UInt64'
* 'Int64'
- **Firma:** `def any_int_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_numeric_ea_dtype(request)` (Línea 1660)
- **Propósito:** Parameterized fixture for any nullable integer dtype and
any float ea dtypes.

* 'UInt8'
* 'Int8'
* 'UInt16'
* 'Int16'
* 'UInt32'
* 'Int32'
* 'UInt64'
* 'Int64'
* 'Float32'
* 'Float64'
- **Firma:** `def any_numeric_ea_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_numeric_ea_and_arrow_dtype(request)` (Línea 1687)
- **Propósito:** Parameterized fixture for any nullable integer dtype and
any float ea dtypes.

* 'UInt8'
* 'Int8'
* 'UInt16'
* 'Int16'
* 'UInt32'
* 'Int32'
* 'UInt64'
* 'Int64'
* 'Float32'
* 'Float64'
* 'uint8[pyarrow]'
* 'int8[pyarrow]'
* 'uint16[pyarrow]'
* 'int16[pyarrow]'
* 'uint32[pyarrow]'
* 'int32[pyarrow]'
* 'uint64[pyarrow]'
* 'int64[pyarrow]'
* 'float32[pyarrow]'
* 'float64[pyarrow]'
- **Firma:** `def any_numeric_ea_and_arrow_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_signed_int_ea_dtype(request)` (Línea 1717)
- **Propósito:** Parameterized fixture for any signed nullable integer dtype.

* 'Int8'
* 'Int16'
* 'Int32'
* 'Int64'
- **Firma:** `def any_signed_int_ea_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_real_numpy_dtype(request)` (Línea 1730)
- **Propósito:** Parameterized fixture for any (purely) real numeric dtype.

* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
* float
* 'float32'
* 'float64'
- **Firma:** `def any_real_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_real_numeric_dtype(request)` (Línea 1751)
- **Propósito:** Parameterized fixture for any (purely) real numeric dtype.

* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
* float
* 'float32'
* 'float64'

and associated ea dtypes.
- **Firma:** `def any_real_numeric_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_numpy_dtype(request)` (Línea 1774)
- **Propósito:** Parameterized fixture for all numpy dtypes.

* bool
* 'bool'
* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
* float
* 'float32'
* 'float64'
* complex
* 'complex64'
* 'complex128'
* str
* 'str'
* 'U'
* bytes
* 'bytes'
* 'datetime64[ns]'
* 'M8[ns]'
* 'timedelta64[ns]'
* 'm8[ns]'
* object
* 'object'
- **Firma:** `def any_numpy_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_real_nullable_dtype(request)` (Línea 1811)
- **Propósito:** Parameterized fixture for all real dtypes that can hold NA.

* float
* 'float32'
* 'float64'
* 'Float32'
* 'Float64'
* 'UInt8'
* 'UInt16'
* 'UInt32'
* 'UInt64'
* 'Int8'
* 'Int16'
* 'Int32'
* 'Int64'
* 'uint8[pyarrow]'
* 'uint16[pyarrow]'
* 'uint32[pyarrow]'
* 'uint64[pyarrow]'
* 'int8[pyarrow]'
* 'int16[pyarrow]'
* 'int32[pyarrow]'
* 'int64[pyarrow]'
* 'float[pyarrow]'
* 'double[pyarrow]'
- **Firma:** `def any_real_nullable_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_numeric_dtype(request)` (Línea 1843)
- **Propósito:** Parameterized fixture for all numeric dtypes.

* int
* 'int8'
* 'uint8'
* 'int16'
* 'uint16'
* 'int32'
* 'uint32'
* 'int64'
* 'uint64'
* float
* 'float32'
* 'float64'
* complex
* 'complex64'
* 'complex128'
* 'UInt8'
* 'Int8'
* 'UInt16'
* 'Int16'
* 'UInt32'
* 'Int32'
* 'UInt64'
* 'Int64'
* 'Float32'
* 'Float64'
- **Firma:** `def any_numeric_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def any_skipna_inferred_dtype(request)` (Línea 1910)
- **Propósito:** Fixture for all inferred dtypes from _libs.lib.infer_dtype

The covered (inferred) types are:
* 'string'
* 'empty'
* 'bytes'
* 'mixed'
* 'mixed-integer'
* 'mixed-integer-float'
* 'floating'
* 'integer'
* 'decimal'
* 'boolean'
* 'datetime64'
* 'datetime'
* 'date'
* 'timedelta'
* 'time'
* 'period'
* 'interval'

Returns
-------
inferred_dtype : str
    The string for the inferred dtype from _libs.lib.infer_dtype
values : np.ndarray
    An array of object dtype that will be inferred to have
    `inferred_dtype`

Examples
--------
>>> from pandas._libs import lib
>>>
>>> def test_something(any_skipna_inferred_dtype):
...     inferred_dtype, values = any_skipna_inferred_dtype
...     # will pass
...     assert lib.infer_dtype(values, skipna=True) == inferred_dtype
- **Firma:** `def any_skipna_inferred_dtype(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ip()` (Línea 1961)
- **Propósito:** Get an instance of IPython.InteractiveShell.

Will raise a skip if IPython is not installed.
- **Firma:** `def ip()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def mpl_cleanup()` (Línea 1980)
- **Propósito:** Ensure Matplotlib is cleaned up around a test.

Before a test is run:

1) Set the backend to "template" to avoid requiring a GUI.

After a test is run:

1) Reset units registry
2) Reset rc_context
3) Close all figures

See matplotlib/testing/decorators.py#L24.
- **Firma:** `def mpl_cleanup()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def tick_classes(request)` (Línea 2019)
- **Propósito:** Fixture for Tick based datetime offsets available for a time series.
- **Firma:** `def tick_classes(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sort_by_key(request)` (Línea 2027)
- **Propósito:** Simple fixture for testing keys in sorting methods.
Tests None (no key) and the identity key.
- **Firma:** `def sort_by_key(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def names(request)` (Línea 2047)
- **Propósito:** A 3-tuple of names, the first two for operands, the last for a result.
- **Firma:** `def names(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_sli(request)` (Línea 2055)
- **Propósito:** Parametrize over __setitem__, loc.__setitem__, iloc.__setitem__
- **Firma:** `def indexer_sli(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_li(request)` (Línea 2063)
- **Propósito:** Parametrize over loc.__getitem__, iloc.__getitem__
- **Firma:** `def indexer_li(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_si(request)` (Línea 2071)
- **Propósito:** Parametrize over __setitem__, iloc.__setitem__
- **Firma:** `def indexer_si(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_sl(request)` (Línea 2079)
- **Propósito:** Parametrize over __setitem__, loc.__setitem__
- **Firma:** `def indexer_sl(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_al(request)` (Línea 2087)
- **Propósito:** Parametrize over at.__setitem__, loc.__setitem__
- **Firma:** `def indexer_al(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indexer_ial(request)` (Línea 2095)
- **Propósito:** Parametrize over iat.__setitem__, iloc.__setitem__
- **Firma:** `def indexer_ial(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def performance_warning(request)` (Línea 2103)
- **Propósito:** Fixture to check if performance warnings are enabled. Either produces
``PerformanceWarning`` if they are enabled, otherwise ``False``.
- **Firma:** `def performance_warning(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def using_infer_string()` (Línea 2113)
- **Propósito:** Fixture to check if infer string option is enabled.
- **Firma:** `def using_infer_string()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def using_python_scalars()` (Línea 2121)
- **Propósito:** Sin docstring.
- **Firma:** `def using_python_scalars()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def warsaw(request)` (Línea 2131)
- **Propósito:** tzinfo for Europe/Warsaw using pytz, dateutil, or zoneinfo.
- **Firma:** `def warsaw(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def temp_file(tmp_path)` (Línea 2139)
- **Propósito:** Generate a unique file for testing use. See link for removal policy.
https://docs.pytest.org/en/7.1.x/how-to/tmp_path.html#the-default-base-temporary-directory
- **Firma:** `def temp_file(tmp_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def monkeysession()` (Línea 2150)
- **Propósito:** Sin docstring.
- **Firma:** `def monkeysession()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def using_nan_is_na(request)` (Línea 2156)
- **Propósito:** Sin docstring.
- **Firma:** `def using_nan_is_na(request)`
- **Retorno / Efectos:** Consulta código fuente.
