# Documentación Técnica: `venv_build/Lib/site-packages/pandas/io/formats/format.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/io/formats/format.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2080
- **Fecha de inspección:** 2026-08-05 18:36:17

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `SeriesFormatter` (Línea 189)
- **Docstring:** _Implement the main logic of Series.to_string, which underlies
Series.__repr__._
- **Métodos:**
  - `def __init__(self, series)` (Línea 195): Sin docstring.
  - `def _chk_truncate(self)` (Línea 227): Sin docstring.
  - `def _get_footer(self)` (Línea 258): Sin docstring.
  - `def _get_formatted_values(self)` (Línea 300): Sin docstring.
  - `def to_string(self)` (Línea 309): Sin docstring.

#### Clase `DataFrameFormatter` (Línea 415)
- **Docstring:** _Class for processing dataframe formatting options and data.

Used by DataFrame.to_string, which backs DataFrame.__repr__._
- **Métodos:**
  - `def __init__(self, frame, columns, col_space, header, index, na_rep, formatters, justify, float_format, sparsify, index_names, max_rows, min_rows, max_cols, show_dimensions, decimal, bold_rows, escape)` (Línea 425): Sin docstring.
  - `def get_strcols(self)` (Línea 472): Render a DataFrame to a list of columns (as lists of strings).
  - `def should_show_dimensions(self)` (Línea 485): Sin docstring.
  - `def is_truncated(self)` (Línea 491): Sin docstring.
  - `def is_truncated_horizontally(self)` (Línea 495): Sin docstring.
  - `def is_truncated_vertically(self)` (Línea 499): Sin docstring.
  - `def dimensions_info(self)` (Línea 503): Sin docstring.
  - `def has_index_names(self)` (Línea 507): Sin docstring.
  - `def has_column_names(self)` (Línea 511): Sin docstring.
  - `def show_row_idx_names(self)` (Línea 515): Sin docstring.
  - `def show_col_idx_names(self)` (Línea 519): Sin docstring.
  - `def max_rows_displayed(self)` (Línea 523): Sin docstring.
  - `def _initialize_sparsify(self, sparsify)` (Línea 526): Sin docstring.
  - `def _initialize_formatters(self, formatters)` (Línea 531): Sin docstring.
  - `def _initialize_justify(self, justify)` (Línea 544): Sin docstring.
  - `def _initialize_columns(self, columns)` (Línea 550): Sin docstring.
  - `def _initialize_colspace(self, col_space)` (Línea 558): Sin docstring.
  - `def _calc_max_cols_fitted(self)` (Línea 582): Number of columns fitting the screen.
  - `def _calc_max_rows_fitted(self)` (Línea 593): Number of rows with data fitting the screen.
  - `def _adjust_max_rows(self, max_rows)` (Línea 612): Adjust max_rows using display logic.

See description here:
https://pandas.pydata.org/docs/dev/user_guide/options.html#frequently-used-options

GH #37359
  - `def _is_in_terminal(self)` (Línea 626): Check if the output is to be shown in terminal.
  - `def _is_screen_narrow(self, max_width)` (Línea 630): Sin docstring.
  - `def _is_screen_short(self, max_height)` (Línea 633): Sin docstring.
  - `def _get_number_of_auxiliary_rows(self)` (Línea 636): Get number of rows occupied by prompt, dots and dimension info.
  - `def truncate(self)` (Línea 650): Check whether the frame should be truncated. If so, slice the frame up.
  - `def _truncate_horizontally(self)` (Línea 660): Remove columns, which are not to be displayed and adjust formatters.

Attributes affected:
    - tr_frame
    - formatters
    - tr_col_num
  - `def _truncate_vertically(self)` (Línea 686): Remove rows, which are not to be displayed.

Attributes affected:
    - tr_frame
    - tr_row_num
  - `def _get_strcols_without_index(self)` (Línea 704): Sin docstring.
  - `def format_col(self, i)` (Línea 751): Sin docstring.
  - `def _get_formatter(self, i)` (Línea 764): Sin docstring.
  - `def _get_formatted_column_labels(self, frame)` (Línea 776): Sin docstring.
  - `def _get_formatted_index(self, frame)` (Línea 801): Sin docstring.
  - `def _get_column_name_list(self)` (Línea 842): Sin docstring.
  - `def _validate_float_format(self, fmt)` (Línea 851): Validates and processes the float_format argument.
Converts new-style format strings to callables.

#### Clase `DataFrameRenderer` (Línea 875)
- **Docstring:** _Class for creating dataframe output in multiple formats.

Called in pandas.core.generic.NDFrame:
    - to_csv
    - to_latex

Called in pandas.DataFrame:
    - to_html
    - to_string

Parameters
----------
fmt : DataFrameFormatter
    Formatter with the formatting options._
- **Métodos:**
  - `def __init__(self, fmt)` (Línea 892): Sin docstring.
  - `def to_html(self, buf, encoding, classes, notebook, border, table_id, render_links)` (Línea 895): Render a DataFrame to an html table.

Parameters
----------
buf : str, path object, file-like object, or None, default None
    String, path object (implementing ``os.PathLike[str]``), or file-like
    object implementing a string ``write()`` function. If None, the result is
    returned as a string.
encoding : str, default “utf-8”
    Set character encoding.
classes : str or list-like
    classes to include in the `class` attribute of the opening
    ``<table>`` tag, in addition to the default "dataframe".
notebook : {True, False}, optional, default False
    Whether the generated HTML is for IPython Notebook.
border : int or bool
    When an integer value is provided, it sets the border attribute in
    the opening tag, specifying the thickness of the border.
    If ``False`` or ``0`` is passed, the border attribute will not
    be present in the ``<table>`` tag.
    The default value for this parameter is governed by
    ``pd.options.display.html.border``.
table_id : str, optional
    A css id is included in the opening `<table>` tag if specified.
render_links : bool, default False
    Convert URLs to HTML links.
  - `def to_string(self, buf, encoding, line_width)` (Línea 950): Render a DataFrame to a console-friendly tabular output.

Parameters
----------
buf : str, path object, file-like object, or None, default None
    String, path object (implementing ``os.PathLike[str]``), or file-like
    object implementing a string ``write()`` function. If None, the result is
    returned as a string.
encoding: str, default “utf-8”
    Set character encoding.
line_width : int, optional
    Width to wrap a line in characters.
  - `def to_csv(self, path_or_buf, encoding, sep, columns, index_label, mode, compression, quoting, quotechar, lineterminator, chunksize, date_format, doublequote, escapechar, errors, storage_options)` (Línea 976): Render dataframe as comma-separated file.

#### Clase `_GenericArrayFormatter` (Línea 1175)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, values, digits, formatter, na_rep, space, float_format, justify, decimal, quoting, fixed_width, leading_space, fallback_formatter)` (Línea 1176): Sin docstring.
  - `def get_result(self)` (Línea 1204): Sin docstring.
  - `def _format_strings(self)` (Línea 1208): Sin docstring.

#### Clase `FloatArrayFormatter` (Línea 1279)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 1280): Sin docstring.
  - `def _value_formatter(self, float_format, threshold)` (Línea 1292): Returns a function to be applied on each value to format it
  - `def get_result_as_array(self)` (Línea 1348): Returns the float values converted into strings using
the parameters given at initialisation, as a numpy array
  - `def _format_strings(self)` (Línea 1479): Sin docstring.

#### Clase `_IntArrayFormatter` (Línea 1483)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _format_strings(self)` (Línea 1484): Sin docstring.

#### Clase `_Datetime64Formatter` (Línea 1494)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, values, nat_rep, date_format)` (Línea 1497): Sin docstring.
  - `def _format_strings(self)` (Línea 1508): we by definition have DO NOT have a TZ

#### Clase `_ExtensionArrayFormatter` (Línea 1521)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _format_strings(self)` (Línea 1524): Sin docstring.

#### Clase `_Datetime64TZFormatter` (Línea 1668)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _format_strings(self)` (Línea 1671): we by definition have a TZ

#### Clase `_Timedelta64Formatter` (Línea 1683)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, values, nat_rep)` (Línea 1686): Sin docstring.
  - `def _format_strings(self)` (Línea 1696): Sin docstring.

#### Clase `EngFormatter` (Línea 1859)
- **Docstring:** _Formats float values according to engineering format.

Based on matplotlib.ticker.EngFormatter_
- **Métodos:**
  - `def __init__(self, accuracy, use_eng_prefix)` (Línea 1887): Sin docstring.
  - `def __call__(self, num)` (Línea 1893): Formats a number in engineering notation, appending a letter
representing the power of 1000 of the original number. Some examples:
>>> format_eng = EngFormatter(accuracy=0, use_eng_prefix=True)
>>> format_eng(0)
' 0'
>>> format_eng = EngFormatter(accuracy=1, use_eng_prefix=True)
>>> format_eng(1_000_000)
' 1.0M'
>>> format_eng = EngFormatter(accuracy=2, use_eng_prefix=False)
>>> format_eng("-1e-6")
'-1.00E-06'

@param num: the value to represent
@type num: either a numeric value or a string that can be converted to
           a numeric value (as per decimal.Decimal constructor)

@return: engineering formatted string

### Funciones independientes:

#### `def get_dataframe_repr_params()` (Línea 355)
- **Propósito:** Get the parameters used to repr(dataFrame) calls using DataFrame.to_string.

Supplying these parameters to DataFrame.to_string is equivalent to calling
``repr(DataFrame)``. This is useful if you want to adjust the repr output.

Example
-------
>>> import pandas as pd
>>>
>>> df = pd.DataFrame([[1, 2], [3, 4]])
>>> repr_params = pd.io.formats.format.get_dataframe_repr_params()
>>> repr(df) == df.to_string(**repr_params)
True
- **Firma:** `def get_dataframe_repr_params()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_series_repr_params()` (Línea 386)
- **Propósito:** Get the parameters used to repr(Series) calls using Series.to_string.

Supplying these parameters to Series.to_string is equivalent to calling
``repr(series)``. This is useful if you want to adjust the series repr output.

Example
-------
>>> import pandas as pd
>>>
>>> ser = pd.Series([1, 2, 3, 4])
>>> repr_params = pd.io.formats.format.get_series_repr_params()
>>> repr(ser) == ser.to_string(**repr_params)
True
- **Firma:** `def get_series_repr_params()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def save_to_buffer(string, buf, encoding)` (Línea 1036)
- **Propósito:** Perform serialization. Write to buf or return as string if buf is None.
- **Firma:** `def save_to_buffer(string, buf, encoding)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_buffer(buf, encoding)` (Línea 1053)
- **Propósito:** Context manager to open, yield and close buffer for filenames or Path-like
objects, otherwise yield buf unchanged.
- **Firma:** `def _get_buffer(buf, encoding)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def format_array(values, formatter, float_format, na_rep, digits, space, justify, decimal, leading_space, quoting, fallback_formatter)` (Línea 1090)
- **Propósito:** Format an array for printing.

Parameters
----------
values : np.ndarray or ExtensionArray
formatter
float_format
na_rep
digits
space
justify
decimal
leading_space : bool, optional, default True
    Whether the array should be formatted with a leading space.
    When an array as a column of a Series or DataFrame, we do want
    the leading space to pad between columns.

    When formatting an Index subclass
    (e.g. IntervalIndex._get_values_for_csv), we don't want the
    leading space since it should be left-aligned.
fallback_formatter

Returns
-------
List[str]
- **Firma:** `def format_array(values, formatter, float_format, na_rep, digits, space, justify, decimal, leading_space, quoting, fallback_formatter)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def format_percentiles(percentiles)` (Línea 1554)
- **Propósito:** Outputs rounded and formatted percentiles.

Parameters
----------
percentiles : list-like, containing floats from interval [0,1]

Returns
-------
formatted : list of strings

Notes
-----
Rounding precision is chosen so that: (1) if any two elements of
``percentiles`` differ, they remain different after rounding
(2) no entry is *rounded* to 0% or 100%.
Any non-integer is always rounded to at least 1 decimal place.

Examples
--------
Keeps all entries different after rounding:

>>> format_percentiles([0.01999, 0.02001, 0.5, 0.666666, 0.9999])
['1.999%', '2.001%', '50%', '66.667%', '99.99%']

No element is rounded to 0% or 100% (unless already equal to it).
Duplicates are allowed:

>>> format_percentiles([0, 0.5, 0.02001, 0.5, 0.666666, 0.9999])
['0%', '50%', '2.0%', '50%', '66.67%', '99.99%']
- **Firma:** `def format_percentiles(percentiles)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_precision(array)` (Línea 1620)
- **Propósito:** Sin docstring.
- **Firma:** `def get_precision(array)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _format_datetime64(x, nat_rep)` (Línea 1630)
- **Propósito:** Sin docstring.
- **Firma:** `def _format_datetime64(x, nat_rep)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _format_datetime64_dateonly(x, nat_rep, date_format)` (Línea 1639)
- **Propósito:** Sin docstring.
- **Firma:** `def _format_datetime64_dateonly(x, nat_rep, date_format)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_format_datetime64(is_dates_only, nat_rep, date_format)` (Línea 1654)
- **Propósito:** Return a formatter callable taking a datetime64 as input and providing
a string as output
- **Firma:** `def get_format_datetime64(is_dates_only, nat_rep, date_format)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_format_timedelta64(values, nat_rep, box)` (Línea 1703)
- **Propósito:** Return a formatter function for a range of timedeltas.
These will all have the same format argument

If box, then show the return in quotes
- **Firma:** `def get_format_timedelta64(values, nat_rep, box)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _make_fixed_width(strings, justify, minimum, adj)` (Línea 1737)
- **Propósito:** Sin docstring.
- **Firma:** `def _make_fixed_width(strings, justify, minimum, adj)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _trim_zeros_complex(str_complexes, decimal)` (Línea 1771)
- **Propósito:** Separates the real and imaginary parts from the complex number, and
executes the _trim_zeros_float method on each of those.
- **Firma:** `def _trim_zeros_complex(str_complexes, decimal)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _trim_zeros_single_float(str_float)` (Línea 1804)
- **Propósito:** Trims trailing zeros after a decimal point,
leaving just one if necessary.
- **Firma:** `def _trim_zeros_single_float(str_float)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _trim_zeros_float(str_floats, decimal)` (Línea 1816)
- **Propósito:** Trims the maximum number of trailing zeros equally from
all numbers containing decimals, leaving just one if
necessary.
- **Firma:** `def _trim_zeros_float(str_floats, decimal)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _has_names(index)` (Línea 1852)
- **Propósito:** Sin docstring.
- **Firma:** `def _has_names(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_eng_float_format(accuracy, use_eng_prefix)` (Línea 1956)
- **Propósito:** Format float representation in DataFrame with SI notation.

Sets the floating-point display format for ``DataFrame`` objects using engineering
notation (SI units), allowing easier readability of values across wide ranges.

Parameters
----------
accuracy : int, default 3
    Number of decimal digits after the floating point.
use_eng_prefix : bool, default False
    Whether to represent a value with SI prefixes.

Returns
-------
None
    This method does not return a value. it updates the global display format
    for floats in DataFrames.

See Also
--------
set_option : Set the value of the specified option or options.
reset_option : Reset one or more options to their default value.

Examples
--------
>>> df = pd.DataFrame([1e-9, 1e-3, 1, 1e3, 1e6])
>>> df
              0
0  1.000000e-09
1  1.000000e-03
2  1.000000e+00
3  1.000000e+03
4  1.000000e+06

>>> pd.set_eng_float_format(accuracy=1)
>>> df
         0
0  1.0E-09
1  1.0E-03
2  1.0E+00
3  1.0E+03
4  1.0E+06

>>> pd.set_eng_float_format(use_eng_prefix=True)
>>> df
        0
0  1.000n
1  1.000m
2   1.000
3  1.000k
4  1.000M

>>> pd.set_eng_float_format(accuracy=1, use_eng_prefix=True)
>>> df
      0
0  1.0n
1  1.0m
2   1.0
3  1.0k
4  1.0M

>>> pd.set_option("display.float_format", None)  # unset option
- **Firma:** `def set_eng_float_format(accuracy, use_eng_prefix)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_level_lengths(levels, sentinel)` (Línea 2024)
- **Propósito:** For each index in each level the function returns lengths of indexes.

Parameters
----------
levels : list of lists
    List of values on for level.
sentinel : string, optional
    Value which states that no new index starts on there.

Returns
-------
Returns list of maps. For each level returns map of indexes (key is index
in row and value is length of index).
- **Firma:** `def get_level_lengths(levels, sentinel)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def buffer_put_lines(buf, lines)` (Línea 2067)
- **Propósito:** Appends lines to a buffer.

Parameters
----------
buf
    The buffer to write to
lines
    The lines to append.
- **Firma:** `def buffer_put_lines(buf, lines)`
- **Retorno / Efectos:** Consulta código fuente.
