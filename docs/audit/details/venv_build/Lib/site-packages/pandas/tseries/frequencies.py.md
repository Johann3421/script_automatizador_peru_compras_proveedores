# Documentación Técnica: `venv_build/Lib/site-packages/pandas/tseries/frequencies.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/tseries/frequencies.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 623
- **Fecha de inspección:** 2026-08-05 18:36:30

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_FrequencyInferer` (Línea 180)
- **Docstring:** _Not sure if I can avoid the state machine here_
- **Métodos:**
  - `def __init__(self, index)` (Línea 185): Sin docstring.
  - `def deltas(self)` (Línea 217): Sin docstring.
  - `def deltas_asi8(self)` (Línea 221): Sin docstring.
  - `def is_unique(self)` (Línea 227): Sin docstring.
  - `def is_unique_asi8(self)` (Línea 231): Sin docstring.
  - `def get_freq(self)` (Línea 234): Find the appropriate frequency string to describe the inferred
frequency of self.i8values

Returns
-------
str or None
  - `def day_deltas(self)` (Línea 285): Sin docstring.
  - `def hour_deltas(self)` (Línea 290): Sin docstring.
  - `def fields(self)` (Línea 295): Sin docstring.
  - `def rep_stamp(self)` (Línea 299): Sin docstring.
  - `def month_position_check(self)` (Línea 302): Sin docstring.
  - `def mdiffs(self)` (Línea 306): Sin docstring.
  - `def ydiffs(self)` (Línea 311): Sin docstring.
  - `def _infer_daily_rule(self)` (Línea 314): Sin docstring.
  - `def _get_daily_rule(self)` (Línea 346): Sin docstring.
  - `def _get_annual_rule(self)` (Línea 357): Sin docstring.
  - `def _get_quarterly_rule(self)` (Línea 371): Sin docstring.
  - `def _get_monthly_rule(self)` (Línea 385): Sin docstring.
  - `def _is_business_daily(self)` (Línea 395): Sin docstring.
  - `def _get_wom_rule(self)` (Línea 414): Sin docstring.

#### Clase `_TimedeltaFrequencyInferer` (Línea 432)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _infer_daily_rule(self)` (Línea 433): Sin docstring.

### Funciones independientes:

#### `def get_period_alias(offset_str)` (Línea 79)
- **Propósito:** Alias to closest period strings BQ->Q etc.
- **Firma:** `def get_period_alias(offset_str)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def infer_freq(index)` (Línea 91)
- **Propósito:** Infer the most likely frequency given the input index.

This method attempts to deduce the most probable frequency (e.g., 'D' for daily,
'H' for hourly) from a sequence of datetime-like objects. It is particularly useful
when the frequency of a time series is not explicitly set or known but can be
inferred from its values.

Parameters
----------
index : DatetimeIndex, TimedeltaIndex, Series or array-like
  If passed a Series will use the values of the series (NOT THE INDEX).

Returns
-------
str or None
    None if no discernible frequency.

Raises
------
TypeError
    If the index is not datetime-like.
ValueError
    If there are fewer than three values.

See Also
--------
date_range : Return a fixed frequency DatetimeIndex.
timedelta_range : Return a fixed frequency TimedeltaIndex with day as the default.
period_range : Return a fixed frequency PeriodIndex.
DatetimeIndex.freq : Return the frequency object if it is set, otherwise None.

Examples
--------
>>> idx = pd.date_range(start="2020/12/01", end="2020/12/30", periods=30)
>>> pd.infer_freq(idx)
'D'
- **Firma:** `def infer_freq(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_multiple(us, mult)` (Línea 438)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_multiple(us, mult)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_add_count(base, count)` (Línea 442)
- **Propósito:** Sin docstring.
- **Firma:** `def _maybe_add_count(base, count)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_subperiod(source, target)` (Línea 455)
- **Propósito:** Returns True if downsampling is possible between source and target
frequencies

Parameters
----------
source : str or DateOffset
    Frequency converting from
target : str or DateOffset
    Frequency converting to

Returns
-------
bool
- **Firma:** `def is_subperiod(source, target)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_superperiod(source, target)` (Línea 510)
- **Propósito:** Returns True if upsampling is possible between source and target
frequencies

Parameters
----------
source : str or DateOffset
    Frequency converting from
target : str or DateOffset
    Frequency converting to

Returns
-------
bool
- **Firma:** `def is_superperiod(source, target)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_coerce_freq(code)` (Línea 568)
- **Propósito:** we might need to coerce a code to a rule_code
and uppercase it

Parameters
----------
source : str or DateOffset
    Frequency converting from

Returns
-------
str
- **Firma:** `def _maybe_coerce_freq(code)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _quarter_months_conform(source, target)` (Línea 590)
- **Propósito:** Sin docstring.
- **Firma:** `def _quarter_months_conform(source, target)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_annual(rule)` (Línea 596)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_annual(rule)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_quarterly(rule)` (Línea 601)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_quarterly(rule)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_monthly(rule)` (Línea 606)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_monthly(rule)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_weekly(rule)` (Línea 611)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_weekly(rule)`
- **Retorno / Efectos:** Consulta código fuente.
