# Documentación Técnica: `venv_build/Lib/site-packages/pandas/tests/extension/conftest.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/tests/extension/conftest.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 214
- **Fecha de inspección:** 2026-08-05 18:36:19

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def dtype()` (Línea 9)
- **Propósito:** A fixture providing the ExtensionDtype to validate.
- **Firma:** `def dtype()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data()` (Línea 15)
- **Propósito:** Length-10 array for this type.

* data[0] and data[1] should both be non missing
* data[0] and data[1] should not be equal
- **Firma:** `def data()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_for_twos(dtype)` (Línea 26)
- **Propósito:** Length-10 array in which all the elements are two.

Call pytest.skip in your fixture if the dtype does not support divmod.
- **Firma:** `def data_for_twos(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_missing()` (Línea 41)
- **Propósito:** Length-2 array with [NA, Valid]
- **Firma:** `def data_missing()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def all_data(request, data, data_missing)` (Línea 47)
- **Propósito:** Parametrized fixture giving 'data' and 'data_missing'
- **Firma:** `def all_data(request, data, data_missing)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_repeated(data)` (Línea 56)
- **Propósito:** Generate many datasets.

Parameters
----------
data : fixture implementing `data`

Returns
-------
Callable[[int], Generator]:
    A callable that takes a `count` argument and
    returns a generator yielding `count` datasets.
- **Firma:** `def data_repeated(data)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_for_sorting()` (Línea 79)
- **Propósito:** Length-3 array with a known sort order.

This should be three items [B, C, A] with
A < B < C

For boolean dtypes (for which there are only 2 values available),
set B=C=True
- **Firma:** `def data_for_sorting()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_missing_for_sorting()` (Línea 93)
- **Propósito:** Length-3 array with a known sort order.

This should be three items [B, NA, A] with
A < B and NA missing.
- **Firma:** `def data_missing_for_sorting()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def na_cmp()` (Línea 104)
- **Propósito:** Binary operator for comparing NA values.

Should return a function of two arguments that returns
True if both arguments are (scalar) NA for your type.

By default, uses ``operator.is_``
- **Firma:** `def na_cmp()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def na_value(dtype)` (Línea 117)
- **Propósito:** The scalar missing value for this type. Default dtype.na_value.

TODO: can be removed in 3.x (see https://github.com/pandas-dev/pandas/pull/54930)
- **Firma:** `def na_value(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_for_grouping()` (Línea 127)
- **Propósito:** Data for factorization, grouping, and unique tests.

Expected to be like [B, B, NA, NA, A, A, B, C]

Where A < B < C and NA is missing.

If a dtype has _is_boolean = True, i.e. only 2 unique non-NA entries,
then set C=B.
- **Firma:** `def data_for_grouping()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def box_in_series(request)` (Línea 142)
- **Propósito:** Whether to box the data in a Series
- **Firma:** `def box_in_series(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def groupby_apply_op(request)` (Línea 156)
- **Propósito:** Functions to test groupby.apply().
- **Firma:** `def groupby_apply_op(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def as_frame(request)` (Línea 164)
- **Propósito:** Boolean fixture to support Series and Series.to_frame() comparison testing.
- **Firma:** `def as_frame(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def as_series(request)` (Línea 172)
- **Propósito:** Boolean fixture to support arr and Series(arr) comparison testing.
- **Firma:** `def as_series(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def use_numpy(request)` (Línea 180)
- **Propósito:** Boolean fixture to support comparison testing of ExtensionDtype array
and numpy array.
- **Firma:** `def use_numpy(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def fillna_method(request)` (Línea 189)
- **Propósito:** Parametrized fixture giving method parameters 'ffill' and 'bfill' for
Series.<method> testing.
- **Firma:** `def fillna_method(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def as_array(request)` (Línea 198)
- **Propósito:** Boolean fixture to support ExtensionDtype _from_sequence method testing.
- **Firma:** `def as_array(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def invalid_scalar(data)` (Línea 206)
- **Propósito:** A scalar that *cannot* be held by this ExtensionArray.

The default should work for most subclasses, but is not guaranteed.

If the array can hold any item (i.e. object dtype), then use pytest.skip.
- **Firma:** `def invalid_scalar(data)`
- **Retorno / Efectos:** Consulta código fuente.
