# Documentación Técnica: `venv_build/Lib/site-packages/pandas/core/internals/managers.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/core/internals/managers.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2564
- **Fecha de inspección:** 2026-08-05 18:36:16

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `BaseBlockManager` (Línea 143)
- **Docstring:** _Core internal data structure to implement DataFrame, Series, etc.

Manage a bunch of labeled 2D mixed-type ndarrays. Essentially it's a
lightweight blocked set of labeled data to be manipulated by the DataFrame
public API class

Attributes
----------
shape
ndim
axes
values
items

Methods
-------
set_axis(axis, new_labels)
copy(deep=True)

get_dtypes

apply(func, axes, block_filter_fn)

get_bool_data
get_numeric_data

get_slice(slice_like, axis)
get(label)
iget(loc)

take(indexer, axis)
reindex_axis(new_labels, axis)
reindex_indexer(new_labels, indexer, axis)

delete(label)
insert(loc, label, value)
set(label, value)

Parameters
----------
blocks: Sequence of Block
axes: Sequence of Index
verify_integrity: bool, default True

Notes
-----
This is *not* a public API class_
- **Métodos:**
  - `def ndim(self)` (Línea 202): Sin docstring.
  - `def __init__(self, blocks, axes, verify_integrity)` (Línea 208): Sin docstring.
  - `def __len__(self)` (Línea 212): Sin docstring.
  - `def shape(self)` (Línea 216): Sin docstring.
  - `def from_blocks(cls, blocks, axes)` (Línea 220): Sin docstring.
  - `def blknos(self)` (Línea 224): Suppose we want to find the array corresponding to our i'th column.

blknos[i] identifies the block from self.blocks that contains this column.

blklocs[i] identifies the column of interest within
self.blocks[self.blknos[i]]
  - `def blklocs(self)` (Línea 240): See blknos.__doc__
  - `def make_empty(self, axes)` (Línea 250): return an empty BlockManager with the items axis of len 0
  - `def __bool__(self)` (Línea 268): Sin docstring.
  - `def set_axis(self, axis, new_labels)` (Línea 271): Sin docstring.
  - `def _validate_set_axis(self, axis, new_labels)` (Línea 277): Sin docstring.
  - `def is_single_block(self)` (Línea 294): Sin docstring.
  - `def items(self)` (Línea 299): Sin docstring.
  - `def _has_no_reference(self, i)` (Línea 302): Check for column `i` if it has references.
(whether it references another array or is itself being referenced)
Returns True if the column has no references.
  - `def _has_no_reference_block(self, blkno)` (Línea 311): Check for block `i` if it has references.
(whether it references another array or is itself being referenced)
Returns True if the block has no references.
  - `def add_references(self, mgr)` (Línea 319): Adds the references from one manager to another. We assume that both
managers have the same block structure.
  - `def references_same_values(self, mgr, blkno)` (Línea 331): Checks if two blocks from two different block managers reference the
same underlying values.
  - `def get_unique_dtypes(self)` (Línea 339): Sin docstring.
  - `def get_dtypes(self)` (Línea 342): Sin docstring.
  - `def arrays(self)` (Línea 347): Quick access to the backing arrays of the Blocks.

Only for compatibility with ArrayManager for testing convenience.
Not to be used in actual code, and return value is not the same as the
ArrayManager method (list of 1D arrays vs iterator of 2D ndarrays / 1D EAs).

Warning! The returned arrays don't handle Copy-on-Write, so this should
be used with caution (only in read-mode).
  - `def __repr__(self)` (Línea 362): Sin docstring.
  - `def _equal_values(self, other)` (Línea 374): To be implemented by the subclasses. Only check the column values
assuming shape and indexes have already been checked.
  - `def equals(self, other)` (Línea 382): Implementation for DataFrame.equals
  - `def apply(self, f, align_keys)` (Línea 399): Iterate over the blocks, collect and create a new BlockManager.

Parameters
----------
f : str or callable
    Name of the Block method to apply.
align_keys: List[str] or None, default None
**kwargs
    Keywords to pass to `f`

Returns
-------
BlockManager
  - `def isna(self, func)` (Línea 452): Sin docstring.
  - `def fillna(self, value, limit, inplace)` (Línea 456): Sin docstring.
  - `def where(self, other, cond, align)` (Línea 469): Sin docstring.
  - `def putmask(self, mask, new, align)` (Línea 484): Sin docstring.
  - `def round(self, decimals)` (Línea 499): Sin docstring.
  - `def replace(self, to_replace, value, inplace)` (Línea 503): Sin docstring.
  - `def replace_regex(self)` (Línea 516): Sin docstring.
  - `def replace_list(self, src_list, dest_list, inplace, regex)` (Línea 520): do a list replace
  - `def interpolate(self, inplace)` (Línea 540): Sin docstring.
  - `def pad_or_backfill(self, inplace)` (Línea 543): Sin docstring.
  - `def shift(self, periods, fill_value)` (Línea 546): Sin docstring.
  - `def setitem(self, indexer, value)` (Línea 552): Set values with indexer.

For SingleBlockManager, this backs s[indexer] = value
  - `def diff(self, n)` (Línea 609): Sin docstring.
  - `def astype(self, dtype, errors)` (Línea 613): Sin docstring.
  - `def convert(self)` (Línea 616): Sin docstring.
  - `def convert_dtypes(self)` (Línea 619): Sin docstring.
  - `def get_values_for_csv(self)` (Línea 622): Convert values to native types (strings / python objects) that are used
in formatting (repr / csv).
  - `def any_extension_types(self)` (Línea 639): Whether any of the blocks in this manager are extension blocks
  - `def is_view(self)` (Línea 644): return a boolean if we are a single block and are a view
  - `def _get_data_subset(self, predicate)` (Línea 658): Sin docstring.
  - `def _get_data_subset_indices(self, predicate)` (Línea 662): Sin docstring.
  - `def get_bool_data(self)` (Línea 667): Select blocks that are bool-dtype and columns from object-dtype blocks
that are all-bool.
  - `def get_numeric_data(self)` (Línea 684): Sin docstring.
  - `def _combine(self, blocks, index)` (Línea 692): return a new manager with the blocks
  - `def nblocks(self)` (Línea 723): Sin docstring.
  - `def copy(self)` (Línea 726): Make deep or shallow copy of BlockManager

Parameters
----------
deep : bool, string or None, default True
    If False, return a shallow copy (do not copy data)

Returns
-------
BlockManager
  - `def is_consolidated(self)` (Línea 756): Sin docstring.
  - `def consolidate(self)` (Línea 759): Join together blocks having same dtype

Returns
-------
y : BlockManager
  - `def _consolidate_inplace(self)` (Línea 776): Sin docstring.
  - `def reindex_axis(self, new_index, axis, fill_value, only_slice)` (Línea 780): Conform data manager to new index.
  - `def reindex_indexer(self, new_axis, indexer, axis, fill_value, allow_dups, only_slice)` (Línea 800): Parameters
----------
new_axis : Index
indexer : ndarray[intp] or None
axis : int
fill_value : object, default None
allow_dups : bool, default False
only_slice : bool, default False
    Whether to take views, not copies, along columns.
use_na_proxy : bool, default False
    Whether to use an np.void ndarray for newly introduced columns.

pandas-indexer with -1's only.
  - `def _slice_take_blocks_ax0(self, slice_or_indexer, fill_value, only_slice)` (Línea 887): Slice/take blocks along axis=0.

Overloaded for SingleBlock

Parameters
----------
slice_or_indexer : slice or np.ndarray[int64]
fill_value : scalar, default lib.no_default
only_slice : bool, default False
    If True, we always return views on existing arrays, never copies.
    This is used when called from ops.blockwise.operate_blockwise.
use_na_proxy : bool, default False
    Whether to use an np.void ndarray for newly introduced columns.
ref_inplace_op: bool, default False
    Don't track refs if True because we operate inplace

Yields
------
Block : New Block
  - `def _make_na_block(self, placement, fill_value, use_na_proxy)` (Línea 1027): Sin docstring.
  - `def take(self, indexer, axis, verify)` (Línea 1052): Take items along any axis.

indexer : np.ndarray[np.intp]
axis : int, default 1
verify : bool, default True
    Check that all entries are between 0 and len(self) - 1, inclusive.
    Pass verify=False if this check has been done by the caller.

Returns
-------
BlockManager

#### Clase `BlockManager` (Línea 1085)
- **Docstring:** _BaseBlockManager that holds 2D blocks._
- **Métodos:**
  - `def __init__(self, blocks, axes, verify_integrity)` (Línea 1095): Sin docstring.
  - `def _verify_integrity(self)` (Línea 1117): Sin docstring.
  - `def from_blocks(cls, blocks, axes)` (Línea 1131): Constructor for BlockManager and SingleBlockManager with same signature.
  - `def fast_xs(self, loc)` (Línea 1140): Return the array corresponding to `frame.iloc[loc]`.

Parameters
----------
loc : int

Returns
-------
np.ndarray or ExtensionArray
  - `def iget(self, i, track_ref)` (Línea 1213): Return the data as a SingleBlockManager.
  - `def iget_values(self, i)` (Línea 1227): Return the data for column i as the values (ndarray or ExtensionArray).

Warning! The returned array is a view but doesn't handle Copy-on-Write,
so this should be used with caution.
  - `def column_arrays(self)` (Línea 1240): Used in the JSON C code to access column arrays.
This optimizes compared to using `iget_values` by converting each

Warning! This doesn't handle Copy-on-Write, so should be used with
caution (current use case of consuming this in the JSON code is fine).
  - `def iset(self, loc, value, inplace, refs)` (Línea 1267): Set new item in-place. Does not consolidate. Adds new Block if not
contained in the current set of items
  - `def _iset_split_block(self, blkno_l, blk_locs, value, refs)` (Línea 1417): Removes columns from a block by splitting the block.

Avoids copying the whole block through slicing and updates the manager
after determining the new block structure. Optionally adds a new block,
otherwise has to be done by the caller.

Parameters
----------
blkno_l: The block number to operate on, relevant for updating the manager
blk_locs: The locations of our block that should be deleted.
value: The value to set as a replacement.
refs: The reference tracking object of the value to set.
  - `def _iset_single(self, loc, value, inplace, blkno, blk, refs)` (Línea 1469): Fastpath for iset when we are only setting a single position and
the Block currently in that position is itself single-column.

In this case we can swap out the entire Block and blklocs and blknos
are unaffected.
  - `def column_setitem(self, loc, idx, value, inplace_only)` (Línea 1499): Set values ("setitem") into a single column (not setting the full column).

This is a method on the BlockManager level, to avoid creating an
intermediate Series at the DataFrame level (`s = df[loc]; s[idx] = value`)
  - `def insert(self, loc, item, value, refs)` (Línea 1531): Insert item at selected position.

Parameters
----------
loc : int
item : hashable
value : np.ndarray or ExtensionArray
refs : The reference tracking object of the value to set.
  - `def _insert_update_mgr_locs(self, loc)` (Línea 1582): When inserting a new Block at location 'loc', we increment
all of the mgr_locs of blocks above that by one.
  - `def _insert_update_blklocs_and_blknos(self, loc)` (Línea 1594): When inserting a new Block at location 'loc', we update our
_blklocs and _blknos.
  - `def idelete(self, indexer)` (Línea 1616): Delete selected locations, returning a new BlockManager.
  - `def grouped_reduce(self, func)` (Línea 1632): Apply grouped reduction function blockwise, returning a new BlockManager.

Parameters
----------
func : grouped reduction function

Returns
-------
BlockManager
  - `def reduce(self, func)` (Línea 1666): Apply reduction function blockwise, returning a single-row BlockManager.

Parameters
----------
func : reduction function

Returns
-------
BlockManager
  - `def operate_blockwise(self, other, array_op)` (Línea 1687): Apply array_op blockwise with another (aligned) BlockManager.
  - `def _equal_values(self, other)` (Línea 1693): Used in .equals defined in base class. Only check the column values
assuming shape and indexes have already been checked.
  - `def quantile(self)` (Línea 1700): Iterate over blocks applying quantile reduction.
This routine is intended for reduction type operations and
will do inference on the generated blocks.

Parameters
----------
interpolation : type of interpolation, default 'linear'
qs : list of the quantiles to be computed

Returns
-------
BlockManager
  - `def unstack(self, unstacker, fill_value)` (Línea 1735): Return a BlockManager with all blocks unstacked.

Parameters
----------
unstacker : reshape._Unstacker
fill_value : Any
    fill_value for newly introduced missing values.

Returns
-------
unstacked : BlockManager
  - `def to_iter_dict(self)` (Línea 1796): Yield a tuple of (str(dtype), BlockManager)

Returns
-------
values : a tuple of (str(dtype), BlockManager)
  - `def as_array(self, dtype, copy, na_value)` (Línea 1809): Convert the blockmanager data into a numpy array.

Parameters
----------
dtype : np.dtype or None, default None
    Data type of the return array.
copy : bool, default False
    If True then guarantee that a copy is returned. A value of
    False does not guarantee that the underlying data is not
    copied.
na_value : object, default lib.no_default
    Value to be used as the missing value sentinel.

Returns
-------
arr : ndarray
  - `def _interleave(self, dtype, na_value)` (Línea 1885): Return ndarray from blocks with specified item order
Items must be contained in the blocks
  - `def is_consolidated(self)` (Línea 1944): Return True if more than one block with the same dtype
  - `def _consolidate_check(self)` (Línea 1952): Sin docstring.
  - `def _consolidate_inplace(self)` (Línea 1962): Sin docstring.
  - `def concat_horizontal(cls, mgrs, axes)` (Línea 1973): Concatenate uniformly-indexed BlockManagers horizontally.
  - `def concat_vertical(cls, mgrs, axes)` (Línea 1995): Concatenate uniformly-indexed BlockManagers vertically.

#### Clase `SingleBlockManager` (Línea 2002)
- **Docstring:** _manage a single block with_
- **Métodos:**
  - `def ndim(self)` (Línea 2006): Sin docstring.
  - `def __init__(self, block, axis, verify_integrity)` (Línea 2014): Sin docstring.
  - `def from_blocks(cls, blocks, axes)` (Línea 2028): Constructor for BlockManager and SingleBlockManager with same signature.
  - `def from_array(cls, array, index, refs)` (Línea 2041): Constructor for if we have an array that is not yet a Block.
  - `def to_2d_mgr(self, columns)` (Línea 2052): Manager analogue of Series.to_frame
  - `def _has_no_reference(self, i)` (Línea 2063): Check for column `i` if it has references.
(whether it references another array or is itself being referenced)
Returns True if the column has no references.
  - `def __getstate__(self)` (Línea 2071): Sin docstring.
  - `def __setstate__(self, state)` (Línea 2090): Sin docstring.
  - `def _post_setstate(self)` (Línea 2114): Sin docstring.
  - `def _block(self)` (Línea 2118): Sin docstring.
  - `def array(self)` (Línea 2123): Quick access to the backing array of the Block.
  - `def _blknos(self)` (Línea 2131): compat with BlockManager
  - `def _blklocs(self)` (Línea 2137): compat with BlockManager
  - `def get_rows_with_mask(self, indexer)` (Línea 2141): Sin docstring.
  - `def get_slice(self, slobj, axis)` (Línea 2161): Sin docstring.
  - `def index(self)` (Línea 2177): Sin docstring.
  - `def dtype(self)` (Línea 2181): Sin docstring.
  - `def get_dtypes(self)` (Línea 2184): Sin docstring.
  - `def external_values(self)` (Línea 2187): The array that Series.values returns
  - `def internal_values(self)` (Línea 2191): The array that Series._values returns
  - `def array_values(self)` (Línea 2195): The array that Series.array returns
  - `def get_numeric_data(self)` (Línea 2199): Sin docstring.
  - `def _can_hold_na(self)` (Línea 2205): Sin docstring.
  - `def setitem_inplace(self, indexer, value)` (Línea 2208): Set values with indexer.

For SingleBlockManager, this backs s[indexer] = value

This is an inplace version of `setitem()`, mutating the manager/values
in place, not returning a new Manager (and Block), and thus never changing
the dtype.
  - `def idelete(self, indexer)` (Línea 2236): Delete single location from SingleBlockManager.

Ensures that self.blocks doesn't become empty.
  - `def fast_xs(self, loc)` (Línea 2248): fast path for getting a cross-section
return a view of the data
  - `def set_values(self, values)` (Línea 2255): Set the values of the single block in place.

Use at your own risk! This does not check if the passed values are
valid for the current Block/SingleBlockManager (length, dtype, etc),
and this does not properly keep track of references.
  - `def _equal_values(self, other)` (Línea 2268): Used in .equals defined in base class. Only check the column values
assuming shape and indexes have already been checked.
  - `def grouped_reduce(self, func)` (Línea 2280): Sin docstring.

### Funciones independientes:

#### `def interleaved_dtype(dtypes)` (Línea 111)
- **Propósito:** Find the common dtype for `blocks`.

Parameters
----------
blocks : List[DtypeObj]

Returns
-------
dtype : np.dtype, ExtensionDtype, or None
    None is returned when `blocks` is empty.
- **Firma:** `def interleaved_dtype(dtypes)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_np_dtype(dtype)` (Línea 130)
- **Propósito:** Sin docstring.
- **Firma:** `def ensure_np_dtype(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def create_block_manager_from_blocks(blocks, axes, consolidate, verify_integrity)` (Línea 2293)
- **Propósito:** Sin docstring.
- **Firma:** `def create_block_manager_from_blocks(blocks, axes, consolidate, verify_integrity)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def create_block_manager_from_column_arrays(arrays, axes, consolidate, refs)` (Línea 2319)
- **Propósito:** Sin docstring.
- **Firma:** `def create_block_manager_from_column_arrays(arrays, axes, consolidate, refs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def raise_construction_error(tot_items, block_shape, axes, e)` (Línea 2346)
- **Propósito:** raise a helpful message about our construction
- **Firma:** `def raise_construction_error(tot_items, block_shape, axes, e)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _grouping_func(tup)` (Línea 2375)
- **Propósito:** Sin docstring.
- **Firma:** `def _grouping_func(tup)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _form_blocks(arrays, consolidate, refs)` (Línea 2388)
- **Propósito:** Sin docstring.
- **Firma:** `def _form_blocks(arrays, consolidate, refs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _tuples_to_blocks_no_consolidate(tuples, refs)` (Línea 2434)
- **Propósito:** Sin docstring.
- **Firma:** `def _tuples_to_blocks_no_consolidate(tuples, refs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _stack_arrays(tuples, dtype)` (Línea 2444)
- **Propósito:** Sin docstring.
- **Firma:** `def _stack_arrays(tuples, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _consolidate(blocks)` (Línea 2457)
- **Propósito:** Merge blocks having same dtype, exclude non-consolidating blocks
- **Firma:** `def _consolidate(blocks)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _merge_blocks(blocks, dtype, can_consolidate)` (Línea 2474)
- **Propósito:** Sin docstring.
- **Firma:** `def _merge_blocks(blocks, dtype, can_consolidate)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _preprocess_slice_or_indexer(slice_or_indexer, length, allow_fill)` (Línea 2509)
- **Propósito:** Sin docstring.
- **Firma:** `def _preprocess_slice_or_indexer(slice_or_indexer, length, allow_fill)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def make_na_array(dtype, shape, fill_value)` (Línea 2532)
- **Propósito:** Sin docstring.
- **Firma:** `def make_na_array(dtype, shape, fill_value)`
- **Retorno / Efectos:** Consulta código fuente.
