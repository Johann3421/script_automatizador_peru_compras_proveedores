# Documentación Técnica: `venv_build/Lib/site-packages/pandas/core/internals/blocks.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/core/internals/blocks.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2395
- **Fecha de inspección:** 2026-08-05 18:36:16

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `Block` (Línea 138)
- **Docstring:** _Canonical n-dimensional unit of homogeneous dtype contained in a pandas
data structure

Index-ignorant; let the container take care of that_
- **Métodos:**
  - `def _validate_ndim(self)` (Línea 156): We validate dimension for blocks that can hold 2D values, which for now
means numpy dtypes or EA dtypes like DatetimeTZDtype and PeriodDtype.
  - `def is_object(self)` (Línea 165): Sin docstring.
  - `def is_extension(self)` (Línea 170): Sin docstring.
  - `def _can_consolidate(self)` (Línea 175): Sin docstring.
  - `def _consolidate_key(self)` (Línea 181): Sin docstring.
  - `def _can_hold_na(self)` (Línea 186): Can we store NA values in this Block?
  - `def is_bool(self)` (Línea 197): We can be bool if a) we are bool dtype or b) object dtype with bool objects.
  - `def external_values(self)` (Línea 204): Sin docstring.
  - `def fill_value(self)` (Línea 209): Sin docstring.
  - `def _standardize_fill_value(self, value)` (Línea 214): Sin docstring.
  - `def mgr_locs(self)` (Línea 221): Sin docstring.
  - `def mgr_locs(self, new_mgr_locs)` (Línea 225): Sin docstring.
  - `def make_block(self, values, placement, refs)` (Línea 229): Create a new block, with type inference propagate any values that are
not specified
  - `def make_block_same_class(self, values, placement, refs)` (Línea 247): Wrap given values in a block of same type as self.
  - `def __repr__(self)` (Línea 263): Sin docstring.
  - `def __len__(self)` (Línea 275): Sin docstring.
  - `def slice_block_columns(self, slc)` (Línea 279): Perform __getitem__-like, return result as block.
  - `def take_block_columns(self, indices)` (Línea 290): Perform __getitem__-like, return result as block.

Only supports slices that preserve dimensionality.
  - `def getitem_block_columns(self, slicer, new_mgr_locs, ref_inplace_op)` (Línea 305): Perform __getitem__-like, return result as block.

Only supports slices that preserve dimensionality.
  - `def _can_hold_element(self, element)` (Línea 318): require the same dtype as ourselves
  - `def should_store(self, value)` (Línea 324): Should we set self.values[indexer] = value inplace or do we need to cast?

Parameters
----------
value : np.ndarray or ExtensionArray

Returns
-------
bool
  - `def apply(self, func)` (Línea 342): apply the function to my values; return a block if we are not
one
  - `def reduce(self, func)` (Línea 353): Sin docstring.
  - `def _split_op_result(self, result)` (Línea 368): Sin docstring.
  - `def _split(self)` (Línea 390): Split a block into a list of single-column blocks.
  - `def split_and_operate(self, func)` (Línea 404): Split the block and apply func column-by-column.

Parameters
----------
func : Block method
*args
**kwargs

Returns
-------
List[Block]
  - `def coerce_to_target_dtype(self, other, raise_on_upcast)` (Línea 430): coerce the current block to a dtype compat for other
we will return a block, possibly object, and not raise

we can also safely try to coerce to the same dtype
and will receive the same block
  - `def convert(self)` (Línea 488): Attempt to coerce any object types to better types. Return a copy
of the block (if copy = True).
  - `def convert_dtypes(self, infer_objects, convert_string, convert_integer, convert_boolean, convert_floating, dtype_backend)` (Línea 524): Sin docstring.
  - `def dtype(self)` (Línea 575): Sin docstring.
  - `def astype(self, dtype, errors, squeeze)` (Línea 579): Coerce to the new dtype.

Parameters
----------
dtype : np.dtype or ExtensionDtype
errors : str, {'raise', 'ignore'}, default 'raise'
    - ``raise`` : allow exceptions to be raised
    - ``ignore`` : suppress exceptions. On error return original object
squeeze : bool, default False
    squeeze values to ndim=1 if only one column is given

Returns
-------
Block
  - `def get_values_for_csv(self)` (Línea 625): convert to our native types format
  - `def copy(self)` (Línea 640): copy constructor
  - `def _maybe_copy(self, inplace, deep)` (Línea 655): Sin docstring.
  - `def _get_refs_and_copy(self, inplace)` (Línea 661): Sin docstring.
  - `def replace(self, to_replace, value, inplace, mask)` (Línea 675): replace the to_replace value with value, possible to create new
blocks here this is just a call to putmask.
  - `def _replace_regex(self, to_replace, value, inplace, mask)` (Línea 742): Replace elements by the given value.

Parameters
----------
to_replace : object or pattern
    Scalar to replace or regular expression to match.
value : object
    Replacement object.
inplace : bool, default False
    Perform inplace modification.
mask : array-like of bool, optional
    True indicate corresponding element is ignored.

Returns
-------
List[Block]
  - `def replace_list(self, src_list, dest_list, inplace, regex)` (Línea 790): See BlockManager.replace_list docstring.
  - `def _replace_coerce(self, to_replace, value, mask, inplace, regex)` (Línea 882): Replace value corresponding to the given boolean array with another
value.

Parameters
----------
to_replace : object or pattern
    Scalar to replace or regular expression to match.
value : object
    Replacement object.
mask : np.ndarray[bool]
    True indicate corresponding element is ignored.
inplace : bool, default True
    Perform inplace modification.
regex : bool, default False
    If true, perform regular expression substitution.

Returns
-------
List[Block]
  - `def _maybe_squeeze_arg(self, arg)` (Línea 943): For compatibility with 1D-only ExtensionArrays.
  - `def _unwrap_setitem_indexer(self, indexer)` (Línea 949): For compatibility with 1D-only ExtensionArrays.
  - `def shape(self)` (Línea 958): Sin docstring.
  - `def iget(self, i)` (Línea 961): Sin docstring.
  - `def _slice(self, slicer)` (Línea 970): return a slice of my values
  - `def set_inplace(self, locs, values, copy)` (Línea 977): Modify block values in-place with new item value.

If copy=True, first copy the underlying values in place before modifying
(for Copy-on-Write).

Notes
-----
`set_inplace` never creates a new array or new Block, whereas `setitem`
_may_ create a new array and always creates a new Block.

Caller is responsible for checking values.dtype == self.dtype.
  - `def take_nd(self, indexer, axis, new_mgr_locs, fill_value)` (Línea 996): Take values according to indexer and return them as a block.
  - `def _unstack(self, unstacker, fill_value, new_placement, needs_masking)` (Línea 1035): Return a list of unstacked blocks of self

Parameters
----------
unstacker : reshape._Unstacker
fill_value : int
    Only used in ExtensionBlock._unstack
new_placement : np.ndarray[np.intp]
allow_fill : bool
needs_masking : np.ndarray[bool]

Returns
-------
blocks : list of Block
    New blocks of unstacked values.
mask : array-like of bool
    The mask of columns of `blocks` we should keep.
  - `def setitem(self, indexer, value)` (Línea 1081): Attempt self.values[indexer] = value, possibly creating a new array.

Parameters
----------
indexer : tuple, list-like, array-like, slice, int
    The subset of self.values to set
value : object
    The value being set

Returns
-------
Block

Notes
-----
`indexer` is a direct slice/positional indexer. `value` must
be a compatible shape.
  - `def putmask(self, mask, new)` (Línea 1144): putmask the data to the block; it is possible that we may create a
new dtype of block

Return the resulting block(s).

Parameters
----------
mask : np.ndarray[bool], SparseArray[bool], or BooleanArray
new : an ndarray/object

Returns
-------
List[Block]
  - `def where(self, other, cond)` (Línea 1211): evaluate the block; return result block(s) from the result

Parameters
----------
other : an ndarray/object
cond : np.ndarray[bool], SparseArray[bool], or BooleanArray

Returns
-------
List[Block]
  - `def fillna(self, value, limit, inplace)` (Línea 1314): fillna on the block with the value. If we fail, then convert to
block to hold objects instead and try again
  - `def pad_or_backfill(self)` (Línea 1347): Sin docstring.
  - `def interpolate(self)` (Línea 1375): Sin docstring.
  - `def diff(self, n)` (Línea 1418): return block for the diff of the values
  - `def shift(self, periods, fill_value)` (Línea 1425): shift the block by periods, possibly upcast
  - `def quantile(self, qs, interpolation)` (Línea 1472): compute the quantiles of the

Parameters
----------
qs : Index
    The quantiles to be computed in float64.
interpolation : str, default 'linear'
    Type of interpolation.

Returns
-------
Block
  - `def round(self, decimals)` (Línea 1502): Rounds the values.
If the block is not of an integer or float dtype, nothing happens.
This is consistent with DataFrame.round behavior.
(Note: Series.round would raise)

Parameters
----------
decimals: int,
    Number of decimal places to round to.
    Caller is responsible for validating this
  - `def delete(self, loc)` (Línea 1541): Deletes the locs from the block.

We split the block to avoid copying the underlying data. We create new
blocks for every connected segment of the initial block that is not deleted.
The new blocks point to the initial array.
  - `def is_view(self)` (Línea 1590): return a boolean if I am possibly a view
  - `def array_values(self)` (Línea 1595): The array that Series.array returns. Always an ExtensionArray.
  - `def get_values(self, dtype)` (Línea 1601): return an internal format, currently just the ndarray
this is often overridden to handle to_dense like operations

#### Clase `EABackedBlock` (Línea 1609)
- **Docstring:** _Mixin for Block subclasses backed by ExtensionArray._
- **Métodos:**
  - `def shift(self, periods, fill_value)` (Línea 1617): Shift the block by `periods`.

Dispatches to underlying ExtensionArray and re-boxes in an
ExtensionBlock.
  - `def setitem(self, indexer, value)` (Línea 1630): Attempt self.values[indexer] = value, possibly creating a new array.

This differs from Block.setitem by not allowing setitem to change
the dtype of the Block.

Parameters
----------
indexer : tuple, list-like, array-like, slice, int
    The subset of self.values to set
value : object
    The value being set

Returns
-------
Block

Notes
-----
`indexer` is a direct slice/positional indexer. `value` must
be a compatible shape.
  - `def where(self, other, cond)` (Línea 1685): Sin docstring.
  - `def putmask(self, mask, new)` (Línea 1751): See Block.putmask.__doc__
  - `def delete(self, loc)` (Línea 1813): Sin docstring.
  - `def array_values(self)` (Línea 1826): Sin docstring.
  - `def get_values(self, dtype)` (Línea 1830): return object dtype as boxed values, such as Timestamps/Timedelta
  - `def pad_or_backfill(self)` (Línea 1841): Sin docstring.

#### Clase `ExtensionBlock` (Línea 1869)
- **Docstring:** _Block for holding extension types.

Notes
-----
This holds all 3rd-party extension array types. It's also the immediate
parent class for our internal extension types' blocks.

ExtensionArrays are limited to 1-D._
- **Métodos:**
  - `def fillna(self, value, limit, inplace)` (Línea 1883): Sin docstring.
  - `def shape(self)` (Línea 1926): Sin docstring.
  - `def iget(self, i)` (Línea 1932): Sin docstring.
  - `def set_inplace(self, locs, values, copy)` (Línea 1960): Sin docstring.
  - `def _maybe_squeeze_arg(self, arg)` (Línea 1967): If necessary, squeeze a (N, 1) ndarray to (N,)
  - `def _unwrap_setitem_indexer(self, indexer)` (Línea 1989): Adapt a 2D-indexer to our 1D values.

This is intended for 'setitem', not 'iget' or '_slice'.
  - `def is_view(self)` (Línea 2032): Extension arrays are never treated as views.
  - `def is_numeric(self)` (Línea 2038): Sin docstring.
  - `def _slice(self, slicer)` (Línea 2041): Return a slice of my values.

Parameters
----------
slicer : slice, ndarray[int], or ndarray[bool]
    Valid (non-reducing) indexer for self.values.

Returns
-------
ExtensionArray
  - `def slice_block_rows(self, slicer)` (Línea 2082): Perform __getitem__-like specialized to slicing along index.
  - `def _unstack(self, unstacker, fill_value, new_placement, needs_masking)` (Línea 2091): Sin docstring.

#### Clase `NumpyBlock` (Línea 2135)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def is_view(self)` (Línea 2140): return a boolean if I am possibly a view
  - `def array_values(self)` (Línea 2145): Sin docstring.
  - `def get_values(self, dtype)` (Línea 2148): Sin docstring.
  - `def is_numeric(self)` (Línea 2154): Sin docstring.

#### Clase `NDArrayBackedExtensionBlock` (Línea 2161)
- **Docstring:** _Block backed by an NDArrayBackedExtensionArray_
- **Métodos:**
  - `def is_view(self)` (Línea 2169): return a boolean if I am possibly a view

#### Clase `DatetimeLikeBlock` (Línea 2175)
- **Docstring:** _Block for datetime64[ns], timedelta64[ns]._

### Funciones independientes:

#### `def maybe_coerce_values(values)` (Línea 2187)
- **Propósito:** Input validation for values passed to __init__. Ensure that
any datetime64/timedelta64 dtypes are in nanoseconds.  Ensure
that we do not have string dtypes.

Parameters
----------
values : np.ndarray or ExtensionArray

Returns
-------
values : np.ndarray or ExtensionArray
- **Firma:** `def maybe_coerce_values(values)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_block_type(dtype)` (Línea 2216)
- **Propósito:** Find the appropriate Block subclass to use for the given values and dtype.

Parameters
----------
dtype : numpy or pandas dtype

Returns
-------
cls : class, subclass of Block
- **Firma:** `def get_block_type(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def new_block_2d(values, placement, refs)` (Línea 2245)
- **Propósito:** Sin docstring.
- **Firma:** `def new_block_2d(values, placement, refs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def new_block(values, placement)` (Línea 2258)
- **Propósito:** Sin docstring.
- **Firma:** `def new_block(values, placement)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def check_ndim(values, placement, ndim)` (Línea 2273)
- **Propósito:** ndim inference and validation.

Validates that values.ndim and ndim are consistent.
Validates that len(values) and len(placement) are consistent.

Parameters
----------
values : array-like
placement : BlockPlacement
ndim : int

Raises
------
ValueError : the number of dimensions do not match
- **Firma:** `def check_ndim(values, placement, ndim)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def extract_pandas_array(values, dtype, ndim)` (Línea 2314)
- **Propósito:** Ensure that we don't allow NumpyExtensionArray / NumpyEADtype in internals.
- **Firma:** `def extract_pandas_array(values, dtype, ndim)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def extend_blocks(result, blocks)` (Línea 2336)
- **Propósito:** return a new extended blocks, given the result
- **Firma:** `def extend_blocks(result, blocks)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_block_shape(values, ndim)` (Línea 2352)
- **Propósito:** Reshape if possible to have values.ndim == ndim.
- **Firma:** `def ensure_block_shape(values, ndim)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def external_values(values)` (Línea 2368)
- **Propósito:** The array that Series.values returns (public attribute).

This has some historical constraints, and is overridden in block
subclasses to return the correct array (e.g. period returns
object ndarray and datetimetz a datetime64[ns] ndarray instead of
proper extension array).
- **Firma:** `def external_values(values)`
- **Retorno / Efectos:** Consulta código fuente.
