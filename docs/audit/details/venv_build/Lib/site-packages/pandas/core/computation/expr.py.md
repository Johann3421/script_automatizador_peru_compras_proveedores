# Documentación Técnica: `venv_build/Lib/site-packages/pandas/core/computation/expr.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/core/computation/expr.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 850
- **Fecha de inspección:** 2026-08-05 18:36:15

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `BaseExprVisitor` (Línea 348)
- **Docstring:** _Custom ast walker. Parsers of other engines should subclass this class
if necessary.

Parameters
----------
env : Scope
engine : str
parser : str
preparser : callable_
- **Métodos:**
  - `def __init__(self, env, engine, parser, preparser)` (Línea 401): Sin docstring.
  - `def visit(self, node)` (Línea 408): Sin docstring.
  - `def visit_Module(self, node)` (Línea 422): Sin docstring.
  - `def visit_Expr(self, node)` (Línea 428): Sin docstring.
  - `def _rewrite_membership_op(self, node, left, right)` (Línea 431): Sin docstring.
  - `def _maybe_transform_eq_ne(self, node, left, right)` (Línea 458): Sin docstring.
  - `def _maybe_downcast_constants(self, left, right)` (Línea 466): Sin docstring.
  - `def _maybe_eval(self, binop, eval_in_python)` (Línea 489): Sin docstring.
  - `def _maybe_evaluate_binop(self, op, op_class, lhs, rhs, eval_in_python, maybe_eval_in_python)` (Línea 500): Sin docstring.
  - `def visit_BinOp(self, node)` (Línea 540): Sin docstring.
  - `def visit_UnaryOp(self, node)` (Línea 545): Sin docstring.
  - `def visit_Name(self, node)` (Línea 550): Sin docstring.
  - `def visit_NameConstant(self, node)` (Línea 554): Sin docstring.
  - `def visit_Num(self, node)` (Línea 558): Sin docstring.
  - `def visit_Constant(self, node)` (Línea 561): Sin docstring.
  - `def visit_Str(self, node)` (Línea 565): Sin docstring.
  - `def visit_List(self, node)` (Línea 569): Sin docstring.
  - `def visit_Index(self, node)` (Línea 575): df.index[4]
  - `def visit_Subscript(self, node)` (Línea 579): Sin docstring.
  - `def visit_Slice(self, node)` (Línea 599): df.index[slice(4,6)]
  - `def visit_Assign(self, node)` (Línea 613): support a single assignment node, like

c = a + b

set the assigner at the top level, must be a Name node which
might or might not exist in the resolvers
  - `def visit_Attribute(self, node)` (Línea 643): Sin docstring.
  - `def visit_Call(self, node, side)` (Línea 667): Sin docstring.
  - `def translate_In(self, op)` (Línea 718): Sin docstring.
  - `def visit_Compare(self, node)` (Línea 721): Sin docstring.
  - `def _try_visit_binop(self, bop)` (Línea 742): Sin docstring.
  - `def visit_BoolOp(self, node)` (Línea 747): Sin docstring.

#### Clase `PandasExprVisitor` (Línea 767)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, env, engine, parser, preparser)` (Línea 768): Sin docstring.

#### Clase `PythonExprVisitor` (Línea 782)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, env, engine, parser, preparser)` (Línea 783): Sin docstring.

#### Clase `Expr` (Línea 789)
- **Docstring:** _Object encapsulating an expression.

Parameters
----------
expr : str
engine : str, optional, default 'numexpr'
parser : str, optional, default 'pandas'
env : Scope, optional, default None
level : int, optional, default 2_
- **Métodos:**
  - `def __init__(self, expr, engine, parser, env, level)` (Línea 806): Sin docstring.
  - `def assigner(self)` (Línea 822): Sin docstring.
  - `def __call__(self)` (Línea 825): Sin docstring.
  - `def __repr__(self)` (Línea 828): Sin docstring.
  - `def __len__(self)` (Línea 831): Sin docstring.
  - `def parse(self)` (Línea 834): Parse an expression.
  - `def names(self)` (Línea 841): Get the names in an expression.

### Funciones independientes:

#### `def _rewrite_assign(tok)` (Línea 55)
- **Propósito:** Rewrite the assignment operator for PyTables expressions that use ``=``
as a substitute for ``==``.

Parameters
----------
tok : tuple of int, str
    ints correspond to the all caps constants in the tokenize module

Returns
-------
tuple of int, str
    Either the input or token or the replacement values
- **Firma:** `def _rewrite_assign(tok)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _replace_booleans(tok)` (Línea 74)
- **Propósito:** Replace ``&`` with ``and`` and ``|`` with ``or`` so that bitwise
precedence is changed to boolean precedence.

Parameters
----------
tok : tuple of int, str
    ints correspond to the all caps constants in the tokenize module

Returns
-------
tuple of int, str
    Either the input or token or the replacement values
- **Firma:** `def _replace_booleans(tok)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _replace_locals(tok)` (Línea 99)
- **Propósito:** Replace local variables with a syntactically valid name.

Parameters
----------
tok : tuple of int, str
    ints correspond to the all caps constants in the tokenize module

Returns
-------
tuple of int, str
    Either the input or token or the replacement values

Notes
-----
This is somewhat of a hack in that we rewrite a string such as ``'@a'`` as
``'__pd_eval_local_a'`` by telling the tokenizer that ``__pd_eval_local_``
is a ``tokenize.OP`` and to replace the ``'@'`` symbol with it.
- **Firma:** `def _replace_locals(tok)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _compose2(f, g)` (Línea 125)
- **Propósito:** Compose 2 callables.
- **Firma:** `def _compose2(f, g)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _compose()` (Línea 132)
- **Propósito:** Compose 2 or more callables.
- **Firma:** `def _compose()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _preparse(source, f)` (Línea 140)
- **Propósito:** Compose a collection of tokenization functions.

Parameters
----------
source : str
    A Python source code string
f : callable
    This takes a tuple of (toknum, tokval) as its argument and returns a
    tuple with the same structure but possibly different elements. Defaults
    to the composition of ``_rewrite_assign``, ``_replace_booleans``, and
    ``_replace_locals``.

Returns
-------
str
    Valid Python source code

Notes
-----
The `f` parameter can be any callable that takes *and* returns input of the
form ``(toknum, tokval)``, where ``toknum`` is one of the constants from
the ``tokenize`` module and ``tokval`` is a string.
- **Firma:** `def _preparse(source, f)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_type(t)` (Línea 177)
- **Propósito:** Factory for a type checking function of type ``t`` or tuple of types.
- **Firma:** `def _is_type(t)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _filter_nodes(superclass, all_nodes)` (Línea 196)
- **Propósito:** Filter out AST nodes that are subclasses of ``superclass``.
- **Firma:** `def _filter_nodes(superclass, all_nodes)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _node_not_implemented(node_name)` (Línea 261)
- **Propósito:** Return a function that raises a NotImplementedError with a passed node name.
- **Firma:** `def _node_not_implemented(node_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def disallow(nodes)` (Línea 278)
- **Propósito:** Decorator to disallow certain nodes from parsing. Raises a
NotImplementedError instead.

Returns
-------
callable
- **Firma:** `def disallow(nodes)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _op_maker(op_class, op_symbol)` (Línea 302)
- **Propósito:** Return a function to create an op class with its symbol already passed.

Returns
-------
callable
- **Firma:** `def _op_maker(op_class, op_symbol)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def add_ops(op_classes)` (Línea 327)
- **Propósito:** Decorator to add default implementation of ops.
- **Firma:** `def add_ops(op_classes)`
- **Retorno / Efectos:** Consulta código fuente.
