# Documentación Técnica: `venv_build/Lib/site-packages/pandas/core/dtypes/cast.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/core/dtypes/cast.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1879
- **Fecha de inspección:** 2026-08-05 18:36:16

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def maybe_convert_platform(values)` (Línea 127)
- **Propósito:** try to do platform conversion, allow ndarray or list here
- **Firma:** `def maybe_convert_platform(values)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_nested_object(obj)` (Línea 147)
- **Propósito:** return a boolean if we have a nested object, e.g. a Series with 1 or
more Series elements

This may not be necessarily be performant.
- **Firma:** `def is_nested_object(obj)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_box_datetimelike(value, dtype)` (Línea 162)
- **Propósito:** Cast scalar to Timestamp or Timedelta if scalar is datetime-like
and dtype is not object.

Parameters
----------
value : scalar
dtype : Dtype, optional

Returns
-------
scalar
- **Firma:** `def maybe_box_datetimelike(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_box_native(value)` (Línea 186)
- **Propósito:** If passed a scalar cast the scalar to a python native type.

Parameters
----------
value : scalar or Series

Returns
-------
scalar or Series
- **Firma:** `def maybe_box_native(value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_unbox_datetimelike(value, dtype)` (Línea 211)
- **Propósito:** Convert a Timedelta or Timestamp to timedelta64 or datetime64 for setting
into a numpy array.  Failing to unbox would risk dropping nanoseconds.

Notes
-----
Caller is responsible for checking dtype.kind in "mM"
- **Firma:** `def _maybe_unbox_datetimelike(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _disallow_mismatched_datetimelike(value, dtype)` (Línea 237)
- **Propósito:** numpy allows np.array(dt64values, dtype="timedelta64[ns]") and
vice-versa, but we do not want to allow this, so we need to
check explicitly
- **Firma:** `def _disallow_mismatched_datetimelike(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_to_dtype(result, dtype)` (Línea 253)
- **Propósito:** Sin docstring.
- **Firma:** `def maybe_downcast_to_dtype(result, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_to_dtype(result, dtype)` (Línea 257)
- **Propósito:** Sin docstring.
- **Firma:** `def maybe_downcast_to_dtype(result, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_to_dtype(result, dtype)` (Línea 262)
- **Propósito:** try to cast to the specified dtype (e.g. convert back to bool/int
or could be an astype of float64->float32
- **Firma:** `def maybe_downcast_to_dtype(result, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_numeric(result, dtype, do_round)` (Línea 297)
- **Propósito:** Sin docstring.
- **Firma:** `def maybe_downcast_numeric(result, dtype, do_round)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_numeric(result, dtype, do_round)` (Línea 303)
- **Propósito:** Sin docstring.
- **Firma:** `def maybe_downcast_numeric(result, dtype, do_round)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_downcast_numeric(result, dtype, do_round)` (Línea 308)
- **Propósito:** Subset of maybe_downcast_to_dtype restricted to numeric dtypes.

Parameters
----------
result : ndarray or ExtensionArray
dtype : np.dtype or ExtensionDtype
do_round : bool

Returns
-------
ndarray or ExtensionArray
- **Firma:** `def maybe_downcast_numeric(result, dtype, do_round)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_upcast_numeric_to_64bit(arr)` (Línea 394)
- **Propósito:** If array is an int/uint/float bit size lower than 64 bit, upcast it to 64 bit.

Parameters
----------
arr : ndarray or ExtensionArray

Returns
-------
ndarray or ExtensionArray
- **Firma:** `def maybe_upcast_numeric_to_64bit(arr)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_dtype_can_hold_na(dtype)` (Línea 418)
- **Propósito:** Sin docstring.
- **Firma:** `def ensure_dtype_can_hold_na(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_dtype_can_hold_na(dtype)` (Línea 422)
- **Propósito:** Sin docstring.
- **Firma:** `def ensure_dtype_can_hold_na(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_dtype_can_hold_na(dtype)` (Línea 425)
- **Propósito:** If we have a dtype that cannot hold NA values, find the best match that can.
- **Firma:** `def ensure_dtype_can_hold_na(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_promote(dtype, fill_value)` (Línea 451)
- **Propósito:** Find the minimal dtype that can hold both the given dtype and fill_value.

Parameters
----------
dtype : np.dtype
fill_value : scalar, default np.nan

Returns
-------
dtype
    Upcasted from dtype argument if necessary.
fill_value
    Upcasted from fill_value argument if necessary.

Raises
------
ValueError
    If fill_value is a non-scalar and dtype is not object.
- **Firma:** `def maybe_promote(dtype, fill_value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_promote_cached(dtype, fill_value, fill_value_type)` (Línea 509)
- **Propósito:** Sin docstring.
- **Firma:** `def _maybe_promote_cached(dtype, fill_value, fill_value_type)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_promote(dtype, fill_value)` (Línea 516)
- **Propósito:** Sin docstring.
- **Firma:** `def _maybe_promote(dtype, fill_value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _ensure_dtype_type(value, dtype)` (Línea 640)
- **Propósito:** Ensure that the given value is an instance of the given dtype.

e.g. if out dtype is np.complex64_, we should have an instance of that
as opposed to a python complex object.

Parameters
----------
value : object
dtype : np.dtype

Returns
-------
object
- **Firma:** `def _ensure_dtype_type(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def infer_dtype_from(val)` (Línea 665)
- **Propósito:** Interpret the dtype from a scalar or array.

Parameters
----------
val : object
- **Firma:** `def infer_dtype_from(val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def infer_dtype_from_scalar(val)` (Línea 678)
- **Propósito:** Interpret the dtype from a scalar.

Parameters
----------
val : object
- **Firma:** `def infer_dtype_from_scalar(val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def dict_compat(d)` (Línea 767)
- **Propósito:** Convert datetimelike-keyed dicts to a Timestamp-keyed dict.

Parameters
----------
d: dict-like object

Returns
-------
dict
- **Firma:** `def dict_compat(d)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def infer_dtype_from_array(arr)` (Línea 782)
- **Propósito:** Infer the dtype from an array.

Parameters
----------
arr : array

Returns
-------
tuple (pandas-compat dtype, array)


Examples
--------
>>> np.asarray([1, "1"])
array(['1', '1'], dtype='<U21')

>>> infer_dtype_from_array([1, "1"])
(dtype('O'), [1, '1'])
- **Firma:** `def infer_dtype_from_array(arr)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_infer_dtype_type(element)` (Línea 825)
- **Propósito:** Try to infer an object's dtype, for use in arithmetic ops.

Uses `element.dtype` if that's available.
Objects implementing the iterator protocol are cast to a NumPy array,
and from there the array's type is used.

Parameters
----------
element : object
    Possibly has a `.dtype` attribute, and possibly the iterator
    protocol.

Returns
-------
tipo : type

Examples
--------
>>> from collections import namedtuple
>>> Foo = namedtuple("Foo", "dtype")
>>> _maybe_infer_dtype_type(Foo(np.dtype("i8")))
dtype('int64')
- **Firma:** `def _maybe_infer_dtype_type(element)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def invalidate_string_dtypes(dtype_set)` (Línea 859)
- **Propósito:** Change string like dtypes to object for
``DataFrame.select_dtypes()``.
- **Firma:** `def invalidate_string_dtypes(dtype_set)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def coerce_indexer_dtype(indexer, categories)` (Línea 878)
- **Propósito:** coerce the indexer input array to the smallest dtype possible
- **Firma:** `def coerce_indexer_dtype(indexer, categories)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def convert_dtypes(input_array, convert_string, convert_integer, convert_boolean, convert_floating, infer_objects, dtype_backend)` (Línea 890)
- **Propósito:** Convert objects to best possible type, and optionally,
to types supporting ``pd.NA``.

Parameters
----------
input_array : ExtensionArray or np.ndarray
convert_string : bool, default True
    Whether object dtypes should be converted to ``StringDtype()``.
convert_integer : bool, default True
    Whether, if possible, conversion can be done to integer extension types.
convert_boolean : bool, defaults True
    Whether object dtypes should be converted to ``BooleanDtypes()``.
convert_floating : bool, defaults True
    Whether, if possible, conversion can be done to floating extension types.
    If `convert_integer` is also True, preference will be give to integer
    dtypes if the floats can be faithfully casted to integers.
infer_objects : bool, defaults False
    Whether to also infer objects to float/int if possible. Is only hit if the
    object array contains pd.NA.
dtype_backend : {'numpy_nullable', 'pyarrow'}, default 'numpy_nullable'
    Back-end data type applied to the resultant :class:`DataFrame`
    (still experimental). Behaviour is as follows:

    * ``"numpy_nullable"``: returns nullable-dtype
    * ``"pyarrow"``: returns pyarrow-backed nullable :class:`ArrowDtype`

    .. versionadded:: 2.0

Returns
-------
np.dtype, or ExtensionDtype
- **Firma:** `def convert_dtypes(input_array, convert_string, convert_integer, convert_boolean, convert_floating, infer_objects, dtype_backend)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_cast_to_datetime(value, dtype)` (Línea 1076)
- **Propósito:** try to cast the array/value to a datetimelike dtype, converting float
nan to iNaT

Caller is responsible for handling ExtensionDtype cases and non dt64/td64
cases.
- **Firma:** `def maybe_cast_to_datetime(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _ensure_nanosecond_dtype(dtype)` (Línea 1116)
- **Propósito:** Convert dtypes with granularity less than nanosecond to nanosecond

>>> _ensure_nanosecond_dtype(np.dtype("M8[us]"))

>>> _ensure_nanosecond_dtype(np.dtype("M8[D]"))
Traceback (most recent call last):
    ...
TypeError: dtype=datetime64[D] is not supported. Supported resolutions are 's', 'ms', 'us', and 'ns'

>>> _ensure_nanosecond_dtype(np.dtype("m8[ps]"))
Traceback (most recent call last):
    ...
TypeError: dtype=timedelta64[ps] is not supported. Supported resolutions are 's', 'ms', 'us', and 'ns'
- **Firma:** `def _ensure_nanosecond_dtype(dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_result_type(left_dtype, right)` (Línea 1160)
- **Propósito:** Find the type/dtype for the result of an operation between objects.

This is similar to find_common_type, but looks at the right object instead
of just its dtype. This can be useful in particular when the right
object does not have a `dtype`.

Parameters
----------
left_dtype : np.dtype or ExtensionDtype
right : Any

Returns
-------
np.dtype or ExtensionDtype

See also
--------
find_common_type
numpy.result_type
- **Firma:** `def find_result_type(left_dtype, right)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def common_dtype_categorical_compat(objs, dtype)` (Línea 1231)
- **Propósito:** Update the result of find_common_type to account for NAs in a Categorical.

Parameters
----------
objs : list[np.ndarray | ExtensionArray | Index]
dtype : np.dtype or ExtensionDtype

Returns
-------
np.dtype or ExtensionDtype
- **Firma:** `def common_dtype_categorical_compat(objs, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def np_find_common_type()` (Línea 1268)
- **Propósito:** np.find_common_type implementation pre-1.25 deprecation using np.result_type
https://github.com/pandas-dev/pandas/pull/49569#issuecomment-1308300065

Parameters
----------
dtypes : np.dtypes

Returns
-------
np.dtype
- **Firma:** `def np_find_common_type()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_common_type(types)` (Línea 1295)
- **Propósito:** Sin docstring.
- **Firma:** `def find_common_type(types)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_common_type(types)` (Línea 1299)
- **Propósito:** Sin docstring.
- **Firma:** `def find_common_type(types)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_common_type(types)` (Línea 1303)
- **Propósito:** Sin docstring.
- **Firma:** `def find_common_type(types)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_common_type(types)` (Línea 1306)
- **Propósito:** Find a common data type among the given dtypes.

Parameters
----------
types : list of dtypes

Returns
-------
pandas extension or numpy dtype

See Also
--------
numpy.find_common_type
- **Firma:** `def find_common_type(types)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def construct_2d_arraylike_from_scalar(value, length, width, dtype, copy)` (Línea 1361)
- **Propósito:** Sin docstring.
- **Firma:** `def construct_2d_arraylike_from_scalar(value, length, width, dtype, copy)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def construct_1d_arraylike_from_scalar(value, length, dtype)` (Línea 1392)
- **Propósito:** create an np.ndarray / pandas type of specified shape and dtype
filled with values

Parameters
----------
value : scalar value
length : int
dtype : pandas_dtype or np.dtype

Returns
-------
np.ndarray / pandas type of length, filled with value
- **Firma:** `def construct_1d_arraylike_from_scalar(value, length, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_unbox_numpy_scalar(value)` (Línea 1442)
- **Propósito:** Sin docstring.
- **Firma:** `def maybe_unbox_numpy_scalar(value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _maybe_box_and_unbox_datetimelike(value, dtype)` (Línea 1458)
- **Propósito:** Sin docstring.
- **Firma:** `def _maybe_box_and_unbox_datetimelike(value, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def construct_1d_object_array_from_listlike(values)` (Línea 1468)
- **Propósito:** Transform any list-like object in a 1-dimensional numpy array of object
dtype.

Parameters
----------
values : any iterable which has a len()

Raises
------
TypeError
    * If `values` does not have a len()

Returns
-------
1-dimensional numpy array of dtype object
- **Firma:** `def construct_1d_object_array_from_listlike(values)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_cast_to_integer_array(arr, dtype)` (Línea 1491)
- **Propósito:** Takes any dtype and returns the casted version, raising for when data is
incompatible with integer/unsigned integer dtypes.

Parameters
----------
arr : np.ndarray or list
    The array to cast.
dtype : np.dtype
    The integer dtype to cast the array to.

Returns
-------
ndarray
    Array of integer or unsigned integer dtype.

Raises
------
OverflowError : the dtype is incompatible with the data
ValueError : loss of precision has occurred during casting

Examples
--------
If you try to coerce negative values to unsigned integers, it raises:

>>> pd.Series([-1], dtype="uint64")
Traceback (most recent call last):
    ...
OverflowError: Trying to coerce negative values to unsigned integers

Also, if you try to coerce float values to integers, it raises:

>>> maybe_cast_to_integer_array([1, 2, 3.5], dtype=np.dtype("int64"))
Traceback (most recent call last):
    ...
ValueError: Trying to coerce float values to integers
- **Firma:** `def maybe_cast_to_integer_array(arr, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def can_hold_element(arr, element)` (Línea 1609)
- **Propósito:** Can we do an inplace setitem with this element in an array with this dtype?

Parameters
----------
arr : np.ndarray or ExtensionArray
element : Any

Returns
-------
bool
- **Firma:** `def can_hold_element(arr, element)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def np_can_hold_element(dtype, element)` (Línea 1654)
- **Propósito:** Raise if we cannot losslessly set this element into an ndarray with this dtype.

Specifically about places where we disagree with numpy.  i.e. there are
cases where numpy will raise in doing the setitem that we do not check
for here, e.g. setting str "X" into a numeric ndarray.

Returns
-------
Any
    The element, potentially cast to the dtype.

Raises
------
ValueError : If we cannot losslessly store this element with this dtype.
- **Firma:** `def np_can_hold_element(dtype, element)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _dtype_can_hold_range(rng, dtype)` (Línea 1850)
- **Propósito:** _maybe_infer_dtype_type infers to int64 (and float64 for very large endpoints),
but in many cases a range can be held by a smaller integer dtype.
Check if this is one of those cases.
- **Firma:** `def _dtype_can_hold_range(rng, dtype)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def np_can_cast_scalar(element, dtype)` (Línea 1861)
- **Propósito:** np.can_cast pandas-equivalent for pre 2-0 behavior that allowed scalar
inference

Parameters
----------
element : Scalar
dtype : np.dtype

Returns
-------
bool
- **Firma:** `def np_can_cast_scalar(element, dtype)`
- **Retorno / Efectos:** Consulta código fuente.
