# Documentación Técnica: `venv_build/Lib/site-packages/pandas/core/apply.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/core/apply.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2132
- **Fecha de inspección:** 2026-08-05 18:36:15

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `BaseExecutionEngine` (Línea 82)
- **Docstring:** _Base class for execution engines for map and apply methods.

An execution engine receives all the parameters of a call to
``apply`` or ``map``, such as the data container, the function,
etc. and takes care of running the execution.

Supporting different engines allows functions to be JIT compiled,
run in parallel, and others. Besides the default executor which
simply runs the code with the Python interpreter and pandas._
- **Métodos:**
  - `def map(data, func, args, kwargs, decorator, skip_na)` (Línea 97): Executor method to run functions elementwise.

In general, pandas uses ``map`` for running functions elementwise,
but ``Series.apply`` with the default ``by_row='compat'`` will also
call this executor function.

Parameters
----------
data : Series, DataFrame or NumPy ndarray
    The object to use for the data. Some methods implement a ``raw``
    parameter which will convert the original pandas object to a
    NumPy array, which will then be passed here to the executor.
func : function or NumPy ufunc
    The function to execute.
args : tuple
    Positional arguments to be passed to ``func``.
kwargs : dict
    Keyword arguments to be passed to ``func``.
decorator : function, optional
    For JIT compilers and other engines that need to decorate the
    function ``func``, this is the decorator to use. While the
    executor may already know which is the decorator to use, this
    is useful as for a single executor the user can specify for
    example ``numba.jit`` or ``numba.njit(nogil=True)``, and this
    decorator parameter will contain the exact decorator from the
    executor the user wants to use.
skip_na : bool
    Whether the function should be called for missing values or not.
    This is specified by the pandas user as ``map(na_action=None)``
    or ``map(na_action='ignore')``.
  - `def apply(data, func, args, kwargs, decorator, axis)` (Línea 140): Executor method to run functions by an axis.

While we can see ``map`` as executing the function for each cell
in a ``DataFrame`` (or ``Series``), ``apply`` will execute the
function for each column (or row).

Parameters
----------
data : Series, DataFrame or NumPy ndarray
    The object to use for the data. Some methods implement a ``raw``
    parameter which will convert the original pandas object to a
    NumPy array, which will then be passed here to the executor.
func : function or NumPy ufunc
    The function to execute.
args : tuple
    Positional arguments to be passed to ``func``.
kwargs : dict
    Keyword arguments to be passed to ``func``.
decorator : function, optional
    For JIT compilers and other engines that need to decorate the
    function ``func``, this is the decorator to use. While the
    executor may already know which is the decorator to use, this
    is useful as for a single executor the user can specify for
    example ``numba.jit`` or ``numba.njit(nogil=True)``, and this
    decorator parameter will contain the exact decorator from the
    executor the user wants to use.
axis : {0 or 'index', 1 or 'columns'}
    0 or 'index' should execute the function passing each column as
    parameter. 1 or 'columns' should execute the function passing
    each row as parameter. The default executor engine passes rows
    as pandas ``Series``. Other executor engines should probably
    expect functions to be implemented this way for compatibility.
    But passing rows as other data structures is technically possible
    as far as the function ``func`` is implemented accordingly.

#### Clase `Apply` (Línea 225)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, obj, func, raw, result_type)` (Línea 228): Sin docstring.
  - `def apply(self)` (Línea 264): Sin docstring.
  - `def agg_or_apply_list_like(self, op_name)` (Línea 268): Sin docstring.
  - `def agg_or_apply_dict_like(self, op_name)` (Línea 274): Sin docstring.
  - `def agg(self)` (Línea 279): Provide an implementation for the aggregators.

Returns
-------
Result of aggregation, or None if agg cannot be performed by
this method.
  - `def transform(self)` (Línea 302): Transform a DataFrame or Series.

Returns
-------
DataFrame or Series
    Result of applying ``func`` along the given axis of the
    Series or DataFrame.

Raises
------
ValueError
    If the transform function fails or does not transform.
  - `def transform_dict_like(self, func)` (Línea 365): Compute transform in the case of a dict-like func
  - `def transform_str_or_callable(self, func)` (Línea 389): Compute transform in the case of a string or callable func
  - `def agg_list_like(self)` (Línea 406): Compute aggregation in the case of a list-like argument.

Returns
-------
Result of aggregation.
  - `def compute_list_like(self, op_name, selected_obj, kwargs)` (Línea 416): Compute agg/apply results for like-like input.

Parameters
----------
op_name : {"agg", "apply"}
    Operation being performed.
selected_obj : Series or DataFrame
    Data to perform operation on.
kwargs : dict
    Keyword arguments to pass to the functions.

Returns
-------
keys : list[Hashable] or Index
    Index labels for result.
results : list
    Data for result. When aggregating with a Series, this can contain any
    Python objects.
  - `def wrap_results_list_like(self, keys, results)` (Línea 482): Sin docstring.
  - `def agg_dict_like(self)` (Línea 503): Compute aggregation in the case of a dict-like argument.

Returns
-------
Result of aggregation.
  - `def compute_dict_like(self, op_name, selected_obj, selection, kwargs)` (Línea 513): Compute agg/apply results for dict-like input.

Parameters
----------
op_name : {"agg", "apply"}
    Operation being performed.
selected_obj : Series or DataFrame
    Data to perform operation on.
selection : hashable or sequence of hashables
    Used by GroupBy, Window, and Resample if selection is applied to the object.
kwargs : dict
    Keyword arguments to pass to the functions.

Returns
-------
keys : list[hashable]
    Index labels for result.
results : list
    Data for result. When aggregating with a Series, this can contain any
    Python object.
  - `def wrap_results_dict_like(self, selected_obj, result_index, result_data)` (Línea 608): Sin docstring.
  - `def apply_str(self)` (Línea 668): Compute apply in case of a string.

Returns
-------
result: Series or DataFrame
  - `def apply_list_or_dict_like(self)` (Línea 703): Compute apply in case of a list-like or dict-like.

Returns
-------
result: Series, DataFrame, or None
    Result when self.func is a list-like or dict-like, None otherwise.
  - `def normalize_dictlike_arg(self, how, obj, func)` (Línea 734): Handler for dict-like argument.

Ensures that necessary columns exist if obj is a DataFrame, and
that a nested renamer is not passed. Also normalizes to all lists
when values consists of a mix of list and non-lists.
  - `def _apply_str(self, obj, func)` (Línea 780): if arg is a string, then try to operate on it:
- try to find a function (or attribute) on obj
- try to find a numpy function
- raise

#### Clase `NDFrameApply` (Línea 808)
- **Docstring:** _Methods shared by FrameApply and SeriesApply but
not GroupByApply or ResamplerWindowApply_
- **Métodos:**
  - `def index(self)` (Línea 817): Sin docstring.
  - `def agg_axis(self)` (Línea 821): Sin docstring.
  - `def agg_or_apply_list_like(self, op_name)` (Línea 824): Sin docstring.
  - `def agg_or_apply_dict_like(self, op_name)` (Línea 847): Sin docstring.

#### Clase `FrameApply` (Línea 869)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, obj, func, raw, result_type)` (Línea 872): Sin docstring.
  - `def result_index(self)` (Línea 904): Sin docstring.
  - `def result_columns(self)` (Línea 909): Sin docstring.
  - `def series_generator(self)` (Línea 914): Sin docstring.
  - `def generate_numba_apply_func(func, nogil, nopython, parallel)` (Línea 920): Sin docstring.
  - `def apply_with_numba(self)` (Línea 926): Sin docstring.
  - `def validate_values_for_numba(self)` (Línea 929): Sin docstring.
  - `def wrap_results_for_axis(self, results, res_index)` (Línea 944): Sin docstring.
  - `def res_columns(self)` (Línea 952): Sin docstring.
  - `def columns(self)` (Línea 956): Sin docstring.
  - `def values(self)` (Línea 960): Sin docstring.
  - `def apply(self)` (Línea 963): compute the results
  - `def agg(self)` (Línea 1017): Sin docstring.
  - `def apply_empty_result(self)` (Línea 1040): we have an empty result; at least 1 axis is 0

we will try to apply the function to an empty
series in order to see if this is a reduction function
  - `def apply_raw(self, engine, engine_kwargs)` (Línea 1086): apply to the values as a numpy array
  - `def apply_broadcast(self, target)` (Línea 1137): Sin docstring.
  - `def apply_standard(self)` (Línea 1165): Sin docstring.
  - `def apply_series_generator(self)` (Línea 1174): Sin docstring.
  - `def apply_series_numba(self)` (Línea 1191): Sin docstring.
  - `def wrap_results(self, results, res_index)` (Línea 1204): Sin docstring.
  - `def apply_str(self)` (Línea 1225): Sin docstring.

#### Clase `FrameRowApply` (Línea 1236)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def series_generator(self)` (Línea 1240): Sin docstring.
  - `def generate_numba_apply_func(func, nogil, nopython, parallel)` (Línea 1245): Sin docstring.
  - `def apply_with_numba(self)` (Línea 1272): Sin docstring.
  - `def result_index(self)` (Línea 1292): Sin docstring.
  - `def result_columns(self)` (Línea 1296): Sin docstring.
  - `def wrap_results_for_axis(self, results, res_index)` (Línea 1299): return the results for the rows

#### Clase `FrameColumnApply` (Línea 1341)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def apply_broadcast(self, target)` (Línea 1344): Sin docstring.
  - `def series_generator(self)` (Línea 1349): Sin docstring.
  - `def generate_numba_apply_func(func, nogil, nopython, parallel)` (Línea 1386): Sin docstring.
  - `def apply_with_numba(self)` (Línea 1414): Sin docstring.
  - `def result_index(self)` (Línea 1436): Sin docstring.
  - `def result_columns(self)` (Línea 1440): Sin docstring.
  - `def wrap_results_for_axis(self, results, res_index)` (Línea 1443): return the results for the columns
  - `def infer_to_same_shape(self, results, res_index)` (Línea 1464): infer the results to the same shape as the input object

#### Clase `SeriesApply` (Línea 1478)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, obj, func)` (Línea 1483): Sin docstring.
  - `def apply(self)` (Línea 1502): Sin docstring.
  - `def agg(self)` (Línea 1522): Sin docstring.
  - `def apply_empty_result(self)` (Línea 1532): Sin docstring.
  - `def apply_compat(self)` (Línea 1538): compat apply method for funcs in listlikes and dictlikes.

 Used for each callable when giving listlikes and dictlikes of callables to
 apply. Needed for compatibility with Pandas < v2.1.

.. versionadded:: 2.1.0
  - `def apply_standard(self)` (Línea 1560): Sin docstring.

#### Clase `GroupByApply` (Línea 1590)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, obj, func)` (Línea 1593): Sin docstring.
  - `def apply(self)` (Línea 1612): Sin docstring.
  - `def transform(self)` (Línea 1615): Sin docstring.
  - `def agg_or_apply_list_like(self, op_name)` (Línea 1618): Sin docstring.
  - `def agg_or_apply_dict_like(self, op_name)` (Línea 1644): Sin docstring.

#### Clase `ResamplerWindowApply` (Línea 1684)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, obj, func)` (Línea 1688): Sin docstring.
  - `def apply(self)` (Línea 1705): Sin docstring.
  - `def transform(self)` (Línea 1708): Sin docstring.

### Funciones independientes:

#### `def frame_apply(obj, func, axis, raw, result_type, by_row, engine, engine_kwargs, args, kwargs)` (Línea 186)
- **Propósito:** construct and return a row or column based frame apply object
- **Firma:** `def frame_apply(obj, func, axis, raw, result_type, by_row, engine, engine_kwargs, args, kwargs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def reconstruct_func(func)` (Línea 1712)
- **Propósito:** This is the internal function to reconstruct func given if there is relabeling
or not and also normalize the keyword to get new order of columns.

If named aggregation is applied, `func` will be None, and kwargs contains the
column and aggregation function information to be parsed;
If named aggregation is not applied, `func` is either string (e.g. 'min') or
Callable, or list of them (e.g. ['min', np.max]), or the dictionary of column name
and str/Callable/list of them (e.g. {'A': 'min'}, or {'A': [np.min, lambda x: x]})

If relabeling is True, will return relabeling, reconstructed func, column
names, and the reconstructed order of columns.
If relabeling is False, the columns and order will be None.

Parameters
----------
func: agg function (e.g. 'min' or Callable) or list of agg functions
    (e.g. ['min', np.max]) or dictionary (e.g. {'A': ['min', np.max]}).
**kwargs: dict, kwargs used in is_multi_agg_with_relabel and
    normalize_keyword_aggregation function for relabelling

Returns
-------
relabelling: bool, if there is relabelling or not
func: normalized and mangled func
columns: tuple of column names
order: array of columns indices

Examples
--------
>>> reconstruct_func(None, **{"foo": ("col", "min")})
(True, defaultdict(<class 'list'>, {'col': ['min']}), ('foo',), array([0]))

>>> reconstruct_func("min")
(False, 'min', None, None)
- **Firma:** `def reconstruct_func(func)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_multi_agg_with_relabel()` (Línea 1799)
- **Propósito:** Check whether kwargs passed to .agg look like multi-agg with relabeling.

Parameters
----------
**kwargs : dict

Returns
-------
bool

Examples
--------
>>> is_multi_agg_with_relabel(a="max")
False
>>> is_multi_agg_with_relabel(a_max=("a", "max"), a_min=("a", "min"))
True
>>> is_multi_agg_with_relabel()
False
- **Firma:** `def is_multi_agg_with_relabel()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_keyword_aggregation(kwargs)` (Línea 1825)
- **Propósito:** Normalize user-provided "named aggregation" kwargs.
Transforms from the new ``Mapping[str, NamedAgg]`` style kwargs
to the old Dict[str, List[scalar]]].

Parameters
----------
kwargs : dict

Returns
-------
aggspec : dict
    The transformed kwargs.
columns : tuple[str, ...]
    The user-provided keys.
col_idx_order : List[int]
    List of columns indices.

Examples
--------
>>> normalize_keyword_aggregation({"output": ("input", "sum")})
(defaultdict(<class 'list'>, {'input': ['sum']}), ('output',), array([0]))
- **Firma:** `def normalize_keyword_aggregation(kwargs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _make_unique_kwarg_list(seq)` (Línea 1886)
- **Propósito:** Uniquify aggfunc name of the pairs in the order list

Examples:
--------
>>> kwarg_list = [("a", "<lambda>"), ("a", "<lambda>"), ("b", "<lambda>")]
>>> _make_unique_kwarg_list(kwarg_list)
[('a', '<lambda>_0'), ('a', '<lambda>_1'), ('b', '<lambda>')]
- **Firma:** `def _make_unique_kwarg_list(seq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def relabel_result(result, func, columns, order)` (Línea 1904)
- **Propósito:** Internal function to reorder result if relabelling is True for
dataframe.agg, and return the reordered result in dict.

Parameters:
----------
result: Result from aggregation
func: Dict of (column name, funcs)
columns: New columns name for relabelling
order: New order for relabelling

Examples
--------
>>> from pandas.core.apply import relabel_result
>>> result = pd.DataFrame(
...     {"A": [np.nan, 2, np.nan], "C": [6, np.nan, np.nan], "B": [np.nan, 4, 2.5]},
...     index=["max", "mean", "min"],
... )
>>> funcs = {"A": ["max"], "C": ["max"], "B": ["mean", "min"]}
>>> columns = ("foo", "aab", "bar", "dat")
>>> order = [0, 1, 2, 3]
>>> result_in_dict = relabel_result(result, funcs, columns, order)
>>> pd.DataFrame(result_in_dict, index=columns)
       A    C    B
foo  2.0  NaN  NaN
aab  NaN  6.0  NaN
bar  NaN  NaN  4.0
dat  NaN  NaN  2.5
- **Firma:** `def relabel_result(result, func, columns, order)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def reconstruct_and_relabel_result(result, func)` (Línea 1988)
- **Propósito:** Sin docstring.
- **Firma:** `def reconstruct_and_relabel_result(result, func)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _managle_lambda_list(aggfuncs)` (Línea 2015)
- **Propósito:** Possibly mangle a list of aggfuncs.

Parameters
----------
aggfuncs : Sequence

Returns
-------
mangled: list-like
    A new AggSpec sequence, where lambdas have been converted
    to have unique names.

Notes
-----
If just one aggfunc is passed, the name will not be mangled.
- **Firma:** `def _managle_lambda_list(aggfuncs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def maybe_mangle_lambdas(agg_spec)` (Línea 2049)
- **Propósito:** Make new lambdas with unique names.

Parameters
----------
agg_spec : Any
    An argument to GroupBy.agg.
    Non-dict-like `agg_spec` are pass through as is.
    For dict-like `agg_spec` a new spec is returned
    with name-mangled lambdas.

Returns
-------
mangled : Any
    Same type as the input.

Examples
--------
>>> maybe_mangle_lambdas("sum")
'sum'
>>> maybe_mangle_lambdas([lambda: 1, lambda: 2])  # doctest: +SKIP
[<function __main__.<lambda_0>,
 <function pandas...._make_lambda.<locals>.f(*args, **kwargs)>]
- **Firma:** `def maybe_mangle_lambdas(agg_spec)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def validate_func_kwargs(kwargs)` (Línea 2093)
- **Propósito:** Validates types of user-provided "named aggregation" kwargs.
`TypeError` is raised if aggfunc is not `str` or callable.

Parameters
----------
kwargs : dict

Returns
-------
columns : List[str]
    List of user-provided keys.
func : List[Union[str, callable[...,Any]]]
    List of user-provided aggfuncs

Examples
--------
>>> validate_func_kwargs({"one": "min", "two": "max"})
(['one', 'two'], ['min', 'max'])
- **Firma:** `def validate_func_kwargs(kwargs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def include_axis(op_name, colg)` (Línea 2129)
- **Propósito:** Sin docstring.
- **Firma:** `def include_axis(op_name, colg)`
- **Retorno / Efectos:** Consulta código fuente.
