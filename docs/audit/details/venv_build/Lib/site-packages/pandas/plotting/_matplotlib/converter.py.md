# Documentación Técnica: `venv_build/Lib/site-packages/pandas/plotting/_matplotlib/converter.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pandas/plotting/_matplotlib/converter.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1130
- **Fecha de inspección:** 2026-08-05 18:36:18

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `TimeConverter` (Línea 156)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def convert(value, unit, axis)` (Línea 158): Sin docstring.
  - `def axisinfo(unit, axis)` (Línea 169): Sin docstring.
  - `def default_units(x, axis)` (Línea 178): Sin docstring.

#### Clase `TimeFormatter` (Línea 183)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, locs)` (Línea 184): Sin docstring.
  - `def __call__(self, x, pos)` (Línea 187): Return the time of day as a formatted string.

Parameters
----------
x : float
    The time of day specified as seconds since 00:00 (midnight),
    with up to microsecond precision.
pos
    Unused

Returns
-------
str
    A string in HH:MM:SS.mmmuuu format. Microseconds,
    milliseconds and seconds are only displayed if non-zero.

#### Clase `PeriodConverter` (Línea 226)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def convert(values, unit, axis)` (Línea 228): Sin docstring.
  - `def convert_from_freq(values, freq)` (Línea 241): Sin docstring.
  - `def _convert_1d(values, freq)` (Línea 249): Sin docstring.

#### Clase `DatetimeConverter` (Línea 290)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def convert(values, unit, axis)` (Línea 292): Sin docstring.
  - `def _convert_1d(values, unit, axis)` (Línea 307): Sin docstring.
  - `def axisinfo(unit, axis)` (Línea 343): Return the :class:`~matplotlib.units.AxisInfo` for *unit*.

*unit* is a tzinfo instance or None.
The *axis* argument is required but not used.

#### Clase `PandasAutoDateFormatter` (Línea 362)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, locator, tz, defaultfmt)` (Línea 363): Sin docstring.

#### Clase `PandasAutoDateLocator` (Línea 367)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def get_locator(self, dmin, dmax)` (Línea 368): Pick the best locator based on a distance.
  - `def _get_unit(self)` (Línea 389): Sin docstring.

#### Clase `MilliSecondLocator` (Línea 393)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tz)` (Línea 396): Sin docstring.
  - `def _get_unit(self)` (Línea 400): Sin docstring.
  - `def get_unit_generic(freq)` (Línea 404): Sin docstring.
  - `def __call__(self)` (Línea 410): Sin docstring.
  - `def _get_interval(self)` (Línea 455): Sin docstring.
  - `def autoscale(self)` (Línea 458): Set the view limits to include the data range.

#### Clase `TimeSeries_DateLocator` (Línea 933)
- **Docstring:** _Locates the ticks along an axis controlled by a :class:`Series`.

Parameters
----------
freq : BaseOffset
    Valid frequency specifier.
minor_locator : {False, True}, optional
    Whether the locator is for minor ticks (True) or not.
dynamic_mode : {True, False}, optional
    Whether the locator should work in dynamic mode.
base : {int}, optional
quarter : {int}, optional
month : {int}, optional
day : {int}, optional_
- **Métodos:**
  - `def __init__(self, freq, minor_locator, dynamic_mode, base, quarter, month, day, plot_obj)` (Línea 953): Sin docstring.
  - `def _get_default_locs(self, vmin, vmax)` (Línea 974): Returns the default locations of ticks.
  - `def __call__(self)` (Línea 982): Return the locations of the ticks.
  - `def autoscale(self)` (Línea 1001): Sets the view limits to the nearest multiples of base that contain the
data.

#### Clase `TimeSeries_DateFormatter` (Línea 1022)
- **Docstring:** _Formats the ticks along an axis controlled by a :class:`PeriodIndex`.

Parameters
----------
freq : BaseOffset
    Valid frequency specifier.
minor_locator : bool, default False
    Whether the current formatter should apply to minor ticks (True) or
    major ticks (False).
dynamic_mode : bool, default True
    Whether the formatter works in dynamic mode or not._
- **Métodos:**
  - `def __init__(self, freq, minor_locator, dynamic_mode, plot_obj)` (Línea 1039): Sin docstring.
  - `def _set_default_format(self, vmin, vmax)` (Línea 1057): Returns the default ticks spacing.
  - `def set_locs(self, locs)` (Línea 1068): Sets the locations of the ticks
  - `def __call__(self, x, pos)` (Línea 1080): Sin docstring.

#### Clase `TimeSeries_TimedeltaFormatter` (Línea 1098)
- **Docstring:** _Formats the ticks along an axis controlled by a :class:`TimedeltaIndex`._
- **Métodos:**
  - `def __init__(self, unit)` (Línea 1103): Sin docstring.
  - `def format_timedelta_ticks(x, pos, n_decimals, exp)` (Línea 1110): Convert seconds to 'D days HH:MM:SS.F'
  - `def __call__(self, x, pos)` (Línea 1126): Sin docstring.

### Funciones independientes:

#### `def get_pairs()` (Línea 70)
- **Propósito:** Sin docstring.
- **Firma:** `def get_pairs()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register_pandas_matplotlib_converters(func)` (Línea 82)
- **Propósito:** Decorator applying pandas_converters.
- **Firma:** `def register_pandas_matplotlib_converters(func)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pandas_converters()` (Línea 96)
- **Propósito:** Context manager registering pandas' converters for a plot.

See Also
--------
register_pandas_matplotlib_converters : Decorator that applies this.
- **Firma:** `def pandas_converters()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register()` (Línea 117)
- **Propósito:** Sin docstring.
- **Firma:** `def register()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def deregister()` (Línea 128)
- **Propósito:** Sin docstring.
- **Firma:** `def deregister()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _to_ordinalf(tm)` (Línea 142)
- **Propósito:** Sin docstring.
- **Firma:** `def _to_ordinalf(tm)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def time2num(d)` (Línea 147)
- **Propósito:** Sin docstring.
- **Firma:** `def time2num(d)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_datevalue(date, freq)` (Línea 277)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_datevalue(date, freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_default_annual_spacing(nyears)` (Línea 478)
- **Propósito:** Returns a default spacing between consecutive ticks for annual data.
- **Firma:** `def _get_default_annual_spacing(nyears)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _period_break(dates, period)` (Línea 500)
- **Propósito:** Returns the indices where the given period changes.

Parameters
----------
dates : PeriodIndex
    Array of intervals to monitor.
period : str
    Name of the period to monitor.
- **Firma:** `def _period_break(dates, period)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _period_break_mask(dates, period)` (Línea 515)
- **Propósito:** Sin docstring.
- **Firma:** `def _period_break_mask(dates, period)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def has_level_label(label_flags, vmin)` (Línea 521)
- **Propósito:** Returns true if the ``label_flags`` indicate there is at least one label
for this level.

if the minimum view limit is not an exact integer, then the first tick
label won't be shown, so we must adjust for that.
- **Firma:** `def has_level_label(label_flags, vmin)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_periods_per_ymd(freq)` (Línea 537)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_periods_per_ymd(freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _daily_finder(vmin, vmax, freq)` (Línea 574)
- **Propósito:** Sin docstring.
- **Firma:** `def _daily_finder(vmin, vmax, freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _monthly_finder(vmin, vmax, freq)` (Línea 774)
- **Propósito:** Sin docstring.
- **Firma:** `def _monthly_finder(vmin, vmax, freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _quarterly_finder(vmin, vmax, freq)` (Línea 846)
- **Propósito:** Sin docstring.
- **Firma:** `def _quarterly_finder(vmin, vmax, freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _annual_finder(vmin, vmax, freq)` (Línea 894)
- **Propósito:** Sin docstring.
- **Firma:** `def _annual_finder(vmin, vmax, freq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_finder(freq)` (Línea 916)
- **Propósito:** Sin docstring.
- **Firma:** `def get_finder(freq)`
- **Retorno / Efectos:** Consulta código fuente.
