# Documentación Técnica: `venv_build/Lib/site-packages/PyInstaller/building/utils.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PyInstaller/building/utils.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 853
- **Fecha de inspección:** 2026-08-05 18:36:04

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def _check_guts_eq(attr_name, old_value, new_value, last_build)` (Línea 47)
- **Propósito:** Rebuild is required if values differ.
- **Firma:** `def _check_guts_eq(attr_name, old_value, new_value, last_build)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _check_guts_toc_mtime(attr_name, old_toc, new_toc, last_build)` (Línea 57)
- **Propósito:** Rebuild is required if mtimes of files listed in old TOC are newer than last_build.

Use this for calculated/analysed values read from cache.
- **Firma:** `def _check_guts_toc_mtime(attr_name, old_toc, new_toc, last_build)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _check_guts_toc(attr_name, old_toc, new_toc, last_build)` (Línea 70)
- **Propósito:** Rebuild is required if either TOC content changed or mtimes of files listed in old TOC are newer than last_build.

Use this for input parameters.
- **Firma:** `def _check_guts_toc(attr_name, old_toc, new_toc, last_build)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def destination_name_for_extension(module_name, src_name, typecode)` (Línea 80)
- **Propósito:** Take a TOC entry (dest_name, src_name, typecode) and determine the full destination name for the extension.
- **Firma:** `def destination_name_for_extension(module_name, src_name, typecode)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def process_collected_binary(src_name, dest_name, use_strip, use_upx, upx_exclude, target_arch, codesign_identity, entitlements_file, strict_arch_validation)` (Línea 107)
- **Propósito:** Process the collected binary using strip or UPX (or both), and apply any platform-specific processing. On macOS,
this rewrites the library paths in the headers, and (re-)signs the binary. On-disk cache is used to avoid processing
the same binary with same options over and over.

In addition to given arguments, this function also uses CONF['cachedir'] and CONF['upx_dir'].
- **Firma:** `def process_collected_binary(src_name, dest_name, use_strip, use_upx, upx_exclude, target_arch, codesign_identity, entitlements_file, strict_arch_validation)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _compute_file_digest(filename)` (Línea 356)
- **Propósito:** Sin docstring.
- **Firma:** `def _compute_file_digest(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _check_path_overlap(path)` (Línea 364)
- **Propósito:** Check that path does not overlap with WORKPATH or SPECPATH (i.e., WORKPATH and SPECPATH may not start with path,
which could be caused by a faulty hand-edited specfile).

Raise SystemExit if there is overlap, return True otherwise
- **Firma:** `def _check_path_overlap(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _make_clean_directory(path)` (Línea 387)
- **Propósito:** Create a clean directory from the given directory name.
- **Firma:** `def _make_clean_directory(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _rmtree(path)` (Línea 401)
- **Propósito:** Remove directory and all its contents, but only after user confirmation, or if the -y option is set.
- **Firma:** `def _rmtree(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def format_binaries_and_datas(binaries_or_datas, workingdir)` (Línea 434)
- **Propósito:** Convert the passed list of hook-style 2-tuples into a returned set of `TOC`-style 2-tuples.

Elements of the passed list are 2-tuples `(source_dir_or_glob, target_dir)`.
Elements of the returned set are 2-tuples `(target_file, source_file)`.
For backwards compatibility, the order of elements in the former tuples are the reverse of the order of elements in
the latter tuples!

Parameters
----------
binaries_or_datas : list
    List of hook-style 2-tuples (e.g., the top-level `binaries` and `datas` attributes defined by hooks) whose:
    * The first element is either:
      * A glob matching only the absolute or relative paths of source non-Python data files.
      * The absolute or relative path of a source directory containing only source non-Python data files.
    * The second element is the relative path of the target directory into which these source files will be
      recursively copied.

    If the optional `workingdir` parameter is passed, source paths may be either absolute or relative; else, source
    paths _must_ be absolute.
workingdir : str
    Optional absolute path of the directory to which all relative source paths in the `binaries_or_datas`
    parameter will be prepended by (and hence converted into absolute paths) _or_ `None` if these paths are to be
    preserved as relative. Defaults to `None`.

Returns
----------
set
    Set of `TOC`-style 2-tuples whose:
    * First element is the absolute or relative path of a target file.
    * Second element is the absolute or relative path of the corresponding source file to be copied to this target
      file.
- **Firma:** `def format_binaries_and_datas(binaries_or_datas, workingdir)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_code_object(modname, filename, optimize)` (Línea 556)
- **Propósito:** Get the code-object for a module.

This is a simplifed non-performant version which circumvents __pycache__.
- **Firma:** `def get_code_object(modname, filename, optimize)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def replace_filename_in_code_object(code_object, filename)` (Línea 602)
- **Propósito:** Recursively replace the `co_filename` in the given code object and code objects stored in its `co_consts` entries.
Primarily used to anonymize collected code objects, i.e., by removing the build environment's paths from them.
- **Firma:** `def replace_filename_in_code_object(code_object, filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _should_include_system_binary(binary_tuple, exceptions)` (Línea 616)
- **Propósito:** Return True if the given binary_tuple describes a system binary that should be included.

Exclude all system library binaries other than those with "lib-dynload" in the destination or "python" in the
source, except for those matching the patterns in the exceptions list. Intended to be used from the Analysis
exclude_system_libraries method.
- **Firma:** `def _should_include_system_binary(binary_tuple, exceptions)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def compile_pymodule(name, src_path, workpath, optimize, code_cache)` (Línea 645)
- **Propósito:** Given the name and source file for a pure-python module, compile the module in the specified working directory,
and return the name of resulting .pyc file. The paths in the resulting .pyc module are anonymized by having their
absolute prefix removed.

If a .pyc file with matching name already exists in the target working directory, it is re-used (provided it has
compatible bytecode magic in the header, and that its modification time is newer than that of the source file).

If the specified module is available in binary-only form, the input .pyc file is copied to the target working
directory and post-processed. If the specified module is available in source form, it is compiled only if
corresponding code object is not available in the optional code-object cache; otherwise, it is copied from cache
and post-processed. When compiling the module, the specified byte-code optimization level is used.

It is up to caller to ensure that the optional code-object cache contains only code-objects of target optimization
level, and that if the specified working directory already contains .pyc files, that they were created with target
optimization level.
- **Firma:** `def compile_pymodule(name, src_path, workpath, optimize, code_cache)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _read_pyc_data(filename)` (Línea 731)
- **Propósito:** Helper for reading data from .pyc files. Supports both stand-alone and archive-embedded .pyc files. Used by
`compile_pymodule` and `get_code_object` helper functions.
- **Firma:** `def _read_pyc_data(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def postprocess_binaries_toc_pywin32(binaries)` (Línea 760)
- **Propósito:** Process the given `binaries` TOC list to apply work around for `pywin32` package, fixing the target directory
for collected extensions.
- **Firma:** `def postprocess_binaries_toc_pywin32(binaries)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def postprocess_binaries_toc_pywin32_anaconda(binaries)` (Línea 787)
- **Propósito:** Process the given `binaries` TOC list to apply work around for Anaconda `pywin32` package, fixing the location
of collected `pywintypes3X.dll` and `pythoncom3X.dll`.
- **Firma:** `def postprocess_binaries_toc_pywin32_anaconda(binaries)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def create_base_library_zip(filename, modules_toc, code_cache)` (Línea 825)
- **Propósito:** Create a zip archive with python modules that are needed during python interpreter initialization.
- **Firma:** `def create_base_library_zip(filename, modules_toc, code_cache)`
- **Retorno / Efectos:** Consulta código fuente.
