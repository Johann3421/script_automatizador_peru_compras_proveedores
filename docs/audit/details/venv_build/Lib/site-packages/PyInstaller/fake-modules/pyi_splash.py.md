# Documentación Técnica: `venv_build/Lib/site-packages/PyInstaller/fake-modules/pyi_splash.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PyInstaller/fake-modules/pyi_splash.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 211
- **Fecha de inspección:** 2026-08-05 18:36:05

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def _log(level, msg)` (Línea 57)
- **Propósito:** Conditional wrapper around logging module. If the user excluded logging from the imports or it was not imported,
this function should handle it and avoid using the logger.
- **Firma:** `def _log(level, msg)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _initialize()` (Línea 79)
- **Propósito:** Initialize this module

:return:
- **Firma:** `def _initialize()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _check_connection(func)` (Línea 125)
- **Propósito:** Utility decorator for checking whether the function should be executed.

The wrapped function may raise a ConnectionError if the module was not initialized correctly.
- **Firma:** `def _check_connection(func)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _send_command(cmd, args)` (Línea 156)
- **Propósito:** Send the command followed by args to the splash screen.

:param str cmd: The command to send. All command have to be defined as procedures in the tcl splash screen script.
:param list[str] args: All arguments to send to the receiving function
- **Firma:** `def _send_command(cmd, args)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_alive()` (Línea 173)
- **Propósito:** Indicates whether the module can be used.

Returns False if the module is either not initialized or was disabled by closing the splash screen. Otherwise,
the module should be usable.
- **Firma:** `def is_alive()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def update_text(msg)` (Línea 184)
- **Propósito:** Updates the text on the splash screen window.

:param str msg: the text to be displayed
:raises ConnectionError: If the OS fails to write to the socket.
:raises RuntimeError: If the module is not initialized.
- **Firma:** `def update_text(msg)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def close()` (Línea 195)
- **Propósito:** Close the connection to the ipc tcp server socket.

This will close the splash screen and renders this module unusable. After this function is called, no connection
can be opened to the splash screen again and all functions in this module become unusable.
- **Firma:** `def close()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _exit()` (Línea 210)
- **Propósito:** Sin docstring.
- **Firma:** `def _exit()`
- **Retorno / Efectos:** Consulta código fuente.
