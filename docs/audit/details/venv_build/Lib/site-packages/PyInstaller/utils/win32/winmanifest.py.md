# Documentación Técnica: `venv_build/Lib/site-packages/PyInstaller/utils/win32/winmanifest.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PyInstaller/utils/win32/winmanifest.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 244
- **Fecha de inspección:** 2026-08-05 18:36:05

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def _find_elements_by_tag(root, tag)` (Línea 62)
- **Propósito:** Find all elements with given tag under the given root element.
- **Firma:** `def _find_elements_by_tag(root, tag)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_element_by_tag(root, tag)` (Línea 69)
- **Propósito:** Attempt to find a single element with given tag under the given root element, and return None if no such element
is found. Raises an error if multiple elements are found.
- **Firma:** `def _find_element_by_tag(root, tag)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _set_execution_level(manifest_dom, root_element, uac_admin, uac_uiaccess)` (Línea 85)
- **Propósito:** Find <security> -> <requestedPrivileges> -> <requestedExecutionLevel> element, and set its `level` and `uiAccess`
attributes based on supplied arguments. Create the XML elements if necessary, as they are optional.
- **Firma:** `def _set_execution_level(manifest_dom, root_element, uac_admin, uac_uiaccess)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _ensure_common_controls_dependency(manifest_dom, root_element)` (Línea 120)
- **Propósito:** Scan <dependency> elements for the one whose <<dependentAssembly> -> <assemblyIdentity> corresponds to the
`Microsoft.Windows.Common-Controls`. If found, overwrite its properties. If not, create new <dependency>
element with corresponding sub-elements and attributes.
- **Firma:** `def _ensure_common_controls_dependency(manifest_dom, root_element)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def create_application_manifest(manifest_xml, uac_admin, uac_uiaccess)` (Línea 157)
- **Propósito:** Create application manifest, from built-in or custom manifest XML template. If provided, `manifest_xml` must be
a string or byte string containing XML source. The returned manifest is a byte string, encoded in UTF-8.

This function sets the attributes of `requestedExecutionLevel` based on provided `uac_admin` and `auc_uiacces`
arguments (creating the parent elements in the XML, if necessary). It also scans `dependency` elements for the
entry corresponding to `Microsoft.Windows.Common-Controls` and creates or modifies it as necessary.
- **Firma:** `def create_application_manifest(manifest_xml, uac_admin, uac_uiaccess)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def write_manifest_to_executable(filename, manifest_xml)` (Línea 197)
- **Propósito:** Write the given manifest XML to the given executable's RT_MANIFEST resource.
- **Firma:** `def write_manifest_to_executable(filename, manifest_xml)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def read_manifest_from_executable(filename)` (Línea 213)
- **Propósito:** Read manifest from the given executable."
- **Firma:** `def read_manifest_from_executable(filename)`
- **Retorno / Efectos:** Consulta código fuente.
