# Documentación Técnica: `venv_build/Lib/site-packages/PyInstaller/utils/conftest.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PyInstaller/utils/conftest.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 580
- **Fecha de inspección:** 2026-08-05 18:36:05

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `AppBuilder` (Línea 155)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tmp_path, request, bundle_mode)` (Línea 156): Sin docstring.
  - `def test_spec(self, specfile)` (Línea 165): Test a Python script that is referenced in the supplied .spec file.
  - `def test_source(self, source)` (Línea 175): Test a Python script given as source code.

The source will be written into a file named like the test-function. This file will then be passed to
`test_script`. If you need other related file, e.g., as `.toc`-file for testing the content, put it at at the
normal place. Just mind to take the basnename from the test-function's name.

:param script: Source code to create executable from. This will be saved into a temporary file which is then
               passed on to `test_script`.

:param test_id: Test-id for parametrized tests. If given, it will be appended to the script filename, separated
                by two underscores.

All other arguments are passed straight on to `test_script`.
  - `def _display_message(self, step_name, message)` (Línea 197): Sin docstring.
  - `def test_script(self, script, pyi_args, app_name, app_args, runtime, run_from_path)` (Línea 203): Main method to wrap all phases of testing a Python script.

:param script: Name of script to create executable from.
:param pyi_args: Additional arguments to pass to PyInstaller when creating executable.
:param app_name: Name of the executable. This is equivalent to argument --name=APPNAME.
:param app_args: Additional arguments to pass to
:param runtime: Time in seconds how long to keep executable running.
:param toc_log: List of modules that are expected to be bundled with the executable.
  - `def _test_executables(self, name, args, runtime, run_from_path)` (Línea 249): Run created executable to make sure it works.

Multipackage-tests generate more than one exe-file and all of them have to be run.

:param args: CLI options to pass to the created executable.
:param runtime: Time in seconds how long to keep the executable running.

:return: Exit code of the executable.
  - `def _find_executables(self, name)` (Línea 275): Search for all executables generated by the testcase.

If the test-case is called e.g. 'test_multipackage1', this is searching for each of 'test_multipackage1.exe'
and 'multipackage1_?.exe' in both one-file- and one-dir-mode.

:param name: Name of the executable to look for.

:return: List of executables
  - `def _run_executable(self, prog, args, run_from_path, runtime)` (Línea 312): Run executable created by PyInstaller.

:param args: CLI options to pass to the created executable.
  - `def _run_executable_(self, args, exe_path, prog_env, prog_cwd, runtime)` (Línea 355): Sin docstring.
  - `def _test_building(self, args)` (Línea 445): Run building of test script.

:param args: additional CLI options for PyInstaller.

Return True if build succeeded False otherwise.
  - `def _examine_executable(self, exe, toc_log)` (Línea 488): Compare log files (now used mostly by multipackage test_name).

:return: True if .toc files match

### Funciones independientes:

#### `def pytest_runtest_setup(item)` (Línea 56)
- **Propósito:** Markers to skip tests based on the current platform.
https://pytest.org/en/stable/example/markers.html#marking-platform-specific-tests-with-pytest

Available markers: see pytest.ini markers
    - @pytest.mark.darwin (macOS)
    - @pytest.mark.linux (GNU/Linux)
    - @pytest.mark.win32 (Windows)
- **Firma:** `def pytest_runtest_setup(item)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pytest_runtest_makereport(item, call)` (Línea 75)
- **Propósito:** Sin docstring.
- **Firma:** `def pytest_runtest_makereport(item, call)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_base_dir(request)` (Línea 85)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_base_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_script_dir(request)` (Línea 90)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_script_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_modules_dir(request)` (Línea 95)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_modules_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_logs_dir(request)` (Línea 100)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_logs_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_data_dir(request)` (Línea 105)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_data_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_spec_dir(request)` (Línea 110)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_spec_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def spec_dir(request)` (Línea 115)
- **Propósito:** Return the directory where the test spec-files reside.
- **Firma:** `def spec_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def script_dir(request)` (Línea 123)
- **Propósito:** Return the directory where the test scripts reside.
- **Firma:** `def script_dir(request)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_dir(request, tmp_path)` (Línea 133)
- **Propósito:** Sin docstring.
- **Firma:** `def data_dir(request, tmp_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pyi_modgraph()` (Línea 518)
- **Propósito:** Sin docstring.
- **Firma:** `def pyi_modgraph()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pyi_builder(tmp_path, monkeypatch, request, pyi_modgraph)` (Línea 528)
- **Propósito:** Sin docstring.
- **Firma:** `def pyi_builder(tmp_path, monkeypatch, request, pyi_modgraph)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pyi_builder_spec(tmp_path, request, monkeypatch, pyi_modgraph)` (Línea 549)
- **Propósito:** Sin docstring.
- **Firma:** `def pyi_builder_spec(tmp_path, request, monkeypatch, pyi_modgraph)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pyi_windowed_builder(pyi_builder)` (Línea 569)
- **Propósito:** A pyi_builder equivalent for testing --windowed applications.
- **Firma:** `def pyi_windowed_builder(pyi_builder)`
- **Retorno / Efectos:** Consulta código fuente.
