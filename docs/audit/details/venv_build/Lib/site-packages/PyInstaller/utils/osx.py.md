# Documentación Técnica: `venv_build/Lib/site-packages/PyInstaller/utils/osx.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PyInstaller/utils/osx.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 757
- **Fecha de inspección:** 2026-08-05 18:36:05

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `InvalidBinaryError` (Línea 325)
- **Docstring:** _Exception raised by `get_binary_architectures` when it is passed an invalid binary._

#### Clase `IncompatibleBinaryArchError` (Línea 332)
- **Docstring:** _Exception raised by `binary_to_target_arch` when the passed binary fails the strict architecture check._
- **Métodos:**
  - `def __init__(self, message)` (Línea 336): Sin docstring.

### Funciones independientes:

#### `def is_homebrew_env()` (Línea 46)
- **Propósito:** Check if Python interpreter was installed via Homebrew command 'brew'.

:return: True if Homebrew else otherwise.
- **Firma:** `def is_homebrew_env()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_macports_env()` (Línea 59)
- **Propósito:** Check if Python interpreter was installed via Macports command 'port'.

:return: True if Macports else otherwise.
- **Firma:** `def is_macports_env()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_homebrew_prefix()` (Línea 72)
- **Propósito:** :return: Root path of the Homebrew environment.
- **Firma:** `def get_homebrew_prefix()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_macports_prefix()` (Línea 82)
- **Propósito:** :return: Root path of the Macports environment.
- **Firma:** `def get_macports_prefix()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_version_cmd(header)` (Línea 92)
- **Propósito:** Helper that finds the version command in the given MachO header.
- **Firma:** `def _find_version_cmd(header)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_macos_sdk_version(filename)` (Línea 104)
- **Propósito:** Obtain the version of macOS SDK against which the given binary was built.

NOTE: currently, version is retrieved only from the first arch slice in the binary.

:return: (major, minor, revision) tuple
- **Firma:** `def get_macos_sdk_version(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _hex_triplet(version)` (Línea 119)
- **Propósito:** Sin docstring.
- **Firma:** `def _hex_triplet(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def macosx_version_min(filename)` (Línea 127)
- **Propósito:** Get the -macosx-version-min used to compile a macOS binary.

For fat binaries, the minimum version is selected.
- **Firma:** `def macosx_version_min(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_macos_sdk_version(filename, major, minor, revision)` (Línea 145)
- **Propósito:** Overwrite the macOS SDK version declared in the given binary with the specified version.

NOTE: currently, only version in the first arch slice is modified.
- **Firma:** `def set_macos_sdk_version(filename, major, minor, revision)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def fix_exe_for_code_signing(filename)` (Línea 167)
- **Propósito:** Fixes the Mach-O headers to make code signing possible.

Code signing on macOS does not work out of the box with embedding .pkg archive into the executable.

The fix is done this way:
- Make the embedded .pkg archive part of the Mach-O 'String Table'. 'String Table' is at end of the macOS exe file,
  so just change the size of the table to cover the end of the file.
- Fix the size of the __LINKEDIT segment.

Note: the above fix works only if the single-arch thin executable or the last arch slice in a multi-arch fat
executable is not signed, because LC_CODE_SIGNATURE comes after LC_SYMTAB, and because modification of headers
invalidates the code signature. On modern arm64 macOS, code signature is mandatory, and therefore compilers
create a dummy signature when executable is built. In such cases, that signature needs to be removed before this
function is called.

Mach-O format specification: http://developer.apple.com/documentation/Darwin/Reference/ManPages/man5/Mach-O.5.html
- **Firma:** `def fix_exe_for_code_signing(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_arch_string(header)` (Línea 260)
- **Propósito:** Converts cputype and cpusubtype from mach_o.mach_header_64 into arch string comparible with lipo/codesign.
The list of supported architectures can be found in man(1) arch.
- **Firma:** `def _get_arch_string(header)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def update_exe_identifier(filename, pkg_filename)` (Línea 283)
- **Propósito:** Modifies the Mach-O image UUID stored in the LC_UUID command (if present) in order to ensure that different
frozen applications have different identifiers. See TN3178 for details on why this is required:
https://developer.apple.com/documentation/technotes/tn3178-checking-for-and-resolving-build-uuid-problems
- **Firma:** `def update_exe_identifier(filename, pkg_filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_binary_architectures(filename)` (Línea 341)
- **Propósito:** Inspects the given binary and returns tuple (is_fat, archs), where is_fat is boolean indicating fat/thin binary,
and arch is list of architectures with lipo/codesign compatible names.
- **Firma:** `def get_binary_architectures(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def convert_binary_to_thin_arch(filename, thin_arch, output_filename)` (Línea 353)
- **Propósito:** Convert the given fat binary into thin one with the specified target architecture.
- **Firma:** `def convert_binary_to_thin_arch(filename, thin_arch, output_filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def merge_into_fat_binary(output_filename)` (Línea 364)
- **Propósito:** Merge the given single-arch thin binary files into a fat binary.
- **Firma:** `def merge_into_fat_binary(output_filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def binary_to_target_arch(filename, target_arch, display_name)` (Línea 374)
- **Propósito:** Check that the given binary contains required architecture slice(s) and convert the fat binary into thin one,
if necessary.
- **Firma:** `def binary_to_target_arch(filename, target_arch, display_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def remove_signature_from_binary(filename)` (Línea 402)
- **Propósito:** Remove the signature from all architecture slices of the given binary file using the codesign utility.
- **Firma:** `def remove_signature_from_binary(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sign_binary(filename, identity, entitlements_file, deep)` (Línea 413)
- **Propósito:** Sign the binary using codesign utility. If no identity is provided, ad-hoc signing is performed.
- **Firma:** `def sign_binary(filename, identity, entitlements_file, deep)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_dylib_dependency_paths(filename, target_rpath)` (Línea 437)
- **Propósito:** Modify the given dylib's identity (in LC_ID_DYLIB command) and the paths to dependent dylibs (in LC_LOAD_DYLIB)
commands into `@rpath/<basename>` format, remove any existing rpaths (LC_RPATH commands), and add a new rpath
(LC_RPATH command) with the specified path.

Uses `install-tool-name` utility to make the changes.

The system libraries (e.g., the ones found in /usr/lib) are exempted from path rewrite.

For multi-arch fat binaries, this function extracts each slice into temporary file, processes it separately,
and then merges all processed slices back into fat binary. This is necessary because `install-tool-name` cannot
modify rpaths in cases when an existing rpath is present only in one slice.
- **Firma:** `def set_dylib_dependency_paths(filename, target_rpath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _set_dylib_dependency_paths(filename, target_rpath)` (Línea 471)
- **Propósito:** The actual implementation of set_dylib_dependency_paths functionality.

Implicitly assumes that a single-arch thin binary is given.
- **Firma:** `def _set_dylib_dependency_paths(filename, target_rpath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_framework_bundle_lib(lib_path)` (Línea 593)
- **Propósito:** Check if the given shared library is part of a .framework bundle.
- **Firma:** `def is_framework_bundle_lib(lib_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def collect_files_from_framework_bundles(collected_files)` (Línea 609)
- **Propósito:** Scan the given TOC list of collected files for shared libraries that are collected from macOS .framework bundles,
and collect the bundles' Info.plist files. Additionally, create/restore the following symbolic links:
  - `Versions/Current` pointing to the `Versions/<version>` directory containing the binary
  - `<name>` in the top-level .framework directory, pointing to `Versions/Current/<name>`
  - `Resources` in the top-level .framework directory, pointing to `Versions/Current/Resources`
  - additional directories in top-level .framework directory, pointing to their counterparts in `Versions/Current`
    directory.

Returns updated TOC list with added entries for the discovered Info.plist files and generated symbolic links.
- **Firma:** `def collect_files_from_framework_bundles(collected_files)`
- **Retorno / Efectos:** Consulta código fuente.
