# Documentación Técnica: `venv_build/Lib/site-packages/PIL/ImageChops.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PIL/ImageChops.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 311
- **Fecha de inspección:** 2026-08-05 18:36:04

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def constant(image, value)` (Línea 23)
- **Propósito:** Fill a channel with a given gray level.

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def constant(image, value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def duplicate(image)` (Línea 32)
- **Propósito:** Copy a channel. Alias for :py:meth:`PIL.Image.Image.copy`.

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def duplicate(image)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def invert(image)` (Línea 41)
- **Propósito:** Invert an image (channel). ::

    out = MAX - image

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def invert(image)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def lighter(image1, image2)` (Línea 54)
- **Propósito:** Compares the two images, pixel by pixel, and returns a new image containing
the lighter values. ::

    out = max(image1, image2)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def lighter(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def darker(image1, image2)` (Línea 69)
- **Propósito:** Compares the two images, pixel by pixel, and returns a new image containing
the darker values. ::

    out = min(image1, image2)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def darker(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def difference(image1, image2)` (Línea 84)
- **Propósito:** Returns the absolute value of the pixel-by-pixel difference between the two
images. ::

    out = abs(image1 - image2)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def difference(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def multiply(image1, image2)` (Línea 99)
- **Propósito:** Superimposes two images on top of each other.

If you multiply an image with a solid black image, the result is black. If
you multiply with a solid white image, the image is unaffected. ::

    out = image1 * image2 / MAX

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def multiply(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def screen(image1, image2)` (Línea 116)
- **Propósito:** Superimposes two inverted images on top of each other. ::

    out = MAX - ((MAX - image1) * (MAX - image2) / MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def screen(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def soft_light(image1, image2)` (Línea 130)
- **Propósito:** Superimposes two images on top of each other using the Soft Light algorithm

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def soft_light(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def hard_light(image1, image2)` (Línea 142)
- **Propósito:** Superimposes two images on top of each other using the Hard Light algorithm

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def hard_light(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def overlay(image1, image2)` (Línea 154)
- **Propósito:** Superimposes two images on top of each other using the Overlay algorithm

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def overlay(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def add(image1, image2, scale, offset)` (Línea 166)
- **Propósito:** Adds two images, dividing the result by scale and adding the
offset. If omitted, scale defaults to 1.0, and offset to 0.0. ::

    out = ((image1 + image2) / scale + offset)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def add(image1, image2, scale, offset)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def subtract(image1, image2, scale, offset)` (Línea 183)
- **Propósito:** Subtracts two images, dividing the result by scale and adding the offset.
If omitted, scale defaults to 1.0, and offset to 0.0. ::

    out = ((image1 - image2) / scale + offset)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def subtract(image1, image2, scale, offset)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def add_modulo(image1, image2)` (Línea 200)
- **Propósito:** Add two images, without clipping the result. ::

    out = ((image1 + image2) % MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def add_modulo(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def subtract_modulo(image1, image2)` (Línea 213)
- **Propósito:** Subtract two images, without clipping the result. ::

    out = ((image1 - image2) % MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def subtract_modulo(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def logical_and(image1, image2)` (Línea 226)
- **Propósito:** Logical AND between two images.

Both of the images must have mode "1". If you would like to perform a
logical AND on an image with a mode other than "1", try
:py:meth:`~PIL.ImageChops.multiply` instead, using a black-and-white mask
as the second image. ::

    out = ((image1 and image2) % MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def logical_and(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def logical_or(image1, image2)` (Línea 244)
- **Propósito:** Logical OR between two images.

Both of the images must have mode "1". ::

    out = ((image1 or image2) % MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def logical_or(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def logical_xor(image1, image2)` (Línea 259)
- **Propósito:** Logical XOR between two images.

Both of the images must have mode "1". ::

    out = ((bool(image1) != bool(image2)) % MAX)

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def logical_xor(image1, image2)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def blend(image1, image2, alpha)` (Línea 274)
- **Propósito:** Blend images using constant transparency weight. Alias for
:py:func:`PIL.Image.blend`.

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def blend(image1, image2, alpha)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def composite(image1, image2, mask)` (Línea 284)
- **Propósito:** Create composite using transparency mask. Alias for
:py:func:`PIL.Image.composite`.

:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def composite(image1, image2, mask)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def offset(image, xoffset, yoffset)` (Línea 296)
- **Propósito:** Returns a copy of the image where data has been offset by the given
distances. Data wraps around the edges. If ``yoffset`` is omitted, it
is assumed to be equal to ``xoffset``.

:param image: Input image.
:param xoffset: The horizontal distance.
:param yoffset: The vertical distance.  If omitted, both
    distances are set to the same value.
:rtype: :py:class:`~PIL.Image.Image`
- **Firma:** `def offset(image, xoffset, yoffset)`
- **Retorno / Efectos:** Consulta código fuente.
