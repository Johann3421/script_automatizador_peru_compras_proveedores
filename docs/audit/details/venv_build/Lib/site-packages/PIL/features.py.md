# Documentación Técnica: `venv_build/Lib/site-packages/PIL/features.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/PIL/features.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 343
- **Fecha de inspección:** 2026-08-05 18:36:04

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def check_module(feature)` (Línea 23)
- **Propósito:** Checks if a module is available.

:param feature: The module to check for.
:returns: ``True`` if available, ``False`` otherwise.
:raises ValueError: If the module is not defined in this version of Pillow.
- **Firma:** `def check_module(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def version_module(feature)` (Línea 47)
- **Propósito:** :param feature: The module to check for.
:returns:
    The loaded version number as a string, or ``None`` if unknown or not available.
:raises ValueError: If the module is not defined in this version of Pillow.
- **Firma:** `def version_module(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_supported_modules()` (Línea 62)
- **Propósito:** :returns: A list of all supported modules.
- **Firma:** `def get_supported_modules()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def check_codec(feature)` (Línea 77)
- **Propósito:** Checks if a codec is available.

:param feature: The codec to check for.
:returns: ``True`` if available, ``False`` otherwise.
:raises ValueError: If the codec is not defined in this version of Pillow.
- **Firma:** `def check_codec(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def version_codec(feature)` (Línea 94)
- **Propósito:** :param feature: The codec to check for.
:returns:
    The version number as a string, or ``None`` if not available.
    Checked at compile time for ``jpg``, run-time otherwise.
:raises ValueError: If the codec is not defined in this version of Pillow.
- **Firma:** `def version_codec(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_supported_codecs()` (Línea 115)
- **Propósito:** :returns: A list of all supported codecs.
- **Firma:** `def get_supported_codecs()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def check_feature(feature)` (Línea 134)
- **Propósito:** Checks if a feature is available.

:param feature: The feature to check for.
:returns: ``True`` if available, ``False`` if unavailable, ``None`` if unknown.
:raises ValueError: If the feature is not defined in this version of Pillow.
- **Firma:** `def check_feature(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def version_feature(feature)` (Línea 158)
- **Propósito:** :param feature: The feature to check for.
:returns: The version number as a string, or ``None`` if not available.
:raises ValueError: If the feature is not defined in this version of Pillow.
- **Firma:** `def version_feature(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_supported_features()` (Línea 175)
- **Propósito:** :returns: A list of all supported features.
- **Firma:** `def get_supported_features()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def check(feature)` (Línea 182)
- **Propósito:** :param feature: A module, codec, or feature name.
:returns:
    ``True`` if the module, codec, or feature is available,
    ``False`` or ``None`` otherwise.
- **Firma:** `def check(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def version(feature)` (Línea 200)
- **Propósito:** :param feature:
    The module, codec, or feature to check for.
:returns:
    The version number as a string, or ``None`` if unknown or not available.
- **Firma:** `def version(feature)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_supported()` (Línea 216)
- **Propósito:** :returns: A list of all supported modules, features, and codecs.
- **Firma:** `def get_supported()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def pilinfo(out, supported_formats)` (Línea 227)
- **Propósito:** Prints information about this installation of Pillow.
This function can be called with ``python3 -m PIL``.
It can also be called with ``python3 -m PIL.report`` or ``python3 -m PIL --report``
to have "supported_formats" set to ``False``, omitting the list of all supported
image file formats.

:param out:
    The output stream to print to. Defaults to ``sys.stdout`` if ``None``.
:param supported_formats:
    If ``True``, a list of all supported image file formats will be printed.
- **Firma:** `def pilinfo(out, supported_formats)`
- **Retorno / Efectos:** Consulta código fuente.
