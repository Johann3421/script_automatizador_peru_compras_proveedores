# Documentación Técnica: `venv_build/Lib/site-packages/pefile.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pefile.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 8034
- **Fecha de inspección:** 2026-08-05 18:36:30

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `AddressSet` (Línea 729)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 730): Sin docstring.
  - `def add(self, value)` (Línea 735): Sin docstring.
  - `def diff(self)` (Línea 740): Sin docstring.

#### Clase `UnicodeStringWrapperPostProcessor` (Línea 744)
- **Docstring:** _This class attempts to help the process of identifying strings
that might be plain Unicode or Pascal. A list of strings will be
wrapped on it with the hope the overlappings will help make the
decision about their type._
- **Métodos:**
  - `def __init__(self, pe, rva_ptr)` (Línea 750): Sin docstring.
  - `def get_rva(self)` (Línea 755): Get the RVA of the string.
  - `def __str__(self)` (Línea 759): Return the escaped UTF-8 representation of the string.
  - `def decode(self)` (Línea 763): Sin docstring.
  - `def invalidate(self)` (Línea 768): Make this instance None, to express it's no known string type.
  - `def render_pascal_16(self)` (Línea 772): Sin docstring.
  - `def get_pascal_16_length(self)` (Línea 783): Sin docstring.
  - `def __get_word_value_at_rva(self, rva)` (Línea 786): Sin docstring.
  - `def ask_unicode_16(self, next_rva_ptr)` (Línea 797): The next RVA is taken to be the one immediately following this one.

Such RVA could indicate the natural end of the string and will be checked
to see if there's a Unicode NULL character there.
  - `def render_unicode_16(self)` (Línea 809): Sin docstring.

#### Clase `PEFormatError` (Línea 819)
- **Docstring:** _Generic PE format error exception._
- **Métodos:**
  - `def __init__(self, value)` (Línea 822): Sin docstring.
  - `def __str__(self)` (Línea 825): Sin docstring.

#### Clase `Dump` (Línea 829)
- **Docstring:** _Convenience class for dumping the PE information._
- **Métodos:**
  - `def __init__(self)` (Línea 832): Sin docstring.
  - `def add_lines(self, txt, indent)` (Línea 835): Adds a list of lines.

The list can be indented with the optional argument 'indent'.
  - `def add_line(self, txt, indent)` (Línea 843): Adds a line.

The line can be indented with the optional argument 'indent'.
  - `def add(self, txt, indent)` (Línea 850): Adds some text, no newline will be appended.

The text can be indented with the optional argument 'indent'.
  - `def add_header(self, txt)` (Línea 857): Adds a header element.
  - `def add_newline(self)` (Línea 861): Adds a newline.
  - `def get_text(self)` (Línea 865): Get the text in its current state.

#### Clase `Structure` (Línea 943)
- **Docstring:** _Prepare structure object to extract members from data.

Format is a list containing definitions for the elements
of the structure._
- **Métodos:**
  - `def __init__(self, format, name, file_offset)` (Línea 950): Sin docstring.
  - `def __get_format__(self)` (Línea 978): Sin docstring.
  - `def get_field_absolute_offset(self, field_name)` (Línea 981): Return the offset within the field for the requested field in the structure.
  - `def get_field_relative_offset(self, field_name)` (Línea 985): Return the offset within the structure for the requested field.
  - `def get_file_offset(self)` (Línea 989): Sin docstring.
  - `def set_file_offset(self, offset)` (Línea 992): Sin docstring.
  - `def all_zeroes(self)` (Línea 995): Returns true is the unpacked data is all zeros.
  - `def sizeof(self)` (Línea 1000): Return size of the structure.
  - `def __unpack__(self, data)` (Línea 1005): Sin docstring.
  - `def __pack__(self)` (Línea 1026): Sin docstring.
  - `def __str__(self)` (Línea 1041): Sin docstring.
  - `def __repr__(self)` (Línea 1044): Sin docstring.
  - `def dump(self, indentation)` (Línea 1049): Returns a string representation of the structure.
  - `def dump_dict(self)` (Línea 1103): Returns a dictionary representation of the structure.

#### Clase `SectionStructure` (Línea 1139)
- **Docstring:** _Convenience section handling class._
- **Métodos:**
  - `def __init__(self)` (Línea 1142): Sin docstring.
  - `def get_PointerToRawData_adj(self)` (Línea 1157): Sin docstring.
  - `def get_VirtualAddress_adj(self)` (Línea 1170): Sin docstring.
  - `def get_data(self, start, length, ignore_padding)` (Línea 1180): Get data chunk from a section.

Allows to query data from the section by passing the
addresses where the PE file would be loaded by default.
It is then possible to retrieve code and data by their real
addresses as they would be if loaded.

Note that sections on disk can include padding that would
not be loaded to memory. That is the case if `section.SizeOfRawData`
is greater than `section.Misc_VirtualSize`, and that means
that data past `section.Misc_VirtualSize` is padding.
In case you are not interested in this padding, passing
`ignore_padding=True` will truncate the result in order
not to return the padding (if any).

Returns bytes().
  - `def __setattr__(self, name, val)` (Línea 1224): Sin docstring.
  - `def get_rva_from_offset(self, offset)` (Línea 1239): Sin docstring.
  - `def get_offset_from_rva(self, rva)` (Línea 1242): Sin docstring.
  - `def contains_offset(self, offset)` (Línea 1245): Check whether the section contains the file offset provided.
  - `def contains_rva(self, rva)` (Línea 1257): Check whether the section contains the address provided.
  - `def contains(self, rva)` (Línea 1295): Sin docstring.
  - `def get_entropy(self)` (Línea 1298): Calculate and return the entropy for the section.
  - `def get_hash_sha1(self)` (Línea 1303): Get the SHA-1 hex-digest of the section's data.
  - `def get_hash_sha256(self)` (Línea 1309): Get the SHA-256 hex-digest of the section's data.
  - `def get_hash_sha512(self)` (Línea 1315): Get the SHA-512 hex-digest of the section's data.
  - `def get_hash_md5(self)` (Línea 1321): Get the MD5 hex-digest of the section's data.
  - `def entropy_H(self, data)` (Línea 1327): Calculate the entropy of a chunk of data.

#### Clase `StructureWithBitfields` (Línea 1425)
- **Docstring:** _Extends Structure's functionality with support for bitfields such as:
    ('B:4,LowerHalf', 'B:4,UpperHalf')
To this end, two lists are maintained:
    * self.__keys__ that contains compound fields, for example
      ('B,~LowerHalfUpperHalf'), and is used during packing/unpaking
    * self.__keys_ext__ containing a separate key for each field (ex., LowerHalf,
      UpperHalf) to simplify implementation of dump()
This way the implementation of unpacking/packing and dump() from Structure can be
reused.

In addition, we create a dictionary:
    <comound_field_index_in_keys> -->
        (data type, [ (subfield name, length in bits)+ ] )
that facilitates bitfield paking and unpacking.

With lru_cache() creating only once instance per format string, the memory
overhead is negligible._
- **Métodos:**
  - `def __init__(self, format, name, file_offset)` (Línea 1451): Sin docstring.
  - `def __unpack__(self, data)` (Línea 1467): Sin docstring.
  - `def __pack__(self)` (Línea 1473): Sin docstring.
  - `def dump(self, indentation)` (Línea 1481): Sin docstring.
  - `def dump_dict(self)` (Línea 1490): Sin docstring.
  - `def _unpack_bitfield_attributes(self)` (Línea 1499): Replace compound attributes corresponding to bitfields with separate
sub-fields.
  - `def _pack_bitfield_attributes(self)` (Línea 1518): Pack attributes into a compound bitfield

#### Clase `DataContainer` (Línea 1533)
- **Docstring:** _Generic data container._
- **Métodos:**
  - `def __init__(self)` (Línea 1536): Sin docstring.

#### Clase `ImportDescData` (Línea 1542)
- **Docstring:** _Holds import descriptor information.

dll:        name of the imported DLL
imports:    list of imported symbols (ImportData instances)
struct:     IMAGE_IMPORT_DESCRIPTOR structure_

#### Clase `ImportData` (Línea 1551)
- **Docstring:** _Holds imported symbol's information.

ordinal:    Ordinal of the symbol
name:       Name of the symbol
bound:      If the symbol is bound, this contains
            the address._
- **Métodos:**
  - `def __setattr__(self, name, val)` (Línea 1560): Sin docstring.

#### Clase `ExportDirData` (Línea 1613)
- **Docstring:** _Holds export directory information.

struct:     IMAGE_EXPORT_DIRECTORY structure
symbols:    list of exported symbols (ExportData instances)_

#### Clase `ExportData` (Línea 1620)
- **Docstring:** _Holds exported symbols' information.

ordinal:    ordinal of the symbol
address:    address of the symbol
name:       name of the symbol (None if the symbol is
            exported by ordinal only)
forwarder:  if the symbol is forwarded it will
            contain the name of the target symbol,
            None otherwise._
- **Métodos:**
  - `def __setattr__(self, name, val)` (Línea 1632): Sin docstring.

#### Clase `ResourceDirData` (Línea 1667)
- **Docstring:** _Holds resource directory information.

struct:     IMAGE_RESOURCE_DIRECTORY structure
entries:    list of entries (ResourceDirEntryData instances)_

#### Clase `ResourceDirEntryData` (Línea 1675)
- **Docstring:** _Holds resource directory entry data.

struct:     IMAGE_RESOURCE_DIRECTORY_ENTRY structure
name:       If the resource is identified by name this
            attribute will contain the name string. None
            otherwise. If identified by id, the id is
            available at 'struct.Id'
id:         the id, also in struct.Id
directory:  If this entry has a lower level directory
            this attribute will point to the
            ResourceDirData instance representing it.
data:       If this entry has no further lower directories
            and points to the actual resource data, this
            attribute will reference the corresponding
            ResourceDataEntryData instance.
(Either of the 'directory' or 'data' attribute will exist,
but not both.)_

#### Clase `ResourceDataEntryData` (Línea 1696)
- **Docstring:** _Holds resource data entry information.

struct:     IMAGE_RESOURCE_DATA_ENTRY structure
lang:       Primary language ID
sublang:    Sublanguage ID_

#### Clase `DebugData` (Línea 1705)
- **Docstring:** _Holds debug information.

struct:     IMAGE_DEBUG_DIRECTORY structure
entries:    list of entries (IMAGE_DEBUG_TYPE instances)_

#### Clase `DynamicRelocationData` (Línea 1713)
- **Docstring:** _Holds dynamic relocation information.

struct:        IMAGE_DYNAMIC_RELOCATION structure
symbol:        Symbol to which dynamic relocations must be applied
relocations:   List of dynamic relocations for this symbol (BaseRelocationData instances)_

#### Clase `FunctionOverrideData` (Línea 1722)
- **Docstring:** _Holds Function and bdd dynamic relocation information.

struct:        IMAGE_DYNAMIC_RELOCATION structure
symbol:        Symbol to which dynamic relocations must be applied
bdd_relocs:    List of bdd dynamic relocations (BddDynamicRelocationData instances)
func_relocs:   List of function override dynamic relocations (FunctionOverrideDynamicRelocationData instances)_

#### Clase `FunctionOverrideDynamicRelocationData` (Línea 1732)
- **Docstring:** _Holds Function override dynamic relocation information.

struct:        IMAGE_FUNCTION_OVERRIDE_DYNAMIC_RELOCATION structure
func_rva:      Original function rva
override_rvas: List of overriding function rvas
relocations:   List of dynamic relocations (BaseRelocationData instances)_

#### Clase `BddDynamicRelocationData` (Línea 1742)
- **Docstring:** _Holds Bdd dynamic relocation information.

struct:        IMAGE_BDD_DYNAMIC_RELOCATION structure_

#### Clase `BaseRelocationData` (Línea 1749)
- **Docstring:** _Holds base relocation information.

struct:     IMAGE_BASE_RELOCATION structure
entries:    list of relocation data (RelocationData instances)_

#### Clase `RelocationData` (Línea 1757)
- **Docstring:** _Holds relocation information.

type:       Type of relocation
            The type string can be obtained by
            RELOCATION_TYPE[type]
rva:        RVA of the relocation_
- **Métodos:**
  - `def __setattr__(self, name, val)` (Línea 1766): Sin docstring.

#### Clase `TlsData` (Línea 1789)
- **Docstring:** _Holds TLS information.

struct:     IMAGE_TLS_DIRECTORY structure_

#### Clase `BoundImportDescData` (Línea 1796)
- **Docstring:** _Holds bound import descriptor data.

This directory entry will provide information on the
DLLs this PE file has been bound to (if bound at all).
The structure will contain the name and timestamp of the
DLL at the time of binding so that the loader can know
whether it differs from the one currently present in the
system and must, therefore, re-bind the PE's imports.

struct:     IMAGE_BOUND_IMPORT_DESCRIPTOR structure
name:       DLL name
entries:    list of entries (BoundImportRefData instances)
            the entries will exist if this DLL has forwarded
            symbols. If so, the destination DLL will have an
            entry in this list._

#### Clase `LoadConfigData` (Línea 1815)
- **Docstring:** _Holds Load Config data.

struct:     IMAGE_LOAD_CONFIG_DIRECTORY structure
name:       dll name
dynamic_relocations: dynamic relocation information, if present_

#### Clase `BoundImportRefData` (Línea 1824)
- **Docstring:** _Holds bound import forwarder reference data.

Contains the same information as the bound descriptor but
for forwarded DLLs, if any.

struct:     IMAGE_BOUND_FORWARDER_REF structure
name:       dll name_

#### Clase `ExceptionsDirEntryData` (Línea 1835)
- **Docstring:** _Holds the data related to SEH (and stack unwinding, in particular)

struct      an instance of RUNTIME_FUNTION
unwindinfo  an instance of UNWIND_INFO_

#### Clase `UnwindInfo` (Línea 1843)
- **Docstring:** _Handles the complexities of UNWIND_INFO structure:
* variable number of UWIND_CODEs
* optional ExceptionHandler and FunctionEntry fields_
- **Métodos:**
  - `def __init__(self, file_offset)` (Línea 1849): Sin docstring.
  - `def unpack_in_stages(self, data)` (Línea 1873): Unpacks the UNWIND_INFO "in two calls", with the first call establishing
a full size of the structure and the second, performing the actual unpacking.
  - `def dump(self, indentation)` (Línea 1933): Sin docstring.
  - `def dump_dict(self)` (Línea 1960): Sin docstring.
  - `def __setattr__(self, name, val)` (Línea 1973): Sin docstring.
  - `def sizeof(self)` (Línea 1983): Sin docstring.
  - `def __pack__(self)` (Línea 1986): Sin docstring.
  - `def get_chained_function_entry(self)` (Línea 2004): Sin docstring.
  - `def set_chained_function_entry(self, entry)` (Línea 2007): Sin docstring.

#### Clase `PrologEpilogOp` (Línea 2013)
- **Docstring:** _Meant as an abstract class representing a generic unwind code.
There is a subclass of PrologEpilogOp for each member of UNWIND_OP_CODES enum._
- **Métodos:**
  - `def initialize(self, unw_code, data, unw_info, file_offset)` (Línea 2018): Sin docstring.
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2024): Computes how many UNWIND_CODE structures UNWIND_CODE occupies.
May be called before initialize() and, for that reason, should not rely on
the values of intance attributes.
  - `def is_valid(self)` (Línea 2031): Sin docstring.
  - `def _get_format(self, unw_code)` (Línea 2034): Sin docstring.

#### Clase `PrologEpilogOpPushReg` (Línea 2038)
- **Docstring:** _UWOP_PUSH_NONVOL_
- **Métodos:**
  - `def _get_format(self, unw_code)` (Línea 2041): Sin docstring.
  - `def __str__(self)` (Línea 2044): Sin docstring.

#### Clase `PrologEpilogOpAllocLarge` (Línea 2048)
- **Docstring:** _UWOP_ALLOC_LARGE_
- **Métodos:**
  - `def _get_format(self, unw_code)` (Línea 2051): Sin docstring.
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2062): Sin docstring.
  - `def get_alloc_size(self)` (Línea 2065): Sin docstring.
  - `def __str__(self)` (Línea 2072): Sin docstring.

#### Clase `PrologEpilogOpAllocSmall` (Línea 2076)
- **Docstring:** _UWOP_ALLOC_SMALL_
- **Métodos:**
  - `def _get_format(self, unw_code)` (Línea 2079): Sin docstring.
  - `def get_alloc_size(self)` (Línea 2085): Sin docstring.
  - `def __str__(self)` (Línea 2088): Sin docstring.

#### Clase `PrologEpilogOpSetFP` (Línea 2092)
- **Docstring:** _UWOP_SET_FPREG_
- **Métodos:**
  - `def initialize(self, unw_code, data, unw_info, file_offset)` (Línea 2095): Sin docstring.
  - `def __str__(self)` (Línea 2100): Sin docstring.

#### Clase `PrologEpilogOpSaveReg` (Línea 2109)
- **Docstring:** _UWOP_SAVE_NONVOL_
- **Métodos:**
  - `def length_in_code_structures(self, unwcode, unw_info)` (Línea 2112): Sin docstring.
  - `def get_offset(self)` (Línea 2115): Sin docstring.
  - `def _get_format(self, unw_code)` (Línea 2118): Sin docstring.
  - `def __str__(self)` (Línea 2124): Sin docstring.

#### Clase `PrologEpilogOpSaveRegFar` (Línea 2128)
- **Docstring:** _UWOP_SAVE_NONVOL_FAR_
- **Métodos:**
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2131): Sin docstring.
  - `def get_offset(self)` (Línea 2134): Sin docstring.
  - `def _get_format(self, unw_code)` (Línea 2137): Sin docstring.
  - `def __str__(self)` (Línea 2143): Sin docstring.

#### Clase `PrologEpilogOpSaveXMM` (Línea 2147)
- **Docstring:** _UWOP_SAVE_XMM128_
- **Métodos:**
  - `def _get_format(self, unw_code)` (Línea 2150): Sin docstring.
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2156): Sin docstring.
  - `def get_offset(self)` (Línea 2159): Sin docstring.
  - `def __str__(self)` (Línea 2162): Sin docstring.

#### Clase `PrologEpilogOpSaveXMMFar` (Línea 2166)
- **Docstring:** _UWOP_SAVE_XMM128_FAR_
- **Métodos:**
  - `def _get_format(self, unw_code)` (Línea 2169): Sin docstring.
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2175): Sin docstring.
  - `def get_offset(self)` (Línea 2178): Sin docstring.
  - `def __str__(self)` (Línea 2181): Sin docstring.

#### Clase `PrologEpilogOpPushFrame` (Línea 2185)
- **Docstring:** _UWOP_PUSH_MACHFRAME_
- **Métodos:**
  - `def __str__(self)` (Línea 2188): Sin docstring.

#### Clase `PrologEpilogOpEpilogMarker` (Línea 2192)
- **Docstring:** _UWOP_EPILOG_
- **Métodos:**
  - `def initialize(self, unw_code, data, unw_info, file_offset)` (Línea 2195): Sin docstring.
  - `def _get_format(self, unw_code)` (Línea 2204): Sin docstring.
  - `def length_in_code_structures(self, unw_code, unw_info)` (Línea 2227): Sin docstring.
  - `def get_offset(self)` (Línea 2234): Sin docstring.
  - `def is_valid(self)` (Línea 2239): Sin docstring.
  - `def __str__(self)` (Línea 2242): Sin docstring.

#### Clase `PrologEpilogOpsFactory` (Línea 2254)
- **Docstring:** _A factory for creating unwind codes based on the value of UnwindOp_
- **Métodos:**
  - `def create(unwcode)` (Línea 2271): Sin docstring.

#### Clase `PE` (Línea 2329)
- **Docstring:** _A Portable Executable representation.

This class provides access to most of the information in a PE file.

It expects to be supplied the name of the file to load or PE data
to process and an optional argument 'fast_load' (False by default)
which controls whether to load all the directories information,
which can be quite time consuming.

pe = pefile.PE('module.dll')
pe = pefile.PE(name='module.dll')

would load 'module.dll' and process it. If the data is already
available in a buffer the same can be achieved with:

pe = pefile.PE(data=module_dll_data)

The "fast_load" can be set to a default by setting its value in the
module itself by means, for instance, of a "pefile.fast_load = True".
That will make all the subsequent instances not to load the
whole PE structure. The "full_load" method can be used to parse
the missing data at a later stage.

Basic headers information will be available in the attributes:

DOS_HEADER
NT_HEADERS
FILE_HEADER
OPTIONAL_HEADER

All of them will contain among their attributes the members of the
corresponding structures as defined in WINNT.H

The raw data corresponding to the header (from the beginning of the
file up to the start of the first section) will be available in the
instance's attribute 'header' as a string.

The sections will be available as a list in the 'sections' attribute.
Each entry will contain as attributes all the structure's members.

Directory entries will be available as attributes (if they exist):
(no other entries are processed at this point)

DIRECTORY_ENTRY_IMPORT (list of ImportDescData instances)
DIRECTORY_ENTRY_EXPORT (ExportDirData instance)
DIRECTORY_ENTRY_RESOURCE (ResourceDirData instance)
DIRECTORY_ENTRY_DEBUG (list of DebugData instances)
DIRECTORY_ENTRY_BASERELOC (list of BaseRelocationData instances)
DIRECTORY_ENTRY_TLS
DIRECTORY_ENTRY_BOUND_IMPORT (list of BoundImportData instances)

The following dictionary attributes provide ways of mapping different
constants. They will accept the numeric value and return the string
representation and the opposite, feed in the string and get the
numeric constant:

DIRECTORY_ENTRY
IMAGE_CHARACTERISTICS
SECTION_CHARACTERISTICS
DEBUG_TYPE
SUBSYSTEM_TYPE
MACHINE_TYPE
RELOCATION_TYPE
RESOURCE_TYPE
LANG
SUBLANG_
- **Métodos:**
  - `def __init__(self, name, data, fast_load, max_symbol_exports, max_repeated_symbol)` (Línea 2893): Sin docstring.
  - `def __enter__(self)` (Línea 2946): Sin docstring.
  - `def __exit__(self, type, value, traceback)` (Línea 2949): Sin docstring.
  - `def _close_data(self)` (Línea 2952): Sin docstring.
  - `def close(self)` (Línea 2966): Sin docstring.
  - `def __unpack_data__(self, format, data, file_offset)` (Línea 2969): Apply structure format to raw data.

Returns an unpacked structure object if successful, None otherwise.
  - `def __unpack_data_with_bitfields__(self, format, data, file_offset)` (Línea 2991): Apply structure format to raw data.

Returns an unpacked structure object if successful, None otherwise.
  - `def __parse__(self, fname, data, fast_load)` (Línea 3013): Parse a Portable Executable file.

Loads a PE file, parsing all its structures and making them available
through the instance's attributes.
  - `def parse_rich_header(self)` (Línea 3364): Parses the rich header
see https://www.ntcore.com/files/richsign.htm for more information

Structure:
00 DanS ^ checksum, checksum, checksum, checksum
10 Symbol RVA ^ checksum, Symbol size ^ checksum...
...
XX Rich, checksum, 0, 0,...
  - `def get_warnings(self)` (Línea 3449): Return the list of warnings.

Non-critical problems found when parsing the PE file are
appended to a list of warnings. This method returns the
full list.
  - `def show_warnings(self)` (Línea 3459): Print the list of warnings.

Non-critical problems found when parsing the PE file are
appended to a list of warnings. This method prints the
full list to standard output.
  - `def full_load(self)` (Línea 3470): Process the data directories.

This method will load the data directories which might not have
been loaded if the "fast_load" option was used.

It also parses the rich header, which may or may not present.
  - `def write(self, filename)` (Línea 3495): Write the PE file.

This function will process all headers and components
of the PE file and include all changes made (by just
assigning to attributes in the PE objects) and write
the changes back to a file whose name is provided as
an argument. The filename is optional, if not
provided the data will be returned as a 'str' object.
  - `def parse_sections(self, offset)` (Línea 3547): Fetch the PE file sections.

The sections will be readily available in the "sections" attribute.
Its attributes will contain all the section information plus "data"
a buffer containing the section's data.

The "Characteristics" member will be processed and attributes
representing the section characteristics (with the 'IMAGE_SCN_'
string trimmed from the constant's names) will be added to the
section instance.

Refer to the SectionStructure class for additional info.
  - `def parse_data_directories(self, directories, forwarded_exports_only, import_dllnames_only)` (Línea 3688): Parse and process the PE file's data directories.

If the optional argument 'directories' is given, only
the directories at the specified indexes will be parsed.
Such functionality allows parsing of areas of interest
without the burden of having to parse all others.
The directories can then be specified as:

For export / import only:

  directories = [ 0, 1 ]

or (more verbosely):

  directories = [ DIRECTORY_ENTRY['IMAGE_DIRECTORY_ENTRY_IMPORT'],
    DIRECTORY_ENTRY['IMAGE_DIRECTORY_ENTRY_EXPORT'] ]

If 'directories' is a list, the ones that are processed will be removed,
leaving only the ones that are not present in the image.

If `forwarded_exports_only` is True, the IMAGE_DIRECTORY_ENTRY_EXPORT
attribute will only contain exports that are forwarded to another DLL.

If `import_dllnames_only` is True, symbols will not be parsed from
the import table and the entries in the IMAGE_DIRECTORY_ENTRY_IMPORT
attribute will not have a `symbols` attribute.
  - `def parse_exceptions_directory(self, rva, size)` (Línea 3783): Parses exception directory

All the code related to handling exception directories is documented in
https://auscitte.github.io/systems%20blog/Exception-Directory-pefile#implementation-details
  - `def parse_directory_bound_imports(self, rva, size)` (Línea 3873): Sin docstring.
  - `def parse_directory_tls(self, rva, size)` (Línea 3992): Sin docstring.
  - `def parse_directory_load_config(self, rva, size)` (Línea 4020): Sin docstring.
  - `def parse_dynamic_relocations(self, dynamic_value_reloc_table_offset, dynamic_value_reloc_table_section)` (Línea 4073): Sin docstring.
  - `def parse_function_override_data(self, rva)` (Línea 4175): Sin docstring.
  - `def parse_relocations_directory(self, rva, size)` (Línea 4262): Sin docstring.
  - `def parse_image_base_relocation_list(self, rva, size, fmt)` (Línea 4267): Sin docstring.
  - `def parse_relocations(self, data_rva, rva, size)` (Línea 4326): Sin docstring.
  - `def parse_relocations_with_format(self, data_rva, rva, size, format)` (Línea 4369): Sin docstring.
  - `def parse_debug_directory(self, rva, size)` (Línea 4408): Sin docstring.
  - `def parse_resources_directory(self, rva, size, base_rva, level, dirs)` (Línea 4609): Parse the resources directory.

Given the RVA of the resources directory, it will process all
its entries.

The root will have the corresponding member of its structure,
IMAGE_RESOURCE_DIRECTORY plus 'entries', a list of all the
entries in the directory.

Those entries will have, correspondingly, all the structure's
members (IMAGE_RESOURCE_DIRECTORY_ENTRY) and an additional one,
"directory", pointing to the IMAGE_RESOURCE_DIRECTORY structure
representing upper layers of the tree. This one will also have
an 'entries' attribute, pointing to the 3rd, and last, level.
Another directory with more entries. Those last entries will
have a new attribute (both 'leaf' or 'data_entry' can be used to
access it). This structure finally points to the resource data.
All the members of this structure, IMAGE_RESOURCE_DATA_ENTRY,
are available as its attributes.
  - `def parse_resource_data_entry(self, rva)` (Línea 4914): Parse a data entry from the resources directory.
  - `def parse_resource_entry(self, rva)` (Línea 4938): Parse a directory entry from the resources directory.
  - `def parse_version_information(self, version_struct)` (Línea 4969): Parse version information structure.

The date will be made available in three attributes of the PE object.

VS_VERSIONINFO   will contain the first three fields of the main structure:
    'Length', 'ValueLength', and 'Type'

VS_FIXEDFILEINFO will hold the rest of the fields, accessible as sub-attributes:
    'Signature', 'StrucVersion', 'FileVersionMS', 'FileVersionLS',
    'ProductVersionMS', 'ProductVersionLS', 'FileFlagsMask', 'FileFlags',
    'FileOS', 'FileType', 'FileSubtype', 'FileDateMS', 'FileDateLS'

FileInfo    is a list of all StringFileInfo and VarFileInfo structures.

StringFileInfo structures will have a list as an attribute named 'StringTable'
containing all the StringTable structures. Each of those structures contains a
dictionary 'entries' with all the key / value version information string pairs.

VarFileInfo structures will have a list as an attribute named 'Var' containing
all Var structures. Each Var structure will have a dictionary as an attribute
named 'entry' which will contain the name and value of the Var.
  - `def parse_export_directory(self, rva, size, forwarded_only)` (Línea 5397): Parse the export directory.

Given the RVA of the export directory, it will process all
its entries.

The exports will be made available as a list of ExportData
instances in the 'IMAGE_DIRECTORY_ENTRY_EXPORT' PE attribute.
  - `def dword_align(self, offset, base)` (Línea 5661): Sin docstring.
  - `def normalize_import_va(self, va)` (Línea 5664): Sin docstring.
  - `def parse_delay_import_directory(self, rva, size)` (Línea 5675): Walk and parse the delay import directory.
  - `def get_rich_header_hash(self, algorithm)` (Línea 5784): Sin docstring.
  - `def get_imphash(self)` (Línea 5799): Return the imphash of the PE file.

Creates a hash based on imported symbol names and their specific order within
the executable:
https://www.mandiant.com/resources/blog/tracking-malware-import-hashing

Returns:
    the hexdigest of the MD5 hash of the exported symbols.
  - `def get_exphash(self)` (Línea 5847): Return the exphash of the PE file.

Similar to imphash, but based on exported symbol names and their specific order.

Returns:
    the hexdigest of the SHA256 hash of the exported symbols.
  - `def parse_import_directory(self, rva, size, dllnames_only)` (Línea 5872): Walk and parse the import directory.
  - `def parse_imports(self, original_first_thunk, first_thunk, forwarder_chain, max_length, contains_addresses)` (Línea 5978): Parse the imported symbols.

It will fill a list, which will be available as the dictionary
attribute "imports". Its keys will be the DLL names and the values
of all the symbols imported from that object.
  - `def get_import_table(self, rva, max_length, contains_addresses)` (Línea 6136): Sin docstring.
  - `def get_memory_mapped_image(self, max_virtual_address, ImageBase)` (Línea 6267): Returns the data corresponding to the memory layout of the PE file.

The data includes the PE header and the sections loaded at offsets
corresponding to their relative virtual addresses. (the VirtualAddress
section header member).
Any offset in this data corresponds to the absolute memory address
ImageBase+offset.

The optional argument 'max_virtual_address' provides with means of limiting
which sections are processed.
Any section with their VirtualAddress beyond this value will be skipped.
Normally, sections with values beyond this range are just there to confuse
tools. It's a common trick to see in packed executables.

If the 'ImageBase' optional argument is supplied, the file's relocations
will be applied to the image by calling the 'relocate_image()' method. Beware
that the relocation information is applied permanently.
  - `def get_resources_strings(self)` (Línea 6336): Returns a list of all the strings found within the resources (if any).

This method will scan all entries in the resources directory of the PE, if
there is one, and will return a [] with the strings.

An empty list will be returned otherwise.
  - `def get_data(self, rva, length)` (Línea 6363): Get data regardless of the section where it lies on.

Given a RVA and the size of the chunk to retrieve, this method
will find the section where the data lies and return the data.
  - `def get_rva_from_offset(self, offset)` (Línea 6397): Get the RVA corresponding to this file offset.
  - `def get_offset_from_rva(self, rva)` (Línea 6428): Get the file offset corresponding to this RVA.

Given a RVA , this method will find the section where the
data lies and return the offset within the file.
  - `def get_string_at_rva(self, rva, max_length)` (Línea 6448): Get an ASCII string located at the given address.
  - `def get_bytes_from_data(self, offset, data)` (Línea 6459): .
  - `def get_string_from_data(self, offset, data)` (Línea 6468): Get an ASCII string from data.
  - `def get_string_u_at_rva(self, rva, max_length, encoding)` (Línea 6476): Get an Unicode string located at the given address.
  - `def get_section_by_offset(self, offset)` (Línea 6519): Get the section containing the given file offset.
  - `def get_section_by_rva(self, rva)` (Línea 6528): Get the section containing the given address.
  - `def __str__(self)` (Línea 6544): Sin docstring.
  - `def has_relocs(self)` (Línea 6547): Checks if the PE file has relocation directory
  - `def has_dynamic_relocs(self)` (Línea 6551): Sin docstring.
  - `def print_info(self, encoding)` (Línea 6558): Print all the PE header information in a human readable from.
  - `def dump_info(self, dump, encoding)` (Línea 6562): Dump all the PE header information into human readable string.
  - `def dump_dict(self)` (Línea 6990): Dump all the PE header information into a dictionary.
  - `def get_physical_by_rva(self, rva)` (Línea 7282): Gets the physical address in the PE file from an RVA value.
  - `def get_data_from_dword(self, dword)` (Línea 7293): Return a four byte string representing the double word value (little endian).
  - `def get_dword_from_data(self, data, offset)` (Línea 7297): Convert four bytes of data to a double word (little endian)

'offset' is assumed to index into a dword array. So setting it to
N will return a dword out of the data starting at offset N*4.

Returns None if the data can't be turned into a double word.
  - `def get_dword_at_rva(self, rva)` (Línea 7311): Return the double word value at the given RVA.

Returns None if the value can't be read, i.e. the RVA can't be mapped
to a file offset.
  - `def get_dword_from_offset(self, offset)` (Línea 7323): Return the double word value at the given file offset. (little endian)
  - `def set_dword_at_rva(self, rva, dword)` (Línea 7331): Set the double word value at the file offset corresponding to the given RVA.
  - `def set_dword_at_offset(self, offset, dword)` (Línea 7335): Set the double word value at the given file offset.
  - `def get_data_from_word(self, word)` (Línea 7343): Return a two byte string representing the word value. (little endian).
  - `def get_word_from_data(self, data, offset)` (Línea 7347): Convert two bytes of data to a word (little endian)

'offset' is assumed to index into a word array. So setting it to
N will return a dword out of the data starting at offset N*2.

Returns None if the data can't be turned into a word.
  - `def get_word_at_rva(self, rva)` (Línea 7361): Return the word value at the given RVA.

Returns None if the value can't be read, i.e. the RVA can't be mapped
to a file offset.
  - `def get_word_from_offset(self, offset)` (Línea 7373): Return the word value at the given file offset. (little endian)
  - `def set_word_at_rva(self, rva, word)` (Línea 7381): Set the word value at the file offset corresponding to the given RVA.
  - `def set_word_at_offset(self, offset, word)` (Línea 7385): Set the word value at the given file offset.
  - `def get_data_from_qword(self, word)` (Línea 7393): Return an eight byte string representing the quad-word value (little endian).
  - `def get_qword_from_data(self, data, offset)` (Línea 7397): Convert eight bytes of data to a word (little endian)

'offset' is assumed to index into a word array. So setting it to
N will return a dword out of the data starting at offset N*8.

Returns None if the data can't be turned into a quad word.
  - `def get_qword_at_rva(self, rva)` (Línea 7411): Return the quad-word value at the given RVA.

Returns None if the value can't be read, i.e. the RVA can't be mapped
to a file offset.
  - `def get_qword_from_offset(self, offset)` (Línea 7423): Return the quad-word value at the given file offset. (little endian)
  - `def set_qword_at_rva(self, rva, qword)` (Línea 7431): Set the quad-word value at the file offset corresponding to the given RVA.
  - `def set_qword_at_offset(self, offset, qword)` (Línea 7435): Set the quad-word value at the given file offset.
  - `def set_bytes_at_rva(self, rva, data)` (Línea 7443): Overwrite, with the given string, the bytes at the file offset corresponding
to the given RVA.

Return True if successful, False otherwise. It can fail if the
offset is outside the file's boundaries.
  - `def set_bytes_at_offset(self, offset, data)` (Línea 7460): Overwrite the bytes at the given file offset with the given string.

Return True if successful, False otherwise. It can fail if the
offset is outside the file's boundaries.
  - `def set_data_bytes(self, offset, data)` (Línea 7477): Sin docstring.
  - `def merge_modified_section_data(self)` (Línea 7485): Update the PE image content with any individual section data that has been
modified.
  - `def relocate_image(self, new_ImageBase)` (Línea 7498): Apply the relocation information to the image using the provided image base.

This method will apply the relocation information to the image. Given the new
base, all the relocations will be processed and both the raw data and the
section's data will be fixed accordingly.
The resulting image can be retrieved as well through the method:

    get_memory_mapped_image()

In order to get something that would more closely match what could be found in
memory once the Windows loader finished its work.
  - `def verify_checksum(self)` (Línea 7757): Sin docstring.
  - `def generate_checksum(self)` (Línea 7760): Sin docstring.
  - `def is_exe(self)` (Línea 7810): Check whether the file is a standard executable.

This will return true only if the file has the IMAGE_FILE_EXECUTABLE_IMAGE flag
set and the IMAGE_FILE_DLL not set and the file does not appear to be a driver
either.
  - `def is_dll(self)` (Línea 7829): Check whether the file is a standard DLL.

This will return true only if the image has the IMAGE_FILE_DLL flag set.
  - `def is_driver(self)` (Línea 7842): Check whether the file is a Windows driver.

This will return true only if there are reliable indicators of the image
being a driver.
  - `def get_overlay_data_start_offset(self)` (Línea 7903): Get the offset of data appended to the file and not contained within
the area described in the headers.
  - `def get_overlay(self)` (Línea 7947): Get the data appended to the file and not contained within the area described
in the headers.
  - `def trim(self)` (Línea 7958): Return the just data defined by the PE headers, removing any overlaid data.
  - `def adjust_PointerToRawData(self, val)` (Línea 7974): Sin docstring.
  - `def adjust_SectionAlignment(self, val, section_alignment, file_alignment)` (Línea 7995): Sin docstring.

### Funciones independientes:

#### `def lru_cache(maxsize, typed, copy)` (Línea 50)
- **Propósito:** Sin docstring.
- **Firma:** `def lru_cache(maxsize, typed, copy)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def cache_adjust_SectionAlignment(val, section_alignment, file_alignment)` (Línea 68)
- **Propósito:** Sin docstring.
- **Firma:** `def cache_adjust_SectionAlignment(val, section_alignment, file_alignment)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def count_zeroes(data)` (Línea 83)
- **Propósito:** Sin docstring.
- **Firma:** `def count_zeroes(data)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def two_way_dict(pairs)` (Línea 129)
- **Propósito:** Sin docstring.
- **Firma:** `def two_way_dict(pairs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_sublang_name_for_lang(lang_value, sublang_value)` (Línea 662)
- **Propósito:** Sin docstring.
- **Firma:** `def get_sublang_name_for_lang(lang_value, sublang_value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_strings(data, counter, l)` (Línea 675)
- **Propósito:** Sin docstring.
- **Firma:** `def parse_strings(data, counter, l)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def retrieve_flags(flag_dict, flag_filter)` (Línea 696)
- **Propósito:** Read the flags from a dictionary and return them in a usable form.

Will return a list of (flag, value) for all flags in "flag_dict"
matching the filter "flag_filter".
- **Firma:** `def retrieve_flags(flag_dict, flag_filter)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_flags(obj, flag_field, flags)` (Línea 710)
- **Propósito:** Will process the flags and set attributes in the object accordingly.

The object "obj" will gain attributes named after the flags provided in
"flags" and valued True/False, matching the results of applying each
flag value from "flags" to flag_field.
- **Firma:** `def set_flags(obj, flag_field, flags)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def power_of_two(val)` (Línea 725)
- **Propósito:** Sin docstring.
- **Firma:** `def power_of_two(val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sizeof_type(t)` (Línea 890)
- **Propósito:** Sin docstring.
- **Firma:** `def sizeof_type(t)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_format(format)` (Línea 901)
- **Propósito:** Sin docstring.
- **Firma:** `def set_format(format)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def set_bitfields_format(format)` (Línea 1344)
- **Propósito:** Sin docstring.
- **Firma:** `def set_bitfields_format(format)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_valid_dos_filename(s)` (Línea 2296)
- **Propósito:** Sin docstring.
- **Firma:** `def is_valid_dos_filename(s)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_valid_function_name(s, relax_allowed_characters)` (Línea 2316)
- **Propósito:** Sin docstring.
- **Firma:** `def is_valid_function_name(s, relax_allowed_characters)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def main()` (Línea 8012)
- **Propósito:** Sin docstring.
- **Firma:** `def main()`
- **Retorno / Efectos:** Consulta código fuente.
