# Documentación Técnica: `venv_build/Lib/site-packages/numpy/f2py/crackfortran.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/numpy/f2py/crackfortran.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 3727
- **Fecha de inspección:** 2026-08-05 18:36:11

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def reset_global_f2py_vars()` (Línea 200)
- **Propósito:** Sin docstring.
- **Firma:** `def reset_global_f2py_vars()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def outmess(line, flag)` (Línea 238)
- **Propósito:** Sin docstring.
- **Firma:** `def outmess(line, flag)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def rmbadname1(name)` (Línea 269)
- **Propósito:** Sin docstring.
- **Firma:** `def rmbadname1(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def rmbadname(names)` (Línea 276)
- **Propósito:** Sin docstring.
- **Firma:** `def rmbadname(names)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def undo_rmbadname1(name)` (Línea 280)
- **Propósito:** Sin docstring.
- **Firma:** `def undo_rmbadname1(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def undo_rmbadname(names)` (Línea 287)
- **Propósito:** Sin docstring.
- **Firma:** `def undo_rmbadname(names)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def openhook(filename, mode)` (Línea 301)
- **Propósito:** Ensures that filename is opened with correct encoding parameter.

This function uses charset_normalizer package, when available, for
determining the encoding of the file to be opened. When charset_normalizer
is not available, the function detects only UTF encodings, otherwise, ASCII
encoding is used as fallback.
- **Firma:** `def openhook(filename, mode)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_free_format(fname)` (Línea 332)
- **Propósito:** Check if file is in free format Fortran.
- **Firma:** `def is_free_format(fname)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def readfortrancode(ffile, dowithline, istop)` (Línea 358)
- **Propósito:** Read fortran codes from files and
 1) Get rid of comments, line continuations, and empty lines; lower cases.
 2) Call dowithline(line) on every line.
 3) Recursively call itself when statement "include '<filename>'" is met.
- **Firma:** `def readfortrancode(ffile, dowithline, istop)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def split_by_unquoted(line, characters)` (Línea 664)
- **Propósito:** Splits the line into (line[:i], line[i:]),
where i is the index of first occurrence of one of the characters
not within quotes, or len(line) if no such index exists
- **Firma:** `def split_by_unquoted(line, characters)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _simplifyargs(argsline)` (Línea 684)
- **Propósito:** Sin docstring.
- **Firma:** `def _simplifyargs(argsline)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def crackline(line, reset)` (Línea 697)
- **Propósito:** reset=-1  --- initialize
reset=0   --- crack the line
reset=1   --- final check if mismatch of blocks occurred

Cracked data is saved in grouplist[0].
- **Firma:** `def crackline(line, reset)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def markouterparen(line)` (Línea 852)
- **Propósito:** Sin docstring.
- **Firma:** `def markouterparen(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def markoutercomma(line, comma)` (Línea 870)
- **Propósito:** Sin docstring.
- **Firma:** `def markoutercomma(line, comma)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def unmarkouterparen(line)` (Línea 889)
- **Propósito:** Sin docstring.
- **Firma:** `def unmarkouterparen(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def appenddecl(decl, decl2, force)` (Línea 894)
- **Propósito:** Sin docstring.
- **Firma:** `def appenddecl(decl, decl2, force)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_intent_callback(vdecl)` (Línea 946)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_intent_callback(vdecl)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _resolvetypedefpattern(line)` (Línea 953)
- **Propósito:** Sin docstring.
- **Firma:** `def _resolvetypedefpattern(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_name_for_bind(line)` (Línea 963)
- **Propósito:** Sin docstring.
- **Firma:** `def parse_name_for_bind(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _resolvenameargspattern(line)` (Línea 973)
- **Propósito:** Sin docstring.
- **Firma:** `def _resolvenameargspattern(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def analyzeline(m, case, line)` (Línea 989)
- **Propósito:** Reads each line in the input file in sequence and updates global vars.

Effectively reads and collects information from the input file to the
global variable groupcache, a dictionary containing info about each part
of the fortran module.

At the end of analyzeline, information is filtered into the correct dict
keys, but parameter values and dimensions are not yet interpreted.
- **Firma:** `def analyzeline(m, case, line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def appendmultiline(group, context_name, ml)` (Línea 1556)
- **Propósito:** Sin docstring.
- **Firma:** `def appendmultiline(group, context_name, ml)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def cracktypespec0(typespec, ll)` (Línea 1565)
- **Propósito:** Sin docstring.
- **Firma:** `def cracktypespec0(typespec, ll)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def removespaces(expr)` (Línea 1606)
- **Propósito:** Sin docstring.
- **Firma:** `def removespaces(expr)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def markinnerspaces(line)` (Línea 1621)
- **Propósito:** The function replace all spaces in the input variable line which are
surrounded with quotation marks, with the triplet "@_@".

For instance, for the input "a 'b c'" the function returns "a 'b@_@c'"

Parameters
----------
line : str

Returns
-------
str
- **Firma:** `def markinnerspaces(line)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def updatevars(typespec, selector, attrspec, entitydecl)` (Línea 1658)
- **Propósito:** Returns last_name, the variable name without special chars, parenthesis
    or dimension specifiers.

Alters groupcache to add the name, typespec, attrspec (and possibly value)
of current variable.
- **Firma:** `def updatevars(typespec, selector, attrspec, entitydecl)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def cracktypespec(typespec, selector)` (Línea 1835)
- **Propósito:** Sin docstring.
- **Firma:** `def cracktypespec(typespec, selector)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def setattrspec(decl, attr, force)` (Línea 1893)
- **Propósito:** Sin docstring.
- **Firma:** `def setattrspec(decl, attr, force)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def setkindselector(decl, sel, force)` (Línea 1920)
- **Propósito:** Sin docstring.
- **Firma:** `def setkindselector(decl, sel, force)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def setcharselector(decl, sel, force)` (Línea 1934)
- **Propósito:** Sin docstring.
- **Firma:** `def setcharselector(decl, sel, force)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def getblockname(block, unknown)` (Línea 1949)
- **Propósito:** Sin docstring.
- **Firma:** `def getblockname(block, unknown)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def setmesstext(block)` (Línea 1957)
- **Propósito:** Sin docstring.
- **Firma:** `def setmesstext(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_usedict(block)` (Línea 1966)
- **Propósito:** Sin docstring.
- **Firma:** `def get_usedict(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_useparameters(block, param_map)` (Línea 1975)
- **Propósito:** Sin docstring.
- **Firma:** `def get_useparameters(block, param_map)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def postcrack2(block, tab, param_map)` (Línea 2005)
- **Propósito:** Sin docstring.
- **Firma:** `def postcrack2(block, tab, param_map)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def postcrack(block, args, tab)` (Línea 2037)
- **Propósito:** TODO:
      function return values
      determine expression types if in argument list
- **Firma:** `def postcrack(block, args, tab)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def sortvarnames(vars)` (Línea 2132)
- **Propósito:** Sin docstring.
- **Firma:** `def sortvarnames(vars)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def analyzecommon(block)` (Línea 2166)
- **Propósito:** Sin docstring.
- **Firma:** `def analyzecommon(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def analyzebody(block, args, tab)` (Línea 2208)
- **Propósito:** Sin docstring.
- **Firma:** `def analyzebody(block, args, tab)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def buildimplicitrules(block)` (Línea 2253)
- **Propósito:** Sin docstring.
- **Firma:** `def buildimplicitrules(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def myeval(e, g, l)` (Línea 2272)
- **Propósito:** Like `eval` but returns only integers and floats 
- **Firma:** `def myeval(e, g, l)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def getlincoef(e, xset)` (Línea 2283)
- **Propósito:** Obtain ``a`` and ``b`` when ``e == "a*x+b"``, where ``x`` is a symbol in
xset.

>>> getlincoef('2*x + 1', {'x'})
(2, 1, 'x')
>>> getlincoef('3*x + x*2 + 2 + 1', {'x'})
(5, 3, 'x')
>>> getlincoef('0', {'x'})
(0, 0, None)
>>> getlincoef('0*x', {'x'})
(0, 0, 'x')
>>> getlincoef('x*x', {'x'})
(None, None, None)

This can be tricked by sufficiently complex expressions

>>> getlincoef('(x - 0.5)*(x - 1.5)*(x - 1)*x + 2*x + 3', {'x'})
(2.0, 3.0, 'x')
- **Firma:** `def getlincoef(e, xset)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_depend_dict(name, vars, deps)` (Línea 2354)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_depend_dict(name, vars, deps)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _calc_depend_dict(vars)` (Línea 2376)
- **Propósito:** Sin docstring.
- **Firma:** `def _calc_depend_dict(vars)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_sorted_names(vars)` (Línea 2384)
- **Propósito:** Sin docstring.
- **Firma:** `def get_sorted_names(vars)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _kind_func(string)` (Línea 2402)
- **Propósito:** Sin docstring.
- **Firma:** `def _kind_func(string)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _selected_int_kind_func(r)` (Línea 2413)
- **Propósito:** Sin docstring.
- **Firma:** `def _selected_int_kind_func(r)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _selected_real_kind_func(p, r, radix)` (Línea 2429)
- **Propósito:** Sin docstring.
- **Firma:** `def _selected_real_kind_func(p, r, radix)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_parameters(vars, global_params)` (Línea 2447)
- **Propósito:** Sin docstring.
- **Firma:** `def get_parameters(vars, global_params)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _eval_length(length, params)` (Línea 2548)
- **Propósito:** Sin docstring.
- **Firma:** `def _eval_length(length, params)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _eval_scalar(value, params)` (Línea 2557)
- **Propósito:** Sin docstring.
- **Firma:** `def _eval_scalar(value, params)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def analyzevars(block)` (Línea 2572)
- **Propósito:** Sets correct dimension information for each variable/parameter
- **Firma:** `def analyzevars(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def param_eval(v, g_params, params, dimspec)` (Línea 2966)
- **Propósito:** Creates a dictionary of indices and values for each parameter in a
parameter array to be evaluated later.

WARNING: It is not possible to initialize multidimensional array
parameters e.g. dimension(-3:1, 4, 3:5) at this point. This is because in
Fortran initialization through array constructor requires the RESHAPE
intrinsic function. Since the right-hand side of the parameter declaration
is not executed in f2py, but rather at the compiled c/fortran extension,
later, it is not possible to execute a reshape of a parameter array.
One issue remains: if the user wants to access the array parameter from
python, we should either
1) allow them to access the parameter array using python standard indexing
   (which is often incompatible with the original fortran indexing)
2) allow the parameter array to be accessed in python as a dictionary with
   fortran indices as keys
We are choosing 2 for now.
- **Firma:** `def param_eval(v, g_params, params, dimspec)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def param_parse(d, params)` (Línea 3028)
- **Propósito:** Recursively parse array dimensions.

Parses the declaration of an array variable or parameter
`dimension` keyword, and is called recursively if the
dimension for this array is a previously defined parameter
(found in `params`).

Parameters
----------
d : str
    Fortran expression describing the dimension of an array.
params : dict
    Previously parsed parameters declared in the Fortran source file.

Returns
-------
out : str
    Parsed dimension expression.

Examples
--------

* If the line being analyzed is

  `integer, parameter, dimension(2) :: pa = (/ 3, 5 /)`

  then `d = 2` and we return immediately, with

>>> d = '2'
>>> param_parse(d, params)
2

* If the line being analyzed is

  `integer, parameter, dimension(pa) :: pb = (/1, 2, 3/)`

  then `d = 'pa'`; since `pa` is a previously parsed parameter,
  and `pa = 3`, we call `param_parse` recursively, to obtain

>>> d = 'pa'
>>> params = {'pa': 3}
>>> param_parse(d, params)
3

* If the line being analyzed is

  `integer, parameter, dimension(pa(1)) :: pb = (/1, 2, 3/)`

  then `d = 'pa(1)'`; since `pa` is a previously parsed parameter,
  and `pa(1) = 3`, we call `param_parse` recursively, to obtain

>>> d = 'pa(1)'
>>> params = dict(pa={1: 3, 2: 5})
>>> param_parse(d, params)
3
- **Firma:** `def param_parse(d, params)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def expr2name(a, block, args)` (Línea 3108)
- **Propósito:** Sin docstring.
- **Firma:** `def expr2name(a, block, args)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def analyzeargs(block)` (Línea 3142)
- **Propósito:** Sin docstring.
- **Firma:** `def analyzeargs(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _ensure_exprdict(r)` (Línea 3177)
- **Propósito:** Sin docstring.
- **Firma:** `def _ensure_exprdict(r)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def determineexprtype(expr, vars, rules)` (Línea 3189)
- **Propósito:** Sin docstring.
- **Firma:** `def determineexprtype(expr, vars, rules)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def crack2fortrangen(block, tab, as_interface)` (Línea 3234)
- **Propósito:** Sin docstring.
- **Firma:** `def crack2fortrangen(block, tab, as_interface)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def common2fortran(common, tab)` (Línea 3308)
- **Propósito:** Sin docstring.
- **Firma:** `def common2fortran(common, tab)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def use2fortran(use, tab)` (Línea 3318)
- **Propósito:** Sin docstring.
- **Firma:** `def use2fortran(use, tab)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def true_intent_list(var)` (Línea 3342)
- **Propósito:** Sin docstring.
- **Firma:** `def true_intent_list(var)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def vars2fortran(block, vars, args, tab, as_interface)` (Línea 3356)
- **Propósito:** Sin docstring.
- **Firma:** `def vars2fortran(block, vars, args, tab, as_interface)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def crackfortran(files)` (Línea 3487)
- **Propósito:** Sin docstring.
- **Firma:** `def crackfortran(files)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def crack2fortran(block)` (Línea 3504)
- **Propósito:** Sin docstring.
- **Firma:** `def crack2fortran(block)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_visit_pair(obj)` (Línea 3519)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_visit_pair(obj)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def traverse(obj, visit, parents, result)` (Línea 3525)
- **Propósito:** Traverse f2py data structure with the following visit function:

def visit(item, parents, result, *args, **kwargs):
    """

    parents is a list of key-"f2py data structure" pairs from which
    items are taken from.

    result is a f2py data structure that is filled with the
    return value of the visit function.

    item is 2-tuple (index, value) if parents[-1][1] is a list
    item is 2-tuple (key, value) if parents[-1][1] is a dict

    The return value of visit must be None, or of the same kind as
    item, that is, if parents[-1] is a list, the return value must
    be 2-tuple (new_index, new_value), or if parents[-1] is a
    dict, the return value must be 2-tuple (new_key, new_value).

    If new_index or new_value is None, the return value of visit
    is ignored, that is, it will not be added to the result.

    If the return value is None, the content of obj will be
    traversed, otherwise not.
    """
- **Firma:** `def traverse(obj, visit, parents, result)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def character_backward_compatibility_hook(item, parents, result)` (Línea 3591)
- **Propósito:** Previously, Fortran character was incorrectly treated as
character*1. This hook fixes the usage of the corresponding
variables in `check`, `dimension`, `=`, and `callstatement`
expressions.

The usage of `char*` in `callprotoargument` expression can be left
unchanged because C `character` is C typedef of `char`, although,
new implementations should use `character*` in the corresponding
expressions.

See https://github.com/numpy/numpy/pull/19388 for more information.
- **Firma:** `def character_backward_compatibility_hook(item, parents, result)`
- **Retorno / Efectos:** Consulta código fuente.
