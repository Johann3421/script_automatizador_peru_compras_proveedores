# Documentación Técnica: `venv_build/Lib/site-packages/pip/_internal/models/link.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_internal/models/link.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 590
- **Fecha de inspección:** 2026-08-05 18:36:31

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `LinkHash` (Línea 44)
- **Docstring:** _Links to content may have embedded hash values. This class parses those.

`name` must be any member of `_SUPPORTED_HASHES`.

This class can be converted to and from `ArchiveInfo`. While ArchiveInfo intends to
be JSON-serializable to conform to PEP 610, this class contains the logic for
parsing a hash name and value for correctness, and then checking whether that hash
conforms to a schema with `.is_hash_allowed()`._
- **Métodos:**
  - `def __post_init__(self)` (Línea 68): Sin docstring.
  - `def find_hash_url_fragment(cls, url)` (Línea 73): Search a string for a checksum algorithm name and encoded output value.
  - `def as_dict(self)` (Línea 81): Sin docstring.
  - `def as_hashes(self)` (Línea 84): Return a Hashes instance which checks only for the current hash.
  - `def is_hash_allowed(self, hashes)` (Línea 88): Return True if the current hash is allowed by `hashes`.

#### Clase `MetadataFile` (Línea 98)
- **Docstring:** _Information about a core metadata file associated with a distribution._
- **Métodos:**
  - `def __post_init__(self)` (Línea 103): Sin docstring.

#### Clase `Link` (Línea 182)
- **Docstring:** _Represents a parsed link from a Package Index's simple URL_
- **Métodos:**
  - `def __init__(self, url, comes_from, requires_python, yanked_reason, metadata_file_data, cache_link_parsing, hashes)` (Línea 197): :param url: url of the resource pointed to (href of the link)
:param comes_from: instance of IndexContent where the link was found,
    or string.
:param requires_python: String containing the `Requires-Python`
    metadata field, specified in PEP 345. This may be specified by
    a data-requires-python attribute in the HTML link tag, as
    described in PEP 503.
:param yanked_reason: the reason the file has been yanked, if the
    file has been yanked, or None if the file hasn't been yanked.
    This is the value of the "data-yanked" attribute, if present, in
    a simple repository HTML link. If the file has been yanked but
    no reason was provided, this should be the empty string. See
    PEP 592 for more information and the specification.
:param metadata_file_data: the metadata attached to the file, or None if
    no such metadata is provided. This argument, if not None, indicates
    that a separate metadata file exists, and also optionally supplies
    hashes for that file.
:param cache_link_parsing: A flag that is used elsewhere to determine
    whether resources retrieved from this link should be cached. PyPI
    URLs should generally have this set to False, for example.
:param hashes: A mapping of hash names to digests to allow us to
    determine the validity of a download.
  - `def from_json(cls, file_data, page_url)` (Línea 261): Convert an pypi json document from a simple repository page into a Link.
  - `def from_element(cls, anchor_attribs, page_url, base_url)` (Línea 312): Convert an anchor element's attributes in a simple repository page to a Link.
  - `def __str__(self)` (Línea 363): Sin docstring.
  - `def __repr__(self)` (Línea 373): Sin docstring.
  - `def __hash__(self)` (Línea 376): Sin docstring.
  - `def __eq__(self, other)` (Línea 379): Sin docstring.
  - `def __lt__(self, other)` (Línea 384): Sin docstring.
  - `def url(self)` (Línea 390): Sin docstring.
  - `def filename(self)` (Línea 394): Sin docstring.
  - `def file_path(self)` (Línea 408): Sin docstring.
  - `def scheme(self)` (Línea 412): Sin docstring.
  - `def netloc(self)` (Línea 416): This can contain auth information.
  - `def path(self)` (Línea 423): Sin docstring.
  - `def splitext(self)` (Línea 426): Sin docstring.
  - `def ext(self)` (Línea 430): Sin docstring.
  - `def url_without_fragment(self)` (Línea 434): Sin docstring.
  - `def _egg_fragment(self)` (Línea 445): Sin docstring.
  - `def subdirectory_fragment(self)` (Línea 466): Sin docstring.
  - `def metadata_link(self)` (Línea 472): Return a link to the associated core metadata file (if any).
  - `def as_hashes(self)` (Línea 481): Sin docstring.
  - `def hash(self)` (Línea 485): Sin docstring.
  - `def hash_name(self)` (Línea 489): Sin docstring.
  - `def show_url(self)` (Línea 493): Sin docstring.
  - `def is_file(self)` (Línea 497): Sin docstring.
  - `def is_existing_dir(self)` (Línea 500): Sin docstring.
  - `def is_wheel(self)` (Línea 504): Sin docstring.
  - `def is_vcs(self)` (Línea 508): Sin docstring.
  - `def is_yanked(self)` (Línea 514): Sin docstring.
  - `def has_hash(self)` (Línea 518): Sin docstring.
  - `def is_hash_allowed(self, hashes)` (Línea 521): Return True if the link has a hash and it is allowed by `hashes`.

#### Clase `_CleanResult` (Línea 530)
- **Docstring:** _Convert link for equivalency check.

This is used in the resolver to check whether two URL-specified requirements
likely point to the same distribution and can be considered equivalent. This
equivalency logic avoids comparing URLs literally, which can be too strict
(e.g. "a=1&b=2" vs "b=2&a=1") and produce conflicts unexpecting to users.

Currently this does three things:

1. Drop the basic auth part. This is technically wrong since a server can
   serve different content based on auth, but if it does that, it is even
   impossible to guarantee two URLs without auth are equivalent, since
   the user can input different auth information when prompted. So the
   practical solution is to assume the auth doesn't affect the response.
2. Parse the query to avoid the ordering issue. Note that ordering under the
   same key in the query are NOT cleaned; i.e. "a=1&a=2" and "a=2&a=1" are
   still considered different.
3. Explicitly drop most of the fragment part, except ``subdirectory=`` and
   hash values, since it should have no impact the downloaded content. Note
   that this drops the "egg=" part historically used to denote the requested
   project (and extras), which is wrong in the strictest sense, but too many
   people are supplying it inconsistently to cause superfluous resolution
   conflicts, so we choose to also ignore them._

### Funciones independientes:

#### `def supported_hashes(hashes)` (Línea 108)
- **Propósito:** Sin docstring.
- **Firma:** `def supported_hashes(hashes)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _clean_url_path_part(part)` (Línea 119)
- **Propósito:** Clean a "part" of a URL path (i.e. after splitting on "@" characters).
- **Firma:** `def _clean_url_path_part(part)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _clean_file_url_path(part)` (Línea 127)
- **Propósito:** Clean the first part of a URL path that corresponds to a local
filesystem path (i.e. the first part after splitting on "@" characters).
- **Firma:** `def _clean_file_url_path(part)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _clean_url_path(path, is_local_path)` (Línea 144)
- **Propósito:** Clean the path portion of a URL.
- **Firma:** `def _clean_url_path(path, is_local_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _ensure_quoted_url(url)` (Línea 166)
- **Propósito:** Make sure a link is fully quoted.
For example, if ' ' occurs in the URL, it will be replaced with "%20",
and without double-quoting other characters.
- **Firma:** `def _ensure_quoted_url(url)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _clean_link(link)` (Línea 562)
- **Propósito:** Sin docstring.
- **Firma:** `def _clean_link(link)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def links_equivalent(link1, link2)` (Línea 589)
- **Propósito:** Sin docstring.
- **Firma:** `def links_equivalent(link1, link2)`
- **Retorno / Efectos:** Consulta código fuente.
