# Documentación Técnica: `venv_build/Lib/site-packages/pip/_internal/utils/logging.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_internal/utils/logging.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 347
- **Fecha de inspección:** 2026-08-05 18:36:32

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `BrokenStdoutLoggingError` (Línea 35)
- **Docstring:** _Raised if BrokenPipeError occurs for the stdout stream while logging._

#### Clase `IndentingFormatter` (Línea 73)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 76): A logging.Formatter that obeys the indent_log() context manager.

:param add_timestamp: A bool indicating output lines should be prefixed
    with their record's timestamp.
  - `def get_message_start(self, formatted, levelno)` (Línea 91): Return the start of the formatted log message (not counting the
prefix to add to each line).
  - `def format(self, record)` (Línea 107): Calls the standard formatter, but will indent all of the log message
lines by our current indentation level.

#### Clase `IndentedRenderable` (Línea 125)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __rich_console__(self, console, options)` (Línea 129): Sin docstring.

#### Clase `RichPipStreamHandler` (Línea 140)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, stream, no_color)` (Línea 143): Sin docstring.
  - `def emit(self, record)` (Línea 153): Sin docstring.
  - `def handleError(self, record)` (Línea 181): Called when logging is unable to log some output.

#### Clase `BetterRotatingFileHandler` (Línea 200)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _open(self)` (Línea 201): Sin docstring.

#### Clase `MaxLevelFilter` (Línea 206)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, level)` (Línea 207): Sin docstring.
  - `def filter(self, record)` (Línea 210): Sin docstring.

#### Clase `ExcludeLoggerFilter` (Línea 214)
- **Docstring:** _A logging Filter that excludes records from a logger (or its children)._
- **Métodos:**
  - `def filter(self, record)` (Línea 219): Sin docstring.

### Funciones independientes:

#### `def _is_broken_pipe_error(exc_class, exc)` (Línea 41)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_broken_pipe_error(exc_class, exc)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def indent_log(num)` (Línea 55)
- **Propósito:** A context manager which will cause the log output to be indented for any
log messages emitted inside it.
- **Firma:** `def indent_log(num)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_indentation()` (Línea 69)
- **Propósito:** Sin docstring.
- **Firma:** `def get_indentation()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def setup_logging(verbosity, no_color, user_log_file)` (Línea 225)
- **Propósito:** Configures and sets up all of the logging

Returns the requested logging level, as its integer value.
- **Firma:** `def setup_logging(verbosity, no_color, user_log_file)`
- **Retorno / Efectos:** Consulta código fuente.
