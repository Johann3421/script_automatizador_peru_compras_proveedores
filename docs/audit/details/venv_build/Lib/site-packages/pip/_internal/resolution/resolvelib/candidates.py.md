# Documentación Técnica: `venv_build/Lib/site-packages/pip/_internal/resolution/resolvelib/candidates.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_internal/resolution/resolvelib/candidates.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 574
- **Fecha de inspección:** 2026-08-05 18:36:32

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_InstallRequirementBackedCandidate` (Línea 125)
- **Docstring:** _A candidate backed by an ``InstallRequirement``.

This represents a package request with the target not being already
in the environment, and needs to be fetched and installed. The backing
``InstallRequirement`` is responsible for most of the leg work; this
class exposes appropriate information to the resolver.

:param link: The link passed to the ``InstallRequirement``. The backing
    ``InstallRequirement`` will use this link to fetch the distribution.
:param source_link: The link this candidate "originates" from. This is
    different from ``link`` when the link is found in the wheel cache.
    ``link`` would point to the wheel cache, while this points to the
    found remote link (e.g. from pypi.org)._
- **Métodos:**
  - `def __init__(self, link, source_link, ireq, factory, name, version)` (Línea 144): Sin docstring.
  - `def __str__(self)` (Línea 162): Sin docstring.
  - `def __repr__(self)` (Línea 165): Sin docstring.
  - `def __hash__(self)` (Línea 168): Sin docstring.
  - `def __eq__(self, other)` (Línea 175): Sin docstring.
  - `def source_link(self)` (Línea 181): Sin docstring.
  - `def project_name(self)` (Línea 185): The normalised name of the project the candidate refers to
  - `def name(self)` (Línea 192): Sin docstring.
  - `def version(self)` (Línea 196): Sin docstring.
  - `def format_for_error(self)` (Línea 201): Sin docstring.
  - `def _prepare_distribution(self)` (Línea 207): Sin docstring.
  - `def _check_metadata_consistency(self, dist)` (Línea 210): Check for consistency of project name and version of dist.
  - `def _prepare(self)` (Línea 234): Sin docstring.
  - `def iter_dependencies(self, with_requires)` (Línea 251): Sin docstring.
  - `def get_install_requirement(self)` (Línea 257): Sin docstring.

#### Clase `LinkCandidate` (Línea 261)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, link, template, factory, name, version)` (Línea 264): Sin docstring.
  - `def _prepare_distribution(self)` (Línea 313): Sin docstring.

#### Clase `EditableCandidate` (Línea 318)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, link, template, factory, name, version)` (Línea 321): Sin docstring.
  - `def _prepare_distribution(self)` (Línea 338): Sin docstring.

#### Clase `AlreadyInstalledCandidate` (Línea 342)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, dist, template, factory)` (Línea 346): Sin docstring.
  - `def __str__(self)` (Línea 364): Sin docstring.
  - `def __repr__(self)` (Línea 367): Sin docstring.
  - `def __eq__(self, other)` (Línea 370): Sin docstring.
  - `def __hash__(self)` (Línea 375): Sin docstring.
  - `def project_name(self)` (Línea 379): Sin docstring.
  - `def name(self)` (Línea 383): Sin docstring.
  - `def version(self)` (Línea 387): Sin docstring.
  - `def is_editable(self)` (Línea 393): Sin docstring.
  - `def format_for_error(self)` (Línea 396): Sin docstring.
  - `def iter_dependencies(self, with_requires)` (Línea 399): Sin docstring.
  - `def get_install_requirement(self)` (Línea 409): Sin docstring.

#### Clase `ExtrasCandidate` (Línea 413)
- **Docstring:** _A candidate that has 'extras', indicating additional dependencies.

Requirements can be for a project with dependencies, something like
foo[extra].  The extras don't affect the project/version being installed
directly, but indicate that we need additional dependencies. We model that
by having an artificial ExtrasCandidate that wraps the "base" candidate.

The ExtrasCandidate differs from the base in the following ways:

1. It has a unique name, of the form foo[extra]. This causes the resolver
   to treat it as a separate node in the dependency graph.
2. When we're getting the candidate's dependencies,
   a) We specify that we want the extra dependencies as well.
   b) We add a dependency on the base candidate.
      See below for why this is needed.
3. We return None for the underlying InstallRequirement, as the base
   candidate will provide it, and we don't want to end up with duplicates.

The dependency on the base candidate is needed so that the resolver can't
decide that it should recommend foo[extra1] version 1.0 and foo[extra2]
version 2.0. Having those candidates depend on foo=1.0 and foo=2.0
respectively forces the resolver to recognise that this is a conflict._
- **Métodos:**
  - `def __init__(self, base, extras)` (Línea 438): :param comes_from: the InstallRequirement that led to this candidate if it
    differs from the base's InstallRequirement. This will often be the
    case in the sense that this candidate's requirement has the extras
    while the base's does not. Unlike the InstallRequirement backed
    candidates, this requirement is used solely for reporting purposes,
    it does not do any leg work.
  - `def __str__(self)` (Línea 457): Sin docstring.
  - `def __repr__(self)` (Línea 461): Sin docstring.
  - `def __hash__(self)` (Línea 464): Sin docstring.
  - `def __eq__(self, other)` (Línea 467): Sin docstring.
  - `def project_name(self)` (Línea 473): Sin docstring.
  - `def name(self)` (Línea 477): The normalised name of the project the candidate refers to
  - `def version(self)` (Línea 482): Sin docstring.
  - `def format_for_error(self)` (Línea 485): Sin docstring.
  - `def is_installed(self)` (Línea 491): Sin docstring.
  - `def is_editable(self)` (Línea 495): Sin docstring.
  - `def source_link(self)` (Línea 499): Sin docstring.
  - `def iter_dependencies(self, with_requires)` (Línea 502): Sin docstring.
  - `def get_install_requirement(self)` (Línea 530): Sin docstring.

#### Clase `RequiresPythonCandidate` (Línea 537)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, py_version_info)` (Línea 541): Sin docstring.
  - `def __str__(self)` (Línea 552): Sin docstring.
  - `def project_name(self)` (Línea 556): Sin docstring.
  - `def name(self)` (Línea 560): Sin docstring.
  - `def version(self)` (Línea 564): Sin docstring.
  - `def format_for_error(self)` (Línea 567): Sin docstring.
  - `def iter_dependencies(self, with_requires)` (Línea 570): Sin docstring.
  - `def get_install_requirement(self)` (Línea 573): Sin docstring.

### Funciones independientes:

#### `def as_base_candidate(candidate)` (Línea 44)
- **Propósito:** The runtime version of BaseCandidate.
- **Firma:** `def as_base_candidate(candidate)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def make_install_req_from_link(link, template)` (Línea 56)
- **Propósito:** Sin docstring.
- **Firma:** `def make_install_req_from_link(link, template)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def make_install_req_from_editable(link, template)` (Línea 81)
- **Propósito:** Sin docstring.
- **Firma:** `def make_install_req_from_editable(link, template)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _make_install_req_from_dist(dist, template)` (Línea 101)
- **Propósito:** Sin docstring.
- **Firma:** `def _make_install_req_from_dist(dist, template)`
- **Retorno / Efectos:** Consulta código fuente.
