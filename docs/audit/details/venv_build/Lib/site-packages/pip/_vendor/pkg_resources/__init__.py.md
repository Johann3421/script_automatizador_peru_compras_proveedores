# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/pkg_resources/__init__.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/pkg_resources/__init__.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 3676
- **Fecha de inspección:** 2026-08-05 18:36:33

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_LoaderProtocol` (Línea 135)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def load_module()` (Línea 136): Sin docstring.

#### Clase `_ZipLoaderModule` (Línea 139)
- **Docstring:** _Sin docstring._

#### Clase `PEP440Warning` (Línea 146)
- **Docstring:** _Used when there is an issue with a version or specifier not complying with
PEP 440._

#### Clase `ResolutionError` (Línea 309)
- **Docstring:** _Abstract base for dependency resolution errors_
- **Métodos:**
  - `def __repr__(self)` (Línea 312): Sin docstring.

#### Clase `VersionConflict` (Línea 316)
- **Docstring:** _An already-installed version conflicts with the requested version.

Should be initialized with the installed Distribution and the requested
Requirement._
- **Métodos:**
  - `def dist(self)` (Línea 327): Sin docstring.
  - `def req(self)` (Línea 331): Sin docstring.
  - `def report(self)` (Línea 334): Sin docstring.
  - `def with_context(self, required_by)` (Línea 337): If required_by is non-empty, return a version of self that is a
ContextualVersionConflict.

#### Clase `ContextualVersionConflict` (Línea 348)
- **Docstring:** _A VersionConflict that accepts a third parameter, the set of the
requirements that required the installed Distribution._
- **Métodos:**
  - `def required_by(self)` (Línea 357): Sin docstring.

#### Clase `DistributionNotFound` (Línea 361)
- **Docstring:** _A requested distribution was not found_
- **Métodos:**
  - `def req(self)` (Línea 370): Sin docstring.
  - `def requirers(self)` (Línea 374): Sin docstring.
  - `def requirers_str(self)` (Línea 378): Sin docstring.
  - `def report(self)` (Línea 383): Sin docstring.
  - `def __str__(self)` (Línea 386): Sin docstring.

#### Clase `UnknownExtra` (Línea 390)
- **Docstring:** _Distribution doesn't have an "extra feature" of the given name_

#### Clase `IMetadataProvider` (Línea 568)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def has_metadata(self, name)` (Línea 569): Does the package's distribution contain the named metadata?
  - `def get_metadata(self, name)` (Línea 572): The named metadata resource as a string
  - `def get_metadata_lines(self, name)` (Línea 575): Yield named metadata resource as list of non-blank non-comment lines

Leading and trailing whitespace is stripped from each line, and lines
with ``#`` as the first non-blank character are omitted.
  - `def metadata_isdir(self, name)` (Línea 581): Is the named metadata a directory?  (like ``os.path.isdir()``)
  - `def metadata_listdir(self, name)` (Línea 584): List of metadata names in the directory (like ``os.listdir()``)
  - `def run_script(self, script_name, namespace)` (Línea 587): Execute the named script in the supplied namespace dictionary

#### Clase `IResourceProvider` (Línea 591)
- **Docstring:** _An object that provides access to package resources_
- **Métodos:**
  - `def get_resource_filename(self, manager, resource_name)` (Línea 594): Return a true filesystem path for `resource_name`

`manager` must be a ``ResourceManager``
  - `def get_resource_stream(self, manager, resource_name)` (Línea 601): Return a readable file-like object for `resource_name`

`manager` must be a ``ResourceManager``
  - `def get_resource_string(self, manager, resource_name)` (Línea 608): Return the contents of `resource_name` as :obj:`bytes`

`manager` must be a ``ResourceManager``
  - `def has_resource(self, resource_name)` (Línea 615): Does the package contain the named resource?
  - `def resource_isdir(self, resource_name)` (Línea 618): Is the named resource a directory?  (like ``os.path.isdir()``)
  - `def resource_listdir(self, resource_name)` (Línea 621): List of resource names in the directory (like ``os.listdir()``)

#### Clase `WorkingSet` (Línea 625)
- **Docstring:** _A collection of active distributions on sys.path (or a similar list)_
- **Métodos:**
  - `def __init__(self, entries)` (Línea 628): Create working set from list of path entries (default=sys.path)
  - `def _build_master(cls)` (Línea 643): Prepare the master working set.
  - `def _build_from_requirements(cls, req_spec)` (Línea 663): Build a working set from a requirement spec. Rewrites sys.path.
  - `def add_entry(self, entry)` (Línea 684): Add a path item to ``.entries``, finding any distributions on it

``find_distributions(entry, True)`` is used to find distributions
corresponding to the path entry, and they are added.  `entry` is
always appended to ``.entries``, even if it is already present.
(This is because ``sys.path`` can contain the same value more than
once, and the ``.entries`` of the ``sys.path`` WorkingSet should always
equal ``sys.path``.)
  - `def __contains__(self, dist)` (Línea 699): True if `dist` is the active distribution for its project
  - `def find(self, req)` (Línea 703): Find a distribution matching requirement `req`

If there is an active distribution for the requested project, this
returns it as long as it meets the version requirement specified by
`req`.  But, if there is an active distribution for the project and it
does *not* meet the `req` requirement, ``VersionConflict`` is raised.
If there is no active distribution for the requested project, ``None``
is returned.
  - `def iter_entry_points(self, group, name)` (Línea 727): Yield entry point objects from `group` matching `name`

If `name` is None, yields all entry points in `group` from all
distributions in the working set, otherwise only ones matching
both `group` and `name` are yielded (in distribution order).
  - `def run_script(self, requires, script_name)` (Línea 741): Locate distribution for `requires` and run `script_name` script
  - `def __iter__(self)` (Línea 749): Yield distributions for non-duplicate projects in the working set

The yield order is the order in which the items' path entries were
added to the working set.
  - `def add(self, dist, entry, insert, replace)` (Línea 766): Add `dist` to working set, associated with `entry`

If `entry` is unspecified, it defaults to the ``.location`` of `dist`.
On exit from this routine, `entry` is added to the end of the working
set's ``.entries`` (if it wasn't already present).

`dist` is only added to the working set if it's for a project that
doesn't already have a distribution in the set, unless `replace=True`.
If it's added, any callbacks registered with the ``subscribe()`` method
will be called.
  - `def resolve(self, requirements, env, installer, replace_conflicting, extras)` (Línea 805): Sin docstring.
  - `def resolve(self, requirements, env)` (Línea 814): Sin docstring.
  - `def resolve(self, requirements, env, installer, replace_conflicting, extras)` (Línea 824): Sin docstring.
  - `def resolve(self, requirements, env, installer, replace_conflicting, extras)` (Línea 832): List all distributions needed to (recursively) meet `requirements`

`requirements` must be a sequence of ``Requirement`` objects.  `env`,
if supplied, should be an ``Environment`` instance.  If
not supplied, it defaults to all distributions available within any
entry or distribution in the working set.  `installer`, if supplied,
will be invoked with each requirement that cannot be met by an
already-installed distribution; it should return a ``Distribution`` or
``None``.

Unless `replace_conflicting=True`, raises a VersionConflict exception
if
any requirements are found on the path that have the correct name but
the wrong version.  Otherwise, if an `installer` is supplied it will be
invoked to obtain the correct version of the requirement and activate
it.

`extras` is a list of the extras to be used with these requirements.
This is important because extra requirements may look like `my_req;
extra = "my_extra"`, which would otherwise be interpreted as a purely
optional requirement.  Instead, we want to be able to assert that these
requirements are truly required.
  - `def _resolve_dist(self, req, best, replace_conflicting, env, installer, required_by, to_activate)` (Línea 906): Sin docstring.
  - `def find_plugins(self, plugin_env, full_env, installer, fallback)` (Línea 938): Sin docstring.
  - `def find_plugins(self, plugin_env, full_env)` (Línea 946): Sin docstring.
  - `def find_plugins(self, plugin_env, full_env, installer, fallback)` (Línea 955): Sin docstring.
  - `def find_plugins(self, plugin_env, full_env, installer, fallback)` (Línea 962): Find all activatable distributions in `plugin_env`

Example usage::

    distributions, errors = working_set.find_plugins(
        Environment(plugin_dirlist)
    )
    # add plugins+libs to sys.path
    map(working_set.add, distributions)
    # display errors
    print('Could not load', errors)

The `plugin_env` should be an ``Environment`` instance that contains
only distributions that are in the project's "plugin directory" or
directories. The `full_env`, if supplied, should be an ``Environment``
contains all currently-available distributions.  If `full_env` is not
supplied, one is created automatically from the ``WorkingSet`` this
method is called on, which will typically mean that every directory on
``sys.path`` will be scanned for distributions.

`installer` is a standard installer callback as used by the
``resolve()`` method. The `fallback` flag indicates whether we should
attempt to resolve older versions of a plugin if the newest version
cannot be resolved.

This method returns a 2-tuple: (`distributions`, `error_info`), where
`distributions` is a list of the distributions found in `plugin_env`
that were loadable, along with any other distributions that are needed
to resolve their dependencies.  `error_info` is a dictionary mapping
unloadable plugin distributions to an exception instance describing the
error that occurred. Usually this will be a ``DistributionNotFound`` or
``VersionConflict`` instance.
  - `def require(self)` (Línea 1052): Ensure that distributions matching `requirements` are activated

`requirements` must be a string or a (possibly-nested) sequence
thereof, specifying the distributions and versions required.  The
return value is a sequence of the distributions that needed to be
activated to fulfill the requirements; all relevant distributions are
included, even if they were already activated in this working set.
  - `def subscribe(self, callback, existing)` (Línea 1068): Invoke `callback` for all distributions

If `existing=True` (default),
call on all existing ones, as well.
  - `def _added_new(self, dist)` (Línea 1084): Sin docstring.
  - `def __getstate__(self)` (Línea 1088): Sin docstring.
  - `def __setstate__(self, e_k_b_n_c)` (Línea 1097): Sin docstring.

#### Clase `_ReqExtras` (Línea 1106)
- **Docstring:** _Map each requirement to the extras that demanded it._
- **Métodos:**
  - `def markers_pass(self, req, extras)` (Línea 1111): Evaluate markers for req against each extra that
demanded it.

Return False if the req has a marker and fails
evaluation. Otherwise, return True.

#### Clase `Environment` (Línea 1126)
- **Docstring:** _Searchable snapshot of distributions on a search path_
- **Métodos:**
  - `def __init__(self, search_path, platform, python)` (Línea 1129): Snapshot distributions available on a search path

Any distributions found on `search_path` are added to the environment.
`search_path` should be a sequence of ``sys.path`` items.  If not
supplied, ``sys.path`` is used.

`platform` is an optional string specifying the name of the platform
that platform-specific distributions must be compatible with.  If
unspecified, it defaults to the current platform.  `python` is an
optional string naming the desired version of Python (e.g. ``'3.6'``);
it defaults to the current version.

You may explicitly set `platform` (and/or `python`) to ``None`` if you
wish to map *all* distributions, not just those compatible with the
running platform or Python version.
  - `def can_add(self, dist)` (Línea 1156): Is distribution `dist` acceptable for this environment?

The distribution must match the platform and python version
requirements specified when this environment was created, or False
is returned.
  - `def remove(self, dist)` (Línea 1170): Remove `dist` from the environment
  - `def scan(self, search_path)` (Línea 1174): Scan `search_path` for distributions usable in this environment

Any distributions found are added to the environment.
`search_path` should be a sequence of ``sys.path`` items.  If not
supplied, ``sys.path`` is used.  Only distributions conforming to
the platform/python version defined at initialization are added.
  - `def __getitem__(self, project_name)` (Línea 1189): Return a newest-to-oldest list of distributions for `project_name`

Uses case-insensitive `project_name` comparison, assuming all the
project's distributions use their project's name converted to all
lowercase as their key.
  - `def add(self, dist)` (Línea 1200): Add `dist` if we ``can_add()`` it and it has not already been added
  - `def best_match(self, req, working_set, installer, replace_conflicting)` (Línea 1209): Sin docstring.
  - `def best_match(self, req, working_set, installer, replace_conflicting)` (Línea 1217): Sin docstring.
  - `def best_match(self, req, working_set, installer, replace_conflicting)` (Línea 1224): Find distribution best matching `req` and usable on `working_set`

This calls the ``find(req)`` method of the `working_set` to see if a
suitable distribution is already active.  (This may raise
``VersionConflict`` if an unsuitable version of the project is already
active in the specified `working_set`.)  If a suitable distribution
isn't active, this method returns the newest distribution in the
environment that meets the ``Requirement`` in `req`.  If no suitable
distribution is found, and `installer` is supplied, then the result of
calling the environment's ``obtain(req, installer)`` method will be
returned.
  - `def obtain(self, requirement, installer)` (Línea 1258): Sin docstring.
  - `def obtain(self, requirement, installer)` (Línea 1264): Sin docstring.
  - `def obtain(self, requirement, installer)` (Línea 1270): Sin docstring.
  - `def obtain(self, requirement, installer)` (Línea 1275): Obtain a distribution matching `requirement` (e.g. via download)

Obtain a distro that matches requirement (e.g. via download).  In the
base ``Environment`` class, this routine just returns
``installer(requirement)``, unless `installer` is None, in which case
None is returned instead.  This method is a hook that allows subclasses
to attempt other ways of obtaining a distribution before falling back
to the `installer` argument.
  - `def __iter__(self)` (Línea 1293): Yield the unique project names of the available distributions
  - `def __iadd__(self, other)` (Línea 1299): In-place addition of a distribution or environment
  - `def __add__(self, other)` (Línea 1311): Add an environment or distribution to an environment

#### Clase `ExtractionError` (Línea 1323)
- **Docstring:** _An error occurred extracting a resource

The following attributes are available from instances of this exception:

manager
    The resource manager that raised this exception

cache_path
    The base directory for resource extraction

original_error
    The exception instance that caused extraction to fail_

#### Clase `ResourceManager` (Línea 1343)
- **Docstring:** _Manage resource extraction and packages_
- **Métodos:**
  - `def __init__(self)` (Línea 1348): Sin docstring.
  - `def resource_exists(self, package_or_requirement, resource_name)` (Línea 1351): Does the named resource exist?
  - `def resource_isdir(self, package_or_requirement, resource_name)` (Línea 1355): Is the named resource an existing directory?
  - `def resource_filename(self, package_or_requirement, resource_name)` (Línea 1359): Return a true filesystem path for specified resource
  - `def resource_stream(self, package_or_requirement, resource_name)` (Línea 1367): Return a readable file-like object for specified resource
  - `def resource_string(self, package_or_requirement, resource_name)` (Línea 1373): Return specified resource as :obj:`bytes`
  - `def resource_listdir(self, package_or_requirement, resource_name)` (Línea 1381): List the contents of the named resource directory
  - `def extraction_error(self)` (Línea 1385): Give an error message for problems extracting file(s)
  - `def get_cache_path(self, archive_name, names)` (Línea 1415): Return absolute location in cache for `archive_name` and `names`

The parent directory of the resulting path will be created if it does
not already exist.  `archive_name` should be the base filename of the
enclosing egg (which may not be the name of the enclosing zipfile!),
including its ".egg" extension.  `names`, if provided, should be a
sequence of path name parts "under" the egg's extraction location.

This method should only be called by resource providers that need to
obtain an extraction location, and only for names they intend to
extract, as it tracks the generated names for possible cleanup later.
  - `def _warn_unsafe_extraction_path(path)` (Línea 1441): If the default extraction path is overridden and set to an insecure
location, such as /tmp, it opens up an opportunity for an attacker to
replace an extracted file with an unauthorized payload. Warn the user
if a known insecure location is used.

See Distribute #375 for more details.
  - `def postprocess(self, tempname, filename)` (Línea 1467): Perform any platform-specific postprocessing of `tempname`

This is where Mac header rewrites should be done; other platforms don't
have anything special they should do.

Resource providers should call this method ONLY after successfully
extracting a compressed resource.  They must NOT call it on resources
that are already in the filesystem.

`tempname` is the current (temporary) name of the file, and `filename`
is the name it will be renamed to by the caller after this routine
returns.
  - `def set_extraction_path(self, path)` (Línea 1487): Set the base path where resources will be extracted to, if needed.

If you do not call this routine before any extractions take place, the
path defaults to the return value of ``get_default_cache()``.  (Which
is based on the ``PYTHON_EGG_CACHE`` environment variable, with various
platform-specific fallbacks.  See that routine's documentation for more
details.)

Resources are extracted to subdirectories of this path based upon
information given by the ``IResourceProvider``.  You may set this to a
temporary directory, but then you must call ``cleanup_resources()`` to
delete the extracted files when done.  There is no guarantee that
``cleanup_resources()`` will be able to remove all extracted files.

(Note: you may not change the extraction path for a given resource
manager once resources have been extracted, unless you first call
``cleanup_resources()``.)
  - `def cleanup_resources(self, force)` (Línea 1511): Delete all extracted resource files and directories, returning a list
of the file and directory names that could not be successfully removed.
This function does not have any concurrency protection, so it should
generally only be called when the extraction path is a temporary
directory exclusive to a single process.  This method is not
automatically called; you must call it explicitly or register it as an
``atexit`` function if you wish to ensure cleanup of a temporary
directory used for extractions.

#### Clase `NullProvider` (Línea 1633)
- **Docstring:** _Try to implement resources and metadata for arbitrary PEP 302 loaders_
- **Métodos:**
  - `def __init__(self, module)` (Línea 1640): Sin docstring.
  - `def get_resource_filename(self, manager, resource_name)` (Línea 1644): Sin docstring.
  - `def get_resource_stream(self, manager, resource_name)` (Línea 1647): Sin docstring.
  - `def get_resource_string(self, manager, resource_name)` (Línea 1650): Sin docstring.
  - `def has_resource(self, resource_name)` (Línea 1655): Sin docstring.
  - `def _get_metadata_path(self, name)` (Línea 1658): Sin docstring.
  - `def has_metadata(self, name)` (Línea 1661): Sin docstring.
  - `def get_metadata(self, name)` (Línea 1668): Sin docstring.
  - `def get_metadata_lines(self, name)` (Línea 1681): Sin docstring.
  - `def resource_isdir(self, resource_name)` (Línea 1684): Sin docstring.
  - `def metadata_isdir(self, name)` (Línea 1687): Sin docstring.
  - `def resource_listdir(self, resource_name)` (Línea 1690): Sin docstring.
  - `def metadata_listdir(self, name)` (Línea 1693): Sin docstring.
  - `def run_script(self, script_name, namespace)` (Línea 1698): Sin docstring.
  - `def _has(self, path)` (Línea 1727): Sin docstring.
  - `def _isdir(self, path)` (Línea 1732): Sin docstring.
  - `def _listdir(self, path)` (Línea 1737): Sin docstring.
  - `def _fn(self, base, resource_name)` (Línea 1742): Sin docstring.
  - `def _validate_resource_path(path)` (Línea 1753): Validate the resource paths according to the docs.
https://setuptools.pypa.io/en/latest/pkg_resources.html#basic-resource-access

>>> warned = getfixture('recwarn')
>>> warnings.simplefilter('always')
>>> vrp = NullProvider._validate_resource_path
>>> vrp('foo/bar.txt')
>>> bool(warned)
False
>>> vrp('../foo/bar.txt')
>>> bool(warned)
True
>>> warned.clear()
>>> vrp('/foo/bar.txt')
>>> bool(warned)
True
>>> vrp('foo/../../bar.txt')
>>> bool(warned)
True
>>> warned.clear()
>>> vrp('foo/f../bar.txt')
>>> bool(warned)
False

Windows path separators are straight-up disallowed.
>>> vrp(r'\foo/bar.txt')
Traceback (most recent call last):
...
ValueError: Use of .. or absolute path in a resource path is not allowed.

>>> vrp(r'C:\foo/bar.txt')
Traceback (most recent call last):
...
ValueError: Use of .. or absolute path in a resource path is not allowed.

Blank values are allowed

>>> vrp('')
>>> bool(warned)
False

Non-string values are not.

>>> vrp(None)
Traceback (most recent call last):
...
AttributeError: ...
  - `def _get(self, path)` (Línea 1827): Sin docstring.

#### Clase `EggProvider` (Línea 1850)
- **Docstring:** _Provider based on a virtual filesystem_
- **Métodos:**
  - `def __init__(self, module)` (Línea 1853): Sin docstring.
  - `def _setup_prefix(self)` (Línea 1857): Sin docstring.
  - `def _set_egg(self, path)` (Línea 1864): Sin docstring.

#### Clase `DefaultProvider` (Línea 1870)
- **Docstring:** _Provides access to package resources in the filesystem_
- **Métodos:**
  - `def _has(self, path)` (Línea 1873): Sin docstring.
  - `def _isdir(self, path)` (Línea 1876): Sin docstring.
  - `def _listdir(self, path)` (Línea 1879): Sin docstring.
  - `def get_resource_stream(self, manager, resource_name)` (Línea 1882): Sin docstring.
  - `def _get(self, path)` (Línea 1885): Sin docstring.
  - `def _register(cls)` (Línea 1890): Sin docstring.

#### Clase `EmptyProvider` (Línea 1903)
- **Docstring:** _Provider that returns nothing for all requests_
- **Métodos:**
  - `def _get(self, path)` (Línea 1911): Sin docstring.
  - `def _listdir(self, path)` (Línea 1914): Sin docstring.
  - `def __init__(self)` (Línea 1917): Sin docstring.

#### Clase `ZipManifests` (Línea 1924)
- **Docstring:** _zip manifest builder_
- **Métodos:**
  - `def build(cls, path)` (Línea 1931): Build a dictionary similar to the zipimport directory
caches, except instead of tuples, store ZipInfo objects.

Use a platform-specific path separator (os.sep) for the path keys
for compatibility with pypy on Windows.

#### Clase `MemoizedZipManifests` (Línea 1952)
- **Docstring:** _Memoized zipfile manifests._
- **Métodos:**
  - `def load(self, path)` (Línea 1961): Load a manifest at path or return a suitable manifest already loaded.

#### Clase `ZipProvider` (Línea 1975)
- **Docstring:** _Resource support for zips and eggs_
- **Métodos:**
  - `def __init__(self, module)` (Línea 1983): Sin docstring.
  - `def _zipinfo_name(self, fspath)` (Línea 1987): Sin docstring.
  - `def _parts(self, zip_path)` (Línea 1997): Sin docstring.
  - `def zipinfo(self)` (Línea 2006): Sin docstring.
  - `def get_resource_filename(self, manager, resource_name)` (Línea 2009): Sin docstring.
  - `def _get_date_and_size(zip_stat)` (Línea 2023): Sin docstring.
  - `def _extract_resource(self, manager, zip_path)` (Línea 2032): Sin docstring.
  - `def _is_current(self, file_path, zip_path)` (Línea 2086): Return True if the file_path is current for this zip_path
  - `def _get_eager_resources(self)` (Línea 2102): Sin docstring.
  - `def _index(self)` (Línea 2111): Sin docstring.
  - `def _has(self, fspath)` (Línea 2128): Sin docstring.
  - `def _isdir(self, fspath)` (Línea 2132): Sin docstring.
  - `def _listdir(self, fspath)` (Línea 2135): Sin docstring.
  - `def _eager_to_zip(self, resource_name)` (Línea 2138): Sin docstring.
  - `def _resource_to_zip(self, resource_name)` (Línea 2141): Sin docstring.

#### Clase `FileMetadata` (Línea 2148)
- **Docstring:** _Metadata handler for standalone PKG-INFO files

Usage::

    metadata = FileMetadata("/path/to/PKG-INFO")

This provider rejects all data and metadata requests except for PKG-INFO,
which is treated as existing, and will be the contents of the file at
the provided location._
- **Métodos:**
  - `def __init__(self, path)` (Línea 2160): Sin docstring.
  - `def _get_metadata_path(self, name)` (Línea 2163): Sin docstring.
  - `def has_metadata(self, name)` (Línea 2166): Sin docstring.
  - `def get_metadata(self, name)` (Línea 2169): Sin docstring.
  - `def _warn_on_replacement(self, metadata)` (Línea 2178): Sin docstring.
  - `def get_metadata_lines(self, name)` (Línea 2185): Sin docstring.

#### Clase `PathMetadata` (Línea 2189)
- **Docstring:** _Metadata provider for egg directories

Usage::

    # Development eggs:

    egg_info = "/path/to/PackageName.egg-info"
    base_dir = os.path.dirname(egg_info)
    metadata = PathMetadata(base_dir, egg_info)
    dist_name = os.path.splitext(os.path.basename(egg_info))[0]
    dist = Distribution(basedir, project_name=dist_name, metadata=metadata)

    # Unpacked egg directories:

    egg_path = "/path/to/PackageName-ver-pyver-etc.egg"
    metadata = PathMetadata(egg_path, os.path.join(egg_path,'EGG-INFO'))
    dist = Distribution.from_filename(egg_path, metadata=metadata)_
- **Métodos:**
  - `def __init__(self, path, egg_info)` (Línea 2209): Sin docstring.

#### Clase `EggMetadata` (Línea 2214)
- **Docstring:** _Metadata provider for .egg files_
- **Métodos:**
  - `def __init__(self, importer)` (Línea 2217): Create a metadata provider from a zipimporter

#### Clase `NoDists` (Línea 2330)
- **Docstring:** _>>> bool(NoDists())
False

>>> list(NoDists()('anything'))
[]_
- **Métodos:**
  - `def __bool__(self)` (Línea 2339): Sin docstring.
  - `def __call__(self, fullpath)` (Línea 2342): Sin docstring.

#### Clase `EntryPoint` (Línea 2681)
- **Docstring:** _Object representing an advertised importable object_
- **Métodos:**
  - `def __init__(self, name, module_name, attrs, extras, dist)` (Línea 2684): Sin docstring.
  - `def __str__(self)` (Línea 2700): Sin docstring.
  - `def __repr__(self)` (Línea 2708): Sin docstring.
  - `def load(self, require, env, installer)` (Línea 2712): Sin docstring.
  - `def load(self, require)` (Línea 2719): Sin docstring.
  - `def load(self, require)` (Línea 2725): Require packages for this EntryPoint, then resolve it.
  - `def resolve(self)` (Línea 2747): Resolve the entry point from its module and attrs.
  - `def require(self, env, installer)` (Línea 2757): Sin docstring.
  - `def parse(cls, src, dist)` (Línea 2785): Parse a single entry point from string `src`

Entry point syntax follows the form::

    name = some.module:some.attr [extra1, extra2]

The entry name and module name are required, but the ``:attrs`` and
``[extras]`` parts are optional
  - `def _parse_extras(cls, extras_spec)` (Línea 2805): Sin docstring.
  - `def parse_group(cls, group, lines, dist)` (Línea 2814): Parse an entry point group
  - `def parse_map(cls, data, dist)` (Línea 2832): Parse a map of entry point groups

#### Clase `Distribution` (Línea 2871)
- **Docstring:** _Wrap an actual or potential sys.path entry w/metadata_
- **Métodos:**
  - `def __init__(self, location, metadata, project_name, version, py_version, platform, precedence)` (Línea 2876): Sin docstring.
  - `def from_location(cls, location, basename, metadata)` (Línea 2896): Sin docstring.
  - `def _reload_version(self)` (Línea 2923): Sin docstring.
  - `def hashcmp(self)` (Línea 2927): Sin docstring.
  - `def __hash__(self)` (Línea 2937): Sin docstring.
  - `def __lt__(self, other)` (Línea 2940): Sin docstring.
  - `def __le__(self, other)` (Línea 2943): Sin docstring.
  - `def __gt__(self, other)` (Línea 2946): Sin docstring.
  - `def __ge__(self, other)` (Línea 2949): Sin docstring.
  - `def __eq__(self, other)` (Línea 2952): Sin docstring.
  - `def __ne__(self, other)` (Línea 2958): Sin docstring.
  - `def key(self)` (Línea 2966): Sin docstring.
  - `def parsed_version(self)` (Línea 2974): Sin docstring.
  - `def _forgiving_parsed_version(self)` (Línea 2988): Sin docstring.
  - `def version(self)` (Línea 3014): Sin docstring.
  - `def _dep_map(self)` (Línea 3029): A map of extra to its list of (direct) requirements
for this distribution, including the null extra.
  - `def _filter_extras(dm)` (Línea 3041): Given a mapping of extras to dependencies, strip off
environment markers and filter out any dependencies
not matching the markers.
  - `def _build_dep_map(self)` (Línea 3061): Sin docstring.
  - `def requires(self, extras)` (Línea 3068): List of Requirements needed for this distro if `extras` are used
  - `def _get_metadata_path_for_display(self, name)` (Línea 3082): Return the path to the given metadata file, if available.
  - `def _get_metadata(self, name)` (Línea 3099): Sin docstring.
  - `def _get_version(self)` (Línea 3103): Sin docstring.
  - `def activate(self, path, replace)` (Línea 3107): Ensure distribution is importable on `path` (default=sys.path)
  - `def egg_name(self)` (Línea 3118): Return what this distribution's standard .egg filename should be
  - `def __repr__(self)` (Línea 3130): Sin docstring.
  - `def __str__(self)` (Línea 3136): Sin docstring.
  - `def __getattr__(self, attr)` (Línea 3144): Delegate all unrecognized public attributes to .metadata provider
  - `def __dir__(self)` (Línea 3150): Sin docstring.
  - `def from_filename(cls, filename, metadata)` (Línea 3157): Sin docstring.
  - `def as_requirement(self)` (Línea 3167): Return a ``Requirement`` that matches this distribution exactly
  - `def load_entry_point(self, group, name)` (Línea 3176): Return the `name` entry point of `group` or raise ImportError
  - `def get_entry_map(self, group)` (Línea 3184): Sin docstring.
  - `def get_entry_map(self, group)` (Línea 3186): Sin docstring.
  - `def get_entry_map(self, group)` (Línea 3187): Return the entry point map for `group`, or the full entry map
  - `def get_entry_info(self, group, name)` (Línea 3197): Return the EntryPoint object for `group`+`name`, or ``None``
  - `def insert_on(self, path, loc, replace)` (Línea 3202): Ensure self.location is on path

If replace=False (default):
    - If location is already in path anywhere, do nothing.
    - Else:
      - If it's an egg and its parent directory is on path,
        insert just ahead of the parent.
      - Else: add to the end of path.
If replace=True:
    - If location is already on path anywhere (not eggs)
      or higher priority than its parent (eggs)
      do nothing.
    - Else:
      - If it's an egg and its parent directory is on path,
        insert just ahead of the parent,
        removing any lower-priority entries.
      - Else: add it to the front of path.
  - `def check_version_conflict(self)` (Línea 3275): Sin docstring.
  - `def has_version(self)` (Línea 3301): Sin docstring.
  - `def clone(self)` (Línea 3312): Copy this distribution, substituting in any changed keyword args
  - `def extras(self)` (Línea 3322): Sin docstring.

#### Clase `EggInfoDistribution` (Línea 3326)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def _reload_version(self)` (Línea 3327): Packages installed by distutils (e.g. numpy or scipy),
which uses an old safe_version, and so
their version numbers can get mangled when
converted to filenames (e.g., 1.11.0.dev0+2329eae to
1.11.0.dev0_2329eae). These distributions will not be
parsed properly
downstream by Distribution and safe_version, so
take an extra step and try to get the version number from
the metadata file itself instead of the filename.

#### Clase `DistInfoDistribution` (Línea 3345)
- **Docstring:** _Wrap an actual or potential sys.path entry
w/metadata, .dist-info style._
- **Métodos:**
  - `def _parsed_pkg_info(self)` (Línea 3355): Parse and cache metadata
  - `def _dep_map(self)` (Línea 3365): Sin docstring.
  - `def _compute_dependencies(self)` (Línea 3372): Recompute this distribution's dependencies.

#### Clase `RequirementParseError` (Línea 3427)
- **Docstring:** _Compatibility wrapper for InvalidRequirement_

#### Clase `Requirement` (Línea 3431)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, requirement_string)` (Línea 3432): DO NOT CALL THIS UNDOCUMENTED METHOD; use Requirement.parse()!
  - `def __eq__(self, other)` (Línea 3450): Sin docstring.
  - `def __ne__(self, other)` (Línea 3453): Sin docstring.
  - `def __contains__(self, item)` (Línea 3456): Sin docstring.
  - `def __hash__(self)` (Línea 3468): Sin docstring.
  - `def __repr__(self)` (Línea 3471): Sin docstring.
  - `def parse(s)` (Línea 3475): Sin docstring.

#### Clase `PkgResourcesDeprecationWarning` (Línea 3564)
- **Docstring:** _Base class for warning about deprecations in ``pkg_resources``

This class is not derived from ``DeprecationWarning``, and as such is
visible by default._

### Funciones independientes:

#### `def _declare_state(vartype, varname, initial_value)` (Línea 159)
- **Propósito:** Sin docstring.
- **Firma:** `def _declare_state(vartype, varname, initial_value)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def __getstate__()` (Línea 164)
- **Propósito:** Sin docstring.
- **Firma:** `def __getstate__()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def __setstate__(state)` (Línea 172)
- **Propósito:** Sin docstring.
- **Firma:** `def __setstate__(state)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _sget_dict(val)` (Línea 179)
- **Propósito:** Sin docstring.
- **Firma:** `def _sget_dict(val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _sset_dict(key, ob, state)` (Línea 183)
- **Propósito:** Sin docstring.
- **Firma:** `def _sset_dict(key, ob, state)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _sget_object(val)` (Línea 188)
- **Propósito:** Sin docstring.
- **Firma:** `def _sget_object(val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _sset_object(key, ob, state)` (Línea 192)
- **Propósito:** Sin docstring.
- **Firma:** `def _sset_object(key, ob, state)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_supported_platform()` (Línea 199)
- **Propósito:** Return this platform's maximum compatible version.

distutils.util.get_platform() normally reports the minimum version
of macOS that would be required to *use* extensions produced by
distutils.  But what we want when checking compatibility is to know the
version of macOS that we are *running*.  To allow usage of packages that
explicitly require a newer version of macOS, we must also know the
current version of the OS.

If this condition occurs for any other platform with a version in its
platform strings, this function should be extended accordingly.
- **Firma:** `def get_supported_platform()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register_loader_type(loader_type, provider_factory)` (Línea 404)
- **Propósito:** Register `provider_factory` to make providers for `loader_type`

`loader_type` is the type or class of a PEP 302 ``module.__loader__``,
and `provider_factory` is a function that, passed a *module* object,
returns an ``IResourceProvider`` for that module.
- **Firma:** `def register_loader_type(loader_type, provider_factory)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_provider(moduleOrReq)` (Línea 417)
- **Propósito:** Sin docstring.
- **Firma:** `def get_provider(moduleOrReq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_provider(moduleOrReq)` (Línea 419)
- **Propósito:** Sin docstring.
- **Firma:** `def get_provider(moduleOrReq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_provider(moduleOrReq)` (Línea 420)
- **Propósito:** Return an IResourceProvider for the named module or requirement
- **Firma:** `def get_provider(moduleOrReq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _macos_vers()` (Línea 434)
- **Propósito:** Sin docstring.
- **Firma:** `def _macos_vers()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _macos_arch(machine)` (Línea 447)
- **Propósito:** Sin docstring.
- **Firma:** `def _macos_arch(machine)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_build_platform()` (Línea 451)
- **Propósito:** Return this platform's string for platform-specific distributions

XXX Currently this is the same as ``distutils.util.get_platform()``, but it
needs some hacks for Linux and macOS.
- **Firma:** `def get_build_platform()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def compatible_platforms(provided, required)` (Línea 482)
- **Propósito:** Can code for the `provided` platform run on the `required` platform?

Returns true if either platform is ``None``, or the platforms are equal.

XXX Needs compatibility checks for Linux and other unixy OSes.
- **Firma:** `def compatible_platforms(provided, required)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_distribution(dist)` (Línea 532)
- **Propósito:** Sin docstring.
- **Firma:** `def get_distribution(dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_distribution(dist)` (Línea 534)
- **Propósito:** Sin docstring.
- **Firma:** `def get_distribution(dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_distribution(dist)` (Línea 535)
- **Propósito:** Return a current distribution object for a Requirement or string
- **Firma:** `def get_distribution(dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def load_entry_point(dist, group, name)` (Línea 547)
- **Propósito:** Return `name` entry point of `group` for `dist` or raise ImportError
- **Firma:** `def load_entry_point(dist, group, name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_entry_map(dist, group)` (Línea 553)
- **Propósito:** Sin docstring.
- **Firma:** `def get_entry_map(dist, group)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_entry_map(dist, group)` (Línea 557)
- **Propósito:** Sin docstring.
- **Firma:** `def get_entry_map(dist, group)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_entry_map(dist, group)` (Línea 558)
- **Propósito:** Return the entry point map for `group`, or the full entry map
- **Firma:** `def get_entry_map(dist, group)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_entry_info(dist, group, name)` (Línea 563)
- **Propósito:** Return the EntryPoint object for `group`+`name`, or ``None``
- **Firma:** `def get_entry_info(dist, group, name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_default_cache()` (Línea 1526)
- **Propósito:** Return the ``PYTHON_EGG_CACHE`` environment variable
or a platform-relevant user cache dir for an app
named "Python-Eggs".
- **Firma:** `def get_default_cache()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def safe_name(name)` (Línea 1535)
- **Propósito:** Convert an arbitrary string to a standard distribution name

Any runs of non-alphanumeric/. characters are replaced with a single '-'.
- **Firma:** `def safe_name(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def safe_version(version)` (Línea 1543)
- **Propósito:** Convert an arbitrary string to a standard version string
- **Firma:** `def safe_version(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _forgiving_version(version)` (Línea 1555)
- **Propósito:** Fallback when ``safe_version`` is not safe enough
>>> parse_version(_forgiving_version('0.23ubuntu1'))
<Version('0.23.dev0+sanitized.ubuntu1')>
>>> parse_version(_forgiving_version('0.23-'))
<Version('0.23.dev0+sanitized')>
>>> parse_version(_forgiving_version('0.-_'))
<Version('0.dev0+sanitized')>
>>> parse_version(_forgiving_version('42.+?1'))
<Version('42.dev0+sanitized.1')>
>>> parse_version(_forgiving_version('hello world'))
<Version('0.dev0+sanitized.hello.world')>
- **Firma:** `def _forgiving_version(version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _safe_segment(segment)` (Línea 1580)
- **Propósito:** Convert an arbitrary string into a safe segment
- **Firma:** `def _safe_segment(segment)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def safe_extra(extra)` (Línea 1587)
- **Propósito:** Convert an arbitrary string to a standard 'extra' name

Any runs of non-alphanumeric characters are replaced with a single '_',
and the result is always lowercased.
- **Firma:** `def safe_extra(extra)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def to_filename(name)` (Línea 1596)
- **Propósito:** Convert a project or version name to its filename-escaped form

Any '-' characters are currently replaced with '_'.
- **Firma:** `def to_filename(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def invalid_marker(text)` (Línea 1604)
- **Propósito:** Validate text as a PEP 508 environment marker; return an exception
if invalid or False otherwise.
- **Firma:** `def invalid_marker(text)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def evaluate_marker(text, extra)` (Línea 1618)
- **Propósito:** Evaluate a PEP 508 environment marker.
Return a boolean indicating the marker result in this environment.
Raise SyntaxError if marker is invalid.

This implementation uses the 'pyparsing' module.
- **Firma:** `def evaluate_marker(text, extra)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _parents(path)` (Línea 1839)
- **Propósito:** yield all parents of path including path
- **Firma:** `def _parents(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register_finder(importer_type, distribution_finder)` (Línea 2234)
- **Propósito:** Register `distribution_finder` to find distributions in sys.path items

`importer_type` is the type or class of a PEP 302 "Importer" (sys.path item
handler), and `distribution_finder` is a callable that, passed a path
item and the importer instance, yields ``Distribution`` instances found on
that path item.  See ``pkg_resources.find_on_path`` for an example.
- **Firma:** `def register_finder(importer_type, distribution_finder)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_distributions(path_item, only)` (Línea 2244)
- **Propósito:** Yield distributions accessible via `path_item`
- **Firma:** `def find_distributions(path_item, only)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_eggs_in_zip(importer, path_item, only)` (Línea 2251)
- **Propósito:** Find eggs in zip files; possibly multiple nested eggs.
- **Firma:** `def find_eggs_in_zip(importer, path_item, only)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_nothing(importer, path_item, only)` (Línea 2282)
- **Propósito:** Sin docstring.
- **Firma:** `def find_nothing(importer, path_item, only)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def find_on_path(importer, path_item, only)` (Línea 2291)
- **Propósito:** Yield distributions accessible on a sys.path directory
- **Firma:** `def find_on_path(importer, path_item, only)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def dist_factory(path_item, entry, only)` (Línea 2311)
- **Propósito:** Return a dist_factory for the given entry.
- **Firma:** `def dist_factory(path_item, entry, only)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def safe_listdir(path)` (Línea 2346)
- **Propósito:** Attempt to list contents of path, but suppress some exceptions.
- **Firma:** `def safe_listdir(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def distributions_from_metadata(path)` (Línea 2362)
- **Propósito:** Sin docstring.
- **Firma:** `def distributions_from_metadata(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def non_empty_lines(path)` (Línea 2380)
- **Propósito:** Yield non-empty lines from file at path
- **Firma:** `def non_empty_lines(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def resolve_egg_link(path)` (Línea 2390)
- **Propósito:** Given a path to an .egg-link, resolve distributions
present in the referenced path.
- **Firma:** `def resolve_egg_link(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def register_namespace_handler(importer_type, namespace_handler)` (Línea 2416)
- **Propósito:** Register `namespace_handler` to declare namespace packages

`importer_type` is the type or class of a PEP 302 "Importer" (sys.path item
handler), and `namespace_handler` is a callable like this::

    def namespace_handler(importer, path_entry, moduleName, module):
        # return a path_entry to use for child packages

Namespace handlers are only called if the importer object has already
agreed that it can handle the relevant path item, and they should only
return a subpath if the module __path__ does not already contain an
equivalent subpath.  For an example namespace handler, see
``pkg_resources.file_ns_handler``.
- **Firma:** `def register_namespace_handler(importer_type, namespace_handler)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _handle_ns(packageName, path_item)` (Línea 2436)
- **Propósito:** Ensure that named package includes a subpath of path_item (if needed)
- **Firma:** `def _handle_ns(packageName, path_item)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _rebuild_mod_path(orig_path, package_name, module)` (Línea 2473)
- **Propósito:** Rebuild module.__path__ ensuring that all entries are ordered
corresponding to their sys.path order
- **Firma:** `def _rebuild_mod_path(orig_path, package_name, module)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def declare_namespace(packageName)` (Línea 2507)
- **Propósito:** Declare that package 'packageName' is a namespace package
- **Firma:** `def declare_namespace(packageName)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def fixup_namespace_packages(path_item, parent)` (Línea 2550)
- **Propósito:** Ensure that previously-declared namespace packages include path_item
- **Firma:** `def fixup_namespace_packages(path_item, parent)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def file_ns_handler(importer, path_item, packageName, module)` (Línea 2562)
- **Propósito:** Compute an ns-package subpath for a filesystem or zipfile importer
- **Firma:** `def file_ns_handler(importer, path_item, packageName, module)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def null_ns_handler(importer, path_item, packageName, module)` (Línea 2587)
- **Propósito:** Sin docstring.
- **Firma:** `def null_ns_handler(importer, path_item, packageName, module)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_path(filename)` (Línea 2600)
- **Propósito:** Sin docstring.
- **Firma:** `def normalize_path(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_path(filename)` (Línea 2602)
- **Propósito:** Sin docstring.
- **Firma:** `def normalize_path(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_path(filename)` (Línea 2603)
- **Propósito:** Normalize a file/dir name for comparison purposes
- **Firma:** `def normalize_path(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _cygwin_patch(filename)` (Línea 2608)
- **Propósito:** Contrary to POSIX 2008, on Cygwin, getcwd (3) contains
symlink components. Using
os.path.abspath() works around this limitation. A fix in os.getcwd()
would probably better, in Cygwin even more so, except
that this seems to be by design...
- **Firma:** `def _cygwin_patch(filename)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_egg_path(path)` (Línea 2634)
- **Propósito:** Determine if given path appears to be an egg.
- **Firma:** `def _is_egg_path(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_zip_egg(path)` (Línea 2641)
- **Propósito:** Sin docstring.
- **Firma:** `def _is_zip_egg(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_unpacked_egg(path)` (Línea 2649)
- **Propósito:** Determine if given path appears to be an unpacked egg.
- **Firma:** `def _is_unpacked_egg(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _set_parent_ns(packageName)` (Línea 2658)
- **Propósito:** Sin docstring.
- **Firma:** `def _set_parent_ns(packageName)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _version_from_file(lines)` (Línea 2856)
- **Propósito:** Given an iterable of lines from a Metadata file, return
the value of the Version field, if present, or None otherwise.
- **Firma:** `def _version_from_file(lines)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def issue_warning()` (Línea 3405)
- **Propósito:** Sin docstring.
- **Firma:** `def issue_warning()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_requirements(strs)` (Línea 3418)
- **Propósito:** Yield ``Requirement`` objects for each specification in `strs`.

`strs` must be a string, or a (possibly-nested) iterable thereof.
- **Firma:** `def parse_requirements(strs)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _always_object(classes)` (Línea 3480)
- **Propósito:** Ensure object appears in the mro even
for old-style classes.
- **Firma:** `def _always_object(classes)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_adapter(registry, ob)` (Línea 3490)
- **Propósito:** Return an adapter factory for `ob` from `registry`
- **Firma:** `def _find_adapter(registry, ob)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_directory(path)` (Línea 3501)
- **Propósito:** Ensure that the parent directory of `path` exists
- **Firma:** `def ensure_directory(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _bypass_ensure_directory(path)` (Línea 3507)
- **Propósito:** Sandbox-bypassing version of ensure_directory()
- **Firma:** `def _bypass_ensure_directory(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def split_sections(s)` (Línea 3520)
- **Propósito:** Split a string or iterable thereof into (section, content) pairs

Each ``section`` is a stripped version of the section header ("[section]")
and each ``content`` is a list of stripped lines excluding blank lines and
comment-only lines.  If there are any such lines before the first section
header, they're returned in a first ``section`` of ``None``.
- **Firma:** `def split_sections(s)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _mkstemp()` (Línea 3546)
- **Propósito:** Sin docstring.
- **Firma:** `def _mkstemp()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _read_utf8_with_fallback(file, fallback_encoding)` (Línea 3577)
- **Propósito:** See setuptools.unicode_utils._read_utf8_with_fallback
- **Firma:** `def _read_utf8_with_fallback(file, fallback_encoding)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _call_aside(f)` (Línea 3606)
- **Propósito:** Sin docstring.
- **Firma:** `def _call_aside(f)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _initialize(g)` (Línea 3612)
- **Propósito:** Set up global resource manager (deliberately not state-saved)
- **Firma:** `def _initialize(g)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _initialize_master_working_set()` (Línea 3624)
- **Propósito:** Prepare the master working set and make the ``require()``
API available.

This function has explicit effects on the global state
of pkg_resources. It is intended to be invoked once at
the initialization of this module.

Invocation by other packages is unsupported and done
at their own risk.
- **Firma:** `def _initialize_master_working_set()`
- **Retorno / Efectos:** Consulta código fuente.
