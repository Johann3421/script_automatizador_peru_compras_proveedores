# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/pygments/formatters/img.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/pygments/formatters/img.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 685
- **Fecha de inspección:** 2026-08-05 18:36:33

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `PilNotAvailable` (Línea 52)
- **Docstring:** _When Python imaging library is not available_

#### Clase `FontNotFound` (Línea 56)
- **Docstring:** _When there are no usable fonts specified_

#### Clase `FontManager` (Línea 60)
- **Docstring:** _Manages a set of fonts: normal, italic, bold, etc..._
- **Métodos:**
  - `def __init__(self, font_name, font_size)` (Línea 65): Sin docstring.
  - `def _get_nix_font_path(self, name, style)` (Línea 92): Sin docstring.
  - `def _create_nix(self)` (Línea 106): Sin docstring.
  - `def _get_mac_font_path(self, font_map, name, style)` (Línea 126): Sin docstring.
  - `def _create_mac(self)` (Línea 129): Sin docstring.
  - `def _lookup_win(self, key, basename, styles, fail)` (Línea 157): Sin docstring.
  - `def _create_win(self)` (Línea 171): Sin docstring.
  - `def get_char_size(self)` (Línea 210): Get the character size.
  - `def get_text_size(self, text)` (Línea 216): Get the text size (width, height).
  - `def get_font(self, bold, oblique)` (Línea 226): Get the font based on bold and italic flags.
  - `def get_style(self, style)` (Línea 251): Get the specified style of the font if it is a variable font.
If not found, return the normal font.

#### Clase `ImageFormatter` (Línea 269)
- **Docstring:** _Create a PNG image from source code. This uses the Python Imaging Library to
generate a pixmap from the source code.

.. versionadded:: 0.10

Additional options accepted:

`image_format`
    An image format to output to that is recognised by PIL, these include:

    * "PNG" (default)
    * "JPEG"
    * "BMP"
    * "GIF"

`line_pad`
    The extra spacing (in pixels) between each line of text.

    Default: 2

`font_name`
    The font name to be used as the base font from which others, such as
    bold and italic fonts will be generated.  This really should be a
    monospace font to look sane.
    If a filename or a file-like object is specified, the user must
    provide different styles of the font.

    Default: "Courier New" on Windows, "Menlo" on Mac OS, and
             "DejaVu Sans Mono" on \*nix

`font_size`
    The font size in points to be used.

    Default: 14

`image_pad`
    The padding, in pixels to be used at each edge of the resulting image.

    Default: 10

`line_numbers`
    Whether line numbers should be shown: True/False

    Default: True

`line_number_start`
    The line number of the first line.

    Default: 1

`line_number_step`
    The step used when printing line numbers.

    Default: 1

`line_number_bg`
    The background colour (in "#123456" format) of the line number bar, or
    None to use the style background color.

    Default: "#eed"

`line_number_fg`
    The text color of the line numbers (in "#123456"-like format).

    Default: "#886"

`line_number_chars`
    The number of columns of line numbers allowable in the line number
    margin.

    Default: 2

`line_number_bold`
    Whether line numbers will be bold: True/False

    Default: False

`line_number_italic`
    Whether line numbers will be italicized: True/False

    Default: False

`line_number_separator`
    Whether a line will be drawn between the line number area and the
    source code area: True/False

    Default: True

`line_number_pad`
    The horizontal padding (in pixels) between the line number margin, and
    the source code area.

    Default: 6

`hl_lines`
    Specify a list of lines to be highlighted.

    .. versionadded:: 1.2

    Default: empty list

`hl_color`
    Specify the color for highlighting lines.

    .. versionadded:: 1.2

    Default: highlight color of the selected style_
- **Métodos:**
  - `def __init__(self)` (Línea 389): See the class docstring for explanation of options.
  - `def get_style_defs(self, arg)` (Línea 445): Sin docstring.
  - `def _get_line_height(self)` (Línea 449): Get the height of a line.
  - `def _get_line_y(self, lineno)` (Línea 455): Get the Y coordinate of a line number.
  - `def _get_char_width(self)` (Línea 461): Get the width of a character.
  - `def _get_char_x(self, linelength)` (Línea 467): Get the X coordinate of a character position.
  - `def _get_text_pos(self, linelength, lineno)` (Línea 473): Get the actual position for a character and line position.
  - `def _get_linenumber_pos(self, lineno)` (Línea 479): Get the actual position for the start of a line number.
  - `def _get_text_color(self, style)` (Línea 485): Get the correct color for the token from the style.
  - `def _get_text_bg_color(self, style)` (Línea 495): Get the correct background color for the token from the style.
  - `def _get_style_font(self, style)` (Línea 505): Get the correct font for the style.
  - `def _get_image_size(self, maxlinelength, maxlineno)` (Línea 511): Get the required image size.
  - `def _draw_linenumber(self, posno, lineno)` (Línea 518): Remember a line number drawable to paint later.
  - `def _draw_text(self, pos, text, font, text_fg, text_bg)` (Línea 531): Remember a single drawable tuple to paint later.
  - `def _create_drawables(self, tokensource)` (Línea 537): Create drawables for the token content.
  - `def _draw_line_numbers(self)` (Línea 577): Create drawables for the line numbers.
  - `def _paint_line_number_bg(self, im)` (Línea 588): Paint the line number background on the image.
  - `def format(self, tokensource, outfile)` (Línea 605): Format ``tokensource``, an iterable of ``(tokentype, tokenstring)``
tuples and write it into ``outfile``.

This implementation calculates where it should draw each token on the
pixmap, then calculates the required pixmap size and draws the items.

#### Clase `GifImageFormatter` (Línea 646)
- **Docstring:** _Create a GIF image from source code. This uses the Python Imaging Library to
generate a pixmap from the source code.

.. versionadded:: 1.0_

#### Clase `JpgImageFormatter` (Línea 660)
- **Docstring:** _Create a JPEG image from source code. This uses the Python Imaging Library to
generate a pixmap from the source code.

.. versionadded:: 1.0_

#### Clase `BmpImageFormatter` (Línea 674)
- **Docstring:** _Create a bitmap image from source code. This uses the Python Imaging Library to
generate a pixmap from the source code.

.. versionadded:: 1.0_
