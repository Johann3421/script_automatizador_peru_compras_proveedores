# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/distlib/util.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/distlib/util.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1984
- **Fecha de inspección:** 2026-08-05 18:36:32

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `cached_property` (Línea 443)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, func)` (Línea 445): Sin docstring.
  - `def __get__(self, obj, cls)` (Línea 450): Sin docstring.

#### Clase `FileOperator` (Línea 486)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, dry_run)` (Línea 488): Sin docstring.
  - `def _init_record(self)` (Línea 493): Sin docstring.
  - `def record_as_written(self, path)` (Línea 498): Sin docstring.
  - `def newer(self, source, target)` (Línea 502): Tell if the target is newer than the source.

Returns true if 'source' exists and is more recently modified than
'target', or if 'source' exists and 'target' doesn't.

Returns false if both exist and 'target' is the same age or younger
than 'source'. Raise PackagingFileError if 'source' does not exist.

Note that this test is not very accurate: files created in the same
second will have the same "age".
  - `def copy_file(self, infile, outfile, check)` (Línea 521): Copy a file respecting dry-run and force flags.
        
  - `def copy_stream(self, instream, outfile, encoding)` (Línea 538): Sin docstring.
  - `def write_binary_file(self, path, data)` (Línea 553): Sin docstring.
  - `def write_text_file(self, path, data, encoding)` (Línea 562): Sin docstring.
  - `def set_mode(self, bits, mask, files)` (Línea 565): Sin docstring.
  - `def ensure_dir(self, path)` (Línea 579): Sin docstring.
  - `def byte_compile(self, path, optimize, force, prefix, hashed_invalidation)` (Línea 591): Sin docstring.
  - `def ensure_removed(self, path)` (Línea 610): Sin docstring.
  - `def is_writable(self, path)` (Línea 631): Sin docstring.
  - `def commit(self)` (Línea 643): Commit recorded changes, turn off recording, return
changes.
  - `def rollback(self)` (Línea 653): Sin docstring.

#### Clase `ExportEntry` (Línea 687)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, name, prefix, suffix, flags)` (Línea 689): Sin docstring.
  - `def value(self)` (Línea 696): Sin docstring.
  - `def __repr__(self)` (Línea 699): Sin docstring.
  - `def __eq__(self, other)` (Línea 702): Sin docstring.

#### Clase `Cache` (Línea 963)
- **Docstring:** _A class implementing a cache for resources that need to live in the file system
e.g. shared libraries. This class was moved from resources to here because it
could be used by other modules, e.g. the wheel module._
- **Métodos:**
  - `def __init__(self, base)` (Línea 970): Initialise an instance.

:param base: The base directory where the cache should be located.
  - `def prefix_to_dir(self, prefix, use_abspath)` (Línea 984): Converts a resource prefix to a directory name in the cache.
  - `def clear(self)` (Línea 990): Clear the cache.

#### Clase `EventMixin` (Línea 1007)
- **Docstring:** _A very simple publish/subscribe system._
- **Métodos:**
  - `def __init__(self)` (Línea 1012): Sin docstring.
  - `def add(self, event, subscriber, append)` (Línea 1015): Add a subscriber for an event.

:param event: The name of an event.
:param subscriber: The subscriber to be added (and called when the
                   event is published).
:param append: Whether to append or prepend the subscriber to an
               existing subscriber list for the event.
  - `def remove(self, event, subscriber)` (Línea 1035): Remove a subscriber for an event.

:param event: The name of an event.
:param subscriber: The subscriber to be removed.
  - `def get_subscribers(self, event)` (Línea 1047): Return an iterator for the subscribers for an event.
:param event: The event to return subscribers for.
  - `def publish(self, event)` (Línea 1054): Publish a event and return a list of values returned by its
subscribers.

:param event: The event to publish.
:param args: The positional arguments to pass to the event's
             subscribers.
:param kwargs: The keyword arguments to pass to the event's
               subscribers.

#### Clase `Sequencer` (Línea 1080)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 1082): Sin docstring.
  - `def add_node(self, node)` (Línea 1087): Sin docstring.
  - `def remove_node(self, node, edges)` (Línea 1090): Sin docstring.
  - `def add(self, pred, succ)` (Línea 1106): Sin docstring.
  - `def remove(self, pred, succ)` (Línea 1111): Sin docstring.
  - `def is_step(self, step)` (Línea 1124): Sin docstring.
  - `def get_steps(self, final)` (Línea 1127): Sin docstring.
  - `def strong_connections(self)` (Línea 1153): Sin docstring.
  - `def dot(self)` (Línea 1205): Sin docstring.

#### Clase `Progress` (Línea 1313)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, minval, maxval)` (Línea 1316): Sin docstring.
  - `def update(self, curval)` (Línea 1324): Sin docstring.
  - `def increment(self, incr)` (Línea 1334): Sin docstring.
  - `def start(self)` (Línea 1338): Sin docstring.
  - `def stop(self)` (Línea 1342): Sin docstring.
  - `def maximum(self)` (Línea 1348): Sin docstring.
  - `def percentage(self)` (Línea 1352): Sin docstring.
  - `def format_duration(self, duration)` (Línea 1362): Sin docstring.
  - `def ETA(self)` (Línea 1372): Sin docstring.
  - `def speed(self)` (Línea 1391): Sin docstring.

#### Clase `Transport` (Línea 1543)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, timeout, use_datetime)` (Línea 1545): Sin docstring.
  - `def make_connection(self, host)` (Línea 1549): Sin docstring.

#### Clase `ServerProxy` (Línea 1576)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, uri)` (Línea 1578): Sin docstring.

#### Clase `CSVBase` (Línea 1612)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __enter__(self)` (Línea 1619): Sin docstring.
  - `def __exit__(self)` (Línea 1622): Sin docstring.

#### Clase `CSVReader` (Línea 1626)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 1628): Sin docstring.
  - `def __iter__(self)` (Línea 1639): Sin docstring.
  - `def next(self)` (Línea 1642): Sin docstring.

#### Clase `CSVWriter` (Línea 1653)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, fn)` (Línea 1655): Sin docstring.
  - `def writerow(self, row)` (Línea 1659): Sin docstring.

#### Clase `Configurator` (Línea 1675)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, config, base)` (Línea 1680): Sin docstring.
  - `def configure_custom(self, config)` (Línea 1684): Sin docstring.
  - `def __getitem__(self, key)` (Línea 1716): Sin docstring.
  - `def inc_convert(self, value)` (Línea 1722): Default converter for the inc:// protocol.

#### Clase `SubprocessMixin` (Línea 1731)
- **Docstring:** _Mixin for running subprocesses and capturing their output_
- **Métodos:**
  - `def __init__(self, verbose, progress)` (Línea 1736): Sin docstring.
  - `def reader(self, stream, context)` (Línea 1740): Read lines from a subprocess' output stream and either pass to a progress
callable (if specified) or write progress information to sys.stderr.
  - `def run_command(self, cmd)` (Línea 1761): Sin docstring.

#### Clase `PyPIRCFile` (Línea 1794)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, fn, url)` (Línea 1799): Sin docstring.
  - `def read(self)` (Línea 1805): Sin docstring.
  - `def update(self, username, password)` (Línea 1858): Sin docstring.

### Funciones independientes:

#### `def parse_marker(marker_string)` (Línea 54)
- **Propósito:** Parse a marker string and return a dictionary containing a marker expression.

The dictionary will contain keys "op", "lhs" and "rhs" for non-terminals in
the expression grammar, or strings. A string contained in quotes is to be
interpreted as a literal string, and a string not contained in quotes is a
variable (such as os_name).
- **Firma:** `def parse_marker(marker_string)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_requirement(req)` (Línea 144)
- **Propósito:** Parse a requirement passed in as a string. Return a Container
whose attributes contain the various parts of the requirement.
- **Firma:** `def parse_requirement(req)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_resources_dests(resources_root, rules)` (Línea 268)
- **Propósito:** Find destinations for resources files
- **Firma:** `def get_resources_dests(resources_root, rules)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def in_venv()` (Línea 294)
- **Propósito:** Sin docstring.
- **Firma:** `def in_venv()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_executable()` (Línea 304)
- **Propósito:** Sin docstring.
- **Firma:** `def get_executable()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def proceed(prompt, allowed_chars, error_prompt, default)` (Línea 322)
- **Propósito:** Sin docstring.
- **Firma:** `def proceed(prompt, allowed_chars, error_prompt, default)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def extract_by_key(d, keys)` (Línea 338)
- **Propósito:** Sin docstring.
- **Firma:** `def extract_by_key(d, keys)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def read_exports(stream)` (Línea 348)
- **Propósito:** Sin docstring.
- **Firma:** `def read_exports(stream)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def write_exports(exports, stream)` (Línea 395)
- **Propósito:** Sin docstring.
- **Firma:** `def write_exports(exports, stream)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def tempdir()` (Línea 415)
- **Propósito:** Sin docstring.
- **Firma:** `def tempdir()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def chdir(d)` (Línea 424)
- **Propósito:** Sin docstring.
- **Firma:** `def chdir(d)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def socket_timeout(seconds)` (Línea 434)
- **Propósito:** Sin docstring.
- **Firma:** `def socket_timeout(seconds)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def convert_path(pathname)` (Línea 459)
- **Propósito:** Return 'pathname' as a name that will work on the native filesystem.

The path is split on '/' and put back together again using the current
directory separator.  Needed because filenames in the setup script are
always supplied in Unix style, and have to be converted to the local
convention before we can actually use them in the filesystem.  Raises
ValueError on non-Unix-ish systems if 'pathname' either starts or
ends with a slash.
- **Firma:** `def convert_path(pathname)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def resolve(module_name, dotted_path)` (Línea 672)
- **Propósito:** Sin docstring.
- **Firma:** `def resolve(module_name, dotted_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_export_entry(specification)` (Línea 720)
- **Propósito:** Sin docstring.
- **Firma:** `def get_export_entry(specification)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_cache_base(suffix)` (Línea 751)
- **Propósito:** Return the default base location for distlib caches. If the directory does
not exist, it is created. Use the suffix provided for the base directory,
and default to '.distlib' if it isn't provided.

On Windows, if LOCALAPPDATA is defined in the environment, then it is
assumed to be a directory, and will be the parent directory of the result.
On POSIX, and on Windows if LOCALAPPDATA is not defined, the user's home
directory - using os.expanduser('~') - will be the parent directory of
the result.

The result is just the directory '.distlib' in the parent directory as
determined above, or with the name specified with ``suffix``.
- **Firma:** `def get_cache_base(suffix)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def path_to_cache_dir(path, use_abspath)` (Línea 792)
- **Propósito:** Convert an absolute path to a directory name for use in a cache.

The algorithm used is:

#. On Windows, any ``':'`` in the drive is replaced with ``'---'``.
#. Any occurrence of ``os.sep`` is replaced with ``'--'``.
#. ``'.cache'`` is appended.
- **Firma:** `def path_to_cache_dir(path, use_abspath)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def ensure_slash(s)` (Línea 809)
- **Propósito:** Sin docstring.
- **Firma:** `def ensure_slash(s)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_credentials(netloc)` (Línea 815)
- **Propósito:** Sin docstring.
- **Firma:** `def parse_credentials(netloc)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_process_umask()` (Línea 830)
- **Propósito:** Sin docstring.
- **Firma:** `def get_process_umask()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_string_sequence(seq)` (Línea 836)
- **Propósito:** Sin docstring.
- **Firma:** `def is_string_sequence(seq)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def split_filename(filename, project_name)` (Línea 852)
- **Propósito:** Extract name, version, python version from a filename (no extension)

Return name, version, pyver or None
- **Firma:** `def split_filename(filename, project_name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def parse_name_and_version(p)` (Línea 882)
- **Propósito:** A utility method used to get name and version from a string.

From e.g. a Provides-Dist value.

:param p: A value in a form 'foo (1.0)'
:return: The name and version as a tuple.
- **Firma:** `def parse_name_and_version(p)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_extras(requested, available)` (Línea 898)
- **Propósito:** Sin docstring.
- **Firma:** `def get_extras(requested, available)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_external_data(url)` (Línea 926)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_external_data(url)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_project_data(name)` (Línea 950)
- **Propósito:** Sin docstring.
- **Firma:** `def get_project_data(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_package_data(name, version)` (Línea 957)
- **Propósito:** Sin docstring.
- **Firma:** `def get_package_data(name, version)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def unarchive(archive_filename, dest_dir, format, check)` (Línea 1224)
- **Propósito:** Sin docstring.
- **Firma:** `def unarchive(archive_filename, dest_dir, format, check)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def zip_dir(directory)` (Línea 1292)
- **Propósito:** zip a directory tree into a BytesIO object
- **Firma:** `def zip_dir(directory)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def iglob(path_glob)` (Línea 1412)
- **Propósito:** Extended globbing function that supports ** and {opt1,opt2,opt3}.
- **Firma:** `def iglob(path_glob)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _iglob(path_glob)` (Línea 1423)
- **Propósito:** Sin docstring.
- **Firma:** `def _iglob(path_glob)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _csv_open(fn, mode)` (Línea 1601)
- **Propósito:** Sin docstring.
- **Firma:** `def _csv_open(fn, mode)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def normalize_name(name)` (Línea 1777)
- **Propósito:** Normalize a python package name a la PEP 503
- **Firma:** `def normalize_name(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _load_pypirc(index)` (Línea 1871)
- **Propósito:** Read the PyPI access configuration as supported by distutils.
- **Firma:** `def _load_pypirc(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _store_pypirc(index)` (Línea 1878)
- **Propósito:** Sin docstring.
- **Firma:** `def _store_pypirc(index)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_host_platform()` (Línea 1888)
- **Propósito:** Return a string that identifies the current platform.  This is used mainly to
distinguish platform-specific build directories and platform-specific built
distributions.  Typically includes the OS name and version and the
architecture (as supplied by 'os.uname()'), although the exact information
included depends on the OS; eg. on Linux, the kernel version isn't
particularly important.

Examples of returned values:
   linux-i586
   linux-alpha (?)
   solaris-2.6-sun4u

Windows will return one of:
   win-amd64 (64bit Windows on AMD64 (aka x86_64, Intel64, EM64T, etc)
   win32 (all others - specifically, sys.platform is returned)

For other non-POSIX platforms, currently just returns 'sys.platform'.
- **Firma:** `def get_host_platform()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_platform()` (Línea 1978)
- **Propósito:** Sin docstring.
- **Firma:** `def get_platform()`
- **Retorno / Efectos:** Consulta código fuente.
