# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/distlib/database.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/distlib/database.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1329
- **Fecha de inspección:** 2026-08-05 18:36:32

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_Cache` (Línea 41)
- **Docstring:** _A simple cache mapping names and .dist-info paths to distributions_
- **Métodos:**
  - `def __init__(self)` (Línea 46): Initialise an instance. There is normally one for each DistributionPath.
  - `def clear(self)` (Línea 54): Clear the cache, setting it to its initial state.
  - `def add(self, dist)` (Línea 62): Add a distribution to the cache.
:param dist: The distribution to add.

#### Clase `DistributionPath` (Línea 72)
- **Docstring:** _Represents a set of distributions installed on a path (typically sys.path)._
- **Métodos:**
  - `def __init__(self, path, include_egg)` (Línea 77): Create an instance from a path, optionally including legacy (distutils/
setuptools/distribute) distributions.
:param path: The path to use, as a list of directories. If not specified,
             sys.path is used.
:param include_egg: If True, this instance will look for and return legacy
                    distributions as well as those based on PEP 376.
  - `def _get_cache_enabled(self)` (Línea 97): Sin docstring.
  - `def _set_cache_enabled(self, value)` (Línea 100): Sin docstring.
  - `def clear_cache(self)` (Línea 105): Clears the internal cache.
  - `def _yield_distributions(self)` (Línea 112): Yield .dist-info and/or .egg(-info) distributions.
  - `def _generate_cache(self)` (Línea 158): Scan the path for distributions and populate the cache with
those that are found.
  - `def distinfo_dirname(cls, name, version)` (Línea 178): The *name* and *version* parameters are converted into their
filename-escaped form, i.e. any ``'-'`` characters are replaced
with ``'_'`` other than the one in ``'dist-info'`` and the one
separating the name from the version number.

:parameter name: is converted to a standard distribution name by replacing
                 any runs of non- alphanumeric characters with a single
                 ``'-'``.
:type name: string
:parameter version: is converted to a standard version string. Spaces
                    become dots, and all other non-alphanumeric characters
                    (except dots) become dashes, with runs of multiple
                    dashes condensed to a single dash.
:type version: string
:returns: directory name
:rtype: string
  - `def get_distributions(self)` (Línea 199): Provides an iterator that looks for distributions and returns
:class:`InstalledDistribution` or
:class:`EggInfoDistribution` instances for each one of them.

:rtype: iterator of :class:`InstalledDistribution` and
        :class:`EggInfoDistribution` instances
  - `def get_distribution(self, name)` (Línea 221): Looks for a named distribution on the path.

This function only returns the first result found, as no more than one
value is expected. If nothing is found, ``None`` is returned.

:rtype: :class:`InstalledDistribution`, :class:`EggInfoDistribution`
        or ``None``
  - `def provides_distribution(self, name, version)` (Línea 247): Iterates over all distributions to find which distributions provide *name*.
If a *version* is provided, it will be used to filter the results.

This function only returns the first result found, since no more than
one values are expected. If the directory is not found, returns ``None``.

:parameter version: a version specifier that indicates the version
                    required, conforming to the format in ``PEP-345``

:type name: string
:type version: string
  - `def get_file_path(self, name, relative_path)` (Línea 287): Return the path to a resource file.
  - `def get_exported_entries(self, category, name)` (Línea 296): Return all of the exported entries in a particular category.

:param category: The category to search for entries.
:param name: If specified, only entries with that name are returned.

#### Clase `Distribution` (Línea 315)
- **Docstring:** _A base class for distributions, whether installed or from indexes.
Either way, it must have some metadata, so that's all that's needed
for construction._
- **Métodos:**
  - `def __init__(self, metadata)` (Línea 333): Initialise an instance.
:param metadata: The instance of :class:`Metadata` describing this
distribution.
  - `def source_url(self)` (Línea 351): The source archive download URL for this distribution.
  - `def name_and_version(self)` (Línea 360): A utility property which displays the name and version in parentheses.
  - `def provides(self)` (Línea 367): A set of distribution names and versions provided by this distribution.
:return: A set of "name (version)" strings.
  - `def _get_requirements(self, req_attr)` (Línea 378): Sin docstring.
  - `def run_requires(self)` (Línea 385): Sin docstring.
  - `def meta_requires(self)` (Línea 389): Sin docstring.
  - `def build_requires(self)` (Línea 393): Sin docstring.
  - `def test_requires(self)` (Línea 397): Sin docstring.
  - `def dev_requires(self)` (Línea 401): Sin docstring.
  - `def matches_requirement(self, req)` (Línea 404): Say if this instance matches (fulfills) a requirement.
:param req: The requirement to match.
:rtype req: str
:return: True if it matches, else False.
  - `def __repr__(self)` (Línea 437): Return a textual representation of this instance,
  - `def __eq__(self, other)` (Línea 447): See if this distribution is the same as another.
:param other: The distribution to compare with. To be equal to one
              another. distributions must have the same type, name,
              version and source_url.
:return: True if it is the same, else False.
  - `def __hash__(self)` (Línea 461): Compute hash in a way which matches the equality test.

#### Clase `BaseInstalledDistribution` (Línea 468)
- **Docstring:** _This is the base class for installed distributions (whether PEP 376 or
legacy)._
- **Métodos:**
  - `def __init__(self, metadata, path, env)` (Línea 476): Initialise an instance.
:param metadata: An instance of :class:`Metadata` which describes the
                 distribution. This will normally have been initialised
                 from a metadata file in the ``path``.
:param path:     The path of the ``.dist-info`` or ``.egg-info``
                 directory for the distribution.
:param env:      This is normally the :class:`DistributionPath`
                 instance where this distribution was found.
  - `def get_hash(self, data, hasher)` (Línea 491): Get the hash of some data, using a particular hash algorithm, if
specified.

:param data: The data to be hashed.
:type data: bytes
:param hasher: The name of a hash implementation, supported by hashlib,
               or ``None``. Examples of valid values are ``'sha1'``,
               ``'sha224'``, ``'sha384'``, '``sha256'``, ``'md5'`` and
               ``'sha512'``. If no hasher is specified, the ``hasher``
               attribute of the :class:`InstalledDistribution` instance
               is used. If the hasher is determined to be ``None``, MD5
               is used as the hashing algorithm.
:returns: The hash of the data. If a hasher was explicitly specified,
          the returned hash will be prefixed with the specified hasher
          followed by '='.
:rtype: str

#### Clase `InstalledDistribution` (Línea 523)
- **Docstring:** _Created with the *path* of the ``.dist-info`` directory provided to the
constructor. It reads the metadata contained in ``pydist.json`` when it is
instantiated., or uses a passed in Metadata instance (useful for when
dry-run mode is being used)._
- **Métodos:**
  - `def __init__(self, path, metadata, env)` (Línea 533): Sin docstring.
  - `def __repr__(self)` (Línea 566): Sin docstring.
  - `def __str__(self)` (Línea 569): Sin docstring.
  - `def _get_records(self)` (Línea 572): Get the list of installed files for the distribution
:return: A list of tuples of path, hash and size. Note that hash and
         size might be ``None`` for some entries. The path is exactly
         as stored in the file (which is as in PEP 376).
  - `def exports(self)` (Línea 596): Return the information exported by this distribution.
:return: A dictionary of exports, mapping an export category to a dict
         of :class:`ExportEntry` instances describing the individual
         export entries, and keyed by name.
  - `def read_exports(self)` (Línea 609): Read exports data from a file in .ini format.

:return: A dictionary of exports, mapping an export category to a list
         of :class:`ExportEntry` instances describing the individual
         export entries.
  - `def write_exports(self, exports)` (Línea 624): Write a dictionary of exports to a file in .ini format.
:param exports: A dictionary of exports, mapping an export category to
                a list of :class:`ExportEntry` instances describing the
                individual export entries.
  - `def get_resource_path(self, relative_path)` (Línea 635): NOTE: This API may change in the future.

Return the absolute path to a resource file with the given relative
path.

:param relative_path: The path, relative to .dist-info, of the resource
                      of interest.
:return: The absolute path where the resource is to be found.
  - `def list_installed_files(self)` (Línea 655): Iterates over the ``RECORD`` entries and returns a tuple
``(path, hash, size)`` for each line.

:returns: iterator of (path, hash, size)
  - `def write_installed_files(self, paths, prefix, dry_run)` (Línea 665): Writes the ``RECORD`` file, using the ``paths`` iterable passed in. Any
existing ``RECORD`` file is silently overwritten.

prefix is used to determine when to write absolute paths.
  - `def check_installed_files(self)` (Línea 699): Checks that the hashes and sizes of the files in ``RECORD`` are
matched by the files themselves. Returns a (possibly empty) list of
mismatches. Each entry in the mismatch list will be a tuple consisting
of the path, 'exists', 'size' or 'hash' according to what didn't match
(existence is checked first, then size, then hash), the expected
value and the actual value.
  - `def shared_locations(self)` (Línea 735): A dictionary of shared locations whose keys are in the set 'prefix',
'purelib', 'platlib', 'scripts', 'headers', 'data' and 'namespace'.
The corresponding value is the absolute path of that category for
this distribution, and takes into account any paths selected by the
user at installation time (e.g. via command-line arguments). In the
case of the 'namespace' key, this would be a list of absolute paths
for the roots of namespace packages in this distribution.

The first time this property is accessed, the relevant information is
read from the SHARED file in the .dist-info directory.
  - `def write_shared_locations(self, paths, dry_run)` (Línea 761): Write shared location information to the SHARED file in .dist-info.
:param paths: A dictionary as described in the documentation for
:meth:`shared_locations`.
:param dry_run: If True, the action is logged but no file is actually
                written.
:return: The path of the file written to.
  - `def get_distinfo_resource(self, path)` (Línea 786): Sin docstring.
  - `def get_distinfo_file(self, path)` (Línea 795): Returns a path located under the ``.dist-info`` directory. Returns a
string representing the path.

:parameter path: a ``'/'``-separated path relative to the
                 ``.dist-info`` directory or an absolute path;
                 If *path* is an absolute path and doesn't start
                 with the ``.dist-info`` directory path,
                 a :class:`DistlibException` is raised
:type path: str
:rtype: str
  - `def list_distinfo_files(self)` (Línea 823): Iterates over the ``RECORD`` entries and returns paths for each line if
the path is pointing to a file located in the ``.dist-info`` directory
or one of its subdirectories.

:returns: iterator of paths
  - `def __eq__(self, other)` (Línea 839): Sin docstring.

#### Clase `EggInfoDistribution` (Línea 846)
- **Docstring:** _Created with the *path* of the ``.egg-info`` directory or file provided
to the constructor. It reads the metadata contained in the file itself, or
if the given path happens to be a directory, the metadata is read from the
file ``PKG-INFO`` under that directory._
- **Métodos:**
  - `def __init__(self, path, env)` (Línea 855): Sin docstring.
  - `def _get_metadata(self, path)` (Línea 877): Sin docstring.
  - `def __repr__(self)` (Línea 968): Sin docstring.
  - `def __str__(self)` (Línea 971): Sin docstring.
  - `def check_installed_files(self)` (Línea 974): Checks that the hashes and sizes of the files in ``RECORD`` are
matched by the files themselves. Returns a (possibly empty) list of
mismatches. Each entry in the mismatch list will be a tuple consisting
of the path, 'exists', 'size' or 'hash' according to what didn't match
(existence is checked first, then size, then hash), the expected
value and the actual value.
  - `def list_installed_files(self)` (Línea 993): Iterates over the ``installed-files.txt`` entries and returns a tuple
``(path, hash, size)`` for each line.

:returns: a list of (path, hash, size)
  - `def list_distinfo_files(self, absolute)` (Línea 1031): Iterates over the ``installed-files.txt`` entries and returns paths for
each line if the path is pointing to a file located in the
``.egg-info`` directory or one of its subdirectories.

:parameter absolute: If *absolute* is ``True``, each returned path is
                  transformed into a local absolute path. Otherwise the
                  raw value from ``installed-files.txt`` is returned.
:type absolute: boolean
:returns: iterator of paths
  - `def __eq__(self, other)` (Línea 1060): Sin docstring.

#### Clase `DependencyGraph` (Línea 1071)
- **Docstring:** _Represents a dependency graph between distributions.

The dependency relationships are stored in an ``adjacency_list`` that maps
distributions to a list of ``(other, label)`` tuples where  ``other``
is a distribution and the edge is labeled with ``label`` (i.e. the version
specifier, if such was provided). Also, for more efficient traversal, for
every distribution ``x``, a list of predecessors is kept in
``reverse_list[x]``. An edge from distribution ``a`` to
distribution ``b`` means that ``a`` depends on ``b``. If any missing
dependencies are found, they are stored in ``missing``, which is a
dictionary that maps distributions to a list of requirements that were not
provided by any other distributions._
- **Métodos:**
  - `def __init__(self)` (Línea 1087): Sin docstring.
  - `def add_distribution(self, distribution)` (Línea 1092): Add the *distribution* to the graph.

:type distribution: :class:`distutils2.database.InstalledDistribution`
                    or :class:`distutils2.database.EggInfoDistribution`
  - `def add_edge(self, x, y, label)` (Línea 1102): Add an edge from distribution *x* to distribution *y* with the given
*label*.

:type x: :class:`distutils2.database.InstalledDistribution` or
         :class:`distutils2.database.EggInfoDistribution`
:type y: :class:`distutils2.database.InstalledDistribution` or
         :class:`distutils2.database.EggInfoDistribution`
:type label: ``str`` or ``None``
  - `def add_missing(self, distribution, requirement)` (Línea 1117): Add a missing *requirement* for the given *distribution*.

:type distribution: :class:`distutils2.database.InstalledDistribution`
                    or :class:`distutils2.database.EggInfoDistribution`
:type requirement: ``str``
  - `def _repr_dist(self, dist)` (Línea 1128): Sin docstring.
  - `def repr_node(self, dist, level)` (Línea 1131): Prints only a subgraph
  - `def to_dot(self, f, skip_disconnected)` (Línea 1144): Writes a DOT output for the graph to the provided file *f*.

If *skip_disconnected* is set to ``True``, then all distributions
that are not dependent on any other distribution are skipped.

:type f: has to support ``file``-like operations
:type skip_disconnected: ``bool``
  - `def topological_sort(self)` (Línea 1175): Perform a topological sort of the graph.
:return: A tuple, the first element of which is a topologically sorted
         list of distributions, and the second element of which is a
         list of distributions that cannot be sorted because they have
         circular dependencies and so form a cycle.
  - `def __repr__(self)` (Línea 1205): Representation of the graph

### Funciones independientes:

#### `def make_graph(dists, scheme)` (Línea 1213)
- **Propósito:** Makes a dependency graph from the given distributions.

:parameter dists: a list of distributions
:type dists: list of :class:`distutils2.database.InstalledDistribution` and
             :class:`distutils2.database.EggInfoDistribution` instances
:rtype: a :class:`DependencyGraph` instance
- **Firma:** `def make_graph(dists, scheme)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_dependent_dists(dists, dist)` (Línea 1265)
- **Propósito:** Recursively generate a list of distributions from *dists* that are
dependent on *dist*.

:param dists: a list of distributions
:param dist: a distribution, member of *dists* for which we are interested
- **Firma:** `def get_dependent_dists(dists, dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_required_dists(dists, dist)` (Línea 1291)
- **Propósito:** Recursively generate a list of distributions from *dists* that are
required by *dist*.

:param dists: a list of distributions
:param dist: a distribution, member of *dists* for which we are interested
             in finding the dependencies.
- **Firma:** `def get_required_dists(dists, dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def make_dist(name, version)` (Línea 1320)
- **Propósito:** A convenience method for making a dist given just a name and version.
- **Firma:** `def make_dist(name, version)`
- **Retorno / Efectos:** Consulta código fuente.
