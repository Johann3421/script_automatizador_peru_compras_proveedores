# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/urllib3/contrib/pyopenssl.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/urllib3/contrib/pyopenssl.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 518
- **Fecha de inspección:** 2026-08-05 18:36:34

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `WrappedSocket` (Línea 276)
- **Docstring:** _API-compatibility wrapper for Python OpenSSL's Connection-class.

Note: _makefile_refs, _drop() and _reuse() are needed for the garbage
collector of pypy._
- **Métodos:**
  - `def __init__(self, connection, socket, suppress_ragged_eofs)` (Línea 283): Sin docstring.
  - `def fileno(self)` (Línea 290): Sin docstring.
  - `def _decref_socketios(self)` (Línea 294): Sin docstring.
  - `def recv(self)` (Línea 300): Sin docstring.
  - `def recv_into(self)` (Línea 325): Sin docstring.
  - `def settimeout(self, timeout)` (Línea 348): Sin docstring.
  - `def _send_until_done(self, data)` (Línea 351): Sin docstring.
  - `def sendall(self, data)` (Línea 362): Sin docstring.
  - `def shutdown(self)` (Línea 370): Sin docstring.
  - `def close(self)` (Línea 374): Sin docstring.
  - `def getpeercert(self, binary_form)` (Línea 384): Sin docstring.
  - `def version(self)` (Línea 398): Sin docstring.
  - `def _reuse(self)` (Línea 401): Sin docstring.
  - `def _drop(self)` (Línea 404): Sin docstring.

#### Clase `PyOpenSSLContext` (Línea 423)
- **Docstring:** _I am a wrapper class for the PyOpenSSL ``Context`` object. I am responsible
for translating the interface of the standard library ``SSLContext`` object
to calls into PyOpenSSL._
- **Métodos:**
  - `def __init__(self, protocol)` (Línea 430): Sin docstring.
  - `def options(self)` (Línea 437): Sin docstring.
  - `def options(self, value)` (Línea 441): Sin docstring.
  - `def verify_mode(self)` (Línea 446): Sin docstring.
  - `def verify_mode(self, value)` (Línea 450): Sin docstring.
  - `def set_default_verify_paths(self)` (Línea 453): Sin docstring.
  - `def set_ciphers(self, ciphers)` (Línea 456): Sin docstring.
  - `def load_verify_locations(self, cafile, capath, cadata)` (Línea 461): Sin docstring.
  - `def load_cert_chain(self, certfile, keyfile, password)` (Línea 473): Sin docstring.
  - `def set_alpn_protocols(self, protocols)` (Línea 481): Sin docstring.
  - `def wrap_socket(self, sock, server_side, do_handshake_on_connect, suppress_ragged_eofs, server_hostname)` (Línea 485): Sin docstring.

### Funciones independientes:

#### `def inject_into_urllib3()` (Línea 130)
- **Propósito:** Monkey-patch urllib3 with PyOpenSSL-backed SSL-support.
- **Firma:** `def inject_into_urllib3()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def extract_from_urllib3()` (Línea 143)
- **Propósito:** Undo monkey-patching by :func:`inject_into_urllib3`.
- **Firma:** `def extract_from_urllib3()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _validate_dependencies_met()` (Línea 154)
- **Propósito:** Verifies that PyOpenSSL's package-level dependencies have been met.
Throws `ImportError` if they are not met.
- **Firma:** `def _validate_dependencies_met()`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _dnsname_to_stdlib(name)` (Línea 180)
- **Propósito:** Converts a dNSName SubjectAlternativeName field to the form used by the
standard library on the given Python version.

Cryptography produces a dNSName as a unicode string that was idna-decoded
from ASCII bytes. We need to idna-encode that string to get it back, and
then on Python 3 we also need to convert to unicode via UTF-8 (the stdlib
uses PyUnicode_FromStringAndSize on it, which decodes via UTF-8).

If the name cannot be idna-encoded then we return None signalling that
the name given should be skipped.
- **Firma:** `def _dnsname_to_stdlib(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_subj_alt_name(peer_cert)` (Línea 223)
- **Propósito:** Given an PyOpenSSL certificate, provides all the subject alternative names.
- **Firma:** `def get_subj_alt_name(peer_cert)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _verify_callback(cnx, x509, err_no, err_depth, return_code)` (Línea 517)
- **Propósito:** Sin docstring.
- **Firma:** `def _verify_callback(cnx, x509, err_no, err_depth, return_code)`
- **Retorno / Efectos:** Consulta código fuente.
