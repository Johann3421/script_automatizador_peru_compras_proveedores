# Documentación Técnica: `venv_build/Lib/site-packages/pip/_vendor/pygments/util.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/pip/_vendor/pygments/util.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 324
- **Fecha de inspección:** 2026-08-05 18:36:33

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `ClassNotFound` (Línea 30)
- **Docstring:** _Raised if one of the lookup functions didn't find a matching class._

#### Clase `OptionError` (Línea 34)
- **Docstring:** _This exception will be raised by all option processing functions if
the type or value of the argument is not correct._

#### Clase `Future` (Línea 265)
- **Docstring:** _Generic class to defer some work.

Handled specially in RegexLexerMeta, to support regex string construction at
first use._
- **Métodos:**
  - `def get(self)` (Línea 271): Sin docstring.

#### Clase `UnclosingTextIOWrapper` (Línea 321)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def close(self)` (Línea 323): Sin docstring.

### Funciones independientes:

#### `def get_choice_opt(options, optname, allowed, default, normcase)` (Línea 40)
- **Propósito:** If the key `optname` from the dictionary is not in the sequence
`allowed`, raise an error, otherwise return it.
- **Firma:** `def get_choice_opt(options, optname, allowed, default, normcase)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_bool_opt(options, optname, default)` (Línea 53)
- **Propósito:** Intuitively, this is `options.get(optname, default)`, but restricted to
Boolean value. The Booleans can be represented as string, in order to accept
Boolean value from the command line arguments. If the key `optname` is
present in the dictionary `options` and is not associated with a Boolean,
raise an `OptionError`. If it is absent, `default` is returned instead.

The valid string values for ``True`` are ``1``, ``yes``, ``true`` and
``on``, the ones for ``False`` are ``0``, ``no``, ``false`` and ``off``
(matched case-insensitively).
- **Firma:** `def get_bool_opt(options, optname, default)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_int_opt(options, optname, default)` (Línea 82)
- **Propósito:** As :func:`get_bool_opt`, but interpret the value as an integer.
- **Firma:** `def get_int_opt(options, optname, default)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def get_list_opt(options, optname, default)` (Línea 94)
- **Propósito:** If the key `optname` from the dictionary `options` is a string,
split it at whitespace and return it. If it is already a list
or a tuple, it is returned as a list.
- **Firma:** `def get_list_opt(options, optname, default)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def docstring_headline(obj)` (Línea 110)
- **Propósito:** Sin docstring.
- **Firma:** `def docstring_headline(obj)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def make_analysator(f)` (Línea 122)
- **Propósito:** Return a static text analyser function that returns float values.
- **Firma:** `def make_analysator(f)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def shebang_matches(text, regex)` (Línea 139)
- **Propósito:** Check if the given regular expression matches the last part of the
shebang if one exists.

    >>> from pygments.util import shebang_matches
    >>> shebang_matches('#!/usr/bin/env python', r'python(2\.\d)?')
    True
    >>> shebang_matches('#!/usr/bin/python2.4', r'python(2\.\d)?')
    True
    >>> shebang_matches('#!/usr/bin/python-ruby', r'python(2\.\d)?')
    False
    >>> shebang_matches('#!/usr/bin/python/ruby', r'python(2\.\d)?')
    False
    >>> shebang_matches('#!/usr/bin/startsomethingwith python',
    ...                 r'python(2\.\d)?')
    True

It also checks for common windows executable file extensions::

    >>> shebang_matches('#!C:\\Python2.4\\Python.exe', r'python(2\.\d)?')
    True

Parameters (``'-f'`` or ``'--foo'`` are ignored so ``'perl'`` does
the same as ``'perl -e'``)

Note that this method automatically searches the whole string (eg:
the regular expression is wrapped in ``'^$'``)
- **Firma:** `def shebang_matches(text, regex)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def doctype_matches(text, regex)` (Línea 184)
- **Propósito:** Check if the doctype matches a regular expression (if present).

Note that this method only checks the first part of a DOCTYPE.
eg: 'html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"'
- **Firma:** `def doctype_matches(text, regex)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def html_doctype_matches(text)` (Línea 197)
- **Propósito:** Check if the file looks like it has a html doctype.
- **Firma:** `def html_doctype_matches(text)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def looks_like_xml(text)` (Línea 205)
- **Propósito:** Check if a doctype exists or if we have some tags.
- **Firma:** `def looks_like_xml(text)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def surrogatepair(c)` (Línea 221)
- **Propósito:** Given a unicode character code with length greater than 16 bits,
return the two 16 bit surrogate pair.
- **Firma:** `def surrogatepair(c)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def format_lines(var_name, seq, raw, indent_level)` (Línea 230)
- **Propósito:** Formats a sequence of strings for output.
- **Firma:** `def format_lines(var_name, seq, raw, indent_level)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def duplicates_removed(it, already_seen)` (Línea 249)
- **Propósito:** Returns a list with duplicates removed from the iterable `it`.

Order is preserved.
- **Firma:** `def duplicates_removed(it, already_seen)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def guess_decode(text)` (Línea 275)
- **Propósito:** Decode *text* with guessed encoding.

First try UTF-8; this should fail for non-UTF-8 encodings.
Then try the preferred locale encoding.
Fall back to latin-1, which always works.
- **Firma:** `def guess_decode(text)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def guess_decode_from_terminal(text, term)` (Línea 296)
- **Propósito:** Decode *text* coming from terminal *term*.

First try the terminal encoding, if given.
Then try UTF-8.  Then try the preferred locale encoding.
Fall back to latin-1, which always works.
- **Firma:** `def guess_decode_from_terminal(text, term)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def terminal_encoding(term)` (Línea 313)
- **Propósito:** Return our best guess of encoding for the given *term*.
- **Firma:** `def terminal_encoding(term)`
- **Retorno / Efectos:** Consulta código fuente.
