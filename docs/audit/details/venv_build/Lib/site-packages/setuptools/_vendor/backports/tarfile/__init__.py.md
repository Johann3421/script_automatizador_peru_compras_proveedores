# Documentación Técnica: `venv_build/Lib/site-packages/setuptools/_vendor/backports/tarfile/__init__.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/setuptools/_vendor/backports/tarfile/__init__.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 2937
- **Fecha de inspección:** 2026-08-05 18:36:37

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `TarError` (Línea 270)
- **Docstring:** _Base exception._

#### Clase `ExtractError` (Línea 273)
- **Docstring:** _General exception for extract errors._

#### Clase `ReadError` (Línea 276)
- **Docstring:** _Exception for unreadable tar archives._

#### Clase `CompressionError` (Línea 279)
- **Docstring:** _Exception for unavailable compression methods._

#### Clase `StreamError` (Línea 282)
- **Docstring:** _Exception for unsupported operations on stream-like TarFiles._

#### Clase `HeaderError` (Línea 285)
- **Docstring:** _Base exception for header errors._

#### Clase `EmptyHeaderError` (Línea 288)
- **Docstring:** _Exception for empty headers._

#### Clase `TruncatedHeaderError` (Línea 291)
- **Docstring:** _Exception for truncated headers._

#### Clase `EOFHeaderError` (Línea 294)
- **Docstring:** _Exception for end of file headers._

#### Clase `InvalidHeaderError` (Línea 297)
- **Docstring:** _Exception for invalid headers._

#### Clase `SubsequentHeaderError` (Línea 300)
- **Docstring:** _Exception for missing and invalid extended headers._

#### Clase `_LowLevelFile` (Línea 307)
- **Docstring:** _Low-level file object. Supports reading and writing.
It is used instead of a regular file object for streaming
access._
- **Métodos:**
  - `def __init__(self, name, mode)` (Línea 313): Sin docstring.
  - `def close(self)` (Línea 322): Sin docstring.
  - `def read(self, size)` (Línea 325): Sin docstring.
  - `def write(self, s)` (Línea 328): Sin docstring.

#### Clase `_Stream` (Línea 331)
- **Docstring:** _Class that serves as an adapter between TarFile and
a stream-like object.  The stream-like object only
needs to have a read() or write() method that works with bytes,
and the method is accessed blockwise.
Use of gzip or bzip2 compression is possible.
A stream-like object could be for example: sys.stdin.buffer,
sys.stdout.buffer, a socket, a tape device etc.

_Stream is intended to be used only internally._
- **Métodos:**
  - `def __init__(self, name, mode, comptype, fileobj, bufsize, compresslevel)` (Línea 343): Construct a _Stream object.
        
  - `def __del__(self)` (Línea 414): Sin docstring.
  - `def _init_write_gz(self, compresslevel)` (Línea 418): Initialize for writing with gzip compression.
        
  - `def write(self, s)` (Línea 435): Write string s to the stream.
        
  - `def __write(self, s)` (Línea 445): Write string s to the stream if a whole new block
is ready to be written.
  - `def close(self)` (Línea 454): Close the _Stream object. No operation should be
done on it afterwards.
  - `def _init_read_gz(self)` (Línea 476): Initialize for reading a gzip compressed fileobj.
        
  - `def tell(self)` (Línea 507): Return the stream's file pointer position.
        
  - `def seek(self, pos)` (Línea 512): Set the stream's file pointer to pos. Negative seeking
is forbidden.
  - `def read(self, size)` (Línea 525): Return the next size number of bytes from the stream.
  - `def _read(self, size)` (Línea 532): Return size bytes from the stream.
        
  - `def __read(self, size)` (Línea 559): Return size bytes from stream. If internal buffer is empty,
read another block from the stream.

#### Clase `_StreamProxy` (Línea 576)
- **Docstring:** _Small proxy class that enables transparent compression
detection for the Stream interface (mode 'r|*')._
- **Métodos:**
  - `def __init__(self, fileobj)` (Línea 581): Sin docstring.
  - `def read(self, size)` (Línea 585): Sin docstring.
  - `def getcomptype(self)` (Línea 589): Sin docstring.
  - `def close(self)` (Línea 599): Sin docstring.

#### Clase `_FileInFile` (Línea 606)
- **Docstring:** _A thin wrapper around an existing file object that
provides a part of its data as an individual file
object._
- **Métodos:**
  - `def __init__(self, fileobj, offset, size, name, blockinfo)` (Línea 612): Sin docstring.
  - `def flush(self)` (Línea 637): Sin docstring.
  - `def mode(self)` (Línea 641): Sin docstring.
  - `def readable(self)` (Línea 644): Sin docstring.
  - `def writable(self)` (Línea 647): Sin docstring.
  - `def seekable(self)` (Línea 650): Sin docstring.
  - `def tell(self)` (Línea 653): Return the current file position.
        
  - `def seek(self, position, whence)` (Línea 658): Seek to a position in the file.
        
  - `def read(self, size)` (Línea 674): Read data from the file.
        
  - `def readinto(self, b)` (Línea 705): Sin docstring.
  - `def close(self)` (Línea 710): Sin docstring.

#### Clase `ExFileObject` (Línea 714)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarfile, tarinfo)` (Línea 716): Sin docstring.

#### Clase `FilterError` (Línea 727)
- **Docstring:** _Sin docstring._

#### Clase `AbsolutePathError` (Línea 730)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarinfo)` (Línea 731): Sin docstring.

#### Clase `OutsideDestinationError` (Línea 735)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarinfo, path)` (Línea 736): Sin docstring.

#### Clase `SpecialFileError` (Línea 742)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarinfo)` (Línea 743): Sin docstring.

#### Clase `AbsoluteLinkError` (Línea 747)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarinfo)` (Línea 748): Sin docstring.

#### Clase `LinkOutsideDestinationError` (Línea 752)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, tarinfo, path)` (Línea 753): Sin docstring.

#### Clase `TarInfo` (Línea 850)
- **Docstring:** _Informational class which holds the details about an
archive member given by a tar header block.
TarInfo objects are returned by TarFile.getmember(),
TarFile.getmembers() and TarFile.gettarinfo() and are
usually created internally._
- **Métodos:**
  - `def __init__(self, name)` (Línea 885): Construct a TarInfo object. name is the optional name
of the member.
  - `def tarfile(self)` (Línea 910): Sin docstring.
  - `def tarfile(self, tarfile)` (Línea 919): Sin docstring.
  - `def path(self)` (Línea 928): In pax headers, "name" is called "path".
  - `def path(self, name)` (Línea 933): Sin docstring.
  - `def linkpath(self)` (Línea 937): In pax headers, "linkname" is called "linkpath".
  - `def linkpath(self, linkname)` (Línea 942): Sin docstring.
  - `def __repr__(self)` (Línea 945): Sin docstring.
  - `def replace(self)` (Línea 948): Return a deep copy of self with the given attributes replaced.
        
  - `def get_info(self)` (Línea 976): Return the TarInfo's attributes as a dictionary.
        
  - `def tobuf(self, format, encoding, errors)` (Línea 1004): Return a tar header as a string of 512 byte blocks.
        
  - `def create_ustar_header(self, info, encoding, errors)` (Línea 1021): Return the object as a ustar header block.
        
  - `def create_gnu_header(self, info, encoding, errors)` (Línea 1034): Return the object as a GNU header block sequence.
        
  - `def create_pax_header(self, info, encoding)` (Línea 1048): Return the object as a ustar header block. If it cannot be
represented this way, prepend a pax extended header sequence
with supplement information.
  - `def create_pax_global_header(cls, pax_headers)` (Línea 1107): Return the object as a pax global header block sequence.
        
  - `def _posix_split_name(self, name, encoding, errors)` (Línea 1112): Split a name longer than 100 chars into a prefix
and a name part.
  - `def _create_header(info, format, encoding, errors)` (Línea 1129): Return a header block. info is a dictionary with file
information, format must be one of the *_FORMAT constants.
  - `def _create_payload(payload)` (Línea 1171): Return the string payload filled with zero bytes
up to the next 512 byte border.
  - `def _create_gnu_long_header(cls, name, type, encoding, errors)` (Línea 1181): Return a GNUTYPE_LONGNAME or GNUTYPE_LONGLINK sequence
for name.
  - `def _create_pax_generic_header(cls, pax_headers, type, encoding)` (Línea 1198): Return a POSIX.1-2008 extended or global header sequence
that contains a list of keyword, value pairs. The values
must be strings.
  - `def frombuf(cls, buf, encoding, errors)` (Línea 1249): Construct a TarInfo object from a 512 byte bytes object.
        
  - `def fromtarfile(cls, tarfile)` (Línea 1312): Return the next TarInfo object from TarFile object
tarfile.
  - `def _proc_member(self, tarfile)` (Línea 1332): Choose the right processing method depending on
the type and call it.
  - `def _proc_builtin(self, tarfile)` (Línea 1345): Process a builtin type or an unknown type which
will be treated as a regular file.
  - `def _proc_gnulong(self, tarfile)` (Línea 1367): Process the blocks that hold a GNU longname
or longlink member.
  - `def _proc_sparse(self, tarfile)` (Línea 1394): Process a GNU sparse header plus extra headers.
        
  - `def _proc_pax(self, tarfile)` (Línea 1422): Process an extended or global header as described in
POSIX.1-2008.
  - `def _proc_gnusparse_00(self, next, pax_headers, buf)` (Línea 1522): Process a GNU tar extended sparse header, version 0.0.
        
  - `def _proc_gnusparse_01(self, next, pax_headers)` (Línea 1533): Process a GNU tar extended sparse header, version 0.1.
        
  - `def _proc_gnusparse_10(self, next, pax_headers, tarfile)` (Línea 1539): Process a GNU tar extended sparse header, version 1.0.
        
  - `def _apply_pax_info(self, pax_headers, encoding, errors)` (Línea 1555): Replace fields with supplemental information from a previous
pax extended or global header.
  - `def _decode_pax_field(self, value, encoding, fallback_encoding, fallback_errors)` (Línea 1578): Decode a single field from a pax record.
        
  - `def _block(self, count)` (Línea 1586): Round up a byte count by BLOCKSIZE and return it,
e.g. _block(834) => 1024.
  - `def isreg(self)` (Línea 1595): Return True if the Tarinfo object is a regular file.
  - `def isfile(self)` (Línea 1599): Return True if the Tarinfo object is a regular file.
  - `def isdir(self)` (Línea 1603): Return True if it is a directory.
  - `def issym(self)` (Línea 1607): Return True if it is a symbolic link.
  - `def islnk(self)` (Línea 1611): Return True if it is a hard link.
  - `def ischr(self)` (Línea 1615): Return True if it is a character device.
  - `def isblk(self)` (Línea 1619): Return True if it is a block device.
  - `def isfifo(self)` (Línea 1623): Return True if it is a FIFO.
  - `def issparse(self)` (Línea 1627): Sin docstring.
  - `def isdev(self)` (Línea 1630): Return True if it is one of character device, block device or FIFO.

#### Clase `TarFile` (Línea 1635)
- **Docstring:** _The TarFile Class provides an interface to tar archives.
    _
- **Métodos:**
  - `def __init__(self, name, mode, fileobj, format, tarinfo, dereference, ignore_zeros, encoding, errors, pax_headers, debug, errorlevel, copybufsize, stream)` (Línea 1663): Open an (uncompressed) tar archive 'name'. 'mode' is either 'r' to
read from an existing archive, 'a' to append data to an existing
file or 'w' to create a new file overwriting an existing one. 'mode'
defaults to 'r'.
If 'fileobj' is given, it is used for reading or writing data. If it
can be determined, 'mode' is overridden by 'fileobj's mode.
'fileobj' is not closed, when TarFile is closed.
  - `def open(cls, name, mode, fileobj, bufsize)` (Línea 1777): Open a tar archive for reading, writing or appending. Return
an appropriate TarFile class.

mode:
'r' or 'r:\*' open for reading with transparent compression
'r:'         open for reading exclusively uncompressed
'r:gz'       open for reading with gzip compression
'r:bz2'      open for reading with bzip2 compression
'r:xz'       open for reading with lzma compression
'a' or 'a:'  open for appending, creating the file if necessary
'w' or 'w:'  open for writing without compression
'w:gz'       open for writing with gzip compression
'w:bz2'      open for writing with bzip2 compression
'w:xz'       open for writing with lzma compression

'x' or 'x:'  create a tarfile exclusively without compression, raise
             an exception if the file is already created
'x:gz'       create a gzip compressed tarfile, raise an exception
             if the file is already created
'x:bz2'      create a bzip2 compressed tarfile, raise an exception
             if the file is already created
'x:xz'       create an lzma compressed tarfile, raise an exception
             if the file is already created

'r|\*'        open a stream of tar blocks with transparent compression
'r|'         open an uncompressed stream of tar blocks for reading
'r|gz'       open a gzip compressed stream of tar blocks
'r|bz2'      open a bzip2 compressed stream of tar blocks
'r|xz'       open an lzma compressed stream of tar blocks
'w|'         open an uncompressed stream for writing
'w|gz'       open a gzip compressed stream for writing
'w|bz2'      open a bzip2 compressed stream for writing
'w|xz'       open an lzma compressed stream for writing
  - `def taropen(cls, name, mode, fileobj)` (Línea 1873): Open uncompressed tar archive name for reading or writing.
        
  - `def gzopen(cls, name, mode, fileobj, compresslevel)` (Línea 1881): Open gzip compressed tar archive name for reading or writing.
Appending is not allowed.
  - `def bz2open(cls, name, mode, fileobj, compresslevel)` (Línea 1914): Open bzip2 compressed tar archive name for reading or writing.
Appending is not allowed.
  - `def xzopen(cls, name, mode, fileobj, preset)` (Línea 1942): Open lzma compressed tar archive name for reading or writing.
Appending is not allowed.
  - `def close(self)` (Línea 1980): Close the TarFile. In write-mode, two finishing zero blocks are
appended to the archive.
  - `def getmember(self, name)` (Línea 2001): Return a TarInfo object for member 'name'. If 'name' can not be
found in the archive, KeyError is raised. If a member occurs more
than once in the archive, its last occurrence is assumed to be the
most up-to-date version.
  - `def getmembers(self)` (Línea 2012): Return the members of the archive as a list of TarInfo objects. The
list has the same order as the members in the archive.
  - `def getnames(self)` (Línea 2022): Return the members of the archive as a list of their names. It has
the same order as the list returned by getmembers().
  - `def gettarinfo(self, name, arcname, fileobj)` (Línea 2028): Create a TarInfo object from the result of os.stat or equivalent
on an existing file. The file is either named by 'name', or
specified as a file object 'fileobj' with a file descriptor. If
given, 'arcname' specifies an alternative name for the file in the
archive, otherwise, the name is taken from the 'name' attribute of
'fileobj', or the 'name' argument. The name should be a text
string.
  - `def list(self, verbose)` (Línea 2127): Print a table of contents to sys.stdout. If 'verbose' is False, only
the names of the members are printed. If it is True, an 'ls -l'-like
output is produced. 'members' is optional and must be a subset of the
list returned by getmembers().
  - `def add(self, name, arcname, recursive)` (Línea 2170): Add the file 'name' to the archive. 'name' may be any type of file
(directory, fifo, symbolic link, etc.). If given, 'arcname'
specifies an alternative name for the file in the archive.
Directories are added recursively by default. This can be avoided by
setting 'recursive' to False. 'filter' is a function
that expects a TarInfo object argument and returns the changed
TarInfo object, if it returns None the TarInfo object will be
excluded from the archive.
  - `def addfile(self, tarinfo, fileobj)` (Línea 2221): Add the TarInfo object 'tarinfo' to the archive. If 'tarinfo' represents
a non zero-size regular file, the 'fileobj' argument should be a binary file,
and tarinfo.size bytes are read from it and added to the archive.
You can create TarInfo objects directly, or by using gettarinfo().
  - `def _get_filter_function(self, filter)` (Línea 2249): Sin docstring.
  - `def extractall(self, path, members)` (Línea 2273): Extract all members from the archive to the current working
directory and set owner, modification time and permissions on
directories afterwards. 'path' specifies a different directory
to extract to. 'members' is optional and must be a subset of the
list returned by getmembers(). If 'numeric_owner' is True, only
the numbers for user/group names are used and not the names.

The 'filter' function will be called on each member just
before extraction.
It can return a changed TarInfo or None to skip the member.
String names of common filters are accepted.
  - `def extract(self, member, path, set_attrs)` (Línea 2318): Extract a member from the archive to the current working directory,
using its full name. Its file information is extracted as accurately
as possible. 'member' may be a filename or a TarInfo object. You can
specify a different directory using 'path'. File attributes (owner,
mtime, mode) are set unless 'set_attrs' is False. If 'numeric_owner'
is True, only the numbers for user/group names are used and not
the names.

The 'filter' function will be called before extraction.
It can return a changed TarInfo or None to skip the member.
String names of common filters are accepted.
  - `def _get_extract_tarinfo(self, member, filter_function, path)` (Línea 2337): Get filtered TarInfo (or None) from member, which might be a str
  - `def _extract_one(self, tarinfo, path, set_attrs, numeric_owner)` (Línea 2360): Extract from filtered tarinfo to disk
  - `def _handle_nonfatal_error(self, e)` (Línea 2373): Handle non-fatal error (ExtractError) according to errorlevel
  - `def _handle_fatal_error(self, e)` (Línea 2380): Handle "fatal" error according to self.errorlevel
  - `def extractfile(self, member)` (Línea 2392): Extract a member from the archive as a file object. 'member' may be
a filename or a TarInfo object. If 'member' is a regular file or
a link, an io.BufferedReader object is returned. For all other
existing members, None is returned. If 'member' does not appear
in the archive, KeyError is raised.
  - `def _extract_member(self, tarinfo, targetpath, set_attrs, numeric_owner)` (Línea 2424): Extract the TarInfo object tarinfo to a physical
file called targetpath.
  - `def makedir(self, tarinfo, targetpath)` (Línea 2473): Make a directory called targetpath.
        
  - `def makefile(self, tarinfo, targetpath)` (Línea 2488): Make a file called targetpath.
        
  - `def makeunknown(self, tarinfo, targetpath)` (Línea 2504): Make a file from a TarInfo object with an unknown type
at targetpath.
  - `def makefifo(self, tarinfo, targetpath)` (Línea 2512): Make a fifo called targetpath.
        
  - `def makedev(self, tarinfo, targetpath)` (Línea 2520): Make a character or block device called targetpath.
        
  - `def makelink(self, tarinfo, targetpath)` (Línea 2538): Make a (symbolic) link called targetpath. If it cannot be created
(platform limitation), we try to make a copy of the referenced file
instead of a link.
  - `def chown(self, tarinfo, targetpath, numeric_owner)` (Línea 2563): Set owner of targetpath according to tarinfo. If numeric_owner
is True, use .gid/.uid instead of .gname/.uname. If numeric_owner
is False, fall back to .gid/.uid when the search based on name
fails.
  - `def chmod(self, tarinfo, targetpath)` (Línea 2597): Set file permissions of targetpath according to tarinfo.
        
  - `def utime(self, tarinfo, targetpath)` (Línea 2607): Set modification time of targetpath according to tarinfo.
        
  - `def next(self)` (Línea 2621): Return the next member of the archive as a TarInfo object, when
TarFile is opened for reading. Return None if there is no more
available.
  - `def _getmember(self, name, tarinfo, normalize)` (Línea 2688): Find an archive member by name from bottom to top.
If tarinfo is given, it is used as the starting point.
  - `def _load(self)` (Línea 2728): Read through the entire archive file and look for readable
members. This should not run if the file is set to stream.
  - `def _check(self, mode)` (Línea 2737): Check if TarFile is still open, and if the operation's mode
corresponds to TarFile's mode.
  - `def _find_link_target(self, tarinfo)` (Línea 2746): Find the target member of a symlink or hardlink member in the
archive.
  - `def __iter__(self)` (Línea 2765): Provide an iterator object.
        
  - `def _dbg(self, level, msg)` (Línea 2796): Write debugging output to sys.stderr.
        
  - `def __enter__(self)` (Línea 2802): Sin docstring.
  - `def __exit__(self, type, value, traceback)` (Línea 2806): Sin docstring.

### Funciones independientes:

#### `def stn(s, length, encoding, errors)` (Línea 159)
- **Propósito:** Convert a string to a null-terminated bytes object.
    
- **Firma:** `def stn(s, length, encoding, errors)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nts(s, encoding, errors)` (Línea 167)
- **Propósito:** Convert a null-terminated bytes object to a string.
    
- **Firma:** `def nts(s, encoding, errors)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def nti(s)` (Línea 175)
- **Propósito:** Convert a number field to a python number.
    
- **Firma:** `def nti(s)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def itn(n, digits, format)` (Línea 195)
- **Propósito:** Convert a python number to a number field.
    
- **Firma:** `def itn(n, digits, format)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def calc_chksums(buf)` (Línea 225)
- **Propósito:** Calculate the checksum for a member's header by summing up all
characters except for the chksum field which is treated as if
it was filled with spaces. According to the GNU tar sources,
some tars (Sun and NeXT) calculate chksum with signed char,
which will be different if there are chars in the buffer with
the high bit set. So we calculate two checksums, unsigned and
signed.
- **Firma:** `def calc_chksums(buf)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def copyfileobj(src, dst, length, exception, bufsize)` (Línea 238)
- **Propósito:** Copy length bytes from fileobj src to fileobj dst.
If length is None, copy the entire content.
- **Firma:** `def copyfileobj(src, dst, length, exception, bufsize)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _safe_print(s)` (Línea 263)
- **Propósito:** Sin docstring.
- **Firma:** `def _safe_print(s)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_filtered_attrs(member, dest_path, for_data)` (Línea 759)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_filtered_attrs(member, dest_path, for_data)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def fully_trusted_filter(member, dest_path)` (Línea 822)
- **Propósito:** Sin docstring.
- **Firma:** `def fully_trusted_filter(member, dest_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def tar_filter(member, dest_path)` (Línea 825)
- **Propósito:** Sin docstring.
- **Firma:** `def tar_filter(member, dest_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def data_filter(member, dest_path)` (Línea 831)
- **Propósito:** Sin docstring.
- **Firma:** `def data_filter(member, dest_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def is_tarfile(name)` (Línea 2820)
- **Propósito:** Return True if name points to a tar archive that we
are able to handle, else return False.

'name' should be a string, file, or file-like object.
- **Firma:** `def is_tarfile(name)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def main()` (Línea 2841)
- **Propósito:** Sin docstring.
- **Firma:** `def main()`
- **Retorno / Efectos:** Consulta código fuente.
