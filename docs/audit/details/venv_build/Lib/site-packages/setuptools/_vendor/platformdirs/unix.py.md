# Documentación Técnica: `venv_build/Lib/site-packages/setuptools/_vendor/platformdirs/unix.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/setuptools/_vendor/platformdirs/unix.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 272
- **Fecha de inspección:** 2026-08-05 18:36:38

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `Unix` (Línea 26)
- **Docstring:** _On Unix/Linux, we follow the `XDG Basedir Spec <https://specifications.freedesktop.org/basedir-spec/basedir-spec-
latest.html>`_.

The spec allows overriding directories with environment variables. The examples shown are the default values,
alongside the name of the environment variable that overrides them. Makes use of the `appname
<platformdirs.api.PlatformDirsABC.appname>`, `version <platformdirs.api.PlatformDirsABC.version>`, `multipath
<platformdirs.api.PlatformDirsABC.multipath>`, `opinion <platformdirs.api.PlatformDirsABC.opinion>`, `ensure_exists
<platformdirs.api.PlatformDirsABC.ensure_exists>`._
- **Métodos:**
  - `def user_data_dir(self)` (Línea 40): :return: data directory tied to the user, e.g. ``~/.local/share/$appname/$version`` or
 ``$XDG_DATA_HOME/$appname/$version``
  - `def _site_data_dirs(self)` (Línea 51): Sin docstring.
  - `def site_data_dir(self)` (Línea 58): :return: data directories shared by users (if `multipath <platformdirs.api.PlatformDirsABC.multipath>` is
 enabled and ``XDG_DATA_DIRS`` is set and a multi path the response is also a multi path separated by the
 OS path separator), e.g. ``/usr/local/share/$appname/$version`` or ``/usr/share/$appname/$version``
  - `def user_config_dir(self)` (Línea 71): :return: config directory tied to the user, e.g. ``~/.config/$appname/$version`` or
 ``$XDG_CONFIG_HOME/$appname/$version``
  - `def _site_config_dirs(self)` (Línea 82): Sin docstring.
  - `def site_config_dir(self)` (Línea 89): :return: config directories shared by users (if `multipath <platformdirs.api.PlatformDirsABC.multipath>`
 is enabled and ``XDG_CONFIG_DIRS`` is set and a multi path the response is also a multi path separated by
 the OS path separator), e.g. ``/etc/xdg/$appname/$version``
  - `def user_cache_dir(self)` (Línea 102): :return: cache directory tied to the user, e.g. ``~/.cache/$appname/$version`` or
 ``~/$XDG_CACHE_HOME/$appname/$version``
  - `def site_cache_dir(self)` (Línea 113): :return: cache directory shared by users, e.g. ``/var/cache/$appname/$version``
  - `def user_state_dir(self)` (Línea 118): :return: state directory tied to the user, e.g. ``~/.local/state/$appname/$version`` or
 ``$XDG_STATE_HOME/$appname/$version``
  - `def user_log_dir(self)` (Línea 129): :return: log directory tied to the user, same as `user_state_dir` if not opinionated else ``log`` in it
  - `def user_documents_dir(self)` (Línea 138): :return: documents directory tied to the user, e.g. ``~/Documents``
  - `def user_downloads_dir(self)` (Línea 143): :return: downloads directory tied to the user, e.g. ``~/Downloads``
  - `def user_pictures_dir(self)` (Línea 148): :return: pictures directory tied to the user, e.g. ``~/Pictures``
  - `def user_videos_dir(self)` (Línea 153): :return: videos directory tied to the user, e.g. ``~/Videos``
  - `def user_music_dir(self)` (Línea 158): :return: music directory tied to the user, e.g. ``~/Music``
  - `def user_desktop_dir(self)` (Línea 163): :return: desktop directory tied to the user, e.g. ``~/Desktop``
  - `def user_runtime_dir(self)` (Línea 168): :return: runtime directory tied to the user, e.g. ``/run/user/$(id -u)/$appname/$version`` or
 ``$XDG_RUNTIME_DIR/$appname/$version``.

 For FreeBSD/OpenBSD/NetBSD, it would return ``/var/run/user/$(id -u)/$appname/$version`` if
 exists, otherwise ``/tmp/runtime-$(id -u)/$appname/$version``, if``$XDG_RUNTIME_DIR``
 is not set.
  - `def site_runtime_dir(self)` (Línea 188): :return: runtime directory shared by users, e.g. ``/run/$appname/$version`` or         ``$XDG_RUNTIME_DIR/$appname/$version``.

Note that this behaves almost exactly like `user_runtime_dir` if ``$XDG_RUNTIME_DIR`` is set, but will
fall back to paths associated to the root user instead of a regular logged-in user if it's not set.

If you wish to ensure that a logged-in root user path is returned e.g. ``/run/user/0``, use `user_runtime_dir`
instead.

For FreeBSD/OpenBSD/NetBSD, it would return ``/var/run/$appname/$version`` if ``$XDG_RUNTIME_DIR`` is not set.
  - `def site_data_path(self)` (Línea 210): :return: data path shared by users. Only return the first item, even if ``multipath`` is set to ``True``
  - `def site_config_path(self)` (Línea 215): :return: config path shared by the users, returns the first item, even if ``multipath`` is set to ``True``
  - `def site_cache_path(self)` (Línea 220): :return: cache path shared by users. Only return the first item, even if ``multipath`` is set to ``True``
  - `def iter_config_dirs(self)` (Línea 224): :yield: all user and site configuration directories.
  - `def iter_data_dirs(self)` (Línea 229): :yield: all user and site data directories.

### Funciones independientes:

#### `def _get_user_media_dir(env_var, fallback_tilde_path)` (Línea 235)
- **Propósito:** Sin docstring.
- **Firma:** `def _get_user_media_dir(env_var, fallback_tilde_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _get_user_dirs_folder(key)` (Línea 245)
- **Propósito:** Return directory from user-dirs.dirs config file.

See https://freedesktop.org/wiki/Software/xdg-user-dirs/.
- **Firma:** `def _get_user_dirs_folder(key)`
- **Retorno / Efectos:** Consulta código fuente.
