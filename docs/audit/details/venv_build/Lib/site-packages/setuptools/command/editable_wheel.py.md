# Documentación Técnica: `venv_build/Lib/site-packages/setuptools/command/editable_wheel.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/setuptools/command/editable_wheel.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 914
- **Fecha de inspección:** 2026-08-05 18:36:38

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_EditableMode` (Línea 53)
- **Docstring:** _Possible editable installation modes:
`lenient` (new files automatically added to the package - DEFAULT);
`strict` (requires a new installation when files are added/removed); or
`compat` (attempts to emulate `python setup.py develop` - DEPRECATED)._
- **Métodos:**
  - `def convert(cls, mode)` (Línea 66): Sin docstring.

#### Clase `editable_wheel` (Línea 104)
- **Docstring:** _Build 'editable' wheel for development.
This command is private and reserved for internal use of setuptools,
users should rely on ``setuptools.build_meta`` APIs._
- **Métodos:**
  - `def initialize_options(self)` (Línea 118): Sin docstring.
  - `def finalize_options(self)` (Línea 124): Sin docstring.
  - `def run(self)` (Línea 130): Sin docstring.
  - `def _ensure_dist_info(self)` (Línea 151): Sin docstring.
  - `def _install_namespaces(self, installation_dir, pth_prefix)` (Línea 162): Sin docstring.
  - `def _find_egg_info_dir(self)` (Línea 172): Sin docstring.
  - `def _configure_build(self, name, unpacked_wheel, build_lib, tmp_dir)` (Línea 177): Configure commands to behave in the following ways:

- Build commands can write to ``build_lib`` if they really want to...
  (but this folder is expected to be ignored and modules are expected to live
  in the project directory...)
- Binary extensions should be built in-place (editable_mode = True)
- Data/header/script files are not part of the "editable" specification
  so they are written directly to the unpacked_wheel directory.
  - `def _set_editable_mode(self)` (Línea 240): Set the ``editable_mode`` flag in the build sub-commands
  - `def _collect_build_outputs(self)` (Línea 251): Sin docstring.
  - `def _run_build_commands(self, dist_name, unpacked_wheel, build_lib, tmp_dir)` (Línea 265): Sin docstring.
  - `def _run_build_subcommands(self)` (Línea 280): Issue #3501 indicates that some plugins/customizations might rely on:

1. ``build_py`` not running
2. ``build_py`` always copying files to ``build_lib``

However both these assumptions may be false in editable_wheel.
This method implements a temporary workaround to support the ecosystem
while the implementations catch up.
  - `def _safely_run(self, cmd_name)` (Línea 302): Sin docstring.
  - `def _create_wheel_file(self, bdist_wheel)` (Línea 330): Sin docstring.
  - `def _run_install(self, category)` (Línea 358): Sin docstring.
  - `def _select_strategy(self, name, tag, build_lib)` (Línea 364): Decides which strategy to use to implement an editable installation.

#### Clase `EditableStrategy` (Línea 391)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __call__(self, wheel, files, mapping)` (Línea 392): Sin docstring.
  - `def __enter__(self)` (Línea 395): Sin docstring.
  - `def __exit__(self, _exc_type, _exc_value, _traceback)` (Línea 396): Sin docstring.

#### Clase `_StaticPth` (Línea 404)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, dist, name, path_entries)` (Línea 405): Sin docstring.
  - `def __call__(self, wheel, files, mapping)` (Línea 410): Sin docstring.
  - `def __enter__(self)` (Línea 417): Sin docstring.
  - `def __exit__(self, _exc_type, _exc_value, _traceback)` (Línea 425): Sin docstring.

#### Clase `_LinkTree` (Línea 434)
- **Docstring:** _Creates a ``.pth`` file that points to a link tree in the ``auxiliary_dir``.

This strategy will only link files (not dirs), so it can be implemented in
any OS, even if that means using hardlinks instead of symlinks.

By collocating ``auxiliary_dir`` and the original source code, limitations
with hardlinks should be avoided._
- **Métodos:**
  - `def __init__(self, dist, name, auxiliary_dir, build_lib)` (Línea 445): Sin docstring.
  - `def __call__(self, wheel, files, mapping)` (Línea 457): Sin docstring.
  - `def _normalize_output(self, file)` (Línea 463): Sin docstring.
  - `def _create_file(self, relative_output, src_file, link)` (Línea 470): Sin docstring.
  - `def _create_links(self, outputs, output_mapping)` (Línea 476): Sin docstring.
  - `def __enter__(self)` (Línea 491): Sin docstring.
  - `def __exit__(self, _exc_type, _exc_value, _traceback)` (Línea 496): Sin docstring.

#### Clase `_TopLevelFinder` (Línea 512)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, dist, name)` (Línea 513): Sin docstring.
  - `def template_vars(self)` (Línea 517): Sin docstring.
  - `def get_implementation(self)` (Línea 544): Sin docstring.
  - `def __call__(self, wheel, files, mapping)` (Línea 553): Sin docstring.
  - `def __enter__(self)` (Línea 559): Sin docstring.
  - `def __exit__(self, _exc_type, _exc_value, _traceback)` (Línea 564): Sin docstring.

#### Clase `_NamespaceInstaller` (Línea 797)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, distribution, installation_dir, editable_name, src_root)` (Línea 798): Sin docstring.
  - `def _get_nspkg_file(self)` (Línea 805): Installation target.
  - `def _get_root(self)` (Línea 809): Where the modules/packages should be loaded from.

#### Clase `LinksNotSupported` (Línea 913)
- **Docstring:** _File system does not seem to support either symlinks or hard links._

### Funciones independientes:

#### `def _encode_pth(content)` (Línea 577)
- **Propósito:** Prior to Python 3.13 (see https://github.com/python/cpython/issues/77102),
.pth files are always read with 'locale' encoding, the recommendation
from the cpython core developers is to write them as ``open(path, "w")``
and ignore warnings (see python/cpython#77102, pypa/setuptools#3937).
This function tries to simulate this behavior without having to create an
actual file, in a way that supports a range of active Python versions.
(There seems to be some variety in the way different version of Python handle
``encoding=None``, not all of them use ``locale.getpreferredencoding(False)``
or ``locale.getencoding()``).
- **Firma:** `def _encode_pth(content)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _can_symlink_files(base_dir)` (Línea 598)
- **Propósito:** Sin docstring.
- **Firma:** `def _can_symlink_files(base_dir)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _simple_layout(packages, package_dir, project_dir)` (Línea 618)
- **Propósito:** Return ``True`` if:
- all packages are contained by the same parent directory, **and**
- all packages become importable if the parent directory is added to ``sys.path``.

>>> _simple_layout(['a'], {"": "src"}, "/tmp/myproj")
True
>>> _simple_layout(['a', 'a.b'], {"": "src"}, "/tmp/myproj")
True
>>> _simple_layout(['a', 'a.b'], {}, "/tmp/myproj")
True
>>> _simple_layout(['a', 'a.a1', 'a.a1.a2', 'b'], {"": "src"}, "/tmp/myproj")
True
>>> _simple_layout(['a', 'a.a1', 'a.a1.a2', 'b'], {"a": "a", "b": "b"}, ".")
True
>>> _simple_layout(['a', 'a.a1', 'a.a1.a2', 'b'], {"a": "_a", "b": "_b"}, ".")
False
>>> _simple_layout(['a', 'a.a1', 'a.a1.a2', 'b'], {"a": "_a"}, "/tmp/myproj")
False
>>> _simple_layout(['a', 'a.a1', 'a.a1.a2', 'b'], {"a.a1.a2": "_a2"}, ".")
False
>>> _simple_layout(['a', 'a.b'], {"": "src", "a.b": "_ab"}, "/tmp/myproj")
False
>>> # Special cases, no packages yet:
>>> _simple_layout([], {"": "src"}, "/tmp/myproj")
True
>>> _simple_layout([], {"a": "_a", "": "src"}, "/tmp/myproj")
False
- **Firma:** `def _simple_layout(packages, package_dir, project_dir)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _parent_path(pkg, pkg_path)` (Línea 659)
- **Propósito:** Infer the parent path containing a package, that if added to ``sys.path`` would
allow importing that package.
When ``pkg`` is directly mapped into a directory with a different name, return its
own path.
>>> _parent_path("a", "src/a")
'src'
>>> _parent_path("b", "src/c")
'src/c'
- **Firma:** `def _parent_path(pkg, pkg_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_packages(dist)` (Línea 673)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_packages(dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_top_level_modules(dist)` (Línea 689)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_top_level_modules(dist)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_package_roots(packages, package_dir, src_root)` (Línea 698)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_package_roots(packages, package_dir, src_root)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _absolute_root(path)` (Línea 711)
- **Propósito:** Works for packages and top-level modules
- **Firma:** `def _absolute_root(path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_virtual_namespaces(pkg_roots)` (Línea 722)
- **Propósito:** By carefully designing ``package_dir``, it is possible to implement the logical
structure of PEP 420 in a package without the corresponding directories.

Moreover a parent package can be purposefully/accidentally skipped in the discovery
phase (e.g. ``find_packages(include=["mypkg.*"])``, when ``mypkg.foo`` is included
by ``mypkg`` itself is not).
We consider this case to also be a virtual namespace (ignoring the original
directory) to emulate a non-editable installation.

This function will try to find these kinds of namespaces.
- **Firma:** `def _find_virtual_namespaces(pkg_roots)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _find_namespaces(packages, pkg_roots)` (Línea 746)
- **Propósito:** Sin docstring.
- **Firma:** `def _find_namespaces(packages, pkg_roots)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _remove_nested(pkg_roots)` (Línea 755)
- **Propósito:** Sin docstring.
- **Firma:** `def _remove_nested(pkg_roots)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _is_nested(pkg, pkg_path, parent, parent_path)` (Línea 768)
- **Propósito:** Return ``True`` if ``pkg`` is nested inside ``parent`` both logically and in the
file system.
>>> _is_nested("a.b", "path/a/b", "a", "path/a")
True
>>> _is_nested("a.b", "path/a/b", "a", "otherpath/a")
False
>>> _is_nested("a.b", "path/a/b", "c", "path/c")
False
>>> _is_nested("a.a", "path/a/a", "a", "path/a")
True
>>> _is_nested("b.a", "path/b/a", "a", "path/a")
False
- **Firma:** `def _is_nested(pkg, pkg_path, parent, parent_path)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _empty_dir(dir_)` (Línea 790)
- **Propósito:** Create a directory ensured to be empty. Existing files may be removed.
- **Firma:** `def _empty_dir(dir_)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _finder_template(name, mapping, namespaces)` (Línea 903)
- **Propósito:** Create a string containing the code for the``MetaPathFinder`` and
``PathEntryFinder``.
- **Firma:** `def _finder_template(name, mapping, namespaces)`
- **Retorno / Efectos:** Consulta código fuente.
