# Documentación Técnica: `venv_build/Lib/site-packages/win32comext/mapi/mapiutil.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/win32comext/mapi/mapiutil.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 211
- **Fecha de inspección:** 2026-08-05 18:36:41

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Funciones independientes:

#### `def GetPropTagName(pt)` (Línea 12)
- **Propósito:** Sin docstring.
- **Firma:** `def GetPropTagName(pt)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetScodeString(hr)` (Línea 65)
- **Propósito:** Sin docstring.
- **Firma:** `def GetScodeString(hr)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetMapiTypeName(propType, rawType)` (Línea 76)
- **Propósito:** Given a mapi type flag, return a string description of the type
- **Firma:** `def GetMapiTypeName(propType, rawType)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetProperties(obj, propList)` (Línea 93)
- **Propósito:** Given a MAPI object and a list of properties, return a list of property values.

Allows a single property to be passed, and the result is a single object.

Each request property can be an integer or a string.  Of a string, it is
automatically converted to an integer via the GetIdsFromNames function.

If the property fetch fails, the result is None.
- **Firma:** `def GetProperties(obj, propList)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def GetAllProperties(obj, make_tag_names)` (Línea 128)
- **Propósito:** Sin docstring.
- **Firma:** `def GetAllProperties(obj, make_tag_names)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def SetPropertyValue(obj, prop, val)` (Línea 155)
- **Propósito:** Sin docstring.
- **Firma:** `def SetPropertyValue(obj, prop, val)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def SetProperties(msg, propDict)` (Línea 175)
- **Propósito:** Given a Python dictionary, set the objects properties.

If the dictionary key is a string, then a property ID is queried
otherwise the ID is assumed native.

Coded for maximum efficiency wrt server calls - ie, maximum of
2 calls made to the object, regardless of the dictionary contents
(only 1 if dictionary full of int keys)
- **Firma:** `def SetProperties(msg, propDict)`
- **Retorno / Efectos:** Consulta código fuente.
