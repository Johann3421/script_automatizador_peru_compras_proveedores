# Documentación Técnica: `venv_build/Lib/site-packages/openpyxl/worksheet/worksheet.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/openpyxl/worksheet/worksheet.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 907
- **Fecha de inspección:** 2026-08-05 18:36:14

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `Worksheet` (Línea 68)
- **Docstring:** _Represents a worksheet.

Do not create worksheets yourself,
use :func:`openpyxl.workbook.Workbook.create_sheet` instead_
- **Métodos:**
  - `def __init__(self, parent, title)` (Línea 105): Sin docstring.
  - `def _setup(self)` (Línea 109): Sin docstring.
  - `def sheet_view(self)` (Línea 148): Sin docstring.
  - `def selected_cell(self)` (Línea 153): Sin docstring.
  - `def active_cell(self)` (Línea 158): Sin docstring.
  - `def array_formulae(self)` (Línea 163): Returns a dictionary of cells with array formulae and the cells in array
  - `def show_gridlines(self)` (Línea 174): Sin docstring.
  - `def freeze_panes(self)` (Línea 179): Sin docstring.
  - `def freeze_panes(self, topLeftCell)` (Línea 185): Sin docstring.
  - `def cell(self, row, column, value)` (Línea 220): Returns a cell object based on the given coordinates.

Usage: cell(row=15, column=1, value=5)

Calling `cell` creates cells in memory when they
are first accessed.

:param row: row index of the cell (e.g. 4)
:type row: int

:param column: column index of the cell (e.g. 3)
:type column: int

:param value: value of the cell (e.g. 5)
:type value: numeric or time or string or bool or none

:rtype: openpyxl.cell.cell.Cell
  - `def _get_cell(self, row, column)` (Línea 251): Internal method for getting a cell from a worksheet.
Will create a new cell if one doesn't already exist.
  - `def _add_cell(self, cell)` (Línea 265): Internal method for adding cell objects.
  - `def __getitem__(self, key)` (Línea 275): Convenience access by Excel style coordinates

The key can be a single cell coordinate 'A1', a range of cells 'A1:D25',
individual rows or columns 'A', 4 or ranges of rows or columns 'A:D',
4:10.

Single cells will always be created if they do not exist.

Returns either a single cell or a tuple of rows or columns.
  - `def __setitem__(self, key, value)` (Línea 316): Sin docstring.
  - `def __iter__(self)` (Línea 320): Sin docstring.
  - `def __delitem__(self, key)` (Línea 324): Sin docstring.
  - `def min_row(self)` (Línea 331): The minimum row index containing data (1-based)

:type: int
  - `def max_row(self)` (Línea 343): The maximum row index containing data (1-based)

:type: int
  - `def min_column(self)` (Línea 355): The minimum column index containing data (1-based)

:type: int
  - `def max_column(self)` (Línea 367): The maximum column index containing data (1-based)

:type: int
  - `def calculate_dimension(self)` (Línea 378): Return the minimum bounding range for all cells containing data (ex. 'A1:M24')

:rtype: string
  - `def dimensions(self)` (Línea 400): Returns the result of :func:`calculate_dimension`
  - `def iter_rows(self, min_row, max_row, min_col, max_col, values_only)` (Línea 405): Produces cells from the worksheet, by row. Specify the iteration range
using indices of rows and columns.

If no indices are specified the range starts at A1.

If no cells are in the worksheet an empty tuple will be returned.

:param min_col: smallest column index (1-based index)
:type min_col: int

:param min_row: smallest row index (1-based index)
:type min_row: int

:param max_col: largest column index (1-based index)
:type max_col: int

:param max_row: largest row index (1-based index)
:type max_row: int

:param values_only: whether only cell values should be returned
:type values_only: bool

:rtype: generator
  - `def _cells_by_row(self, min_col, min_row, max_col, max_row, values_only)` (Línea 444): Sin docstring.
  - `def rows(self)` (Línea 454): Produces all cells in the worksheet, by row (see :func:`iter_rows`)

:type: generator
  - `def values(self)` (Línea 463): Produces all cell values in the worksheet, by row

:type: generator
  - `def iter_cols(self, min_col, max_col, min_row, max_row, values_only)` (Línea 472): Produces cells from the worksheet, by column. Specify the iteration range
using indices of rows and columns.

If no indices are specified the range starts at A1.

If no cells are in the worksheet an empty tuple will be returned.

:param min_col: smallest column index (1-based index)
:type min_col: int

:param min_row: smallest row index (1-based index)
:type min_row: int

:param max_col: largest column index (1-based index)
:type max_col: int

:param max_row: largest row index (1-based index)
:type max_row: int

:param values_only: whether only cell values should be returned
:type values_only: bool

:rtype: generator
  - `def _cells_by_col(self, min_col, min_row, max_col, max_row, values_only)` (Línea 510): Get cells by column
  - `def columns(self)` (Línea 524): Produces all cells in the worksheet, by column  (see :func:`iter_cols`)
  - `def column_groups(self)` (Línea 530): Return a list of column ranges where more than one column
  - `def set_printer_settings(self, paper_size, orientation)` (Línea 537): Set printer settings 
  - `def add_data_validation(self, data_validation)` (Línea 544): Add a data-validation object to the sheet.  The data-validation
object defines the type of data-validation to be applied and the
cell or range of cells it should apply to.
  - `def add_chart(self, chart, anchor)` (Línea 552): Add a chart to the sheet
Optionally provide a cell for the top-left anchor
  - `def add_image(self, img, anchor)` (Línea 562): Add an image to the sheet.
Optionally provide a cell for the top-left anchor
  - `def add_table(self, table)` (Línea 572): Check for duplicate name in definedNames and other worksheet tables
before adding table.
  - `def tables(self)` (Línea 586): Sin docstring.
  - `def add_pivot(self, pivot)` (Línea 590): Sin docstring.
  - `def merge_cells(self, range_string, start_row, start_column, end_row, end_column)` (Línea 594): Set merge on a cell range.  Range is a cell range (e.g. A1:E1) 
  - `def _clean_merge_range(self, mcr)` (Línea 605): Remove all but the top left-cell from a range of merged cells
and recreate the lost border information.
Borders are then applied
  - `def merged_cell_ranges(self)` (Línea 620): Return a copy of cell ranges
  - `def unmerge_cells(self, range_string, start_row, start_column, end_row, end_column)` (Línea 625): Remove merge on a cell range.  Range is a cell range (e.g. A1:E1) 
  - `def append(self, iterable)` (Línea 641): Appends a group of values at the bottom of the current sheet.

* If it's a list: all values are added in order, starting from the first column
* If it's a dict: values are assigned to the columns indicated by the keys (numbers or letters)

:param iterable: list, range or generator, or dict containing values to append
:type iterable: list|tuple|range|generator or dict

Usage:

* append(['This is A1', 'This is B1', 'This is C1'])
* **or** append({'A' : 'This is A1', 'C' : 'This is C1'})
* **or** append({1 : 'This is A1', 3 : 'This is C1'})

:raise: TypeError when iterable is neither a list/tuple nor a dict
  - `def _move_cells(self, min_row, min_col, offset, row_or_col)` (Línea 689): Move either rows or columns around by the offset
  - `def insert_rows(self, idx, amount)` (Línea 717): Insert row or rows before row==idx
  - `def insert_cols(self, idx, amount)` (Línea 725): Insert column or columns before col==idx
  - `def delete_rows(self, idx, amount)` (Línea 732): Delete row or rows from row==idx
  - `def delete_cols(self, idx, amount)` (Línea 753): Delete column or columns from col==idx
  - `def move_range(self, cell_range, rows, cols, translate)` (Línea 771): Move a cell range by the number of rows and/or columns:
down if rows > 0 and up if rows < 0
right if cols > 0 and left if cols < 0
Existing cells will be overwritten.
Formulae and references will not be updated.
  - `def _move_cell(self, row, column, row_offset, col_offset, translate)` (Línea 801): Move a cell from one place to another.
Delete at old index
Rebase coordinate
  - `def _invalid_row(self, iterable)` (Línea 819): Sin docstring.
  - `def _add_column(self)` (Línea 825): Dimension factory for column information
  - `def _add_row(self)` (Línea 830): Dimension factory for row information
  - `def print_title_rows(self)` (Línea 837): Rows to be printed at the top of every page (ex: '1:3')
  - `def print_title_rows(self, rows)` (Línea 844): Set rows to be printed on the top of every page
format `1:3`
  - `def print_title_cols(self)` (Línea 854): Columns to be printed at the left side of every page (ex: 'A:C')
  - `def print_title_cols(self, cols)` (Línea 861): Set cols to be printed on the left of every page
format ``A:C`
  - `def print_titles(self)` (Línea 871): Sin docstring.
  - `def print_area(self)` (Línea 877): The print area for the worksheet, or None if not set. To set, supply a range
like 'A1:D4' or a list of ranges.
  - `def print_area(self, value)` (Línea 887): Range of cells in the form A1:D4 or list of ranges. Print area can be cleared
by passing `None` or an empty list

### Funciones independientes:

#### `def _gutter(idx, offset, max_val)` (Línea 900)
- **Propósito:** When deleting rows and columns are deleted we rely on overwriting.
This may not be the case for a large offset on small set of cells:
range(cells_to_delete) > range(cell_to_be_moved)
- **Firma:** `def _gutter(idx, offset, max_val)`
- **Retorno / Efectos:** Consulta código fuente.
