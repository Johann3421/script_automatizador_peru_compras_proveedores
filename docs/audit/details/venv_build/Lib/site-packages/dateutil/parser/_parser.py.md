# Documentación Técnica: `venv_build/Lib/site-packages/dateutil/parser/_parser.py`

- **Ruta relativa:** `venv_build/Lib/site-packages/dateutil/parser/_parser.py`
- **Tipo de archivo:** `.py`
- **Líneas de código:** 1613
- **Fecha de inspección:** 2026-08-05 18:36:08

---

## 🛠️ Reglas de Modificación (Qué tocar y qué NO tocar)

> [!NOTE]
> **MODULO DE INTERFAZ / UTILIDAD (MODIFICABLE CON PRECAUCIÓN)**
> Este archivo gestiona la presentación, puente PyWebView o formateo de reportes.
> **Regla:** Se pueden hacer mejoras estéticas, agregar eventos de UI o ajustar layouts cuidando no romper la comunicación con el backend.

## 📋 Estructura Interna del Archivo

### Clases definidas:

#### Clase `_timelex` (Línea 58)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, instream)` (Línea 62): Sin docstring.
  - `def get_token(self)` (Línea 77): This function breaks the time string into lexical units (tokens), which
can be parsed by the parser. Lexical units are demarcated by changes in
the character set, so any continuous string of letters is considered
one unit, any continuous string of numbers is considered one unit.

The main complication arises from the fact that dots ('.') can be used
both as separators (e.g. "Sep.20.2009") or decimal points (e.g.
"4:30:21.447"). As such, it is necessary to read the full context of
any dot-separated strings before breaking it into tokens; as such, this
function maintains a "token stack", for when the ambiguous context
demands that multiple tokens be parsed at once.
  - `def __iter__(self)` (Línea 186): Sin docstring.
  - `def __next__(self)` (Línea 189): Sin docstring.
  - `def next(self)` (Línea 196): Sin docstring.
  - `def split(cls, s)` (Línea 200): Sin docstring.
  - `def isword(cls, nextchar)` (Línea 204): Whether or not the next character is part of a word 
  - `def isnum(cls, nextchar)` (Línea 209): Whether the next character is part of a number 
  - `def isspace(cls, nextchar)` (Línea 214): Whether the next character is whitespace 

#### Clase `_resultbase` (Línea 219)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 221): Sin docstring.
  - `def _repr(self, classname)` (Línea 225): Sin docstring.
  - `def __len__(self)` (Línea 233): Sin docstring.
  - `def __repr__(self)` (Línea 237): Sin docstring.

#### Clase `parserinfo` (Línea 241)
- **Docstring:** _Class which handles what inputs are accepted. Subclass this to customize
the language and acceptable values for each parameter.

:param dayfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the day (``True``) or month (``False``). If
    ``yearfirst`` is set to ``True``, this distinguishes between YDM
    and YMD. Default is ``False``.

:param yearfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the year. If ``True``, the first number is taken
    to be the year, otherwise the last number is taken to be the year.
    Default is ``False``._
- **Métodos:**
  - `def __init__(self, dayfirst, yearfirst)` (Línea 294): Sin docstring.
  - `def _convert(self, lst)` (Línea 309): Sin docstring.
  - `def jump(self, name)` (Línea 319): Sin docstring.
  - `def weekday(self, name)` (Línea 322): Sin docstring.
  - `def month(self, name)` (Línea 329): Sin docstring.
  - `def hms(self, name)` (Línea 336): Sin docstring.
  - `def ampm(self, name)` (Línea 342): Sin docstring.
  - `def pertain(self, name)` (Línea 348): Sin docstring.
  - `def utczone(self, name)` (Línea 351): Sin docstring.
  - `def tzoffset(self, name)` (Línea 354): Sin docstring.
  - `def convertyear(self, year, century_specified)` (Línea 360): Converts two-digit years to year within [-50, 49]
range of self._year (current local time)
  - `def validate(self, res)` (Línea 380): Sin docstring.

#### Clase `_ymd` (Línea 394)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self)` (Línea 395): Sin docstring.
  - `def has_year(self)` (Línea 403): Sin docstring.
  - `def has_month(self)` (Línea 407): Sin docstring.
  - `def has_day(self)` (Línea 411): Sin docstring.
  - `def could_be_day(self, value)` (Línea 414): Sin docstring.
  - `def append(self, val, label)` (Línea 428): Sin docstring.
  - `def _resolve_from_stridxs(self, strids)` (Línea 456): Try to resolve the identities of year/month/day elements using
ystridx, mstridx, and dstridx, if enough of these are specified.
  - `def resolve_ymd(self, yearfirst, dayfirst)` (Línea 474): Sin docstring.

#### Clase `parser` (Línea 568)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def __init__(self, info)` (Línea 569): Sin docstring.
  - `def parse(self, timestr, default, ignoretz, tzinfos)` (Línea 572): Parse the date/time string into a :class:`datetime.datetime` object.

:param timestr:
    Any date/time string using the supported formats.

:param default:
    The default datetime object, if this is a datetime object and not
    ``None``, elements specified in ``timestr`` replace elements in the
    default object.

:param ignoretz:
    If set ``True``, time zones in parsed strings are ignored and a
    naive :class:`datetime.datetime` object is returned.

:param tzinfos:
    Additional time zone names / aliases which may be present in the
    string. This argument maps time zone names (and optionally offsets
    from those time zones) to time zones. This parameter can be a
    dictionary with timezone aliases mapping time zone names to time
    zones or a function taking two parameters (``tzname`` and
    ``tzoffset``) and returning a time zone.

    The timezones to which the names are mapped can be an integer
    offset from UTC in seconds or a :class:`tzinfo` object.

    .. doctest::
       :options: +NORMALIZE_WHITESPACE

        >>> from dateutil.parser import parse
        >>> from dateutil.tz import gettz
        >>> tzinfos = {"BRST": -7200, "CST": gettz("America/Chicago")}
        >>> parse("2012-01-19 17:21:00 BRST", tzinfos=tzinfos)
        datetime.datetime(2012, 1, 19, 17, 21, tzinfo=tzoffset(u'BRST', -7200))
        >>> parse("2012-01-19 17:21:00 CST", tzinfos=tzinfos)
        datetime.datetime(2012, 1, 19, 17, 21,
                          tzinfo=tzfile('/usr/share/zoneinfo/America/Chicago'))

    This parameter is ignored if ``ignoretz`` is set.

:param \*\*kwargs:
    Keyword arguments as passed to ``_parse()``.

:return:
    Returns a :class:`datetime.datetime` object or, if the
    ``fuzzy_with_tokens`` option is ``True``, returns a tuple, the
    first element being a :class:`datetime.datetime` object, the second
    a tuple containing the fuzzy tokens.

:raises ParserError:
    Raised for invalid or unknown string format, if the provided
    :class:`tzinfo` is not in a valid format, or if an invalid date
    would be created.

:raises TypeError:
    Raised for non-string or character stream input.

:raises OverflowError:
    Raised if the parsed date exceeds the largest valid C integer on
    your system.
  - `def _parse(self, timestr, dayfirst, yearfirst, fuzzy, fuzzy_with_tokens)` (Línea 666): Private method which performs the heavy lifting of parsing, called from
``parse()``, which passes on its ``kwargs`` to this function.

:param timestr:
    The string to parse.

:param dayfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the day (``True``) or month (``False``). If
    ``yearfirst`` is set to ``True``, this distinguishes between YDM
    and YMD. If set to ``None``, this value is retrieved from the
    current :class:`parserinfo` object (which itself defaults to
    ``False``).

:param yearfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the year. If ``True``, the first number is taken
    to be the year, otherwise the last number is taken to be the year.
    If this is set to ``None``, the value is retrieved from the current
    :class:`parserinfo` object (which itself defaults to ``False``).

:param fuzzy:
    Whether to allow fuzzy parsing, allowing for string like "Today is
    January 1, 2047 at 8:21:00AM".

:param fuzzy_with_tokens:
    If ``True``, ``fuzzy`` is automatically set to True, and the parser
    will return a tuple where the first element is the parsed
    :class:`datetime.datetime` datetimestamp and the second element is
    a tuple containing the portions of the string which were ignored:

    .. doctest::

        >>> from dateutil.parser import parse
        >>> parse("Today is January 1, 2047 at 8:21:00AM", fuzzy_with_tokens=True)
        (datetime.datetime(2047, 1, 1, 8, 21), (u'Today is ', u' ', u'at '))
  - `def _parse_numeric_token(self, tokens, idx, info, ymd, res, fuzzy)` (Línea 875): Sin docstring.
  - `def _find_hms_idx(self, idx, tokens, info, allow_jump)` (Línea 1006): Sin docstring.
  - `def _assign_hms(self, res, value_repr, hms)` (Línea 1040): Sin docstring.
  - `def _could_be_tzname(self, hour, tzname, tzoffset, token)` (Línea 1056): Sin docstring.
  - `def _ampm_valid(self, hour, ampm, fuzzy)` (Línea 1064): For fuzzy parsing, 'a' or 'am' (both valid English words)
may erroneously trigger the AM/PM flag. Deal with that
here.
  - `def _adjust_ampm(self, hour, ampm)` (Línea 1092): Sin docstring.
  - `def _parse_min_sec(self, value)` (Línea 1099): Sin docstring.
  - `def _parse_hms(self, idx, tokens, info, hms_idx)` (Línea 1111): Sin docstring.
  - `def _parsems(self, value)` (Línea 1133): Parse a I[.F] seconds value into (seconds, microseconds).
  - `def _to_decimal(self, val)` (Línea 1141): Sin docstring.
  - `def _build_tzinfo(self, tzinfos, tzname, tzoffset)` (Línea 1159): Sin docstring.
  - `def _build_tzaware(self, naive, res, tzinfos)` (Línea 1177): Sin docstring.
  - `def _build_naive(self, res, default)` (Línea 1217): Sin docstring.
  - `def _assign_tzname(self, dt, tzname)` (Línea 1242): Sin docstring.
  - `def _recombine_skipped(self, tokens, skipped_idxs)` (Línea 1250): >>> tokens = ["foo", " ", "bar", " ", "19June2000", "baz"]
>>> skipped_idxs = [0, 1, 2, 5]
>>> _recombine_skipped(tokens, skipped_idxs)
["foo bar", "baz"]

#### Clase `_tzparser` (Línea 1371)
- **Docstring:** _Sin docstring._
- **Métodos:**
  - `def parse(self, tzstr)` (Línea 1390): Sin docstring.

#### Clase `ParserError` (Línea 1589)
- **Docstring:** _Exception subclass used for any failure to parse a datetime string.

This is a subclass of :py:exc:`ValueError`, and should be raised any time
earlier versions of ``dateutil`` would have raised ``ValueError``.

.. versionadded:: 2.8.1_
- **Métodos:**
  - `def __str__(self)` (Línea 1597): Sin docstring.
  - `def __repr__(self)` (Línea 1603): Sin docstring.

#### Clase `UnknownTimezoneWarning` (Línea 1608)
- **Docstring:** _Raised when the parser finds a timezone it cannot parse into a tzinfo.

.. versionadded:: 2.7.0_

### Funciones independientes:

#### `def parse(timestr, parserinfo)` (Línea 1270)
- **Propósito:** Parse a string in one of the supported formats, using the
``parserinfo`` parameters.

:param timestr:
    A string containing a date/time stamp.

:param parserinfo:
    A :class:`parserinfo` object containing parameters for the parser.
    If ``None``, the default arguments to the :class:`parserinfo`
    constructor are used.

The ``**kwargs`` parameter takes the following keyword arguments:

:param default:
    The default datetime object, if this is a datetime object and not
    ``None``, elements specified in ``timestr`` replace elements in the
    default object.

:param ignoretz:
    If set ``True``, time zones in parsed strings are ignored and a naive
    :class:`datetime` object is returned.

:param tzinfos:
    Additional time zone names / aliases which may be present in the
    string. This argument maps time zone names (and optionally offsets
    from those time zones) to time zones. This parameter can be a
    dictionary with timezone aliases mapping time zone names to time
    zones or a function taking two parameters (``tzname`` and
    ``tzoffset``) and returning a time zone.

    The timezones to which the names are mapped can be an integer
    offset from UTC in seconds or a :class:`tzinfo` object.

    .. doctest::
       :options: +NORMALIZE_WHITESPACE

        >>> from dateutil.parser import parse
        >>> from dateutil.tz import gettz
        >>> tzinfos = {"BRST": -7200, "CST": gettz("America/Chicago")}
        >>> parse("2012-01-19 17:21:00 BRST", tzinfos=tzinfos)
        datetime.datetime(2012, 1, 19, 17, 21, tzinfo=tzoffset(u'BRST', -7200))
        >>> parse("2012-01-19 17:21:00 CST", tzinfos=tzinfos)
        datetime.datetime(2012, 1, 19, 17, 21,
                          tzinfo=tzfile('/usr/share/zoneinfo/America/Chicago'))

    This parameter is ignored if ``ignoretz`` is set.

:param dayfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the day (``True``) or month (``False``). If
    ``yearfirst`` is set to ``True``, this distinguishes between YDM and
    YMD. If set to ``None``, this value is retrieved from the current
    :class:`parserinfo` object (which itself defaults to ``False``).

:param yearfirst:
    Whether to interpret the first value in an ambiguous 3-integer date
    (e.g. 01/05/09) as the year. If ``True``, the first number is taken to
    be the year, otherwise the last number is taken to be the year. If
    this is set to ``None``, the value is retrieved from the current
    :class:`parserinfo` object (which itself defaults to ``False``).

:param fuzzy:
    Whether to allow fuzzy parsing, allowing for string like "Today is
    January 1, 2047 at 8:21:00AM".

:param fuzzy_with_tokens:
    If ``True``, ``fuzzy`` is automatically set to True, and the parser
    will return a tuple where the first element is the parsed
    :class:`datetime.datetime` datetimestamp and the second element is
    a tuple containing the portions of the string which were ignored:

    .. doctest::

        >>> from dateutil.parser import parse
        >>> parse("Today is January 1, 2047 at 8:21:00AM", fuzzy_with_tokens=True)
        (datetime.datetime(2047, 1, 1, 8, 21), (u'Today is ', u' ', u'at '))

:return:
    Returns a :class:`datetime.datetime` object or, if the
    ``fuzzy_with_tokens`` option is ``True``, returns a tuple, the
    first element being a :class:`datetime.datetime` object, the second
    a tuple containing the fuzzy tokens.

:raises ParserError:
    Raised for invalid or unknown string formats, if the provided
    :class:`tzinfo` is not in a valid format, or if an invalid date would
    be created.

:raises OverflowError:
    Raised if the parsed date exceeds the largest valid C integer on
    your system.
- **Firma:** `def parse(timestr, parserinfo)`
- **Retorno / Efectos:** Consulta código fuente.

#### `def _parsetz(tzstr)` (Línea 1585)
- **Propósito:** Sin docstring.
- **Firma:** `def _parsetz(tzstr)`
- **Retorno / Efectos:** Consulta código fuente.
